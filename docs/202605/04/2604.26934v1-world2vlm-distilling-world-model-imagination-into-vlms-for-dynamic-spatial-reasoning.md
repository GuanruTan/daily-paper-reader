---
title: "World2VLM: Distilling World Model Imagination into VLMs for Dynamic Spatial Reasoning"
title_zh: World2VLM：将世界模型的想象力蒸馏至视觉语言模型以实现动态空间推理
authors: "Wanyue Zhang, Wenxiang Wu, Wang Xu, Jiaxin Luo, Helu Zhi, Yibin Huang, Shuo Ren, Zitao Liu, Jiajun Zhang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26934v1"
tags: ["query:tp"]
score: 8.0
evidence: 基于参数化相机轨迹和视觉语言模型的动态空间推理
tldr: 针对视觉语言模型（VLM）在动态空间推理方面的不足，本文提出 World2VLM 框架。该框架通过将生成式世界模型的空间想象能力蒸馏到 VLM 中，利用世界模型合成几何一致的未来视图，为前向和逆向空间推理提供结构化监督。实验表明，该方法在多个空间推理基准测试中显著提升了模型性能，且无需推理时的额外计算开销，实现了高效的空间能力内化。
selection_source: fresh_fetch
motivation: 现有的 VLM 难以理解运动带来的场景变化，且现有的推理时耦合世界模型的方法存在巨大的计算开销。
method: 通过世界模型根据相机轨迹合成几何一致的未来视图，并利用这些数据对 VLM 进行前向和逆向空间推理的两阶段蒸馏训练。
result: World2VLM 在 SAT-Real、VSI-Bench 和 MindCube 等多个基准测试中均优于基础模型和推理时生成方法。
conclusion: 世界模型不仅可以作为推理工具，更能作为高效的训练导师，帮助 VLM 学习并内化复杂的动态空间想象能力。
---

## 摘要
视觉语言模型 (VLM) 在静态视觉理解方面表现出色，但在需要想象场景在自我运动下如何演变的动态空间推理方面仍面临挑战。最近的研究通过使用合成数据扩展空间监督，或在推理时将 VLM 与世界模型耦合来解决这一局限。然而，前者通常缺乏对运动条件状态转移的显式建模，而后者则会产生巨大的计算开销。在这项工作中，我们提出了 World2VLM，这是一个将生成式世界模型的空间想象力蒸馏到视觉语言模型中的训练框架。给定初始观测和参数化的相机轨迹，我们使用视图一致的世界模型来合成几何对齐的未来视图，并为前向（动作到结果）和逆向（结果到动作）空间推理推导出结构化监督。我们使用该流水线生成的紧凑数据集，通过两阶段方案对 VLM 进行后训练，并在多个空间推理基准测试中对其进行评估。World2VLM 在包括 SAT-Real、SAT-Synthesized、VSI-Bench 和 MindCube 在内的多种基准测试中，相较于基础模型实现了持续的改进。它还优于测试时与世界模型耦合的方法，同时消除了昂贵的推理时生成的需要。我们的结果表明，世界模型不仅可以作为推理时的工具，还可以作为有效的训练时教师，使 VLM 能够以可扩展且高效的方式内化空间想象力。

## Abstract
Vision-language models (VLMs) have shown strong performance on static visual understanding, yet they still struggle with dynamic spatial reasoning that requires imagining how scenes evolve under egocentric motion. Recent efforts address this limitation either by scaling spatial supervision with synthetic data or by coupling VLMs with world models at inference time. However, the former often lacks explicit modeling of motion-conditioned state transitions, while the latter incurs substantial computational overhead. In this work, we propose World2VLM, a training framework that distills spatial imagination from a generative world model into a vision-language model. Given an initial observation and a parameterized camera trajectory, we use a view-consistent world model to synthesize geometrically aligned future views and derive structured supervision for both forward (action-to-outcome) and inverse (outcome-to-action) spatial reasoning. We post-train the VLM with a two-stage recipe on a compact dataset generated by this pipeline and evaluate it on multiple spatial reasoning benchmarks. World2VLM delivers consistent improvements over the base model across diverse benchmarks, including SAT-Real, SAT-Synthesized, VSI-Bench, and MindCube. It also outperforms the test-time world-model-coupled methods while eliminating the need for expensive inference-time generation. Our results suggest that world models can serve not only as inference-time tools, but also as effective training-time teachers, enabling VLMs to internalize spatial imagination in a scalable and efficient manner.

---

## 论文详细总结（自动生成）

### World2VLM：将世界模型的想象力蒸馏至视觉语言模型以实现动态空间推理

#### 1. 论文的核心问题与整体含义
视觉语言模型（VLM）在静态图像理解上表现优异，但在**动态空间推理**（Dynamic Spatial Reasoning）方面存在显著短板。这种能力要求模型能够“脑补”在自我运动（如向前走、向右转）下场景如何演变。
*   **研究动机**：现有方案要么依赖静态合成数据（缺乏显式的运动建模），要么在推理阶段外挂世界模型（如 MindJourney），但这会导致巨大的计算延迟和推理成本。
*   **核心目标**：探索世界模型是否可以作为“训练时导师”，将其对物理世界的动态想象力内化（Internalize）到 VLM 的参数中，从而在推理时不增加额外开销的情况下提升空间推理能力。

#### 2. 论文提出的方法论
World2VLM 框架通过将生成式世界模型的预测能力蒸馏到 VLM 中，包含三个关键阶段：
*   **世界模型引导的转移构建**：
    *   给定初始观测图像和参数化的相机轨迹（平移、旋转），利用视图一致的世界模型（如 SVC 或 HY-WorldPlay）合成几何对齐的未来视图。
    *   利用检测器和跟踪器提取物体元数据（如 Bounding Box），并将其序列化为文本，作为结构化监督信号。
*   **双向空间内化任务套件**：
    *   **逆向推理（Outcome-to-Action）**：给定初始图和结果图，推断相机运动（距离、角度、多步序列）。
    *   **前向推理（Action-to-Outcome）**：给定初始图和动作，预测结果（物体位置、可见性、跨视图一致性）。
*   **两阶段后训练方案**：
    *   **阶段 I：SFT（监督微调）**：在约 100K 的合成数据上进行双向任务训练。
    *   **阶段 II：GRPO（群体相对策略优化）**：使用强化学习进一步精炼模型。通过任务感知的奖励函数（涵盖格式、数值精度、空间逻辑、轨迹一致性等）来纠正 SFT 阶段可能出现的数值偏差和格式错误。

#### 3. 实验设计
*   **基础模型**：Qwen2.5-VL-7B-Instruct。
*   **数据集/场景**：
    *   训练数据源：ScanNet（真实室内场景）和 MulSeT（模拟室内场景）。
    *   世界模型导师：Stable Virtual Camera (SVC) 和 HY-WorldPlay。
*   **Benchmark（基准测试）**：
    *   **SAT-Real & SAT-Synthesized**：评估自我运动下的空间能力。
    *   **VSI-Bench**：评估空间配置、测量估计和时空推理。
    *   **MindCube**：评估部分观测下的视角切换和心理模拟。
*   **对比方法**：基础 VLM 模型、MindJourney 风格的“推理时耦合世界模型”方法。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   采用 LoRA（Rank=256）进行参数高效微调。
    *   SFT 阶段：约 103,000 条样本，使用 AdamW 优化器，bfloat16 精度。
    *   GRPO 阶段：使用 1,000 条平衡后的样本，每条 Prompt 采样 4 个响应。
*   **推理开销**：训练完成后，模型仅需标准 VLM 推理，无需调用世界模型，实现了零推理额外开销。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流空间推理基准上进行了全面测试，并针对 SAT-Real 进行了细粒度的任务拆解分析（如视角切换、动作后果等）。
*   **消融实验**：
    *   **方向消融**：对比了“仅前向”、“仅逆向”与“双向”训练的效果，证明了双向约束的必要性。
    *   **数据源消融**：对比了真实场景与模拟场景数据的贡献。
    *   **导师模型对比**：测试了不同生成质量的世界模型对最终效果的影响。
*   **客观性**：实验设置了严格的基准隔离，确保测试集未进入训练环节；对比 MindJourney 时采用了相同的 BackBone，保证了公平性。

#### 6. 论文的主要结论与发现
*   **内化优于外挂**：World2VLM 在所有基准上均显著优于基础模型，且在多数指标上超过了推理时耦合世界模型的方法，证明了“训练时蒸馏”的有效性。
*   **两阶段增益**：SFT 奠定了空间表征基础，而 GRPO 显著提升了模型在数值精度（如距离估计）和结构化输出方面的表现。
*   **跨模型迁移**：该框架对世界模型导师的选择具有鲁棒性，无论是基于扩散的 SVC 还是基于视频生成的 HY-WorldPlay 都能提供有效监督。

#### 7. 优点
*   **高效性**：解决了世界模型推理慢、成本高的痛点，使 VLM 具备了“原生”的想象力。
*   **双向任务设计**：通过前向预测和逆向推断的结合，迫使模型建立更深层的空间几何关联，而非简单的模式匹配。
*   **可扩展性**：利用生成式模型自动产生监督信号，降低了对高质量人工标注动态数据的依赖。

#### 8. 不足与局限
*   **导师质量瓶颈**：VLM 的上限受限于世界模型的生成质量。如果世界模型产生几何畸变或物体漂移，会向 VLM 传递错误信号。
*   **动作空间限制**：目前的实验主要集中在离散的相机动作（平移、旋转），对于更复杂的连续动作或非刚性场景变化的覆盖可能不足。
*   **幻觉风险**：虽然 GRPO 有所缓解，但在长程推理中，模型仍可能产生不符合物理规律的“空间幻觉”。

（完）
