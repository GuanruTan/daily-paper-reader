---
title: "GSDrive: Reinforcing Driving Policies by Multi-mode Trajectory Probing with 3D Gaussian Splatting Environment"
title_zh: GSDrive：通过 3D 高斯泼溅环境下的多模态轨迹探测强化驾驶策略
authors: "Ziang Guo, Chen Min, Xuefeng Zhang, Yixiao Zhou, Zufeng Zhang, Dzmitry Tsetserukou"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28111v2"
tags: ["query:tp"]
score: 9.0
evidence: 自动驾驶的多模式轨迹探测
tldr: 针对端到端自动驾驶中强化学习奖励稀疏且依赖灾难性事件的问题，本文提出GSDrive框架。该框架利用3D高斯泼溅（3DGS）构建可微分的物理模拟环境，并结合流匹配轨迹预测器进行多模式轨迹探测。通过在模拟中评估候选轨迹，为策略改进提供即时且密集的物理反馈，实现了模仿学习与强化学习的有效结合，在nuScenes数据集上表现优异。
selection_source: fresh_fetch
motivation: 传统的强化学习驾驶策略训练依赖于碰撞等稀疏的延迟奖励，导致模型难以收敛到最优行为且数据标注成本高。
method: 提出GSDrive框架，利用3DGS构建模拟环境，并通过流匹配预测器进行多模式轨迹探测，以提供基于物理模拟的密集奖励反馈。
result: 在nuScenes重建数据集的闭环实验中，该方法在性能上超越了现有的基于模拟的强化学习驾驶方法。
conclusion: GSDrive通过将物理模拟信号引入奖励函数，成功建立了模仿学习与强化学习间的双向知识交换，显著提升了端到端驾驶策略的鲁棒性。
---

## 摘要
端到端（E2E）自动驾驶为将感知输入直接转化为驾驶动作提供了一种极具前景的方法。然而，高昂的标注成本和随时间推移的数据质量退化阻碍了其在现实世界中的长期部署。虽然结合模仿学习（IL）和强化学习（RL）是改进策略的常用策略，但传统的 RL 训练依赖于延迟的、基于事件的奖励——策略仅从碰撞等灾难性后果中学习，导致过早收敛到次优行为。为了解决这些局限性，我们引入了 GSDrive，这是一个利用 3D 高斯泼溅（3DGS）在 E2E 驾驶策略改进中进行可微、基于物理的奖励塑造的框架。我们的方法在 3DGS 模拟器中集成了一个基于流匹配（flow matching）的轨迹预测器，实现了多模态轨迹探测，通过展开候选轨迹来评估预期奖励。通过将奖励函数建立在物理模拟的交互信号之上，这在 IL 和 RL 之间建立了双向知识交换，提供了即时的密集反馈，而非稀疏的灾难性事件。在重建的 nuScenes 数据集上的评估表明，我们的方法在闭环实验中超越了现有的基于模拟的 RL 驾驶方法。代码可在 https://github.com/ZionGo6/GSDrive 获取。

## Abstract
End-to-end (E2E) autonomous driving presents a promising approach for translating perceptual inputs directly into driving actions. However, prohibitive annotation costs and temporal data quality degradation hinder long-term real-world deployment. While combining imitation learning (IL) and reinforcement learning (RL) is a common strategy for policy improvement, conventional RL training relies on delayed, event-based rewards-policies learn only from catastrophic outcomes such as collisions, leading to premature convergence to suboptimal behaviors. To address these limitations, we introduce GSDrive, a framework that exploits 3D Gaussian Splatting (3DGS) for differentiable, physics-based reward shaping in E2E driving policy improvement. Our method incorporates a flow matching-based trajectory predictor within the 3DGS simulator, enabling multi-mode trajectory probing where candidate trajectories are rolled out to assess prospective rewards. This establishes a bidirectional knowledge exchange between IL and RL by grounding reward functions in physically simulated interaction signals, offering immediate dense feedback instead of sparse catastrophic events. Evaluated on the reconstructed nuScenes dataset, our method surpasses existing simulation-based RL driving approaches in closed-loop experiments. Code is available at https://github.com/ZionGo6/GSDrive.

---

## 论文详细总结（自动生成）

以下是对论文《GSDrive: Reinforcing Driving Policies by Multi-mode Trajectory Probing with 3D Gaussian Splatting Environment》的详细中文总结：

### 1. 核心问题与整体含义
*   **研究动机**：端到端（E2E）自动驾驶面临标注成本高昂和数据质量随时间退化的问题。虽然结合模仿学习（IL）和强化学习（RL）是主流方案，但传统 RL 训练依赖于稀疏且延迟的奖励（如仅在发生碰撞时给予惩罚），导致策略难以有效归因早期决策，容易收敛至次优行为。
*   **核心目标**：通过引入 3D 高斯泼溅（3DGS）构建可微分的物理模拟环境，并利用多模态轨迹探测（Probing）技术，为 RL 提供即时的、基于物理模拟的密集奖励反馈，从而提升驾驶策略的鲁棒性和安全性。

### 2. 方法论
*   **核心思想**：在 3DGS 模拟环境中，不仅评估当前的动作，还利用预测器生成多个未来的候选轨迹，并在模拟器中“预演”这些轨迹的后果，将未来的预期奖励提前反馈给当前的策略优化。
*   **关键技术细节**：
    *   **3DGS 环境重建**：使用多视图 Transformer（VGGT）从 nuScenes 的 6 摄像头图像中提取特征，通过单次前向传播预测 3D 高斯原语的属性（位置、缩放、旋转、不透明度等），并结合 LiDAR 数据进行几何精细化。
    *   **流匹配（Flow Matching）轨迹预测**：在 IL 阶段，采用基于最优传输（Optimal Transport, OT）引导的流匹配头。相比扩散模型，它能以更少的步数生成更平滑、物理一致的多模态未来轨迹。
    *   **多模态轨迹探测（Probing）**：在 RL 阶段，策略会采样 $K$ 条候选轨迹并在 3DGS 环境中进行模拟。当前动作的奖励由“基础奖励”和“探测奖励”组成，其中探测奖励取所有候选轨迹中未来收益的最大值。
    *   **策略优化**：采用类似 PPO 的剪切代理目标函数，并引入自适应 KL 散度正则化，以防止策略在探索过程中过快偏离模仿学习建立的先验。

### 3. 实验设计
*   **数据集与场景**：基于 **nuScenes** 数据集重建的数字孪生环境。
*   **Benchmark（基准指标）**：包括回合奖励（ER）、平均车速（DS）、最大加速度（MA）、换道次数（LC）、最大加加速度（MAJ）、最大转向角（MSA）和碰撞率（CR）。
*   **对比方法**：
    *   基础强化学习：PPO。
    *   分层/技能强化学习：Skill-Critic。
    *   基于世界模型/逆奖励的方法：RLIR、Q-chunking。
    *   基于 3DGS 的现有方法：RAD。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及总训练时长。文中仅提到在 RL 阶段使用了 4 个并行环境进行数据采集，每个环境运行 8 个步骤。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 50 个测试回合的闭环评估。
    *   展示了完整的训练收敛曲线，对比了不同 RL 框架的稳定性。
    *   **消融实验**：针对轨迹预测头（DDPM vs DDIM vs Flow Matching）、动作头结构（直接预测 vs 残差预测）以及探测奖励的有效性进行了多组对比。
*   **充分性评价**：实验设计较为全面，涵盖了从感知重建到决策控制的全流程评估。通过闭环测试（Closed-loop）而非仅靠开环指标，更客观地反映了模型在动态环境中的表现。

### 6. 主要结论与发现
*   **密集奖励的优势**：通过轨迹探测提供的未来感知奖励，解决了 RL 训练中“信用分配”难的问题，使模型能预见碰撞并提前修正。
*   **性能提升**：GSDrive 在所有闭环指标上均优于对比方法，特别是在保持高车速的同时，显著降低了碰撞率（从 RAD 的 0.19 降至 0.11）和动作不平顺性（MAJ 降低）。
*   **流匹配的有效性**：在低步数推理场景下，流匹配结合分类器自由引导（CFG）比传统的扩散模型更能生成精确且符合多模态分布的驾驶轨迹。

### 7. 优点（亮点）
*   **物理接地（Physically Grounded）**：将 3DGS 的微分渲染特性与物理模拟结合，使奖励函数具有明确的物理意义。
*   **前瞻性决策**：通过“探测未来”来优化“当下”，有效缓解了端到端模型常见的因果混淆和短视问题。
*   **持续学习潜力**：框架支持利用无标注的真实世界数据进行 3DGS 重建，从而实现策略的持续改进，降低了对人工标注的依赖。

### 8. 不足与局限
*   **计算开销**：在 RL 训练过程中，对多条候选轨迹进行实时 3DGS 渲染和物理模拟会带来巨大的计算负担，论文未详细讨论其推理延迟。
*   **环境依赖性**：策略的性能高度依赖于 3DGS 重建的质量。如果重建场景存在伪影或几何缺失，可能会产生错误的物理反馈。
*   **Sim-to-Real 差距**：虽然 3DGS 提供了高保真视觉，但模拟器中的交通流代理（Agent）行为可能与现实世界仍有偏差，影响策略的泛化性。

（完）
