---
title: "Walk With Me: Long-Horizon Social Navigation for Human-Centric Outdoor Assistance"
title_zh: 与我同行：面向以人为中心的户外辅助的长程社交导航
authors: "Lingfeng Zhang, Xiaoshuai Hao, Xizhou Bu, Yingbo Tang, Hongsheng Li, Jinghui Lu, Xiu-shen Wei, Jiayi Ma, Yu Liu, Jing Zhang, Hangjun Ye, Xiaojun Liang, Long Chen, Wenbo Ding"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26839v1"
tags: ["query:tp"]
score: 8.0
evidence: 户外辅助中的社交合规导航与路径点建议
tldr: 本研究针对户外社交导航中高精地图成本高及现有模型难以处理长程任务的挑战，提出了“Walk with Me”无地图框架。该框架利用公共地图API和GPS信息，通过高层视觉语言模型（VLM）将自然语言指令转化为语义目的地并规划路径点。在执行阶段，系统结合底层视觉语言动作模型（VLA）与安全感知推理机制，在复杂社交场景下实现安全避障与长程导航。该方法为以人为中心的户外机器人辅助提供了高效、实用的解决方案。
selection_source: fresh_fetch
motivation: 解决现有户外导航依赖高成本高精地图且难以处理长程、复杂社交场景的问题。
method: 提出一种结合高层视觉语言模型（VLM）进行语义规划与底层视觉语言动作模型（VLA）执行动作的无地图分层导航框架。
result: 成功实现了从高层自然语言指令到安全、长程且符合社交规范的户外导航行为的转化。
conclusion: 该研究证明了结合语义意图落地与安全感知推理是实现实用化户外社交辅助导航的有效途径。
---

## 摘要
在开放世界户外环境中辅助人类，要求机器人将高层自然语言意图转化为安全、长程且符合社交规范的导航行为。现有的基于地图的方法依赖于昂贵的预构建高精地图，而基于学习的策略大多局限于室内和短程场景。为了弥补这一差距，我们提出了 Walk with Me，这是一个根据高层人类指令进行长程社交导航的无地图框架。Walk with Me 利用 GPS 上下文和来自公共地图 API 的轻量级候选兴趣点（POI）进行语义目的地定位和路径点提议。高层视觉语言模型（VLM）将抽象指令落实为具体目的地，并规划粗略的路径点序列。在执行过程中，一种观测感知的路由机制会确定底层视觉-语言-动作（VLA）策略是否能够处理当前情况，或者是否需要来自高层 VLM 的显式安全推理。常规路段由底层 VLA 执行，而复杂情况（如拥挤的路口）则会在不安全时触发高层推理和“停止并等待”行为。通过结合语义意图定位、无地图长程规划、安全感知推理和底层动作生成，Walk with Me 实现了面向以人为中心辅助的实用户外社交导航。

## Abstract
Assisting humans in open-world outdoor environments requires robots to translate high-level natural-language intentions into safe, long-horizon, and socially compliant navigation behavior. Existing map-based methods rely on costly pre-built HD maps, while learning-based policies are mostly limited to indoor and short-horizon settings. To bridge this gap, we propose Walk with Me, a map-free framework for long-horizon social navigation from high-level human instructions. Walk with Me leverages GPS context and lightweight candidate points-of-interest from a public map API for semantic destination grounding and waypoint proposal. A High-Level Vision-Language Model grounds abstract instructions into concrete destinations and plans coarse waypoint sequences. During execution, an observation-aware routing mechanism determines whether the Low-Level Vision-Language-Action policy can handle the current situation or whether explicit safety reasoning from the High-Level VLM is needed. Routine segments are executed by the Low-Level VLA, while complex situations such as crowded crossings trigger high-level reasoning and stop-and-wait behavior when unsafe. By combining semantic intent grounding, map-free long-horizon planning, safety-aware reasoning, and low-level action generation, Walk with Me enables practical outdoor social navigation for human-centric assistance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Walk With Me: Long-Horizon Social Navigation for Human-Centric Outdoor Assistance》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在不依赖昂贵、难以维护的高精地图（HD Map）的情况下，让机器人在复杂的户外开放世界中，根据人类模糊的自然语言指令（如“我想去散步”），实现长程（公里级）且符合社交规范（如安全过马路、避让行人）的导航。
*   **研究动机**：
    *   **现有方法局限**：传统导航依赖高精地图，成本高且缺乏语义理解；学习类方法（如端到端模型）多局限于室内或短程任务。
    *   **应用需求**：以人为中心的辅助场景（如“最后一公里”配送、盲人引导）急需一种能理解高层意图并具备长程规划能力的系统。

### 2. 论文提出的方法论
该论文提出了一个名为 **Walk with Me** 的分层框架，核心思想是**将“去哪里（语义规划）”与“怎么走（局部执行）”解耦**。

*   **高层语义规划（High-Level Semantic Planning）**：
    *   **意图落地**：利用高层视觉语言模型（VLM）结合当前 GPS 上下文，通过公共地图 API（如百度/高德/谷歌地图）查询候选兴趣点（POI），将模糊指令转化为具体目的地。
    *   **路径点生成**：调用公共地图的步行导航 API 获取粗略路径，并将其重采样为一系列地理坐标路径点（Waypoints）。
*   **自适应双策略导航（Adaptive Dual-Policy Navigation）**：
    *   **观测感知路由**：系统实时构建环境观测 $o_t$（包含图像、位姿、历史轨迹、目标路径点）。
    *   **联合决策**：高层 VLM 充当“大脑”，判断当前场景是“常规（Routine）”还是“复杂（Complex）”。
    *   **安全推理**：在路口或拥挤区域，VLM 进行显式安全推理，决定是“继续（Proceed）”还是“停止等待（Stop-and-wait）”。
*   **底层动作执行（Low-Level VLA Execution）**：
    *   对于常规路段，调用底层视觉-语言-动作模型（VLA）生成局部社交合规轨迹。
    *   结合 SLAM 反馈进行闭环控制，确保机器人平滑跟踪目标。

### 3. 实验设计
*   **实验场景**：
    1.  **最后一公里配送**：包括“送奶茶到 B 楼”和“送包裹到 A 楼”。
    2.  **盲人引导**：包括“带我去散步”和“带我去购物”。
*   **Benchmark 与对比方法**：
    *   **主要指标**：成功率（Success Rate, SR）。
    *   **消融实验对比**：
        *   **高层 VLM 对比**：Claude-Sonnet, Gemini-3-Flash, GPT-5, Qwen3-VL-8B, RoboBrain 2.0, MiMo-Embodied。
        *   **底层 VLA 对比**：GNM, ViNT, NoMaD, CityWalker, SocialNav。
*   **实地测试**：在真实的户外街道环境中进行了 20 次独立的长程试验。

### 4. 资源与算力
*   **硬件平台**：Athena 2.0 Pro AGV 轮式机器人，配备 Intel RealSense D455 摄像头、GPS 和 SLAM 模块。
*   **算力支持**：
    *   **推理端**：使用了远程推理服务器，配备 **NVIDIA H20 GPU**。
    *   **部署方式**：由于板载算力限制，VLM 和 VLA 的推理均在远程服务器上完成。
*   **训练时长**：文中未明确说明底层模型的具体训练时长，主要侧重于系统集成与实地推理评估。

### 5. 实验数量与充分性
*   **实验规模**：
    *   核心实验包含 20 次实地长程测试（每个场景 5 次）。
    *   消融实验针对 VLM 和 VLA 分别进行了 10 次测试。
*   **充分性评价**：
    *   **优点**：实验涵盖了从语义理解到物理执行的全流程，且在真实、动态的户外环境中进行，具有较高的实用参考价值。
    *   **不足**：20 次实地测试虽然在机器人领域属于标准规模，但对于统计显著性而言样本量仍偏小。此外，实验主要集中在特定园区/街道，对极端天气或极高密度人群的覆盖有限。

### 6. 论文的主要结论与发现
*   **系统有效性**：Walk with Me 实现了 60% 的平均任务成功率，证明了利用公共地图 API 代替高精地图进行长程导航的可行性。
*   **分层优势**：高层 VLM 在处理路口安全和语义意图方面表现出色，而专门训练的社交导航模型（如 SocialNav）在底层避障和局部路径跟随上优于通用导航模型。
*   **安全机制**：引入“停止并等待”的显式安全推理显著提升了机器人在复杂社交场景（如红绿灯路口）的安全性。

### 7. 优点（亮点）
*   **无地图化（Map-free）**：摆脱了对高精地图的依赖，极大地降低了部署成本和维护难度。
*   **分层路由机制**：巧妙地平衡了高层语义推理的“慢思考”与底层动作控制的“快反应”。
*   **端到端语义理解**：能够处理“我想去散步”这类高度抽象的人类指令，并将其转化为物理世界的具体坐标。

### 8. 不足与局限
*   **依赖外部服务**：系统高度依赖公共地图 API 和 GPS 信号，在 GPS 信号弱（如高楼林立区）或地图数据过时的区域可能失效。
*   **延迟风险**：由于依赖远程服务器推理（H20 GPU），网络延迟可能影响实时避障性能。
*   **安全边界**：虽然有 VLM 推理，但在极端复杂的交通流中，仅靠视觉判断安全性仍存在风险，缺乏多传感器冗余（如激光雷达的长距检测）。
*   **泛化性限制**：实验场景相对单一，未探讨在不同国家/地区交通规则差异下的适应性。

（完）
