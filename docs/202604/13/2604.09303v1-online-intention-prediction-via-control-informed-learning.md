---
title: Online Intention Prediction via Control-Informed Learning
title_zh: 基于控制引导学习的在线意图预测
authors: "Tianyu Zhou, Zihao Liang, Zehui Lu, Shaoshuai Mou"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09303v1"
tags: ["query:tp"]
score: 9.0
evidence: 自主系统目标状态估计的在线意图预测
tldr: 本文提出一种针对自主系统的在线意图预测框架，旨在实时估计随时间变化的目标状态，即使在动力学参数未知的情况下也能有效工作。研究将意图预测建模为逆最优控制问题，引入移位时域策略以淡化过时信息，并利用控制知情学习实现高效的梯度计算与在线参数更新。通过仿真和四旋翼无人机实验验证，该方法在复杂环境下展现出高精度和强自适应性。
selection_source: fresh_fetch
motivation: 针对自主系统在动态环境和参数未知时，难以实时且准确预测随时间变化的意图目标这一挑战。
method: 将意图预测转化为逆最优控制问题，采用移位时域策略和控制知情学习算法进行高效的在线参数估计。
result: 仿真与四旋翼无人机硬件实验证明，该方法在多种噪声水平和复杂场景下均能实现精准的意图预测。
conclusion: 该研究为复杂动态系统提供了一种高效、实时的在线意图估计方案，显著提升了系统的自适应能力。
---

## 摘要
本文提出了一种在线意图预测框架，用于实时估计自主系统的目标状态，即使在意图随时间变化且系统动力学或目标函数包含未知参数的情况下也能有效工作。该问题被建模为逆最优控制/逆强化学习任务，并将意图视为目标函数中的一个参数。通过移动时界策略折减过时信息，同时利用在线控制引导学习实现高效的梯度计算和在线参数更新。在不同噪声水平下的仿真以及四旋翼无人机上的硬件实验表明，所提方法在复杂环境中实现了准确且具有自适应性的意图预测。

## Abstract
This paper presents an online intention prediction framework for estimating the goal state of autonomous systems in real time, even when intention is time-varying, and system dynamics or objectives include unknown parameters. The problem is formulated as an inverse optimal control / inverse reinforcement learning task, with the intention treated as a parameter in the objective. A shifting horizon strategy discounts outdated information, while online control-informed learning enables efficient gradient computation and online parameter updates. Simulations under varying noise levels and hardware experiments on a quadrotor drone demonstrate that the proposed approach achieves accurate, adaptive intention prediction in complex environments.

---

## 论文详细总结（自动生成）

以下是对论文《Online Intention Prediction via Control-Informed Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
意图预测旨在根据观察到的轨迹推断自主代理（如机器人或人类）的未来目标。现有方法主要面临三大挑战：
*   **物理模型依赖性：** 物理方法计算快但依赖精确动力学模型，难以应对现实中的扰动。
*   **学习方法局限性：** 深度学习方法通常需要大量离线数据，且在训练分布外泛化能力差，计算成本高。
*   **动态环境适应性：** 现有研究多假设目标是固定或离散的，难以处理**随时间变化（Time-varying）的目标**以及**动力学/目标函数参数未知**的复杂实时场景。
本文旨在提出一种在线框架，在参数未知且目标动态切换的情况下，实现连续空间内的实时意图估计。

### 2. 方法论
该研究将意图预测建模为**逆最优控制（IOC）/ 逆强化学习（IRL）**问题，核心步骤如下：
*   **参数化建模：** 将未知目标状态 $x_g$、动力学参数 $p$ 以及代价函数权重 $\omega$ 统一视为待估计的参数向量 $\theta$。
*   **移位时域策略（Shifting Horizon Strategy）：** 引入一个有限长度的记忆缓冲区（Memory Buffer）。通过只保留最近的观测数据，算法能够“淡化”过时的目标信息，从而快速适应意图的切换。
*   **在线控制引导学习（OCIL）：** 采用一种类似于扩展卡尔曼滤波（EKF）的在线更新律，根据预测轨迹与实际观测之间的残差实时修正参数估计。
*   **庞特里亚金微分编程（PDP）：** 利用 PDP 技术在最优控制问题的求解过程中高效计算状态对参数的梯度（Jacobian 矩阵），这是实现实时在线更新的关键。

### 3. 实验设计
*   **实验场景：** 主要针对四旋翼无人机（Quadrotor）进行仿真和硬件实验。
*   **对比基准（Benchmark）：** 
    *   对比了不同噪声水平（$\sigma$ 从 0 到 0.5）下的预测表现。
    *   对比了**标准 OCIL 算法**（不带移位时域策略），以验证处理目标切换的能力。
*   **实验设置：** 
    *   **仿真：** 100 次随机试验，包含目标不切换、两次切换及三次切换的场景。
    *   **硬件：** 在 5m x 3m 的室内空间，利用动捕系统获取无人机状态，验证算法在真实物理环境下的实时性。

### 4. 资源与算力
*   **硬件平台：** 仿真在 Intel i9-14900K CPU 上运行。
*   **算力表现：** 
    *   平均每次更新的预测时间约为 **60 ± 4 ms**（仿真）和 **92.3 ms**（硬件实验）。
    *   由于算法步长设定为 150ms 或 100ms，该计算速度满足**实时性（Real-time）**要求。
*   **训练时长：** 该方法属于在线学习（Online Learning），无需离线预训练过程。

### 5. 实验数量与充分性
*   **实验规模：** 针对每种噪声水平进行了 100 组随机试验，并提供了均值和标准差（阴影区域），数据具有统计学意义。
*   **消融/对比：** 通过与不带移位时域的 OCIL 对比，清晰展示了该策略在应对“意图切换”时的必要性。
*   **客观性：** 实验涵盖了从理想仿真到高噪声环境，再到真实硬件约束的完整链路，验证较为充分且客观。

### 6. 主要结论与发现
*   **高精度：** 即使在动力学参数（如质量、转动惯量）未知的情况下，算法也能准确收敛至真实目标。
*   **快速自适应：** 移位时域策略使系统在目标切换后，预测误差能迅速出现峰值并随即下降，证明了其对动态意图的追踪能力。
*   **鲁棒性：** 在高测量噪声（$\sigma=0.5$）下，算法依然能保持稳定的预测轨迹。
*   **实时性：** 算法复杂度适中，能够在低功耗嵌入式设备或常规 CPU 上实时运行。

### 7. 优点
*   **无需预定义目标集：** 与许多将意图预测视为“多选一”分类任务的方法不同，本文在连续空间内估计目标，适用范围更广。
*   **物理与学习结合：** 结合了最优控制的严谨性和在线学习的灵活性，减少了对精确先验模型的依赖。
*   **动态适应：** 移位时域策略巧妙地解决了在线学习中“历史数据干扰当前判断”的矛盾。

### 8. 不足与局限
*   **全状态观测假设：** 实验假设可以获得完整且噪声较小的状态测量，未来需进一步研究在部分观测或非线性输出测量下的表现。
*   **计算复杂度：** 虽然目前在无人机上可行，但对于状态维度极高或动力学极其复杂的系统，PDP 的梯度计算量可能会显著增加。
*   **观测噪声限制：** 算法依赖于测量噪声较小的假设，在极端恶劣的传感器环境下可能失效。

（完）
