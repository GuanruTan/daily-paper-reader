---
title: "ActivityEditor: Learning to Synthesize Physically Valid Human Mobility"
title_zh: ActivityEditor：学习合成物理有效的人类移动性
authors: "Chenjie Yang, Yutian Jiang, Anqi Liang, Wei Qi, Chenyu Wu, Junbo Zhang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05529v2"
tags: ["query:tp"]
score: 8.0
evidence: 基于意图的智能体用于移动轨迹生成
tldr: 针对城市应用中人类移动轨迹数据稀缺的问题，本文提出ActivityEditor框架。该框架采用双LLM智能体协作模式：意图智能体基于人口统计学先验生成粗略活动链，编辑智能体通过强化学习结合物理约束对轨迹进行迭代优化。该方法实现了零样本跨区域轨迹合成，在保持高统计保真度和物理有效性的同时，为数据匮乏地区的移动模拟提供了强泛化性的解决方案。
selection_source: fresh_fetch
motivation: 现有的数据驱动方法受限于数据稀缺，难以在缺乏历史轨迹的地区进行有效的移动建模。
method: 提出双LLM智能体框架，通过意图智能体生成社会语义一致的活动链，并利用强化学习训练的编辑智能体依据物理规律迭代修正轨迹。
result: 实验表明，ActivityEditor在跨城市零样本迁移中表现优异，生成的轨迹具有极高的物理有效性和统计保真度。
conclusion: 该研究为解决移动建模中的数据孤岛问题提供了一种具备物理感知能力且高度可泛化的生成式方案。
---

## 摘要
人类移动性建模对于各种城市应用至关重要。然而，现有的数据驱动方法通常面临数据稀缺问题，限制了它们在历史轨迹不可用或受限地区的适用性。为了弥补这一差距，我们提出了 ActivityEditor，这是一个专为零样本跨区域轨迹生成设计的新型双大语言模型（LLM）智能体框架。我们的框架将复杂的合成任务分解为两个协作阶段。具体而言，基于意图的智能体利用人口统计驱动的先验知识生成结构化的人类意图和粗粒度的活动链，以确保高层级的社会语义一致性。随后，编辑智能体通过强制执行人类移动规律的迭代修订来细化这些输出，从而获得移动轨迹。这种能力是通过基于真实物理约束的多重奖励强化学习获得的，使智能体能够内化移动规律并确保高保真轨迹的生成。大量实验表明，ActivityEditor 在跨不同城市环境迁移时表现出卓越的零样本性能。它保持了高统计保真度和物理有效性，为数据稀缺场景下的移动性模拟提供了一种鲁棒且具有高度泛化能力的解决方案。我们的代码可在以下地址获取：https://anonymous.4open.science/r/ActivityEditor-066B。

## Abstract
Human mobility modeling is indispensable for diverse urban applications. However, existing data-driven methods often suffer from data scarcity, limiting their applicability in regions where historical trajectories are unavailable or restricted. To bridge this gap, we propose \textbf{ActivityEditor}, a novel dual-LLM-agent framework designed for zero-shot cross-regional trajectory generation. Our framework decomposes the complex synthesis task into two collaborative stages. Specifically, an intention-based agent, which leverages demographic-driven priors to generate structured human intentions and coarse activity chains to ensure high-level socio-semantic coherence. These outputs are then refined by editor agent to obtain mobility trajectories through iteratively revisions that enforces human mobility law. This capability is acquired through reinforcement learning with multiple rewards grounded in real-world physical constraints, allowing the agent to internalize mobility regularities and ensure high-fidelity trajectory generation. Extensive experiments demonstrate that \textbf{ActivityEditor} achieves superior zero-shot performance when transferred across diverse urban contexts. It maintains high statistical fidelity and physical validity, providing a robust and highly generalizable solution for mobility simulation in data-scarce scenarios. Our code is available at: https://anonymous.4open.science/r/ActivityEditor-066B.

---

## 论文详细总结（自动生成）

### ActivityEditor: 学习合成物理有效的人类移动性 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
人类移动性建模（Human Mobility Modeling）是城市规划、交通管理和公共安全的基础。然而，现有的数据驱动方法面临三大挑战：
*   **数据稀缺与隐私限制**：获取大规模、高精度的个人轨迹数据日益困难。
*   **冷启动问题**：在新规划的城市区域，缺乏历史轨迹数据，传统模型无法工作。
*   **物理一致性缺失**：现有的生成模型（包括基于大语言模型的方法）往往缺乏对物理规律（如24小时覆盖、时间不重叠、活动逻辑顺序）的深度理解，容易产生“幻觉”轨迹。

本论文提出了 **ActivityEditor**，旨在通过双 LLM 智能体框架，在**零样本（Zero-shot）**条件下，仅依据人口统计学特征（如年龄、职业、收入）合成既符合社会语义逻辑又满足物理约束的高保真人类移动轨迹。

#### 2. 方法论：核心思想与技术细节
ActivityEditor 采用“**先生成意图，再迭代编辑**”的核心思想，将任务分解为两个阶段：

*   **意图驱动智能体 (Intention-driven Agent)**：
    *   **核心思想**：利用 LLM 作为“世界模型”，将人口统计学属性映射为人类意图链（如：上班 -> 午餐 -> 购物 -> 回家）。
    *   **功能**：生成一个粗略的活动草案（Sdraft），确保高层级的社会语义一致性（例如：退休人员不应有规律的通勤）。

*   **编辑智能体 (Editor Agent)**：
    *   **核心思想**：作为一个规则感知的审计者，通过“增加、删除、平移、替换、拆分”五种操作，对草案进行迭代修正。
    *   **关键技术 - GRPO 强化学习**：为了让智能体自主内化物理规律，研究者使用了**群体相对策略优化（Group Relative Policy Optimization, GRPO）**。
    *   **奖励函数设计**：
        1.  **格式奖励 ($R_{fmt}$)**：确保输出符合预定义的 JSON 结构和思维链（CoT）格式。
        2.  **约束奖励 ($R_{con}$)**：强制执行硬约束（24小时全覆盖、时间无缝衔接、无重叠）和软约束（活动时长符合常识）。
        3.  **保真度奖励 ($R_{sim}$)**：通过计算生成轨迹与真实分布之间的 Jensen-Shannon 散度（JSD），确保统计特征（活动类型分布、时长分布）的真实性。

#### 3. 实验设计
*   **数据集**：使用 2017 年美国全国住户旅行调查（NHTS），提取了加利福尼亚州（CA）、亚利桑那州（AZ）、佐治亚州（GA）和俄克拉荷马州（OK）四个子集，涵盖了不同规模和城市特征。
*   **Benchmark 与对比方法**：
    *   **传统深度学习**：DeepMove, LSTPM（依赖历史序列）。
    *   **特定 LLM 框架**：CoPB（基于计划行为理论）, RAGHome（检索增强生成）。
    *   **通用 LLM**：ChatGPT-5.2, Qwen3-8B, LLaMA3.1-8B（单轮生成 vs. 多智能体模式）。
*   **评估指标**：涵盖序列相似性（Acc, F1）、时间对齐度（Mint）和分布一致性（Atype, JSD）等 12 个维度。

#### 4. 资源与算力
*   **模型基础**：使用 Qwen3-8B 作为学生模型，GPT-5.2 作为教师模型生成 CoT 训练数据。
*   **训练细节**：
    *   **SFT 阶段**：LoRA 微调（r=32, α=64），学习率 5e-5，训练 3 个 epoch。
    *   **GRPO 阶段**：训练 1000 步，学习率 1e-5，每个 Prompt 进行 8 次采样（Roll-out）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但从 8B 参数量及 GRPO 的采样需求推断，通常需要 4-8 张 A100/H100 级别的显卡支持。

#### 5. 实验数量与充分性
*   **实验规模**：在四个不同地理特征的州进行了测试，并进行了跨州（如 CA -> AZ）的零样本迁移实验。
*   **消融实验**：对比了单智能体 vs. 双智能体架构、SFT vs. GRPO 训练策略的效果。
*   **充分性评价**：实验设计较为充分，涵盖了从微观轨迹准确性到宏观统计分布的全面评估。跨区域迁移实验有力地证明了模型学习的是“移动逻辑”而非简单的“地域记忆”，实验过程客观公平。

#### 6. 主要结论与发现
*   **零样本能力卓越**：ActivityEditor 在完全未见过目标城市轨迹的情况下，性能显著优于需要本地数据的传统模型和单轮生成的 LLM。
*   **物理规律内化**：通过 GRPO 训练，编辑智能体能够有效识别并修复时间重叠、逻辑冲突等物理错误，显著提升了轨迹的有效性。
*   **跨区域通用性**：人类的移动意图与人口统计学特征之间的映射具有普适性，这使得模型在不同城市间具有极强的迁移能力。

#### 7. 优点与亮点
*   **架构创新**：将复杂的轨迹合成解耦为“意图生成”与“规则编辑”，模仿了人类“先计划、再调整”的认知过程。
*   **强化学习对齐**：引入 GRPO 算法，无需人工标注偏好，通过程序化规则奖励实现了模型与物理规律的精准对齐。
*   **解决冷启动**：为缺乏数据的城市区域提供了一种低成本、高保真的模拟方案。

#### 8. 不足与局限
*   **空间粒度受限**：目前仅能生成区域级（TAZ）的活动链，无法精确到具体的 POI 坐标（经纬度）。
*   **计算开销**：多智能体迭代和强化学习推理过程比单次生成更耗时，大规模人群模拟时可能存在效率瓶颈。
*   **幻觉风险**：尽管有规则约束，LLM 仍可能生成现实中不存在的地址或逻辑（如“幻影”地点）。
*   **依赖教师模型**：训练数据的质量高度依赖于 GPT-5.2 生成的推理链条。

（完）
