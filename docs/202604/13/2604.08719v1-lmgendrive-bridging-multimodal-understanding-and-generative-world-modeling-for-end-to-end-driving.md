---
title: "LMGenDrive: Bridging Multimodal Understanding and Generative World Modeling for End-to-End Driving"
title_zh: LMGenDrive：融合多模态理解与生成式世界建模的端到端驾驶框架
authors: "Hao Shao, Letian Wang, Yang Zhou, Yuxuan Hu, Zhuofan Zong, Steven L. Waslander, Wei Zhan, Hongsheng Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08719v1"
tags: ["query:tp"]
score: 9.0
evidence: 基于大模型的多模态理解用于驾驶世界建模
tldr: LMGenDrive是首个将基于LLM的多模态理解与生成式世界模型相结合的端到端闭环自动驾驶框架。它通过多视角相机输入和自然语言指令，同时生成未来驾驶视频和控制信号。该模型利用LLM的语义先验和世界模型的时空建模能力，显著提升了在长尾场景下的泛化性与鲁棒性，并在闭环基准测试中表现优异。
selection_source: fresh_fetch
motivation: 旨在解决自动驾驶在长尾和开放世界场景中泛化能力不足的问题，通过统一理解与想象能力来提升决策鲁棒性。
method: 提出一种结合LLM与生成式世界模型的架构，并采用从视觉预训练到长时驾驶的渐进式三阶段训练策略。
result: 在挑战性的闭环基准测试中显著超越前序方法，在指令遵循、时空理解和罕见场景处理方面表现出明显优势。
conclusion: 研究表明统一多模态理解与生成是构建更具泛化性和鲁棒性的具身智能决策系统的有效途径。
---

## 摘要
近年来，自动驾驶取得了显著进展，但对长尾和开放世界场景的泛化能力仍是大规模部署的主要瓶颈。为应对这一挑战，一些研究利用大语言模型（LLM）和视觉语言模型（VLM）进行视觉语言理解与推理，使车辆在生成动作时能够解释罕见且安全关键的情况。另一些研究则探讨生成式世界模型，以捕捉驾驶场景的时空演变，使智能体能够在行动前想象可能的未来。受人类智能统一理解与想象力的启发，我们探索了一种用于自动驾驶的统一模型。我们提出了 LMGenDrive，这是首个将基于 LLM 的多模态理解与生成式世界模型相结合的端到端闭环驾驶框架。在给定多视图摄像头输入和自然语言指令的情况下，LMGenDrive 同时生成未来驾驶视频和控制信号。这种设计提供了互补的优势：视频预测改进了时空场景建模，而 LLM 则通过大规模预训练贡献了强大的语义先验和指令对齐（instruction grounding）。我们进一步提出了一种渐进式三阶段训练策略，从视觉预训练到多步长时程驾驶，以提高稳定性和性能。LMGenDrive 同时支持低延迟在线规划和自回归离线视频生成。实验表明，该方法在具有挑战性的闭环基准测试中显著优于现有方法，在指令遵循、时空理解以及对罕见场景的鲁棒性方面有明显提升。这些结果表明，统一多模态理解与生成是构建更具泛化性和鲁棒性的具身决策系统的一个极具前景的方向。

## Abstract
Recent years have seen remarkable progress in autonomous driving, yet generalization to long-tail and open-world scenarios remains a major bottleneck for large-scale deployment. To address this challenge, some works use LLMs and VLMs for vision-language understanding and reasoning, enabling vehicles to interpret rare and safety-critical situations when generating actions. Others study generative world models to capture the spatio-temporal evolution of driving scenes, allowing agents to imagine possible futures before acting. Inspired by human intelligence, which unifies understanding and imagination, we explore a unified model for autonomous driving. We present LMGenDrive, the first framework that combines LLM-based multimodal understanding with generative world models for end-to-end closed-loop driving. Given multi-view camera inputs and natural-language instructions, LMGenDrive generates both future driving videos and control signals. This design provides complementary benefits: video prediction improves spatio-temporal scene modeling, while the LLM contributes strong semantic priors and instruction grounding from large-scale pretraining. We further propose a progressive three-stage training strategy, from vision pretraining to multi-step long-horizon driving, to improve stability and performance. LMGenDrive supports both low-latency online planning and autoregressive offline video generation. Experiments show that it significantly outperforms prior methods on challenging closed-loop benchmarks, with clear gains in instruction following, spatio-temporal understanding, and robustness to rare scenarios. These results suggest that unifying multimodal understanding and generation is a promising direction for more generalizable and robust embodied decision-making systems.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《LMGenDrive: Bridging Multimodal Understanding and Generative World Modeling for End-to-End Driving》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：自动驾驶在大规模部署中面临的主要瓶颈是**长尾场景（Long-tail cases）**和**开放世界**的泛化能力。
*   **研究现状**：
    *   **理解派**：利用 LLM/VLM 的推理能力解释复杂场景，但缺乏对环境时空演化的建模。
    *   **生成派**：利用世界模型（World Models）预测未来，但通常缺乏语义先验和指令遵循能力。
*   **整体含义**：受人类智能中“理解”与“想象”统一的启发，本文提出了 **LMGenDrive**。这是首个将 **基于 LLM 的多模态理解** 与 **生成式世界模型** 统一在单个架构中的端到端闭环驾驶框架，旨在通过“边理解边想象”来提升决策的鲁棒性。

### 2. 方法论
核心思想是将 LLM 作为“大脑”负责推理，将扩散模型作为“想象力”负责模拟未来。
*   **核心组件**：
    1.  **视觉编码器（Vision Encoder）**：将多视角图像转换为 BEV（鸟瞰图）Token、路径点 Token 和红绿灯 Token。
    2.  **大语言模型（LLM）**：采用 Vicuna-7B。通过 Q-Former 压缩视觉 Token，并结合导航指令、动作查询（Action Query）和世界查询（World Query）进行统一推理。
    3.  **多视角世界生成器（World Generator）**：基于 Stable Diffusion 1.5 和 AnimateDiff。利用 LLM 输出的 World Query 作为条件，生成时空一致的多视角未来视频。
*   **关键技术细节**：
    *   **双查询机制**：Action Query 用于预测未来路径点；World Query 捕捉场景动态，作为扩散模型的条件信号。
    *   **双模式运行**：支持**在线规划模式**（丢弃生成器以降低延迟）和**离线数据生成模式**（自回归生成长时视频）。
*   **算法流程（三阶段训练策略）**：
    *   **阶段 1**：视觉编码器预训练（感知任务，如目标检测、红绿灯识别）。
    *   **阶段 2**：单步规划与生成微调（LLM 学习指令遵循，生成器学习单步视频合成）。
    *   **阶段 3**：多步长时程训练（自回归训练，增强时空一致性和长时推理能力）。

### 3. 实验设计
*   **实验环境**：CARLA 0.9.10.1 模拟器。
*   **Benchmark**：**LangAuto** 基准测试，包含三个子集：LangAuto（标准）、LangAuto-Short（短程）、LangAuto-Tiny（微型）。
*   **对比方法**：
    *   **LMDrive**（基于 LLM 的闭环驾驶基准）。
    *   **AD-H**（分层智能体方法）。
    *   **BEVDriver**（利用 BEV 地图增强 LLM 的方法）。
*   **评估指标**：
    *   驾驶性能：驾驶得分（DS）、路径完成率（RC）、违规得分（IS）。
    *   生成质量：FVD（视频距离）、FID（图像距离）。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA H800 GPU**。
*   **训练时长**：在 DeepSpeed ZeRO-2 优化下，整个流程约需 **2 天** 完成收敛。
*   **参数规模**：LLM 部分为 7B 规模，结合了 Stable Diffusion 1.5。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在三个不同难度的 LangAuto 轨道上进行了多次运行（报告了均值和标准差）。
    *   **消融实验**：针对“世界生成器”、“动作查询”、“视觉预训练”和“第三阶段训练”分别做了移除对比，验证了各模块的必要性。
    *   **生成质量实验**：测试了不同数量的 World Query 对视频质量的影响。
    *   **长时程缩放实验**：测试了从 8 帧到 128 帧的自回归生成稳定性。
*   **充分性评价**：实验设计较为充分，不仅涵盖了闭环驾驶的安全性指标，还通过感知指标评估了生成模型的质量。对比实验选择了当前最先进的同类模型，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能突破**：LMGenDrive 在 LangAuto 榜单上取得了 **62.2 的 DS 得分**，远超 LMDrive (10.7) 和 BEVDriver (48.9)。
*   **互补效应**：实验证明，引入视频生成任务能显著提升 LLM 对时空动态的理解，从而反哺规划任务，使其在处理罕见长尾场景时更鲁棒。
*   **长时建模**：第三阶段的多步训练对于长距离驾驶至关重要，能有效减少闭环控制中的累积误差。

### 7. 优点（亮点）
*   **架构统一**：首次实现了 LLM 推理与扩散模型生成的深度耦合，而非简单的模块堆叠。
*   **闭环验证**：不同于许多只做开环预测的世界模型，该模型在 CARLA 中完成了严格的闭环交互测试。
*   **训练策略科学**：提出的三阶段渐进式训练有效解决了多模态大模型在复杂任务中难以收敛的问题。

### 8. 不足与局限
*   **误差累积**：在长时程视频生成（如 64 帧以上）中，仍存在明显的时空漂移和运动不一致现象（FVD 显著上升）。
*   **模拟器偏差**：所有实验均在 CARLA 模拟器中完成，尚未在真实世界数据集（如 nuScenes 或 Waymo）上验证其生成真实感和迁移能力。
*   **实时性挑战**：虽然提供了在线规划模式，但 LLM+多视角视觉编码器的组合在车载端侧设备的实时部署仍面临算力压力。

（完）
