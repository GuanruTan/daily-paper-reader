---
title: Trajectory-Based Optimization for Air Traffic Control in the Terminal Maneuvering Area
title_zh: 终端管制区空中交通管制的基于轨迹优化框架
authors: "Yutian Pang, Daniel Delahaye, John-Paul Clarke"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17776v1"
tags: ["query:tp"]
score: 8.0
evidence: 基于轨迹的飞机进场排序和路径建模优化
tldr: 本研究提出一种基于轨迹的终端管制区（TMA）进场排序与调度优化框架。不同于传统的节点链路模型，该方法通过非线性规划（NLP）和混合整数线性规划（MILP）协同优化每架飞机的速度剖面与路径扩展，以满足尾流间隔要求。研究对比了FEFS、FOFFS及CPS三种排序策略，并考虑了真实机型组合与风力干扰。实验证明，该框架能在滚动时域内实现近实时决策，显著降低延迟与燃油消耗，为自动化空中交通管制提供了可行方案。
selection_source: fresh_fetch
motivation: 传统调度模型将轨迹简化为时间延迟变量，难以生成包含具体速度指令和路径扩展的可执行飞行剖面。
method: 结合解析路径模型与非线性规划，通过两阶段法（MILP确定顺序，NLP优化轨迹）在滚动时域内进行实时调度。
result: FOFFS策略在延迟和燃油效率上显著优于FEFS，而CPS策略能进一步减少间隔违规，且计算效率满足实时部署需求。
conclusion: 该框架通过协同优化路径与速度，有效提升了终端区进场效率与安全性，是实现自动化空中交通管制的有效工具。
---

## 摘要
我们提出了一种用于终端管制区（TMA）进场排序与调度的基于轨迹的优化框架。与将轨迹简化为时延变量的节点-链路调度模型不同，所提方法计算可执行的单机速度剖面和路径扩展，通过终端空中交通管制动作实现所需的着陆间隔。该框架结合了一个解析 TMA 路径模型（由切线段、径向固定转弯段和最后进近段组成）与非线性规划（NLP），在加权目标下联合优化路径拉伸和各段速度。研究了三种着陆顺序策略：先入先出（FEFS）、先到最后进近段先出（FOFFS）以及带有最多 $k$ 个位置约束位置移位（CPS）的 FOFFS。CPS 通过耦合混合整数线性规划（MILP）与 NLP 的两阶段方法实现，在轨迹优化前选择优化的着陆顺序。航空器群体遵循真实的重量等级机队组合，并具有特定机组对的尾流间隔；每个场景都受到投影到各航段的高斯风样本扰动，以将指令空速转换为地速。在线滚动时域公式在航空器进入时不可撤销地确定其轨迹，从而实现实时决策。在简化的 A80 TMA 上进行的蒙特卡洛实验表明：(i) 通过利用进场流之间的几何不对称性，FOFFS 在延迟、路径拉伸和燃油消耗方面始终优于 FEFS；(ii) CPS 进一步减少了间隔违规和路径拉伸，但收益递减且求解器成本迅速增加；(iii) 来自 BADA 3 和 OpenAP 的燃油估算显示出一致的定性趋势；(iv) 每次进入的优化在近实时内完成，支持实际部署。

## Abstract
We present a trajectory-based optimization framework for arrival sequencing and scheduling in the terminal maneuvering area (TMA). Unlike node-link scheduling models that reduce trajectories to time-delay variables, the proposed method computes implementable per-aircraft speed profiles and path extensions that achieve required landing separation through terminal air traffic control actions. The framework combines an analytic TMA path model, consisting of a tangent leg, a radius-to-fix turn, and a final-approach segment, with a nonlinear program (NLP) that jointly optimizes path stretch and segment speeds under a weighted objective. Three landing-order policies are examined: First-Entry-First-Serve (FEFS), First-on-Final-First-Serve (FOFFS), and FOFFS with Constrained Position Shifting (CPS) up to $k$ positions. CPS is implemented through a two-phase approach coupling mixed-integer linear programming (MILP) with NLP to select an optimized landing order before trajectory optimization. The aircraft population follows a realistic weight-class fleet mix with pair-specific wake-turbulence separation, and each scenario is perturbed by a Gaussian wind sample projected onto each segment to convert commanded airspeeds into ground speeds. An online rolling-horizon formulation commits each aircraft trajectory irrevocably upon entry, enabling real-time decision-making. Monte Carlo experiments on the simplified A80 TMA show that: (i) FOFFS consistently outperforms FEFS in delay, path stretch, and fuel burn by exploiting geometric asymmetries among arrival streams; (ii) CPS further reduces separation violations and path stretch, though with diminishing returns and rapidly increasing solver cost; (iii) fuel estimates from BADA 3 and OpenAP show consistent qualitative trends; and (iv) per-entry optimization completes in near real-time, supporting practical deployment.

---

## 论文详细总结（自动生成）

### 论文总结：终端管制区空中交通管制的基于轨迹优化框架

#### 1. 核心问题与整体含义（研究动机和背景）
终端管制区（TMA）是全球空中交通系统的主要瓶颈。传统的调度模型（如节点-链路模型）通常将飞行轨迹抽象为简单的时间延迟参数，无法生成包含具体速度指令和路径扩展（如底边延长线）的可执行飞行剖面。这导致地面调度指令与机载飞行管理系统（FMS）之间存在不一致。本研究旨在开发一种**基于轨迹的优化框架**，通过精确建模飞机的几何路径和速度剖面，在满足尾流间隔安全标准的同时，最大化跑道吞吐量并降低燃油消耗。

#### 2. 核心方法论
论文提出了一种耦合解析几何模型与数学规划的两阶段优化方法：
*   **解析路径模型**：将进场路径划分为三个航段：切线段（Tangent Leg）、径向固定转弯段（RF Turn）和最后进近段（Final Segment）。通过向量积推导出路径长度关于底边扩展距离 $d_i$ 的闭式非线性函数。
*   **两阶段决策架构**：
    *   **第一阶段（MILP）**：使用混合整数线性规划确定最优着陆顺序。引入**约束位置移位（CPS）**策略，允许飞机在一定范围（$k$ 个位置）内调整顺序以优化整体流。
    *   **第二阶段（NLP）**：在给定顺序下，使用非线性规划联合优化每架飞机的路径扩展距离和各段飞行速度。
*   **滚动时域调度**：采用在线触发机制，当飞机进入 TMA 边界时即刻锁定其轨迹（One-touch commitment），确保决策的实时性和确定性。
*   **不确定性建模**：引入高斯分布的风力模型，并将风速投影到飞机航向上，将指令空速转换为实际影响间隔的地速。

#### 3. 实验设计
*   **场景设置**：基于亚特兰大 A80 终端管制区（KATL 跑道 9R）的简化模型，设置了 4 个角点进场口。
*   **数据集/流量生成**：使用位移泊松过程生成不同密度的进场流（5-60 架次/小时），涵盖重型、大型、小型混合机队。
*   **对比策略（Benchmark）**：
    *   **FEFS**：先入先出（基于进入 TMA 的时间）。
    *   **FOFFS**：先到最后进近段先出（基于无延迟情况下的预计到达时间）。
    *   **FOFFS-CPS $k$**：在 FOFFS 基础上允许最多 $k$ 个位置的顺序调整（$k=1, 2, 3$）。
*   **离散化网格**：对比了不同的路径扩展步长（0.5/1.0 nmi）和速度步长（5/10 kts）。

#### 4. 资源与算力
*   **软件工具**：使用 Pyomo 建模，调用 **IPOPT** 求解非线性规划（NLP），调用 **GLPK** 求解混合整数线性规划（MILP）。
*   **算力说明**：文中未明确指出具体的 CPU/GPU 硬件型号，但提供了详细的**求解耗时分析**。实验结果显示，FEFS、FOFFS 及 CPS ($k \le 2$) 的平均单机计算时间均在 2 秒以内，证明了其在普通计算平台上实时运行的可行性。

#### 5. 实验数量与充分性
*   **实验规模**：进行了大规模蒙特卡洛模拟，包含 1,000 个需求种子，每个种子配合 10 个随机风样本，总计生成了约 **200,000 条运行记录**。
*   **充分性评价**：实验覆盖了从稀疏到极度拥堵的各种流量密度，并对路径/速度的离散化粒度进行了消融实验。通过 BADA 3 和 OpenAP 两种不同的性能模型对燃油消耗进行了交叉验证，实验设计非常充分、客观且具有统计学意义。

#### 6. 主要结论与发现
*   **策略优劣**：FOFFS 在延迟、路径拉伸和燃油效率上显著优于 FEFS，因为它能利用不同进场流之间的几何不对称性进行自然交织。
*   **CPS 收益**：CPS 策略能进一步减少间隔违规，其中 $k=1$ 收益最大，$k=2$ 达到平衡，而 $k=3$ 收益递减且计算成本激增。
*   **敏感性分析**：路径扩展的离散粒度（$\Delta d$）对优化结果的影响远大于速度粒度（$\Delta s$）。
*   **实时性**：该框架在滚动时域下表现出良好的扩展性，能够满足实际空中交通管制的实时决策需求。

#### 7. 优点与亮点
*   **闭式几何推导**：将复杂的终端区机动简化为可微分的解析函数，使 NLP 求解更加高效。
*   **现实约束完备**：考虑了特定机组对的尾流间隔、跑道占用时间、机型特定的速度限制以及风力干扰。
*   **操作可行性**：输出结果直接对应控制器可发布的指令（速度和辅助航点），而非抽象的时间戳。
*   **双模型验证**：使用两种主流航空性能模型验证燃油结论，增强了结果的可信度。

#### 8. 不足与局限
*   **垂直剖面解耦**：目前主要优化水平路径和速度，垂直剖面（如下降率、顶点位置）是预设的，未实现全 4D 轨迹的联合优化。
*   **单跑道限制**：研究集中在单跑道进场，未探讨多跑道协同或进离场交叉的情况。
*   **MILP 尾部延迟**：在极高密度下，当 $k=3$ 时，开源求解器 GLPK 可能会出现分支定界爆炸，导致计算时间显著增加。
*   **机动方式单一**：未考虑等待航线（Holding）或 S 型转弯等极端调配手段。

（完）
