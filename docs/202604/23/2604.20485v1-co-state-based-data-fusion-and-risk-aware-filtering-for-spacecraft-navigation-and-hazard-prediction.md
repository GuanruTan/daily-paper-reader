---
title: Co-State Based Data Fusion and Risk Aware Filtering for Spacecraft Navigation and Hazard Prediction
title_zh: 基于协态的数据融合与风险感知滤波，用于航天器导航与危险预测
authors: "Surya Ratna Prakash D, Soumyendu Raha"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20485v1"
tags: ["query:tp"]
score: 8.0
evidence: 利用共态和创新轨迹进行危险预测和行为模式转换预测
tldr: 本研究提出了一种基于协态（Co-state）的航天器导航与风险预测框架。该框架引入微分代数协态作为拉格朗日乘子，用于监测几何不一致性，并结合连续时间马尔可夫生成器实现概率风险预测。该方法无需预定义故障模型或启发式阈值，在月球着陆遥测数据验证中，能比传统扩展卡尔曼滤波（EKF）更早地检测到模型不一致性，为自主着陆系统提供了具有解释性的预警能力。
selection_source: fresh_fetch
motivation: 旨在解决航天器自主导航中传统滤波方法难以早期发现模型不一致性及缺乏概率风险评估的问题。
method: 引入微分代数协态作为几何不一致性信号，并利用协态与创新轨迹学习马尔可夫生成器以预测风险。
result: 在真实月球着陆数据测试中，该框架比传统EKF更早地识别出结构性模型不一致，且风险指标与物理失效高度相关。
conclusion: 该框架统一了几何投影、随机推理与风险评估，为航天器自主着陆提供了无需先验故障模型的早期预警方案。
---

## 摘要
本文开发了一种基于协态的融合框架，用于航天器导航、一致性监测和危险预报。引入微分代数协态作为瞬时拉格朗日乘子，在微分层面强制执行测量动力学相容性，并提供具有物理可解释性的几何不一致信号。在更长的时间尺度上，利用协态和创新轨迹来学习控制粗粒度行为状态之间转换的连续时间马尔可夫生成器，通过模式概率和平均首次到达时间（MFPT）实现内在的概率风险预测。由此产生的架构在单一在线流水线中统一了几何投影、随机推理和概率风险评估，无需预定义的故障模型、标记的失效数据或启发式阈值。该框架在真实的月球动力下降遥测数据上进行了演示，其检测到结构性内部模型不一致的时间显著早于扩展卡尔曼滤波（EKF）中的物理发散或统计不一致。结果表明，几何不一致性、随机漂移和概率风险在失效前协同上升，为自主着陆系统提供了具有可解释性和操作意义的早期预警能力。

## Abstract
This paper develops a co-state based fusion frame work for spacecraft navigation, consistency monitoring, and hazard forecasting. A differential algebraic co-state is introduced as an instantaneous Lagrange multiplier that enforces measurement dynamics compatibility at the differential level and provides a physically interpretable signal of geometric inconsistency. On a longer time scale, co-state and innovation trajectories are used to learn a continuous time Markov generator governing transitions between coarse behavioural regimes, enabling intrinsic probabilistic risk forecasting through mode probabilities and mean first-passage time (MFPT). The resulting architecture unifies geometric projection, stochastic inference, and probabilistic risk assessment in a single online pipeline without requiring predefined fault models, labelled failure data, or heuristic thresholds. The framework is demonstrated on real lunar powered-descent telemetry, where it detects structural internal model inconsistency significantly earlier than physical divergence or statistical inconsistency in an Extended Kalman Filter (EKF). The results show that geometric inconsistency, stochastic drift, and probabilistic risk rise coherently prior to failure, yielding interpretable and operationally meaningful early-warning capability for autonomous landing systems.

---

## 论文详细总结（自动生成）

### 论文总结：基于协态的数据融合与风险感知滤波，用于航天器导航与危险预测

#### 1. 核心问题与整体含义（研究动机和背景）
航天器在动力下降和着陆（如月球着陆）过程中，面临复杂的动力学环境和有限的计算资源。传统的导航架构主要依赖扩展卡尔曼滤波（EKF）等随机状态估计方法。然而，这些方法仅在**统计意义**上强制执行测量与模型的一致性，当系统出现内部模型不一致（如软件建模错误、执行机构偏差）时，滤波器往往会通过增大不确定性来吸收误差，从而掩盖了早期的失效迹象。

本文的核心问题是如何在不依赖预定义故障模型或标记数据的情况下，**早期检测**航天器内部模型的几何不一致性，并将其转化为可量化的**概率风险预警**。

#### 2. 方法论
论文提出了一种统一了几何投影、随机推理和概率风险评估的在线框架：
*   **微分代数协态（Co-state）公式**：引入一个代数协态变量 $\lambda$ 作为瞬时拉格朗日乘子。它在微分层面将状态演化投影到与测量一致的切流形上。$\lambda$ 的大小直接量化了动力学模型与观测数据之间的**几何不一致性**。
*   **随机漂移-扩散与生成器学习**：在较长的时间尺度上，利用协态轨迹和创新（Innovation）信号，通过数据驱动的方式学习一个连续时间马尔可夫生成器（Markov Generator）。该生成器描述了系统在“标称”、“校正”和“危险”等粗粒度行为模式之间的转换。
*   **风险感知指标**：基于学习到的生成器，计算模式概率和**平均首次到达时间（MFPT）**。这些指标提供了关于系统何时可能进入危险状态的内在概率预测。
*   **算法流程**：
    1.  实时接收测量流，通过正则化逆运算更新状态和协态。
    2.  在线将特征向量（含协态模长、创新值等）聚类为不同模式。
    3.  估计模式间的转移强度，更新连续时间生成器 $L$。
    4.  预测模式概率并计算风险指标，可选地接入模型预测控制（MPC）进行闭环风险规避。

#### 3. 实验设计
*   **数据集/场景**：使用了两组真实的月球动力下降遥测数据（来自印度月球着陆任务，已匿名化）。
    *   **Lander-1**：一次失败的着陆任务，表现为内部模型或软件不一致。
    *   **Lander-2**：一次成功的自主着陆任务。
*   **Benchmark（基准）**：传统的扩展卡尔曼滤波（EKF）统计一致性监测（如残差/创新分析）。
*   **对比方法**：主要对比了协态不一致信号与 EKF 创新信号在检测失效时间点上的表现。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件型号（如 GPU/CPU）或训练时长。
*   但文中强调，该算法的每一步操作（代数投影和生成器更新）都是低维且常数时间的，计算开销与标准 EKF 相当甚至更低，因此非常适合在航天器星载处理器、FPGA 或 SoC 等受限算力平台上实时运行。

#### 5. 实验数量与充分性
*   **实验规模**：主要针对两个关键的真实任务案例进行了深入分析。
*   **充分性与客观性**：
    *   **优点**：使用了极高价值的真实飞行遥测数据，而非纯仿真数据，这使得实验结果具有极强的说服力和工程参考价值。
    *   **局限**：由于真实失效案例稀缺，实验仅涵盖了两个任务。虽然在时间序列点上数据非常密集，但从统计多样性（如不同类型的传感器失效、环境干扰等）来看，实验场景的覆盖面尚有提升空间。
    *   **公平性**：对比实验在相同的遥测输入下进行，客观展示了协态信号在识别“结构性压力”方面优于传统统计残差。

#### 6. 主要结论与发现
*   **早期预警能力**：在 Lander-1 案例中，协态框架检测到内部模型不一致的时间**显著早于**物理状态发散或 EKF 的统计报警。
*   **失效本质识别**：实验证明 Lander-1 的失败并非由于物理失稳或推力丧失，而是源于导航-制导-控制（NGC）链条内部的软件级模型失配（如推力映射错误或定时偏差）。
*   **风险一致性**：几何不一致性、随机漂移和概率风险指标在失效前呈现出高度的相干上升趋势，验证了该框架作为早期预警系统的有效性。

#### 7. 优点
*   **物理可解释性**：协态变量提供了明确的几何意义，能够区分“统计噪声”与“结构性模型错误”。
*   **无需先验知识**：不需要预定义的故障模式库（Fault Library）或人工标注的训练数据，具有很强的自主性。
*   **计算高效**：算法结构简单，易于在嵌入式实时系统中部署。
*   **端到端集成**：将底层几何约束与高层概率风险评估无缝结合。

#### 8. 不足与局限
*   **小噪声假设**：方法论依赖于小噪声操作机制的近似，在极端非线性或高噪声环境下，泰勒展开的高阶项可能不可忽略。
*   **模式聚类依赖**：风险预测的准确性部分取决于在线聚类的质量，若模式划分不当，可能影响生成器学习的精度。
*   **验证场景有限**：目前仅在月球着陆场景下验证，对于其他复杂的深空探测任务（如小行星采样、多机协作）的泛化能力尚待进一步研究。
*   **正则化参数**：在处理病态测量矩阵时，正则化参数 $\epsilon$ 的选择可能需要一定的工程调优。

（完）
