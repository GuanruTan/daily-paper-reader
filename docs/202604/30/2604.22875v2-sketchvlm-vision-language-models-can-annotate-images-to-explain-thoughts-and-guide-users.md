---
title: "SketchVLM: Vision language models can annotate images to explain thoughts and guide users"
title_zh: SketchVLM：视觉语言模型可以通过标注图像来解释思路并引导用户
authors: "Brandon Collins, Logan Bolton, Hung Huy Nguyen, Mohammad Reza Taesiri, Trung Bui, Anh Totti Nguyen"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.22875v2"
tags: ["query:tp"]
score: 8.0
evidence: 基于视觉语言模型的轨迹预测与视觉推理
tldr: 针对现有视觉语言模型（VLM）仅能以文本形式回答、难以验证推理过程的问题，本文提出了 SketchVLM。这是一个无需训练且模型无关的框架，允许 VLM 生成可编辑的 SVG 覆盖层，通过在图像上绘图来解释其推理过程。在视觉推理和绘图任务中，该方法显著提升了模型准确率和标注质量，并支持人机协作。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型缺乏像人类一样通过绘图或标注来解释其推理过程的能力，导致其回答难以被用户验证。
method: 提出一种无需训练的通用框架，使模型能够生成非破坏性、可编辑的 SVG 矢量图形覆盖在输入图像上以辅助推理。
result: "在七项基准测试中，SketchVLM 将视觉推理准确率最高提升了 28.5%，标注质量提升了 1.48 倍，且标注与模型回答的一致性更高。"
conclusion: SketchVLM 证明了通过视觉标注可以有效增强 VLM 的推理能力和可解释性，并为多轮人机协作提供了新可能。
---

## 摘要
在回答有关图像的问题时，人类会自然地通过指点、标注和绘图来解释其推理过程。相比之下，现代视觉语言模型（VLM），如 Gemini-3-Pro 和 GPT-5，仅以文本形式响应，这使得用户难以验证。我们提出了 SketchVLM，这是一个无需训练且与模型无关的框架，它使 VLM 能够在输入图像上生成非破坏性、可编辑的 SVG 叠加层，从而以视觉方式解释其答案。在涵盖视觉推理（迷宫导航、落球轨迹预测和物体计数）和绘图（部件标注、连点成线以及在物体周围绘制形状）的七个基准测试中，相较于图像编辑和经过微调的草图绘制基准模型，SketchVLM 将视觉推理任务的准确率提高了多达 28.5 个百分点，标注质量提高了多达 1.48 倍，同时生成的标注也更忠实于模型给出的答案。我们发现，单轮生成已经能够实现极高的准确率和标注质量，而多轮生成则为人类与 AI 的协作开辟了更多机会。交互式演示和代码可在 https://sketchvlm.github.io/ 获取。

## Abstract
When answering questions about images, humans naturally point, label, and draw to explain their reasoning. In contrast, modern vision-language models (VLMs) such as Gemini-3-Pro and GPT-5 only respond with text, which can be difficult for users to verify. We present SketchVLM, a training-free, model-agnostic framework that enables VLMs to produce non-destructive, editable SVG overlays on the input image to visually explain their answers. Across seven benchmarks spanning visual reasoning (maze navigation, ball-drop trajectory prediction, and object counting) and drawing (part labeling, connecting-the-dots, and drawing shapes around objects), SketchVLM improves visual reasoning task accuracy by up to +28.5 percentage points and annotation quality by up to 1.48x relative to image-editing and fine-tuned sketching baselines, while also producing annotations that are more faithful to the model's stated answer. We find that single-turn generation already achieves strong accuracy and annotation quality, and multi-turn generation opens up further opportunities for human-AI collaboration. An interactive demo and code are at https://sketchvlm.github.io/.

---

## 论文详细总结（自动生成）

以下是对论文《SketchVLM: Vision language models can annotate images to explain thoughts and guide users》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代视觉语言模型（VLM，如 GPT-5、Gemini-3-Pro）在回答图像相关问题时，通常仅提供纯文本回复。这种“黑盒”式的推理过程导致用户难以验证模型是否真正理解了图像内容，且在处理复杂空间任务（如维修指引、物理预测）时不够直观。
*   **研究动机**：人类在解释视觉问题时习惯于通过指点、画圈或标注来辅助说明。论文旨在赋予 VLM 这种“边画边说”的能力，通过在原图上叠加可编辑的矢量图形（SVG），提供可验证的视觉推理轨迹。

### 2. 方法论：核心思想与技术细节
SketchVLM 是一个**无需训练（Training-free）**且**模型无关（Model-agnostic）**的框架，其核心流程如下：
*   **视觉提示（Visual Prompting）**：在输入图像的左侧和下方添加坐标网格（Coordinate Grid），帮助模型建立精确的空间参考坐标系。
*   **系统提示（System Prompt）**：通过精心设计的指令，引导模型以结构化的 XML 格式输出绘图指令（如 `<s1>` 标签包含点集、颜色、粗细等）。支持的图元包括：自由曲线（Freehand）、直线、箭头、矩形、圆形以及文本标签。
*   **SVG 转换与渲染**：
    *   解析模型输出的 XML 坐标点。
    *   使用**最小二乘法拟合三次贝塞尔曲线（Bézier curves）**，将离散点转换为平滑的矢量线条。
    *   将生成的 SVG 作为一个独立的透明图层叠加在原始图像上，实现**非破坏性（Non-destructive）**标注。
*   **交互模式**：支持**单轮（Single-turn）**直接生成完整标注，以及**多轮（Multi-turn）**迭代绘图（模型在每一轮看到上一轮画出的图像并继续添加）。

### 3. 实验设计
*   **任务与数据集**：涵盖 7 项任务，分为两大类：
    1.  **绘图任务**：连点成线（自定义 100 图）、物体形状绘制（COCO 1000 图）、部件标注（PACO/Pascal-Part 985 图）。
    2.  **视觉推理任务**：物体计数（CountBench/Pixmo-Count 746 图）、迷宫导航（自定义 200 图）、物理推理（VPCT 100 图）、落球轨迹预测（基于 PHYRE 生成的 198 图）。
*   **对比方法（Baselines）**：
    *   **图像编辑模型**：Nano Banana Pro（通过修改像素进行标注）。
    *   **微调模型**：ViLaSR（基于 Qwen-2.5-VL 微调）、ThinkMorph（基于 BAGEL-7B 微调）。
    *   **原生 VLM**：纯文本输出的 GPT-5 和 Gemini-3-Pro。

### 4. 资源与算力
*   **算力说明**：由于 SketchVLM 是**无需训练**的方法，其核心实验主要依赖于闭源模型的 API（GPT-5, Gemini-3-Pro-Preview, Gemini-3-Flash）。
*   **本地运行**：对比基准模型（ViLaSR 和 ThinkMorph）在本地环境中运行推理，但论文未明确给出具体的 GPU 型号、数量或推理总时长。

### 5. 实验数量与充分性
*   **实验规模**：测试了超过 3000 张图像，涵盖了从基础几何到复杂物理动力学的多种场景。
*   **评估维度**：
    *   **准确率（Accuracy）**：任务回答的正确性。
    *   **图文一致性（Alignment）**：使用“VLM-as-a-Judge”评估标注是否支持其文本答案。
    *   **标注质量（Quality）**：通过 VLM 评分（1-5 分）和人工评估（2250 次标注）双重验证。
*   **充分性评价**：实验设计非常充分，不仅对比了 SOTA 模型，还进行了详细的消融实验（如网格的影响、单/多轮对比、坐标系适配等），并验证了自动化评估指标与人工评价的相关性（Kappa 系数 0.51）。

### 6. 主要结论与发现
*   **性能卓越**：SketchVLM 在视觉推理任务上的准确率比微调模型高出多达 **28.5%**，标注质量提升了 **1.48 倍**。
*   **忠实度高**：相比于图像编辑模型（容易修改原图内容导致误导），SketchVLM 的 SVG 覆盖层更忠实于模型的实际推理逻辑。
*   **单轮 vs 多轮**：单轮生成在准确率上已足够强大且效率更高（快 5.9 倍），而多轮生成更适合复杂的人机协作场景。
*   **网格效应**：坐标网格显著提升了 GPT-5 的精度，但对某些版本的 Gemini 反而有负面影响（可能与其内部坐标处理机制冲突）。

### 7. 优点与亮点
*   **即插即用**：无需昂贵的微调过程，直接利用前沿 VLM 的指令遵循能力。
*   **可解释性强**：将“思维链（CoT）”视觉化，用户可以直观看到模型认为球会怎么滚、迷宫怎么走。
*   **非破坏性**：SVG 方案不改变原始像素，标注可随时编辑、隐藏或导出，具有极高的实用价值。

### 8. 不足与局限
*   **模型依赖性**：该框架高度依赖模型本身的指令遵循和空间感知能力，在较小的开源模型（如 Qwen2.5-VL-7B）上表现较差。
*   **坐标系敏感**：不同模型对坐标系统的理解存在差异，需要针对特定模型微调系统提示词。
*   **动态交互限制**：目前尚不支持模型在绘图过程中进行“擦除”或“撤销”操作，多轮对话中的错误累积可能影响最终结果。

（完）
