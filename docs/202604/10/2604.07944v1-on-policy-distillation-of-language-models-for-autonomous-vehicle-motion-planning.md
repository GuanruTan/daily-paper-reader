---
title: On-Policy Distillation of Language Models for Autonomous Vehicle Motion Planning
title_zh: 自动驾驶汽车运动规划的语言模型同策略蒸馏
authors: "Amirhossein Afsharrad, Amirhesam Abedsoltan, Ahmadreza Moradipari, Sanjay Lall"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07944v1"
tags: ["query:tp"]
score: 9.0
evidence: 将轨迹预测重新表述为语言生成问题
tldr: 本研究探讨如何将大语言模型（LLM）的自动驾驶运动规划能力迁移至小型模型，以解决车载部署的资源限制问题。基于GPT-Driver框架，研究者对比了在线策略广义知识蒸馏（GKD）与密集反馈强化学习（RL）两种方案。实验表明，GKD通过教师模型的Token级反馈引导学生模型自生成输出，在nuScenes榜单上以5倍小的模型规模实现了接近教师模型的性能，为高效部署LLM规划器提供了有效路径。
selection_source: fresh_fetch
motivation: 大语言模型在自动驾驶规划中表现出色，但其庞大的参数量难以满足车载嵌入式系统的实时部署需求。
method: 采用在线策略广义知识蒸馏（GKD），让学生模型在自身生成的轨迹上接受教师模型的密集Token级概率反馈。
result: 实验证明GKD显著优于强化学习基准，在模型参数量减少5倍的情况下，性能仍能紧追大型教师模型。
conclusion: 在线策略蒸馏是提升小型语言模型在自动驾驶规划任务中表现、实现高效车载部署的一种原则性且有效的方法。
---

## 摘要
大语言模型（LLMs）最近通过将轨迹预测重新表述为语言生成问题，在自动驾驶汽车运动规划方面展现出强大的潜力。然而，在资源受限的车载系统中部署高性能的 LLM 仍然是一个根本性的挑战。在本文中，我们研究了如何有效地将运动规划知识从大型教师 LLM 转移到更小、更易于部署的学生模型中。我们基于 GPT-Driver 框架，该框架将驾驶场景表示为语言提示，并通过思维链（CoT）推理生成路径点轨迹。我们研究了两种学生训练范式：(i) 同策略广义知识蒸馏（GKD），它利用来自教师模型的密集 Token 级反馈，在学生模型自身生成的输出上对其进行训练；(ii) 密集反馈强化学习（RL）基准，它在策略梯度框架中将教师模型的对数概率作为逐 Token 的奖励信号。在 nuScenes 基准测试上的实验表明，尽管模型规模缩小了 5 倍，但 GKD 的表现显著优于 RL 基准，并接近教师模型的性能水平。这些结果突显了同策略蒸馏作为一种原则性且有效的方法，在自动驾驶系统中部署基于 LLM 的规划器的实际价值。

## Abstract
Large language models (LLMs) have recently demonstrated strong potential for autonomous vehicle motion planning by reformulating trajectory prediction as a language generation problem. However, deploying capable LLMs in resource-constrained onboard systems remains a fundamental challenge. In this paper, we study how to effectively transfer motion planning knowledge from a large teacher LLM to a smaller, more deployable student model. We build on the GPT-Driver framework, which represents driving scenes as language prompts and generates waypoint trajectories with chain-of-thought reasoning, and investigate two student training paradigms: (i) on-policy generalized knowledge distillation (GKD), which trains the student on its own self-generated outputs using dense token-level feedback from the teacher, and (ii) a dense-feedback reinforcement learning (RL) baseline that uses the teacher's log-probabilities as per-token reward signals in a policy gradient framework. Experiments on the nuScenes benchmark show that GKD substantially outperforms the RL baseline and closely approaches teacher-level performance despite a 5$\times$ reduction in model size. These results highlight the practical value of on-policy distillation as a principled and effective approach to deploying LLM-based planners in autonomous driving systems.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何通过**同策略蒸馏（On-Policy Distillation）**技术，将大型语言模型（LLM）的自动驾驶运动规划能力迁移到小型模型中，以满足车载嵌入式系统的实时部署需求。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：LLM（如 GPT-Driver）通过将驾驶场景转化为文本提示并生成轨迹坐标，展现了强大的推理和规划能力。
*   **核心问题**：高性能 LLM 参数量巨大，难以在资源受限的车载硬件上实时运行。
*   **技术挑战**：传统的监督蒸馏存在“训练-推理分布不一致”问题。学生模型在推理时基于自身生成的错误 Token 进行预测，会导致误差随时间步累积（级联错误），这在对精度要求极高的轨迹规划任务中是致命的。

### 2. 方法论
论文对比了两种将知识从教师模型（Qwen3-8B）迁移到学生模型（Qwen3-1.7B）的同策略方案：

*   **同策略广义知识蒸馏 (GKD)**：
    *   **核心思想**：让学生模型针对输入提示自行生成（采样）输出序列，然后由教师模型对这些“学生自产”的序列提供 Token 级的全概率分布反馈。
    *   **技术细节**：使用广义 Jensen-Shannon 散度（JSD）作为损失函数，在学生生成的轨迹上最小化学生与教师分布的差异。这种方式让学生模型学习如何从自身的错误中恢复。
*   **密集反馈强化学习 (RL) 基准**：
    *   **核心思想**：将教师模型的对数概率作为奖励信号，在策略梯度框架下训练学生模型。
    *   **区别**：RL 仅提供标量奖励（告诉学生这个 Token 好不好），而 GKD 提供完整的词表分布（告诉学生哪些替代 Token 也是合理的），信息密度更高。

### 3. 实验设计
*   **数据集**：使用 **nuScenes** 自动驾驶数据集，遵循 GPT-Driver 的处理流程，包含 1000 个复杂驾驶场景。
*   **任务形式**：输入包含感知结果（目标位置、预测轨迹）、自车状态和任务目标；输出包含思维链（CoT）推理、元动作和 6 个路径点坐标（3 秒时界）。
*   **对比基准 (Benchmark)**：
    *   **Teacher**：经过微调的 Qwen3-8B。
    *   **GKD Student**：Qwen3-1.7B（使用 GKD 训练）。
    *   **RL Student**：Qwen3-1.7B（使用密集反馈 RL 训练）。
*   **评价指标**：L2 位移误差（STP-3 和 UniAD 标准）、碰撞率（Collision Rate）以及格式错误率。

### 4. 资源与算力
*   **硬件环境**：实验在单节点上运行，配备 **8 台 NVIDIA H200 GPU**。
*   **训练细节**：
    *   教师模型使用 DeepSpeed ZeRO-3 进行微调。
    *   学生模型训练 5 个 Epoch，GKD 学习率为 $5 \times 10^{-5}$，有效 Batch Size 为 8。
    *   论文未明确给出总训练时长，但提到了 Checkpoint 的选择逻辑。

### 5. 实验数量与充分性
*   **实验规模**：在 nuScenes 验证集的 **5,119 个帧**上进行了全面评估。
*   **充分性**：实验设计较为充分，涵盖了从轨迹精度（L2）到安全性（碰撞率）的多维度指标。
*   **公平性**：通过控制变量法，确保 GKD 和 RL 基准使用相同的骨干模型、数据集和提示词，仅改变学习算法，对比结果具有高度客观性。

### 6. 主要结论与发现
*   **5 倍压缩，性能接近**：1.7B 的 GKD 学生模型在 L2 误差上仅比 8B 教师模型高出约 5-6%，表现远超 RL 基准。
*   **安全性领先**：GKD 学生的碰撞率（0.138%）接近教师（0.101%），而 RL 学生碰撞率高达 0.363%。
*   **分布匹配的优势**：全词表分布反馈（GKD）比标量奖励（RL）更适合坐标生成任务，因为它能提供更丰富的物理约束信息。
*   **稳定性**：GKD 训练过程比 RL 更稳定，且随着预测时界的增加，GKD 抑制误差累积的能力显著强于 RL。

### 7. 优点
*   **解决关键痛点**：针对 LLM 规划器的车载部署难题，提出了原则性的蒸馏方案。
*   **端到端可解释性**：继承了 GPT-Driver 的优点，模型在输出坐标前会先输出推理过程，增强了决策的透明度。
*   **高效性**：证明了同策略蒸馏可以在大幅缩减参数量的同时，保持极高的规划精度。

### 8. 不足与局限
*   **开环评估限制**：实验基于 nuScenes 离线数据集（开环），无法完全反映模型在动态闭环仿真或真实路测中的交互表现。
*   **输入模态单一**：目前主要依赖文本化的感知结果，未直接整合矢量地图或原始传感器数据（如 LiDAR/图像）。
*   **泛化性验证**：虽然在 nuScenes 上表现良好，但未在其他数据集（如 Waymo 或 Argoverse）上验证跨域泛化能力。

（完）
