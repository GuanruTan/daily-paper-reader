---
title: Critic-Free Deep Reinforcement Learning for Maritime Coverage Path Planning on Irregular Hexagonal Grids
title_zh: 不规则六边形网格上海事覆盖路径规划的无 Critic 深度强化学习
authors: "Carlos S. Sepúlveda, Gonzalo A. Ruz"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28385v1"
tags: ["query:tp"]
score: 8.0
evidence: 使用深度强化学习和Transformer策略的海洋覆盖路径规划
tldr: 针对复杂海域（如不规则海岸线和岛屿）的覆盖路径规划（CPP）难题，本文提出一种基于深度强化学习的框架。该方法将CPP建模为神经组合优化任务，利用Transformer指针策略在六边形网格上自动构建路径。通过引入无评论者（Critic-free）的群体相对策略优化（GRPO）算法，解决了长程规划中的价值估计不稳定问题。实验表明，该方法在路径成功率、长度和转向次数上显著优于传统启发式算法，且推理速度极快，适用于实时机载部署。
selection_source: fresh_fetch
motivation: 传统覆盖路径规划方法在处理包含岛屿和复杂海岸线的不规则海域时，存在分解困难或计算成本过高的问题。
method: 采用基于Transformer的指针策略进行路径构建，并利用无评论者的群体相对策略优化（GRPO）通过样本间对比来估计优势函数。
result: "在测试中实现了99%的哈密顿路径成功率，且路径长度比基准缩短7%，转向次数减少24%，推理时间低于50毫秒。"
conclusion: 该研究证明了无评论者强化学习在解决复杂几何约束下的海域覆盖规划问题上的高效性与实时应用潜力。
---

## 摘要
海事监视任务（如搜救和环境监测）依赖于在广阔且几何形状复杂的区域内高效分配传感资产。传统的覆盖路径规划（CPP）方法依赖于分解技术，这些技术在处理不规则海岸线、岛屿和禁区时面临挑战，或者需要针对每个实例进行计算昂贵的重新规划。我们提出了一种深度强化学习（DRL）框架，用于解决不规则海域六边形网格表示上的 CPP 问题。与传统方法不同，我们将该问题建模为神经组合优化任务，其中基于 Transformer 的指针策略以自回归方式构建覆盖路径。为了克服长时程路由问题中价值估计的不稳定性，我们实现了一种无 Critic 的群体相对策略优化（GRPO）方案。该方法通过对采样轨迹进行实例内比较来估计优势，而不是依赖于价值函数。在 1,000 个未见过的合成海事环境上的实验表明，训练后的策略实现了 99.0% 的哈密顿成功率，是最佳启发式方法（46.0%）的两倍以上，同时生成的路径比最接近的基准缩短了 7%，航向变化减少了 24%。所有三种推理模式（贪婪、随机采样以及带有 2-opt 优化的采样）在笔记本电脑 GPU 上处理每个实例的时间均低于 50 毫秒，证实了实时机载部署的可行性。

## Abstract
Maritime surveillance missions, such as search and rescue and environmental monitoring, rely on the efficient allocation of sensing assets over vast and geometrically complex areas. Traditional Coverage Path Planning (CPP) approaches depend on decomposition techniques that struggle with irregular coastlines, islands, and exclusion zones, or require computationally expensive re-planning for every instance. We propose a Deep Reinforcement Learning (DRL) framework to solve CPP on hexagonal grid representations of irregular maritime areas. Unlike conventional methods, we formulate the problem as a neural combinatorial optimization task where a Transformer-based pointer policy autoregressively constructs coverage tours. To overcome the instability of value estimation in long-horizon routing problems, we implement a critic-free Group-Relative Policy Optimization (GRPO) scheme. This method estimates advantages through within-instance comparisons of sampled trajectories rather than relying on a value function. Experiments on 1,000 unseen synthetic maritime environments demonstrate that a trained policy achieves a 99.0% Hamiltonian success rate, more than double the best heuristic (46.0%), while producing paths 7% shorter and with 24% fewer heading changes than the closest baseline. All three inference modes (greedy, stochastic sampling, and sampling with 2-opt refinement) operate under 50~ms per instance on a laptop GPU, confirming feasibility for real-time on-board deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Critic-Free Deep Reinforcement Learning for Maritime Coverage Path Planning on Irregular Hexagonal Grids》的结构化分析报告：

### 1. 论文的核心问题与整体含义
*   **研究背景**：海事监视（如搜救、环境监测）需要在包含复杂海岸线、岛屿和禁区的非凸多边形区域内进行高效的覆盖路径规划（CPP）。
*   **核心问题**：传统的几何分解法（如往复式扫描）在处理不规则区域时会产生大量碎片化子区，导致频繁的转向和无效航行；而精确优化算法（如MILP）计算成本高，且无法在不同任务间泛化。
*   **整体含义**：本文旨在开发一种能够泛化至任意不规则几何形状、具备实时推理能力、且能生成运动学平滑路径的深度强化学习（DRL）框架。

### 2. 方法论：核心思想与关键技术
*   **环境建模**：采用**六边形网格（Hexagonal Grid）**对海域进行离散化。相比正方形网格，六边形具有等距邻居和更好的各向同性，能更准确地模拟传感器足迹并减少运动偏差。
*   **神经组合优化（NCO）架构**：
    *   **Encoder**：使用多层Transformer编码器提取图结构特征，通过邻域限制的注意力机制处理稀疏连接。
    *   **Decoder**：基于指针网络（Pointer Network）的自回归解码器，结合当前位置、全局信息和动态环境信号（如覆盖进度、航向）生成下一步动作。
*   **核心算法：无 Critic 的 GRPO**：
    *   针对长程路由问题中价值函数（Critic）难以收敛且估计不准的问题，引入了**群体相对策略优化（Group-Relative Policy Optimization, GRPO）**。
    *   **优势估计**：不依赖学习到的价值函数，而是对同一地图实例采样 $G$ 条轨迹，通过计算这组轨迹回报的均值和标准差进行标准化，以此作为优势函数（Advantage）。
*   **辅助机制**：
    *   **可行性掩码（Action Masking）**：严格禁止访问非邻居节点或已访问节点，确保路径的物理可行性。
    *   **BFS 早期死胡同检测**：在每一步利用广度优先搜索检查剩余未访问节点的可达性，若发现无法完成全覆盖则立即终止并惩罚，从而加速信用分配。

### 3. 实验设计
*   **数据集**：合成生成了 10,000 个不规则海事环境实例（8,000 训练，1,000 验证，1,000 测试）。包含凸多边形、狭长峡湾、带随机岛屿的复杂海域等多种形态。
*   **Benchmark（基准方法）**：对比了 **13 种经典启发式算法**，涵盖 6 大类：
    1.  扫描式（Boustrophedon, One-way, Snake）。
    2.  交错扫描式（Interleave）。
    3.  轮廓/螺旋式（Spiral In/Out, Peel）。
    4.  生成树覆盖（STC）。
    5.  局部图搜索（Warnsdorff 启发式, DFS 回溯）。
    6.  空间填充曲线（Morton Z-order）。
*   **评估指标**：哈密顿成功率（HSR，即不重复访问的全覆盖率）、覆盖完成率（CCR）、路径长度、转向次数、推理延迟。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **NVIDIA GeForce RTX 3070 Laptop GPU (8GB VRAM)** 和 AMD Ryzen 9 5900HX CPU 的笔记本工作站。
*   **训练时长**：总训练时长约为 **440 小时**（执行了 34 个 Epoch，在第 30 个 Epoch 取得最佳验证效果后触发早停）。
*   **模型规模**：神经网络包含约 205 万个可训练参数。

### 5. 实验数量与充分性
*   **实验规模**：在 1,000 个完全未见的测试实例上进行了评估，样本量充足。
*   **对比充分性**：对比了多达 13 种基准方法，几乎涵盖了目前主流的非学习类 CPP 启发式算法，对比非常全面。
*   **消融/模式对比**：对比了三种推理模式：RL-Greedy（贪婪）、RL-BoK16（16样本并行采样）、RL-BoK16+2opt（采样后结合局部搜索优化），展示了不同计算预算下的性能提升。
*   **客观性**：通过哈密顿路径（不重复访问）这一严格标准进行评估，客观揭示了传统启发式算法在复杂拓扑下的局限性。

### 6. 主要结论与发现
*   **性能飞跃**：提出的 RL-BoK16+2opt 实现了 **99.0% 的哈密顿成功率**，而表现最好的启发式算法（Warnsdorff）仅为 46.0%。
*   **路径质量**：在相同成功实例下，RL 生成的路径比最强基准**缩短了约 7%**，且**转向次数减少了 24%**，显著提升了航行效率。
*   **实时性**：尽管模型复杂，但在 GPU 加速下，单实例推理时间（含采样和优化）**低于 50 毫秒**，完全满足无人艇/无人机的实时在线规划需求。

### 7. 优点与亮点
*   **算法创新**：成功将 GRPO 算法从大语言模型领域迁移至神经组合优化领域，解决了长程规划中 Critic 网络难以训练的痛点。
*   **几何适应性**：通过六边形网格和 Transformer 的结合，模型能够理解复杂的地理约束（如狭窄通道、半岛），表现出极强的空间推理能力。
*   **工程实用性**：引入 BFS 早期检测和 2-opt 局部细化，将强化学习的探索能力与经典算法的鲁棒性有机结合。

### 8. 不足与局限
*   **训练成本高**：单次训练耗时近 18 天（笔记本 GPU），对于更大规模的网格（如 >100 节点）可能面临扩展性挑战。
*   **数据偏差风险**：实验完全基于合成数据集，尚未在真实的电子海图（ENC）数据上进行验证，真实环境中的海岸线细节可能更复杂。
*   **约束单一**：目前主要针对“禁止重复访问”的严格哈密顿路径，但在某些极端拓扑下，允许少量重复访问可能是更优或唯一的解，模型目前对这种灵活性的支持有限。
*   **单种子实验**：由于计算成本限制，论文仅报告了单次完整训练的结果，缺乏多随机种子下的稳定性分析。

（完）
