---
title: Multi-view Crowd Tracking Transformer with View-Ground Interactions Under Large Real-World Scenes
title_zh: 大规模真实场景下具有视点-地面交互的多视角人群追踪 Transformer
authors: "Qi Zhang, Jixuan Chen, Kaiyi Zhang, Xinquan Yu, Antoni B. Chan, Hui Huang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19318v1"
tags: ["query:tp"]
score: 9.0
evidence: 多视角人群跟踪与地面轨迹估计
tldr: 针对现有基于CNN的多视角人群追踪方法在处理大规模复杂场景时表现不佳的问题，本文提出了MVTrackTrans，一种基于Transformer的多视角追踪模型。该模型通过相机视角与地面平面的交互增强追踪性能。此外，研究者发布了两个大规模真实场景数据集MVCrowdTrack和CityTrack。实验证明，该模型在大规模场景下优于现有方法，推动了该领域向实际应用的发展。
selection_source: fresh_fetch
motivation: 现有方法主要依赖CNN且在小型数据集上评估，难以应对真实世界中大规模场景和复杂遮挡的挑战。
method: 提出基于Transformer的MVTrackTrans模型，利用相机视角与地面平面之间的交互来提升多视角追踪效果。
result: 在自建的大规模数据集MVCrowdTrack和CityTrack上，MVTrackTrans的性能显著优于现有主流方法。
conclusion: 该研究通过创新的Transformer架构和大规模数据集，有效提升了大规模真实场景下的多视角人群追踪能力。
---

## 摘要
多视角人群追踪旨在估计场景地面上每个人的追踪轨迹。近期的研究工作主要依赖于基于 CNN 的多视角人群追踪架构，且大多数在 Wildtrack 和 MultiviewX 等相对较小的数据集上进行评估和比较。由于这两个数据集是在小型场景中采集的，且在评估阶段仅包含数十帧，因此现有方法难以应用于场景规模和遮挡情况更为复杂的真实世界应用。在本文中，我们提出了一种基于 Transformer 的多视角人群追踪模型 MVTrackTrans，该模型通过摄像机视点与地面之间的交互来增强多视角追踪性能。此外，为了更好地进行评估，我们收集并标注了两个大型真实世界多视角追踪数据集 MVCrowdTrack 和 CityTrack，它们包含更大的场景规模和更长的时间跨度。与这两个大型新数据集上的现有方法相比，所提出的 MVTrackTrans 模型取得了更好的性能，证明了该模型设计在处理大规模场景方面的优势。我们相信所提出的数据集和模型将把该任务的前沿推向更具实际意义的场景。数据集和代码可在以下网址获取：https://github.com/zqyq/MVTrackTrans。

## Abstract
Multi-view crowd tracking estimates each person's tracking trajectories on the ground of the scene. Recent research works mainly rely on CNNs-based multi-view crowd tracking architectures, and most of them are evaluated and compared on relatively small datasets, such as Wildtrack and MultiviewX. Since these two datasets are collected in small scenes and only contain tens of frames in the evaluation stage, it is difficult for the current methods to be applied to real-world applications where scene size and occlusion are more complicated. In this paper, we propose a Transformer-based multi-view crowd tracking model, \textit{MVTrackTrans}, which adopts interactions between camera views and the ground plane for enhanced multi-view tracking performance. Besides, for better evaluation, we collect and label two large real-world multi-view tracking datasets, MVCrowdTrack and CityTrack, which contain a much larger scene size over a longer time period. Compared with existing methods on the two large and new datasets, the proposed MVTrackTrans model achieves better performance, demonstrating the advantages of the model design in dealing with large scenes. We believe the proposed datasets and model will push the frontiers of the task to more practical scenarios, and the datasets and code are available at: https://github.com/zqyq/MVTrackTrans.

---

## 论文详细总结（自动生成）

### 论文总结：大规模真实场景下具有视点-地面交互的多视角人群追踪 Transformer

#### 1. 核心问题与整体含义（研究动机和背景）
多视角人群追踪（Multi-view Crowd Tracking, MVCT）旨在利用多个同步且校准的相机，在地面坐标系下估计每个人的运动轨迹。
*   **研究动机**：现有的主流方法多基于 CNN 架构，且主要在 Wildtrack 和 MultiviewX 等小型数据集上评估。这些数据集场景范围小（仅数百平米）、帧数少（仅数百帧），难以反映真实世界中大规模场景、高密度人群、严重遮挡及长时追踪的挑战。
*   **核心目标**：探索 Transformer 架构在多视角追踪中的潜力，并提供更贴近实际应用的大规模基准数据集。

#### 2. 方法论：核心思想与关键技术
论文提出了 **MVTrackTrans**，这是首个专门针对大规模场景设计的基于 Transformer 的多视角追踪框架。其核心流程分为三个阶段：
*   **特征提取与多视角融合**：使用 ResNet 提取各视角多尺度特征，通过多高度双线性采样（Multi-height bilinear sampling）将图像特征投影至地面（BEV 空间），并融合生成地面特征表示。
*   **多视角追踪编码（核心创新）**：
    *   使用可变形 Transformer 编码器（Deformable Encoder）增强地面特征。
    *   **视点-地面交互模块（View-Ground Interaction）**：这是本文的关键。模型从上一帧地面特征中采样“追踪查询（Track Queries）”，并从各相机原始视角特征中采样“视角查询（View Queries）”。通过**交叉注意力机制（Cross-Attention）**，让追踪查询吸收来自原始视角的视觉细节，弥补 BEV 投影过程中的信息损失。
*   **多视角追踪解码**：
    *   **偏移解码器（Offset Decoder）**：预测目标在地面上的时间位移（$\delta x, \delta y$）。
    *   **热图解码器（Heatmap Decoder）**：预测当前帧的人群中心位置热图。
*   **损失函数**：结合 Focal Loss（热图分类）和 $L_1$ Loss（偏移回归），并采用不确定性加权策略自动平衡各项损失。

#### 3. 实验设计
*   **数据集与场景**：
    *   **MVCrowdTrack（新发布）**：校园场景，120m × 80m，7 个同步相机，4122 帧，342 条轨迹，平均长度 176 帧。
    *   **CityTrack（新发布）**：基于 CityStreet 重新标注，64m × 76m，2588 帧，平均轨迹长度 228 帧。
    *   **Wildtrack & MultiviewX**：用于验证模型在小型标准基准上的泛化能力。
*   **对比方法**：包括 EarlyBird (WACV 2024)、MVFlow (WACV 2023)、TrackTacular (CVPRW 2024) 等 SOTA 方法。
*   **评价指标**：MOTA、MOTP、IDF1、MT（大多被追踪）、ML（大多丢失）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA RTX 4090 GPU**。
*   **训练细节**：Batch size 为 1，训练 50 个 epoch。
*   **模型配置**：主干网络为 ResNet18，输入图像缩放至 1280 × 720。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集（2 个大型新数据集 + 2 个小型传统数据集）上进行了详尽对比。
*   **消融实验**：针对架构组件（2D 预测分支、交互模块）、交互方式（自注意力 vs 交叉注意力）、监督方式（坐标回归 vs 热图回归）进行了多组实验。
*   **充分性评价**：实验设计客观、公平，通过在不同规模数据集上的表现证明了 Transformer 架构在处理复杂空间关联和长时动力学方面的优越性。

#### 6. 主要结论与发现
*   **Transformer 的优越性**：在大型场景（MVCrowdTrack/CityTrack）中，MVTrackTrans 的 MOTA 和 IDF1 显著优于基于 CNN 的方法（如 EarlyBird），证明了其在复杂空间建模上的优势。
*   **交互模块的作用**：视点-地面交互模块能有效提升追踪稳定性，尤其是在长时追踪中，通过引入原始视角特征缓解了 BEV 投影的精度损失。
*   **监督策略**：在多视角任务中，基于热图的密集监督比直接坐标回归更有效，因为热图能更好地处理投影过程中的噪声。

#### 7. 优点
*   **数据集贡献**：填补了多视角人群追踪领域缺乏大规模、长时真实场景数据集的空白。
*   **架构创新**：首次将 Transformer 引入该领域并设计了专门的视点-地面交互机制。
*   **实用性强**：模型在处理大规模、高密度人群时表现出更强的鲁棒性，更接近实际安防和交通管理需求。

#### 8. 不足与局限
*   **相机依赖**：模型仍高度依赖精确的相机内外参校准，在动态相机或校准漂移场景下的表现未被讨论。
*   **小场景优势不明显**：在 Wildtrack 等小型数据集上，虽然达到了 SOTA 水平，但领先幅度不如在大规模数据集上那么显著，说明其参数量和复杂度在简单场景下可能存在冗余。
*   **实时性**：虽然使用了可变形注意力，但 Transformer 架构在多视角、高分辨率输入下的推理延迟（FPS）未做详细量化分析。

（完）
