---
title: Multimodal embodiment-aware navigation transformer
title_zh: 多模态具身感知导航 Transformer
authors: "Louis Dezons, Quentin Picard, Rémi Marsal, François Goulette, David Filliat"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19267v1"
tags: ["query:tp"]
score: 8.0
evidence: 扩散模型为机器人生成可导航轨迹
tldr: ViLiNT是一个多模态、感知机器人本体的导航策略模型，旨在解决地面机器人在环境或配置变化时避障能力下降的问题。该模型融合了RGB图像、3D LiDAR点云、目标嵌入和机器人本体描述符，利用Transformer架构提取特征，并结合扩散模型生成轨迹。此外，通过路径净空预测头对轨迹进行评分和排序。实验表明，ViLiNT在仿真和现实环境中均显著提升了导航成功率和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的地面机器人导航模型在面对环境、机器人或传感器配置变化导致的分布偏移时，避障能力会显著下降。
method: 提出ViLiNT模型，通过Transformer融合多模态数据（RGB、LiDAR、本体描述符），并结合扩散模型生成轨迹及路径净空预测头进行轨迹筛选。
result: "在三种仿真环境中，ViLiNT的导航成功率比最先进的纯视觉基准模型NoMaD平均提高了166%，并在现实世界部署中验证了其有效性。"
conclusion: 结合多模态融合与碰撞预测机制，并引入机器人本体感知，能显著增强机器人在复杂越野环境下的导航鲁棒性。
---

## 摘要
使用监督学习训练的地面机器人目标条件导航模型展现出良好的零样本迁移能力，但其避障能力在分布偏移（即环境、机器人或传感器配置变化）下仍会下降。我们提出了 ViLiNT，这是一种基于注意力的多模态目标导航策略，在来自多个平台和环境的异构数据上进行训练，通过两个关键特性提高了鲁棒性。首先，我们利用 Transformer 架构融合了 RGB 图像、3D 激光雷达（LiDAR）点云、目标嵌入和机器人具身描述符，以捕捉互补的几何与外观线索。Transformer 的输出用于调节一个生成可通行轨迹的扩散模型。其次，利用自动生成的离线标签，我们训练了一个路径净空预测头，用于对扩散模型生成的轨迹进行评分和排序。扩散调节以及轨迹排序头都依赖于机器人的具身标记（embodiment token），这使得我们的模型能够根据机器人的尺寸生成并选择轨迹。在三个模拟环境中，ViLiNT 的成功率比同类最先进的纯视觉基准模型（NoMaD）平均提高了 166%。这种性能提升通过在障碍物区域进行导航的漫游车真实部署得到了证实。这些结果表明，将多模态融合与我们的碰撞预测机制相结合，可以提高越野导航的鲁棒性。

## Abstract
Goal-conditioned navigation models for ground robots trained using supervised learning show promising zero-shot transfer, but their collision-avoidance capability nevertheless degrades under distribution shift, i.e. environmental, robot or sensor configuration changes. We propose ViLiNT a multimodal, attention-based policy for goal navigation, trained on heterogeneous data from multiple platforms and environments, which improves robustness with two key features. First, we fuse RGB images, 3D LiDAR point clouds, a goal embedding and a robot's embodiment descriptor with a transformer architecture to capture complementary geometry and appearance cues. The transformer's output is used to condition a diffusion model that generates navigable trajectories. Second, using automatically generated offline labels, we train a path clearance prediction head for scoring and ranking trajectories produced by the diffusion model. The diffusion conditioning as well as the trajectory ranking head depend on a robot's embodiment token that allows our model to generate and select trajectories with respect to the robot's dimensions. Across three simulated environments, ViLiNT improves Success Rate on average by 166\% over equivalent state-of-the-art vision-only baseline (NoMaD). This increase in performance is confirmed through real-world deployments of a rover navigating in obstacle fields. These results highlight that combining multimodal fusion with our collision prediction mechanism leads to improved off-road navigation robustness.

---

## 论文详细总结（自动生成）

这是一份关于论文《Multimodal embodiment-aware navigation transformer》（多模态具身感知导航 Transformer）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的地面机器人导航模型（如 NoMaD）虽然在零样本迁移方面表现出色，但在面对**分布偏移**（如环境变化、机器人尺寸改变或传感器配置更替）时，其避障能力会显著下降。纯视觉模型在缺乏显著特征或视野受限的环境下容易失效，且大多数端到端模型缺乏显式的机制来处理死锁情况或根据机器人的物理尺寸（具身信息）调整路径。本文旨在开发一种鲁棒的、多模态的局部运动规划架构，使其能同时兼顾导航效率与安全性。

### 2. 方法论：核心思想与技术细节
论文提出了 **ViLiNT** 模型，其核心思想是将多模态感知与具身信息融合，并通过扩散模型生成候选轨迹，再由专门的净空预测头进行筛选。
*   **多模态融合（Transformer Encoder）：** 融合了 RGB 图像序列（提取外观特征）、3D LiDAR 点云（提取几何特征）、2D 目标坐标和机器人具身标记（宽度和长度）。
*   **LiDAR 标记化：** 使用 PointTransformerV3 提取点云特征，并将其映射到极坐标网格（Polar Grid）中，通过广义平均池化（GeM）生成固定数量的 Token。
*   **轨迹生成（Diffusion Policy）：** 使用条件扩散模型生成多组候选路径点序列。该过程受 Transformer 输出的全局场景描述符调节。
*   **路径净空预测（Clearance Prediction）：** 这是一个关键创新。该模块使用交叉注意力机制，以候选轨迹点和机器人尺寸作为查询（Query），在 LiDAR Token 上预测每个路径点的安全边际。
*   **推理与选择：** 在推理时生成 $N$ 条轨迹，优先选择预测净空大于安全阈值且最接近目标的轨迹。若无安全轨迹，则通过掩码目标 Token 进行探索性采样以跳出局部最优。

### 3. 实验设计
*   **数据集：** 整合了 7 个异构数据集（包括 RELLIS-3D, TartanDrive 2.0, SCAND 等），总计约 98.5 小时的训练数据，涵盖越野、人行道等多种地形。
*   **仿真场景：** 在 Isaac-Sim 中构建了三个难度等级的开放环境（灌木丛、狭窄通道、杂乱障碍物）。
*   **真实部署：** 使用配备 Zed2i 相机和 Ouster LiDAR 的 Clearpath Husky 机器人进行实地验证。
*   **对比基准（Benchmarks）：**
    *   **NoMaD-FT：** 针对目标任务微调后的最先进纯视觉扩散策略。
    *   **NoMaD-Col：** 在 NoMaD 基础上增加视觉净空预测头的版本。
    *   **TEB-Elev：** 基于高程图的传统局部规划器（非学习类基准）。
    *   **消融实验：** 包含仅 LiDAR 或仅图像输入的 ViLiNT 变体。

### 4. 资源与算力
*   **硬件：** 真实机器人部署使用了 **Nvidia AGX Orin** 嵌入式计算平台。
*   **训练细节：** 论文未明确说明训练所用的具体 GPU 型号和总训练时长，但提到对基准模型进行了 20 个 Epoch 的微调，并使用了 Flash-Attention 等优化技术。

### 5. 实验数量与充分性
*   **实验规模：** 在仿真环境中针对每个场景进行了 100 次测试（共 300 次），并记录了成功率（SR）和碰撞率（CR）。
*   **充分性：** 实验设计较为全面，不仅对比了最先进的学习模型，还对比了传统导航算法。通过消融实验验证了 LiDAR 和图像模态的互补性。
*   **客观性：** 引入了“具身影响”实验，通过人为改变输入的机器人尺寸参数，观察模型是否能自动调整路径（如从狭窄通道转向开阔地带），证明了模型对具身信息的感知能力。

### 6. 主要结论与发现
*   **性能飞跃：** ViLiNT 在仿真环境中的成功率比纯视觉模型 NoMaD 平均提高了 **166%**，碰撞率降低了 **62%**。
*   **模态互补：** 仅靠 LiDAR 的模型在避障上优于纯视觉，但结合图像后性能进一步提升，说明图像提供了 LiDAR 缺失的语义/动态线索。
*   **具身感知：** 模型能够根据输入的机器人尺寸动态选择路径，实现了真正的“具身感知”导航。
*   **鲁棒性：** 在真实世界的障碍物场中，ViLiNT 达到了 85% 的成功率，远高于 NoMaD-FT 的 15%。

### 7. 优点
*   **显式安全机制：** 不同于大多数端到端模型将避障隐含在模仿学习中，ViLiNT 引入了基于几何数据的净空预测头，大幅增强了安全性。
*   **多模态深度融合：** 极坐标网格标记化方法有效地平衡了 LiDAR 数据的稀疏性与 Transformer 的计算效率。
*   **灵活性：** 能够处理不同尺寸的机器人，且在推理时具备一定的探索能力（通过目标掩码）。

### 8. 不足与局限
*   **轨迹生成限制：** 轨迹选择与生成是解耦的，如果扩散模型生成的初始候选集中没有安全路径，模型只能通过重新采样来解决，效率可能受限。
*   **局部规划局限：** 作为一个局部规划器，它在面对复杂的迷宫式环境（如进入死胡同）时仍可能失败，需要更高层的全局路径规划配合。
*   **计算开销：** 相比纯视觉模型，引入 3D 点云处理和扩散模型多次采样对嵌入式设备的实时计算能力提出了更高要求。

（完）
