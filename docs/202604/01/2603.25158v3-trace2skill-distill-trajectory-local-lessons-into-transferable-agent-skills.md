---
title: "Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills"
title_zh: Trace2Skill：将轨迹局部经验蒸馏为可迁移的智能体技能
authors: "Jingwei Ni, Yihao Liu, Xinpeng Liu, Yutao Sun, Mengyu Zhou, Pengyu Cheng, Dexin Wang, Erchao Zhao, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25158v3"
tags: ["query:tp"]
score: 8.0
evidence: 利用大语言模型将局部轨迹经验提炼为智能体技能
tldr: Trace2Skill是一个旨在提升大语言模型智能体领域技能的框架。针对手动编写技能效率低和自动生成技能易碎片化的问题，该框架模仿人类专家，通过并行分析多样化的执行轨迹，提取局部经验并归纳整合为统一、无冲突的技能库。实验证明，该方法在电子表格、视觉问答和数学推理等任务中显著提升了性能，且生成的技能具有极强的跨模型和跨领域迁移能力。
selection_source: fresh_fetch
motivation: 现有的智能体技能生成方法要么依赖浅层知识，要么过度拟合局部轨迹，导致生成的技能脆弱且难以泛化。
method: 采用并行子智能体分析多样化执行轨迹，通过归纳推理将提取的局部经验层级化地整合为统一的声明式技能目录。
result: 在多个复杂领域显著优于强基准模型，且由较小模型生成的技能能大幅提升更大模型在分布外任务上的表现。
conclusion: 复杂的智能体经验可以被封装为高度可迁移的声明式技能，无需参数更新或外部检索即可实现显著的性能跨越。
---

## 摘要
为大语言模型（LLM）智能体配备特定领域的技能对于处理复杂任务至关重要。然而，人工编写技能面临严重的扩展性瓶颈。相反，自动化的技能生成往往产生脆弱或碎片化的结果，因为它要么依赖于浅层的参数化知识，要么顺序地过拟合于不可泛化的轨迹局部经验。为了克服这一问题，我们提出了 Trace2Skill，这是一个模仿人类专家编写技能方式的框架：通过全面分析广泛的执行经验，将其蒸馏为单一且全面的指南。Trace2Skill 并非顺序地对单个轨迹做出反应，而是派遣一组并行的子智能体来分析多样化的执行池。它提取特定于轨迹的经验，并通过归纳推理将其分层整合为一个统一、无冲突的技能目录。Trace2Skill 既支持深化现有的人工编写技能，也支持从零开始创建新技能。在电子表格、VisionQA 和数学推理等具有挑战性的领域进行的实验表明，Trace2Skill 相比于包括 Anthropic 官方 xlsx 技能在内的强基线有显著提升。至关重要的是，这种基于轨迹的演化不仅是记忆任务实例或特定模型的怪癖：演化出的技能可以跨 LLM 规模迁移，并能泛化到分布外（OOD）设置。例如，由 Qwen3.5-35B 在其自身轨迹上演化出的技能，在 WikiTableQuestions 上将 Qwen3.5-122B 智能体的表现提升了高达 57.65 个绝对百分点。最终，我们的结果表明，复杂的智能体经验可以被打包成高度可迁移的声明式技能——无需参数更新，无需外部检索模块，且可以使用参数量小至 35B 的开源模型。

## Abstract
Equipping Large Language Model (LLM) agents with domain-specific skills is critical for tackling complex tasks. Yet, manual authoring creates a severe scalability bottleneck. Conversely, automated skill generation often yields fragile or fragmented results because it either relies on shallow parametric knowledge or sequentially overfits to non-generalizable trajectory-local lessons. To overcome this, we introduce Trace2Skill, a framework that mirrors how human experts author skills: by holistically analyzing broad execution experience before distilling it into a single, comprehensive guide. Instead of reacting sequentially to individual trajectories, Trace2Skill dispatches a parallel fleet of sub-agents to analyze a diverse pool of executions. It extracts trajectory-specific lessons and hierarchically consolidates them into a unified, conflict-free skill directory via inductive reasoning. Trace2Skill supports both deepening existing human-written skills and creating new ones from scratch. Experiments in challenging domains, such as spreadsheet, VisionQA and math reasoning, show that Trace2Skill significantly improves upon strong baselines, including Anthropic's official xlsx skills. Crucially, this trajectory-grounded evolution does not merely memorize task instances or model-specific quirks: evolved skills transfer across LLM scales and generalize to OOD settings. For example, skills evolved by Qwen3.5-35B on its own trajectories improved a Qwen3.5-122B agent by up to 57.65 absolute percentage points on WikiTableQuestions. Ultimately, our results demonstrate that complex agent experience can be packaged into highly transferable, declarative skills -- requiring no parameter updates, no external retrieval modules, and utilizing open-source models as small as 35B parameters.

---

## 论文详细总结（自动生成）

以下是对论文《Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何高效且自动化地为大语言模型（LLM）智能体构建高质量、可泛化的领域技能？
*   **研究动机**：
    *   **人工瓶颈**：手动编写专家级技能（如标准作业程序 SOP）难以大规模扩展。
    *   **现有自动方法缺陷**：现有的自动生成方法要么仅依赖 LLM 的参数化知识（缺乏领域细节），要么采用“在线顺序更新”模式（容易对局部错误过度拟合，导致技能碎片化且难以泛化）。
*   **整体含义**：论文提出了 **Trace2Skill** 框架，模拟人类专家“先观察大量案例，再总结通用指南”的过程，通过并行分析大量执行轨迹，将局部经验蒸馏为统一、声明式的智能体技能。

### 2. 方法论：核心思想与关键技术
Trace2Skill 包含三个阶段的流水线：
*   **阶段 1：轨迹生成（Trajectory Generation）**
    *   智能体在任务集上并行运行，收集成功（$T^+$）和失败（$T^-$）的执行轨迹。
*   **阶段 2：并行多智能体补丁提议（Parallel Multi-Agent Patch Proposal）**
    *   **成功分析员（Success Analyst）**：识别贡献成功的通用模式。
    *   **错误分析员（Error Analyst）**：采用 **ReAct 风格的智能体循环**，通过检查文件、对比真值来诊断失败根因，并验证修复方案，从而生成高质量的补丁。
    *   **核心思想**：所有分析员并行工作，互不干扰，确保观察结果的多样性。
*   **阶段 3：无冲突补丁整合（Conflict-Free Consolidation）**
    *   **层级化合并**：采用 $\log$ 级别的层级结构，将数百个局部补丁逐步合并。
    *   **归纳推理**：合并过程中，LLM 被要求识别“普遍模式”（在多个独立补丁中重复出现的编辑），将其提炼为通用原则，而丢弃偶然的、特定于实例的修改。
    *   **程序化校验**：通过格式检查和冲突检测（如行号重叠）确保最终技能文件的完整性。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **电子表格（Spreadsheet）**：SpreadsheetBench（验证集、全集）。
    *   **分布外泛化（OOD）**：WikiTableQuestions（将维基百科表格转为 xlsx 格式）。
    *   **数学推理**：DAPO-Math、AIME 2026。
    *   **视觉问答（VQA）**：DocVQA（文档图像理解）。
*   **对比基准（Benchmarks）**：
    *   **No Skill**：无技能输入。
    *   **Human-Written**：Anthropic 官方提供的专家编写技能。
    *   **Parametric**：仅靠 LLM 自身知识生成的初始草案。
    *   **对比方法**：ReasoningBank（基于检索的经验库）、顺序编辑模式（Sequential Editing）。
*   **模型**：主要使用 Qwen3.5-122B 和 Qwen3.5-35B 进行交叉验证（即 A 模型生成的技能给 B 模型用）。

### 4. 资源与算力
*   **算力设备**：使用了 **8 卡 A800 节点**。
*   **执行效率**：
    *   **轨迹生成**：200 条轨迹（每条 50+ 轮对话）在 122B 模型上耗时少于 2 GPU 小时。
    *   **技能整合**：Trace2Skill 的并行模式仅需约 **3 分钟**，而传统的顺序更新模式（Seq-B=1）在相同任务量下需约 60 分钟。
    *   **推理成本**：生成的技能是纯文本，推理时无需外部检索模块，节省了检索延迟和算力。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了电子表格、数学、视觉三大领域，使用了数百个任务实例进行演化。
*   **充分性分析**：
    *   **跨模型验证**：专门测试了 35B 模型生成的技能在 122B 模型上的表现，证明了技能的“模型无关性”。
    *   **消融实验**：对比了“仅错误分析”、“仅成功分析”以及“智能体化分析 vs. 单次调用分析”，实验设计全面。
    *   **客观性**：使用了官方评测指标，并进行了三次随机种子取平均值，结果较为客观公平。

### 6. 主要结论与发现
*   **极强的迁移性**：35B 模型生成的技能在 WikiTQ 任务上让 122B 智能体提升了 **57.65%**。这挑战了“经验必须与特定模型绑定”的假设。
*   **超越人工**：在某些设置下，自动演化的技能优于 Anthropic 官方的人工技能。
*   **并行优于顺序**：并行整合不仅效率高，且通过归纳推理能更好地提取通用 SOP，避免了顺序更新带来的“上下文漂移”。
*   **声明式优于检索式**：将经验蒸馏为单一文档的效果显著优于维护一个庞大的检索经验库（ReasoningBank）。

### 7. 优点
*   **方法论创新**：将技能演化视为一个“归纳推理”过程，而非简单的记忆或顺序编辑。
*   **实用性强**：生成的技能是标准的 Markdown 目录，可直接适配任何智能体框架，无需参数微调。
*   **低门槛**：证明了中等规模的开源模型（35B）即可完成高质量的技能蒸馏，无需依赖昂贵的闭源 API。

### 8. 不足与局限
*   **因果量化缺失**：目前无法精确孤立单个补丁对最终性能提升的边际贡献。
*   **动态剪枝不足**：尚未实现根据推理时的实际需求动态修剪技能中无效或冗余部分的功能。
*   **领域覆盖**：虽然涵盖了三大领域，但在更具开放性的网页浏览或复杂软件操作方面的验证尚在进行中。
*   **幻觉风险**：尽管有程序化校验，但合并过程中 LLM 仍可能产生逻辑矛盾，需进一步增强鲁棒性。

（完）
