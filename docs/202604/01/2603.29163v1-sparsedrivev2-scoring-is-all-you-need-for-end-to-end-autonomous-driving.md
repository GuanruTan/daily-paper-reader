---
title: "SparseDriveV2: Scoring is All You Need for End-to-End Autonomous Driving"
authors: "Wenchao Sun, Xuewu Lin, Keyu Chen, Zixiang Pei, Xiang Li, Yining Shi, Sifa Zheng"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29163v1"
tags: ["query:tp"]
score: 9.0
evidence: 自动驾驶候选轨迹评分
tldr: 评估了端到端自动驾驶规划中静态与动态轨迹词汇表的表现。
selection_source: fresh_fetch
motivation: 自动驾驶候选轨迹评分。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
End-to-end multi-modal planning has been widely adopted to model the uncertainty of driving behavior, typically by scoring candidate trajectories and selecting the optimal one. Existing approaches generally fall into two categories: scoring a large static trajectory vocabulary, or scoring a small set of dynamically generated proposals. While static vocabularies often suffer from coarse discretization of the action space, dynamic proposals provide finer-grained precision and have shown stronger empirical performance on existing benchmarks. However, it remains unclear whether dynamic generation is fundamentally necessary, or whether static vocabularies can already achieve comparable performance when they are sufficiently dense to cover the action space. In this work, we start with a systematic scaling study of Hydra-MDP, a representative scoring-based method, revealing that performance consistently improves as trajectory anchors become denser, without exhibiting saturation before computational constraints are reached. Motivated by this observation, we propose SparseDriveV2 to push the performance boundary of scoring-based planning through two complementary innovations: (1) a scalable vocabulary representation with a factorized structure that decomposes trajectories into geometric paths and velocity profiles, enabling combinatorial coverage of the action space, and (2) a scalable scoring strategy with coarse factorized scoring over paths and velocity profiles followed by fine-grained scoring on a small set of composed trajectories. By combining these two techniques, SparseDriveV2 achieves 92.0 PDMS and 90.1 EPDMS on NAVSIM, with 89.15 Driving Score and 70.00 Success Rate on Bench2Drive with a lightweight ResNet-34 as backbone. Code and model are released at https://github.com/swc-17/SparseDriveV2.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SparseDriveV2** 的端到端自动驾驶规划框架。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：端到端自动驾驶规划通常采用多模态方案，即对候选轨迹进行评分并选择最优者。目前主要分为两类：**静态词汇表评分**（预定义轨迹簇）和**动态提案生成**（如回归或扩散模型）。
*   **核心问题**：业界普遍认为静态词汇表受限于离散化粗糙，性能不如动态生成方法。本文通过对 Hydra-MDP 的扩展性研究发现，性能随轨迹锚点密度的增加而持续提升且未见饱和。因此，核心问题变为：**动态生成是否真的必要？如果静态词汇表足够密集且评分高效，能否达到甚至超越动态生成的性能？**
*   **研究动机**：通过构建超大规模、超密集的静态轨迹词汇表，证明“评分（Scoring）”这一简单范式在端到端规划中的强大潜力。

### 2. 论文提出的方法论
SparseDriveV2 提出了两个核心创新点来解决超密集词汇表的表示与计算压力：
*   **可扩展的词汇表表示（Factorized Representation）**：
    *   **时空解耦**：将轨迹分解为**几何路径（Geometric Path）**和**速度剖面（Velocity Profile）**。
    *   **组合覆盖**：分别对路径和速度进行聚类形成子词汇表。通过两者的组合（$N_p \times N_v$），可以生成远超传统方法的轨迹密度（例如 $1024 \times 256 \approx 26$ 万个锚点，比之前的方法密 32 倍）。
*   **可扩展的评分策略（Scalable Scoring Strategy）**：
    *   **粗粒度分解评分**：首先独立对路径和速度进行评分，利用轻量级 MLP 和场景特征交互，快速筛选出 Top-K 个高质量路径和速度。
    *   **细粒度轨迹评分**：仅对筛选后的少量组合轨迹（如 400 条）进行复杂的时空重构和重调节（Re-conditioning），利用可变形聚合（Deformable Aggregation）进行精确评分。
*   **训练目标**：结合了基于距离的软分类损失（模仿学习）和基于规则的教师系统蒸馏（度量监督）。

### 3. 实验设计
*   **数据集与场景**：
    *   **NAVSIM (v1 & v2)**：大规模离线仿真基准，包含 1000+ 训练场景。
    *   **Bench2Drive**：基于 CARLA 的闭环测试基准，包含 44 种交互场景和 220 条测试路线。
*   **Benchmark 与对比方法**：
    *   对比了 VADv2、UniAD、Hydra-MDP、DiffusionDriveV2（扩散模型）、ipad（回归模型）等当前最先进的端到端方法。
    *   主要指标包括 PDMS（PDM 分数）、EPDMS（扩展 PDM 分数）、Driving Score（驾驶分数）和 Success Rate（成功率）。

### 4. 资源与算力
论文明确提到了实验的算力消耗：
*   **NAVSIM 训练**：使用 **8 台 NVIDIA L20 GPU**（每台 48GB 显存），总 Batch Size 为 128，训练 10 个 Epoch。
*   **Bench2Drive 训练**：使用 **16 台 NVIDIA L20 GPU**，总 Batch Size 为 96，采用两阶段训练（感知任务 100 Epoch + 联合训练 10 Epoch）。
*   **推理效率**：通过两层解码器过滤机制，使得在处理 26 万个候选轨迹时仍能保持高效。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了系统性的扩展性研究（从 1024 到 32768 个锚点的性能对比）、主干网络对比（ResNet-34 vs V2-99）、以及在两个主流 Benchmark 上的全面评测。
*   **消融实验**：针对词汇表规模（$N_p, N_v$ 的不同组合）、评分设计（注意力机制类型、是否进行轨迹重调节）进行了详细对比。
*   **充分性评价**：实验设计非常充分，涵盖了开环和闭环测试，且对比了不同技术路线（评分 vs 生成）的代表性方法，结果具有较强的说服力和客观性。

### 6. 论文的主要结论与发现
*   **静态评分的潜力**：只要词汇表足够密集，纯评分方法可以超越复杂的动态生成方法（如扩散模型）。
*   **性能表现**：SparseDriveV2 在 NAVSIM 上达到 92.0 PDMS，在 Bench2Drive 上达到 89.15 驾驶分数，刷新了多项 SOTA 记录。
*   **效率优势**：使用轻量级的 ResNet-34 作为主干网络，其表现甚至优于使用巨大主干网络（如 V2-99）的其他方法。

### 7. 优点
*   **范式简洁**：回归到简单的评分逻辑，避免了扩散模型复杂的迭代去噪过程或回归模型的不稳定性。
*   **创新性解耦**：通过路径与速度的分解组合，巧妙解决了“覆盖范围”与“计算成本”之间的矛盾。
*   **强泛化性**：在离线数据集和闭环仿真中均表现出色，证明了该方法的鲁棒性。

### 8. 不足与局限
*   **导航信息依赖**：定性分析显示，在某些复杂路口，模型仍可能做出错误的导航决策，这可能源于输入导航信息的表征不足。
*   **数据分布限制**：尽管词汇表很密，但其基础锚点仍来源于人类驾驶数据的聚类，对于极端罕见场景（Out-of-distribution）的覆盖可能仍存在盲区。
*   **传感器配置**：实验主要基于多摄像头输入，未深入探讨在纯激光雷达或多模态融合下的表现差异。

（完）
