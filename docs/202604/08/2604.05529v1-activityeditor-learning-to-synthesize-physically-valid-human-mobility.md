---
title: "ActivityEditor: Learning to Synthesize Physically Valid Human Mobility"
title_zh: ActivityEditor：学习合成符合物理规律的人类移动轨迹
authors: "Chenjie Yang, Yutian Jiang, Anqi Liang, Wei Qi, Chenyu Wu, Junbo Zhang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05529v1"
tags: ["query:tp"]
score: 8.0
evidence: 基于意图的智能体生成移动轨迹
tldr: 针对人类移动建模中数据稀缺和跨区域迁移难的问题，本文提出 ActivityEditor 框架。该框架采用双大语言模型（LLM）智能体协作模式：意图智能体基于人口统计先验生成活动链，编辑智能体通过强化学习结合物理约束对轨迹进行迭代优化。该方法实现了零样本跨区域轨迹合成，在保持统计保真度的同时确保了物理有效性，为数据匮乏地区的城市应用提供了强泛化性的解决方案。
selection_source: fresh_fetch
motivation: 现有的数据驱动方法受限于数据稀缺，难以在缺乏历史轨迹的区域生成符合物理规律的人类移动轨迹。
method: 提出一种双 LLM 智能体框架，通过意图智能体生成语义活动链，并利用基于强化学习的编辑智能体在物理约束下迭代优化轨迹。
result: 实验表明 ActivityEditor 在跨城市零样本迁移中表现优异，生成的轨迹具有极高的统计保真度和物理合理性。
conclusion: 该框架为数据稀缺场景下的移动性模拟提供了一种稳健且具有高度泛化能力的解决方案。
---

## 摘要
人类移动建模对于各种城市应用至关重要。然而，现有的数据驱动方法通常面临数据稀缺的问题，限制了它们在历史轨迹不可用或受限地区的适用性。为了填补这一空白，我们提出了 ActivityEditor，这是一种专为零样本跨区域轨迹生成设计的新型双大语言模型（LLM）智能体框架。我们的框架将复杂的合成任务分解为两个协作阶段。具体而言，基于意图的智能体利用人口统计驱动的先验知识生成结构化的人类意图和粗略的活动链，以确保高层级的社会语义一致性。随后，编辑智能体通过强制执行人类移动规律的迭代修订来细化这些输出，从而获得移动轨迹。这种能力是通过基于现实世界物理约束的多重奖励强化学习获得的，使智能体能够内化移动规律并确保高保真度的轨迹生成。大量实验表明，ActivityEditor 在跨越不同城市环境迁移时表现出卓越的零样本性能。它保持了高统计保真度和物理有效性，为数据稀缺场景下的移动模拟提供了一种鲁棒且具有高度泛化能力的解决方案。我们的代码可在以下网址获取：https://anonymous.4open.science/r/ActivityEditor-066B。

## Abstract
Human mobility modeling is indispensable for diverse urban applications. However, existing data-driven methods often suffer from data scarcity, limiting their applicability in regions where historical trajectories are unavailable or restricted. To bridge this gap, we propose \textbf{ActivityEditor}, a novel dual-LLM-agent framework designed for zero-shot cross-regional trajectory generation. Our framework decomposes the complex synthesis task into two collaborative stages. Specifically, an intention-based agent, which leverages demographic-driven priors to generate structured human intentions and coarse activity chains to ensure high-level socio-semantic coherence. These outputs are then refined by editor agent to obtain mobility trajectories through iteratively revisions that enforces human mobility law. This capability is acquired through reinforcement learning with multiple rewards grounded in real-world physical constraints, allowing the agent to internalize mobility regularities and ensure high-fidelity trajectory generation. Extensive experiments demonstrate that \textbf{ActivityEditor} achieves superior zero-shot performance when transferred across diverse urban contexts. It maintains high statistical fidelity and physical validity, providing a robust and highly generalizable solution for mobility simulation in data-scarce scenarios. Our code is available at: https://anonymous.4open.science/r/ActivityEditor-066B.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ActivityEditor** 的新型框架，旨在解决人类移动轨迹合成中的数据稀缺和物理一致性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：高保真的人类移动轨迹数据对城市规划、交通管理和公共安全至关重要。
*   **核心痛点**：
    *   **数据稀缺与隐私**：受隐私法规限制，大规模轨迹数据难以获取。
    *   **冷启动问题**：新规划区域缺乏历史轨迹，传统数据驱动模型无法工作。
    *   **物理失真**：现有基于大语言模型（LLM）的生成方法通常是单次（Single-pass）生成，缺乏自我修正机制，导致生成的轨迹经常违反物理规律（如时间重叠、活动逻辑不通）。
*   **研究目标**：在完全没有目标区域历史轨迹的情况下（零样本），合成既符合社会语义逻辑又严格遵守物理约束的人类移动轨迹。

### 2. 方法论
ActivityEditor 采用**双 LLM 智能体（Dual-Agent）**协作架构，将任务分解为“生成”与“编辑”两个阶段：
*   **意图驱动智能体（Intention-driven Agent）**：
    *   **核心思想**：利用人口统计学先验（如年龄、职业、收入）推断用户的潜在意图。
    *   **功能**：生成一个粗略的活动草案（Activity Draft），确保高层级的社会语义一致性（例如：退休人员不应有通勤活动）。
*   **编辑智能体（Editor Agent）**：
    *   **核心思想**：作为“规则审计员”，通过迭代修订解决时空冲突。
    *   **关键技术**：
        *   **编辑操作**：支持增加（Add）、删除（Delete）、平移（Shift）、替换（Replace）和拆分（Split）五种操作。
        *   **强化学习对齐（GRPO）**：使用**群体相对策略优化（Group Relative Policy Optimization）**算法。通过定义格式奖励（$R_{fmt}$）、物理约束奖励（$R_{con}$，检查24小时覆盖和连续性）和统计保真度奖励（$R_{sim}$），使模型内化物理规律。
*   **训练流程**：先通过 GPT-5.2 生成思维链（CoT）数据进行监督微调（SFT），再利用 GRPO 进行强化学习对齐。

### 3. 实验设计
*   **数据集**：使用 2017 年美国国家家庭出行调查（NHTS）数据，提取了加利福尼亚（CA）、亚利桑那（AZ）、佐治亚（GA）和俄克拉荷马（OK）四个州的数据。
*   **Benchmark（评估指标）**：共 12 个指标，分为三类：
    *   **序列相似性**：Accuracy, F1-score, Edit Distance, BLEU。
    *   **时间对齐**：JSD 散度评估活动时长和开始时间的分布。
    *   **分布一致性**：评估活动类型分布和轨迹长度的统计特性。
*   **对比方法**：
    *   **传统深度学习**：DeepMove, LSTPM。
    *   **LLM 专用框架**：CoPB, RAGHome。
    *   **通用 LLM**：ChatGPT-5.2, Qwen3-8B, LLaMA3.1-8B。

### 4. 资源与算力
*   **模型规模**：主要基于 **Qwen3-8B** 和 **LLaMA3.1-8B** 进行微调。
*   **微调技术**：使用 LoRA（Rank=32, Alpha=64）。
*   **训练细节**：
    *   SFT 阶段：3 个 Epoch，学习率 5e-5。
    *   GRPO 阶段：1000 个 Step，学习率 1e-5，每个 Prompt 生成 8 个采样分支。
*   **算力说明**：文中未明确提及具体的 GPU 型号和数量，但从参数设置（Batch Size=2, Accumulation Steps=8）来看，该实验可以在消费级或中端服务器级 GPU 集群上完成。

### 5. 实验数量与充分性
*   **实验规模**：在四个不同地理特征的州进行了测试，并进行了跨州迁移实验（如 CA 训练，AZ/GA/OK 测试）。
*   **消融实验**：对比了单智能体 vs. 双智能体、SFT vs. GRPO 的效果，验证了各组件的必要性。
*   **充分性评价**：实验设计较为全面，不仅关注个体轨迹的准确性，还通过 JSD 散度关注了群体统计特征的还原。跨区域的零样本测试有力地证明了模型的泛化能力。

### 6. 主要结论与发现
*   **性能领先**：ActivityEditor 在所有测试区域的统计保真度和物理有效性上均显著优于基准模型。
*   **编辑机制的必要性**：多智能体协作模式比单次生成模式更能有效减少逻辑幻觉。
*   **强化学习的作用**：GRPO 能够帮助模型从单纯的“模仿轨迹”转变为“理解并遵守物理约束”，显著提升了轨迹的合法性。
*   **零样本迁移**：模型成功捕捉到了跨区域通用的移动逻辑（如人口统计学与行为意图的映射），证明了其在冷启动场景下的实用价值。

### 7. 优点与亮点
*   **解耦设计**：将复杂的移动建模分解为语义意图和物理编辑，降低了生成难度。
*   **物理对齐**：引入 GRPO 强化学习，解决了 LLM 在时空推理上的顽疾。
*   **零样本能力**：不依赖目标城市的私有轨迹数据，具有极强的可扩展性和隐私保护特性。

### 8. 不足与局限
*   **空间粒度**：目前仅能生成区域级（Zone-level）的活动链，无法精确到具体的 POI 坐标（经纬度）。
*   **计算开销**：多轮迭代编辑和 LLM 推理相比传统轻量级模型具有更高的延迟。
*   **幻觉风险**：尽管有编辑机制，LLM 仍可能生成现实中不存在的“幻影”地址或逻辑。
*   **应用限制**：目前主要针对 24 小时内的日常活动，对于更长周期的移动模式（如跨城旅行）尚未探讨。

（完）
