---
title: Hypergraph-State Collaborative Reasoning for Multi-Object Tracking
title_zh: 面向多目标跟踪的超图状态协同推理
authors: "Zikai Song, Junqing Yu, Yi-Ping Phoebe Chen, Wei Yang, Xinchao Wang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12665v1"
tags: ["query:tp"]
score: 8.0
evidence: 多目标跟踪中的运动推理与轨迹连续性
tldr: 针对多目标跟踪中运动估计不稳定及遮挡易断连的问题，本文提出了一种协作推理框架。该框架通过HyperSSM架构，将超图计算与状态空间模型（SSM）相结合，利用多个相关目标间的空间运动关联和时间平滑性进行联合推理。这种设计能有效抑制噪声并推断遮挡目标的运动轨迹，在MOT17、DanceTrack等多个主流数据集上达到了SOTA性能。
selection_source: fresh_fetch
motivation: 现有的运动估计方法在面对噪声预测和目标遮挡时表现不稳定，导致轨迹容易断裂。
method: 提出HyperSSM架构，利用动态超图捕捉空间运动相关性，并结合状态空间模型（SSM）确保时间上的平滑过渡。
result: 在MOT17、MOT20、DanceTrack和SportsMOT等四个主流基准测试中均取得了最先进的性能。
conclusion: 空间共识与时间相干性的协同优化能显著提升多目标跟踪在复杂场景下的鲁棒性和稳定性。
---

## 摘要
运动推理是多目标跟踪（MOT）的基石，因为它能够实现跨帧目标的持续关联。然而，现有的运动估计方法面临两个主要局限性：（1）由噪声或概率预测引起的不稳定性；（2）在遮挡情况下的脆弱性，一旦视觉线索消失，轨迹往往会发生断裂。为了克服这些问题，我们提出了一种协同推理框架，通过多个相关目标之间的联合推理来增强运动估计。通过允许具有相似运动状态的目标相互约束和优化，该框架能够稳定带噪声的轨迹，并在目标被遮挡时推断出合理的运动连续性。为了实现这一概念，我们设计了 HyperSSM，这是一种集成超图计算和状态空间模型（SSM）的架构，用于统一的时空推理。超图模块通过动态超边捕捉空间运动相关性，而 SSM 则通过结构化状态转移来保证时间平滑性。这种协同设计实现了空间共识和时间相干性的同步优化，从而实现了鲁棒且稳定的运动估计。在涵盖各种运动模式和场景复杂性的四个主流且多样化的基准测试（MOT17、MOT20、DanceTrack 和 SportsMOT）上进行的大量实验表明，我们的方法在广泛的跟踪场景中均达到了最先进的性能。

## Abstract
Motion reasoning serves as the cornerstone of multi-object tracking (MOT), as it enables consistent association of targets across frames. However, existing motion estimation approaches face two major limitations: (1) instability caused by noisy or probabilistic predictions, and (2) vulnerability under occlusion, where trajectories often fragment once visual cues disappear. To overcome these issues, we propose a collaborative reasoning framework that enhances motion estimation through joint inference among multiple correlated objects. By allowing objects with similar motion states to mutually constrain and refine each other, our framework stabilizes noisy trajectories and infers plausible motion continuity even when target is occluded. To realize this concept, we design HyperSSM, an architecture that integrates Hypergraph computation and a State Space Model (SSM) for unified spatial-temporal reasoning. The Hypergraph module captures spatial motion correlations through dynamic hyperedges, while the SSM enforces temporal smoothness via structured state transitions. This synergistic design enables simultaneous optimization of spatial consensus and temporal coherence, resulting in robust and stable motion estimation. Extensive experiments on four mainstream and diverse benchmarks(MOT17, MOT20, DanceTrack, and SportsMOT) covering various motion patterns and scene complexities, demonstrate that our approach achieves state-of-the-art performance across a wide range of tracking scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《Hypergraph-State Collaborative Reasoning for Multi-Object Tracking》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：多目标跟踪（MOT）中的运动估计面临两大挑战：一是**预测不稳定性**（学习型方法易受噪声干扰，导致轨迹波动）；二是**遮挡脆弱性**（视觉特征消失后，独立预测的轨迹容易断裂或发生身份切换）。
*   **研究背景**：现有的运动估计方法（如卡尔曼滤波或基于学习的预测器）通常将每个目标视为独立个体进行预测，忽略了场景中多个目标之间（如成群结队的行人、共同起舞的舞者）的运动关联性。

### 2. 方法论：HyperSSM 架构
*   **核心思想**：提出**协同推理（Collaborative Reasoning）**框架。通过让运动状态相似的目标相互约束和补充，利用空间共识稳定噪声，并利用跨目标运动迁移在遮挡时推断轨迹。
*   **关键技术细节**：
    *   **超图计算（Hypergraph）**：动态构建运动感知超图。将每个目标视为顶点，通过距离阈值 $\theta$ 将运动状态（速度、方向）相似的目标聚合在同一个“超边”中。
    *   **状态空间模型（SSM）**：利用 SSM 的结构化状态转移特性来捕捉长程时间依赖，确保运动的平滑性。
    *   **HyperSSM Block**：将超图的“聚合-分发”（Gathering-Scattering）机制嵌入到 SSM 的状态转移过程中。公式上，隐藏状态的更新不仅取决于当前输入，还取决于通过超图卷积处理后的空间协同特征。
*   **算法流程**：检测（YOLOX）→ 轨迹嵌入编码 → 多层 HyperSSM 块进行时空协同推理 → 预测下一帧位置 → 多级自适应关联。

### 3. 实验设计
*   **数据集与场景**：
    *   **线性运动场景**：MOT17、MOT20（侧重拥挤场景下的行人跟踪）。
    *   **非线性运动场景**：DanceTrack（复杂的舞者动作）、SportsMOT（高速运动的体育场景）。
*   **对比方法**：
    *   **基于查询的方法**：MOTR, MOTRv3, MOTIP, SAMBA 等。
    *   **基于运动的方法**：ByteTrack, OC-SORT, StrongSORT, TrackSSM, DiffMOT 等。
*   **评价指标**：HOTA（主要指标）、IDF1、MOTA、AssA 等。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA Tesla V100 GPU** 进行实验。
*   **软件环境**：PyTorch 1.22, Python 3.11。
*   **训练细节**：
    *   Batch size 为 128。
    *   MOT17/20/SportsMOT 训练 360 个 epoch，DanceTrack 训练 260 个 epoch。
    *   推理速度：在不同规模的 YOLOX 模型下，FPS 范围约为 17.5 到 28.8。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准数据集上进行了全面测试，并提交至官方服务器评估。
*   **消融实验**：针对 HyperSSM 层数（发现 8 层最优）、轨迹段长度（5 帧最优）、超图阈值参数 $\theta$ 以及不同运动模型（卡尔曼滤波 vs SSM vs HyperSSM）进行了详细对比。
*   **充分性评价**：实验覆盖了从简单线性到复杂非线性的多种运动模式，对比了近三年的最新 SOTA 方法，实验设计客观、公平，验证了协同推理在不同难度场景下的普适性。

### 6. 主要结论与发现
*   **性能领先**：HyperSSM 在所有四个数据集上均达到了 SOTA 性能。例如在 MOT17 上 HOTA 达到 66.9，在 DanceTrack 上 HOTA 达到 65.7。
*   **协同效应**：实验证明，引入高阶空间关系（超图）能显著提升 SSM 在运动预测中的稳定性，尤其是在处理遮挡和复杂非线性轨迹时，比独立预测的 SSM 表现更优。

### 7. 优点与亮点
*   **创新视角**：打破了“独立预测”的传统范式，引入“协同推理”概念，利用群体运动的一致性来解决个体预测的噪声问题。
*   **架构融合**：巧妙地将超图的空间建模能力与 SSM 的时间序列建模能力结合，形成了一个统一的时空推理块。
*   **鲁棒性强**：可视化结果显示，该方法生成的轨迹比基准方法更平滑，且在严重遮挡下能更好地维持目标身份。

### 8. 不足与局限
*   **计算开销**：虽然 SSM 本身效率较高，但动态构建超图和进行多层超图卷积会增加额外的计算负担（FPS 略低于纯 SSM 方法）。
*   **参数敏感性**：超图构建依赖于阈值 $\theta$，在运动极其杂乱或目标密度极低的场景下，如何自适应地确定“协同群体”仍是一个挑战。
*   **依赖检测器**：作为 motion-based 框架，其最终性能仍高度依赖于前端检测器（如 YOLOX）的质量。

（完）
