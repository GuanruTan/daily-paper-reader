---
title: "Forecasting the Past: Gradient-Based Distribution Shift Detection in Trajectory Prediction"
title_zh: 预测过去：轨迹预测中基于梯度的分布偏移检测
authors: "Michele De Vita, Julian Wiederer, Vasileios Belagiannis"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12425v1"
tags: ["query:tp"]
score: 10.0
evidence: 自动驾驶轨迹预测中的分布偏移检测
tldr: 针对自动驾驶中轨迹预测模型因分布偏移导致失效的问题，本文提出一种基于梯度的自监督检测方法。该方法通过训练一个事后解码器，利用已观测轨迹的前半部分预测后半部分，并以预测损失对解码器最后一层梯度的L2范数作为偏移评分。该方法不影响原模型性能，在Shifts和Argoverse数据集上表现优异，并能实现运动规划器的早期碰撞预警。
selection_source: fresh_fetch
motivation: 轨迹预测模型在面临训练与测试环境不一致的分布偏移时容易产生错误预测，危及自动驾驶安全。
method: 采用自监督方式训练一个事后解码器来预测已观测轨迹的后半段，并利用该任务损失函数的梯度范数作为分布偏移的检测指标。
result: 在Shifts和Argoverse数据集上显著提升了分布偏移检测性能，并成功实现了Highway模拟器中深度Q网络规划器的早期碰撞检测。
conclusion: 该方法在不干扰原预测模型的前提下，为自动驾驶系统提供了一种高效且通用的分布偏移与风险检测方案。
---

## 摘要
由于训练和测试条件之间的分布偏移，轨迹预测模型在现实世界的自动驾驶中经常失效。这种分布偏移，无论是行为上的还是环境上的，都会导致模型在陌生情况下做出错误的预测，从而构成严重风险。我们提出了一种自监督方法，以事后（post-hoc）方式在自监督任务上训练解码器，该任务是根据观测轨迹的前半部分预测其后半部分。该预测损失相对于解码器最后一层的梯度的 L2 范数定义了一个用于识别分布偏移的分数。我们的方法首先不影响轨迹预测模型，确保不干扰原始预测性能；其次，在 Shifts 和 Argoverse 数据集的轨迹预测分布偏移检测方面表现出显著改进。此外，我们还展示了该方法可用于在 Highway 模拟器中提前检测深度 Q 网络（DQN）运动规划器的碰撞。源代码可在 https://github.com/Michedev/forecasting-the-past 获取。

## Abstract
Trajectory prediction models often fail in real-world automated driving due to distributional shifts between training and test conditions. Such distributional shifts, whether behavioural or environmental, pose a critical risk by causing the model to make incorrect forecasts in unfamiliar situations. We propose a self-supervised method that trains a decoder in a post-hoc fashion on the self-supervised task of forecasting the second half of observed trajectories from the first half. The L2 norm of the gradient of this forecasting loss with respect to the decoder's final layer defines a score to identify distribution shifts. Our approach, first, does not affect the trajectory prediction model, ensuring no interference with original prediction performance and second, demonstrates substantial improvements on distribution shift detection for trajectory prediction on the Shifts and Argoverse datasets. Moreover, we show that this method can also be used to early detect collisions of a deep Q-Network motion planner in the Highway simulator. Source code is available at https://github.com/Michedev/forecasting-the-past.

---

## 论文详细总结（自动生成）

这是一份关于论文《Forecasting the Past: Gradient-Based Distribution Shift Detection in Trajectory Prediction》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：自动驾驶中的轨迹预测模型在面对“分布偏移”（Distribution Shift, DS）时容易失效。这些偏移包括环境变化（如新城市、极端天气）和行为异常（如违规驾驶、罕见转弯）。
*   **研究动机**：现有的检测方法（如基于重构、一类分类或不确定性估计的方法）存在局限性，例如难以区分输入复杂性与真正的异常、需要复杂的超参数调优，或依赖于模型校准。
*   **核心理念**：提出一种“事后”（Post-hoc）检测方法，通过观察模型在处理未知数据时梯度的变化来识别分布偏移，且不影响原始预测模型的性能。

### 2. 方法论
*   **核心思想**：引入一个自监督任务——**“预测过去”**。即利用已观测到的前半段历史轨迹，去预测后半段已发生的轨迹。
*   **关键技术细节**：
    1.  **轨迹分割**：将观测到的历史轨迹 $X_{past}$ 分为两部分：$X_{past,1}$（前半段）和 $X_{past,2}$（后半段）。
    2.  **事后解码器**：冻结预训练好的轨迹预测编码器，在其基础上训练一个轻量级解码器 $d_{past}$，任务是根据 $X_{past,1}$ 的特征表示预测 $X_{past,2}$。
    3.  **梯度评分**：在测试阶段，计算自监督预测损失 $L_{past}$ 相对于解码器最后一层预激活值 $h_L$ 的梯度。
    4.  **异常分数公式**：使用梯度 $L2$ 范数作为偏移评分：$S = \|\nabla_{h_L} L_{past}\|_2$。高梯度值通常意味着输入数据与训练分布不符。

### 3. 实验设计
*   **数据集与场景**：
    *   **Shifts 数据集**：评估环境偏移（不同城市如莫斯科 vs 特拉维夫、不同天气如晴天 vs 雨雪）。
    *   **Argoverse 数据集**：通过人工剔除特定行为（如左转、右转、超速）来模拟行为分布偏移。
    *   **Highway 模拟器**：在交互式环境中进行在线实验，检测规划器（PPO/DQN）是否即将发生碰撞。
*   **对比方法（Benchmark）**：
    *   包括 RIP-BC、RIP-DIM、lGMM（Shifts 基准）。
    *   传统异常检测算法：KDE（核密度估计）、OC-SVM（一类支持向量机）、IF（孤立森林）。
    *   自监督基准：AE（自动编码器）、MAE（掩码自动编码器）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **推理效率**：提到了运行耗时。在 Highway 模拟器中，该方法的异常评分计算耗时约为 3-4 毫秒，远低于自动驾驶系统通常的 100 毫秒决策周期，证明了其具有实时监测的潜力。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个大型公开数据集和一个交互式模拟器，实验设置包括环境偏移、行为偏移和在线碰撞预警。
*   **消融实验**：对比了基于损失值（Loss-based）与基于梯度（Gradient-based）的评分效果，以及不同自监督任务（AE, MAE vs. Forecasting the Past）的差异。
*   **充分性评价**：实验设计较为全面，跨越了离线评估与在线仿真。但在 Argoverse 上的实验显示出一定的方差，作者坦诚在某些极端行为过滤下训练动态可能不够稳定。

### 6. 主要结论与发现
*   **性能提升**：在 Shifts 数据集上，AUROC 从基准的 56.8% 提升至 **71.0%**。
*   **早期预警**：在 Highway 模拟器中，该方法能在碰撞发生前 **1 秒** 成功发出预警，AUROC 在复杂场景下显著优于重构类方法（如从 64% 提升至 96%）。
*   **梯度优于损失**：实验证明，损失函数本身的数值在检测偏移时效果接近随机猜测（约 50% AUROC），而梯度的范数能捕捉到更丰富的判别信息。

### 7. 优点
*   **非侵入性**：作为 Post-hoc 方法，不需要重新训练或修改原始轨迹预测模型，保证了预测精度不受损。
*   **通用性**：可以适配任何基于“编码器-解码器”架构的轨迹预测模型（如 Transformer, HiVT）。
*   **实时性**：计算开销极小，适合集成到自动驾驶的安全监控模块中。

### 8. 不足与局限
*   **训练稳定性**：在 Argoverse 的行为偏移实验中，观察到 OOD 样本的梯度有时反而低于 ID 样本，这可能与训练数据的极端过滤导致的收敛问题有关。
*   **场景覆盖**：虽然涵盖了多种偏移，但对于传感器故障、地图错误等硬件/感知层面的分布偏移尚未进行验证。
*   **依赖性**：检测效果高度依赖于预训练编码器提取特征的能力，如果编码器本身对某种偏移完全不敏感，该方法可能失效。

（完）
