---
title: Self-Consistency for LLM-Based Motion Trajectory Generation and Verification
title_zh: 基于大语言模型的运动轨迹生成与验证的自一致性研究
authors: "Jiaju Ma, R. Kenny Jones, Jiajun Wu, Maneesh Agrawala"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29301v1"
tags: ["query:tp"]
score: 8.0
evidence: 基于大语言模型的运动轨迹生成与表示
tldr: 本研究将自一致性技术从自然语言推理扩展到视觉运动轨迹生成领域。针对LLM生成的运动路径，提出通过几何变换群（如刚体、相似、仿射变换）来定义轨迹间的一致性，并利用层次聚类识别一致轨迹组。该方法不仅提升了LLM轨迹生成的准确率，还显著增强了验证精度，为视觉任务中的LLM推理提供了轻量化且有效的优化方案。
selection_source: fresh_fetch
motivation: 旨在解决LLM在生成视觉运动轨迹时因缺乏几何约束而导致的一致性差和准确率低的问题。
method: 通过对LLM生成的多个轨迹进行采样，并利用几何变换群建立层次化聚类算法，以识别并提取最具代表性的一致轨迹族。
result: "该方法使LLM轨迹生成的准确率提升了4-6%，并在轨迹验证任务中比视觉语言模型基准提升了11%的精度。"
conclusion: 证明了结合几何变换的自一致性机制是提升LLM在视觉运动生成与验证任务中性能的有效手段。
---

## 摘要
自一致性已被证明是一种以轻量级、无监督方式提升大语言模型（LLM）在自然语言推理任务中表现的有效技术。在本研究中，我们探讨了如何将自一致性应用于视觉领域。具体而言，我们研究了由 LLM 生成的动态图形轨迹的生成与验证。给定一个提示词（例如，“让圆圈沿螺旋路径移动”），我们首先从 LLM 中采样多样化的运动轨迹，然后通过聚类识别出一致的轨迹组。我们的核心见解是将与提示词相关的形状族建模为一个原型轨迹与一组几何变换（如刚性变换、相似变换和仿射变换）的组合。如果两条轨迹在变换群允许的扭曲下可以相互转换，则认为它们是一致的。我们提出了一种算法，利用一组候选变换群之间的层级关系，自动恢复形状族。我们的方法将基于 LLM 的轨迹生成准确率提高了 4-6%。我们进一步扩展了该方法以支持验证，观察到相比视觉语言模型（VLM）基准，精确度提升了 11%。我们的代码和数据集可在 https://majiaju.io/trajectory-self-consistency 获取。

## Abstract
Self-consistency has proven to be an effective technique for improving LLM performance on natural language reasoning tasks in a lightweight, unsupervised manner. In this work, we study how to adapt self-consistency to visual domains. Specifically, we consider the generation and verification of LLM-produced motion graphics trajectories. Given a prompt (e.g., "Move the circle in a spiral path"), we first sample diverse motion trajectories from an LLM, and then identify groups of consistent trajectories via clustering. Our key insight is to model the family of shapes associated with a prompt as a prototype trajectory paired with a group of geometric transformations (e.g., rigid, similarity, and affine). Two trajectories can then be considered consistent if one can be transformed into the other under the warps allowable by the transformation group. We propose an algorithm that automatically recovers a shape family, using hierarchical relationships between a set of candidate transformation groups. Our approach improves the accuracy of LLM-based trajectory generation by 4-6%. We further extend our method to support verification, observing 11% precision gains over VLM baselines. Our code and dataset are available at https://majiaju.io/trajectory-self-consistency .

---

## 论文详细总结（自动生成）

以下是对论文《Self-Consistency for LLM-Based Motion Trajectory Generation and Verification》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：自一致性（Self-Consistency）在提升大语言模型（LLM）的自然语言推理能力方面表现卓越，其核心逻辑是“多数投票制”。然而，在视觉领域（如运动图形轨迹生成），由于输出是连续的坐标序列而非离散的文本，传统的“身份匹配（Identity Matching）”无法直接应用——两条语义相同的轨迹在像素级别几乎不可能完全一致。
*   **核心问题**：如何定义视觉轨迹之间的“一致性”，并利用这种一致性在无监督、无需训练的情况下提升 LLM 生成运动轨迹的准确性与验证能力？

### 2. 方法论
该论文提出了一种基于几何变换群的自一致性框架：
*   **核心思想**：将提示词（Prompt）诱导的轨迹建模为一个**形状族（Shape Family）**。形状族由一个“原型轨迹”和一组“几何变换群”（如李群 Lie Groups）定义。如果轨迹 A 可以通过某种几何变换（平移、旋转、缩放、剪切等）转变为轨迹 B，则认为两者在几何语义上是一致的。
*   **关键技术细节**：
    1.  **几何变换层级**：构建了一个从严苛到宽松的变换层级：刚体变换（Rigid） $\rightarrow$ 相似变换（Similarity） $\rightarrow$ 各向异性相似变换（Anisotropic Similarity） $\rightarrow$ 仿射变换（Affine）。
    2.  **不变性距离度量**：利用改进的迭代最近点算法（ICP）计算两条轨迹在特定变换群下的最小距离 $d_W$。
    3.  **决策准则**：
        *   **多数共识（Majority-Consensus）**：自底向上遍历层级，选择第一个能产生绝对多数簇（>50%）的变换群。
        *   **层级一致性（Hierarchical-Consistency）**：自顶向下遍历，寻找能保持最大簇成员完整性的最严苛变换群。
    4.  **验证机制**：通过判断查询轨迹是否属于由自一致性恢复出的“形状族”（即与原型的距离是否小于阈值 $\tau$）来进行验证。

### 3. 实验设计
*   **数据集与 Benchmark**：
    *   构建了一个包含 **224 个提示词** 的基准测试集，涵盖 35 种基础几何形状（如螺旋、正多边形、李萨如图形等）。
    *   每个提示词关联一个真值形状族（特定的原型和变换群）。
    *   **验证测试集**：包含 2240 条轨迹（每个提示词 5 正 5 负），由 GPT-4.1 和 GPT-5 生成并经人工/自动标注。
*   **对比方法**：
    *   **生成任务**：对比 LLM 直接生成（LLM-Direct）与采用自一致性策略后的生成效果。
    *   **验证任务**：对比本文方法与多模态大模型（VLM，如 GPT-4.1/5）直接观察图像进行验证的效果。

### 4. 资源与算力
*   **算力消耗**：论文未提及模型训练算力，因为该方法是**无需训练（Training-free）**的推理侧优化。
*   **推理效率**：在标准桌面 CPU 上，计算一对轨迹之间的变换不变距离平均耗时 **67 毫秒**。
*   **采样成本**：生成任务中每个提示词采样 $N=19$ 次。实验表明，当采样数 $N \ge 10$ 时，性能趋于稳定。

### 5. 实验数量与充分性
*   **实验规模**：进行了 224 组生成实验和 2240 组验证实验，覆盖了从简单几何体到复杂组合运动的多种场景。
*   **消融与鲁棒性分析**：
    *   对比了不同的 LLM 后端（GPT-4.1 vs GPT-5）。
    *   对聚类阈值 $\tau$ 进行了敏感度分析（在 32 倍范围内变动，F1 仅下降 7.2%）。
    *   对比了不同的决策准则（多数共识 vs 层级一致性）。
*   **客观性**：实验设计考虑了提示词的歧义性和 LLM 的多样性采样，对比基准（VLM）是目前最先进的商业模型，具有较高的说服力。

### 6. 主要结论与发现
*   **生成性能提升**：在无监督设置下，自一致性方法将 LLM 轨迹生成的准确率提升了 **4%–6%**。
*   **验证性能突破**：在轨迹验证任务中，该方法比 VLM 基准在精确度（Precision）上提升了 **11.8%**，F1 分数提升了 **5.6%**。
*   **模型校准**：发现 VLM 在验证视觉轨迹时存在严重的“过度肯定”偏差（预测 True 的比例远超实际），而本文方法通过几何约束实现了更好的校准。

### 7. 优点与亮点
*   **理论严谨**：将经典的几何变换群（李群）理论引入 LLM 推理，为视觉一致性提供了坚实的数学基础。
*   **无监督且轻量**：不需要对模型进行微调，仅通过推理侧的采样和聚类即可实现性能增益。
*   **通用性框架**：提出的“采样-聚类-恢复形状族”流程可以推广到其他具有几何属性的视觉生成任务（如矢量图生成、3D 场景建模）。

### 8. 不足与局限
*   **多原型限制**：目前的定义假设一个提示词对应一个原型。对于具有多个离散解释的提示词（如“七角星”有两种拓扑结构），该方法可能会丢失部分正确解。
*   **LLM 质量依赖**：如果 LLM 发生“模式崩溃（Mode Collapse）”，即生成的错误样本具有高度一致性，自一致性方法会失效。
*   **几何局限性**：目前仅支持全局几何变换，对于局部变形或非线性扭曲（如“像蛇一样移动”）的建模能力有限。
*   **坐标系依赖**：对于某些变换（如各向异性缩放），仍需假设 LLM 生成的样本中至少有一些是轴对齐的。

（完）
