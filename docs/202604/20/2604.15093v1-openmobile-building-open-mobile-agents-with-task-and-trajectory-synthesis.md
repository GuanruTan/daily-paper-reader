---
title: "OpenMobile: Building Open Mobile Agents with Task and Trajectory Synthesis"
title_zh: OpenMobile：通过任务与轨迹合成构建开放式移动智能体
authors: "Kanzhi Cheng, Zehao Li, Zheng Ma, Nuo Chen, Jialin Cao, Qiushi Sun, Zichen Ding, Fangzhi Xu, Hang Yan, Jiajun Chen, Anh Tuan Luu, Jianbing Zhang, Lewei Lu, Dahua Lin"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15093v1"
tags: ["query:tp"]
score: 8.0
evidence: 利用视觉语言模型为移动智能体合成轨迹
tldr: 针对移动智能体训练数据封闭且合成方法不透明的问题，本文提出开源框架 OpenMobile。该框架通过环境记忆驱动的任务合成流水线生成多样化指令，并采用策略切换机制在轨迹生成中捕获关键的错误恢复数据。实验显示，基于该数据微调的 Qwen2.5/3-VL 模型在 AndroidWorld 等基准测试中表现卓越，显著提升了开源移动智能体的性能水平。
selection_source: fresh_fetch
motivation: 当前领先的移动智能体模型训练数据和合成配方大多不透明，阻碍了开源社区的进一步研究。
method: 开发了基于环境探索的任务合成流水线和交替使用学习者与专家模型的策略切换轨迹生成方法。
result: "微调后的 Qwen3-VL 在 AndroidWorld 基准测试中取得 64.7% 的成功率，大幅领先于现有的开源数据方法。"
conclusion: OpenMobile 通过开源高质量的合成数据和方法论，为构建高性能、透明的移动智能体提供了重要支撑。
---

## 摘要
由视觉语言模型驱动的移动智能体在自动化移动任务方面展现出卓越的能力，近期领先的模型实现了显著的性能飞跃，例如在 AndroidWorld 上达到了近 70% 的成功率。然而，这些系统通常不公开其训练数据，且对其任务和轨迹合成方案保持不透明。我们提出了 OpenMobile，这是一个合成高质量任务指令和智能体轨迹的开源框架，包含两个关键组件：(1) 一个可扩展的任务合成流水线，通过探索构建全局环境记忆，并利用其生成多样化且有依据的指令；(2) 一种用于轨迹展开的策略切换策略，通过在学习者模型和专家模型之间交替，捕获标准模仿学习中经常缺失的关键错误恢复数据。在我们的数据上训练的智能体在三个动态移动智能体基准测试中取得了具有竞争力的结果：值得注意的是，我们微调的 Qwen2.5-VL 和 Qwen3-VL 在 AndroidWorld 上分别达到 51.7% 和 64.7%，远超现有的开源数据方法。此外，我们对合成指令与基准测试集之间的重叠进行了透明分析，验证了性能提升源于广泛的功能覆盖，而非基准测试过拟合。我们发布了数据和代码（https://njucckevin.github.io/openmobile/），旨在弥补数据差距并促进更广泛的移动智能体研究。

## Abstract
Mobile agents powered by vision-language models have demonstrated impressive capabilities in automating mobile tasks, with recent leading models achieving a marked performance leap, e.g., nearly 70% success on AndroidWorld. However, these systems keep their training data closed and remain opaque about their task and trajectory synthesis recipes. We present OpenMobile, an open-source framework that synthesizes high-quality task instructions and agent trajectories, with two key components: (1) The first is a scalable task synthesis pipeline that constructs a global environment memory from exploration, then leverages it to generate diverse and grounded instructions. and (2) a policy-switching strategy for trajectory rollout. By alternating between learner and expert models, it captures essential error-recovery data often missing in standard imitation learning. Agents trained on our data achieve competitive results across three dynamic mobile agent benchmarks: notably, our fine-tuned Qwen2.5-VL and Qwen3-VL reach 51.7% and 64.7% on AndroidWorld, far surpassing existing open-data approaches. Furthermore, we conduct transparent analyses on the overlap between our synthetic instructions and benchmark test sets, and verify that performance gains stem from broad functionality coverage rather than benchmark overfitting. We release data and code at https://njucckevin.github.io/openmobile/ to bridge the data gap and facilitate broader mobile agent research.

---

## 论文详细总结（自动生成）

### 论文总结：OpenMobile - 构建开放移动智能体

#### 1. 核心问题与整体含义（研究动机和背景）
当前移动智能体（Mobile Agents）在自动化任务方面取得了显著进展，领先的闭源模型在 AndroidWorld 等基准测试中已接近 70% 的成功率。然而，这些高性能模型背后的**训练数据和合成配方（Recipes）均不透明且未公开**。开源社区目前依赖的公开数据集（如 AndroidControl）存在标注噪声大、缺乏复杂思维链等问题，导致开源模型性能远落后于工业界（约 30% vs 70%）。本文旨在通过提出 **OpenMobile** 框架，提供一套可扩展、高质量的任务与轨迹合成方法，弥补开源数据的鸿沟，并深入探讨驱动智能体性能提升的关键数据属性。

#### 2. 方法论：核心思想与技术细节
OpenMobile 框架由两个核心阶段组成：

*   **可扩展任务合成（Scalable Task Synthesis）：**
    *   **解耦探索与生成：** 不同于以往从单一轨迹推导指令的方法，OpenMobile 先通过随机漫步（Random Walk）探索环境，构建**全局环境记忆（Global Environment Memory）**。
    *   **记忆构建：** 对屏幕进行视觉去重（感知哈希），利用强 VLM 标注 UI 元素功能，并建立语义索引。
    *   **记忆增强合成：** 在生成指令时，结合当前屏幕、短期记忆（邻近屏幕的功能）和长期记忆（语义相关的跨屏幕功能），促使模型生成复杂、多步且符合逻辑的指令。
*   **策略切换轨迹展开（Policy-Switching Rollout）：**
    *   **解决分布偏移：** 传统的专家蒸馏只提供完美轨迹，导致智能体在推理出错时无法恢复。
    *   **错误干预切换（Error-Intervention Switch）：** 在轨迹生成过程中，由学习者模型（Learner）先行，监控器实时检测其是否偏离目标。一旦偏离，立即切换到专家模型（Expert）进行纠正。这种方式捕获了大量的“错误-诊断-修正”信号，显著增强了模型的鲁棒性。

#### 3. 实验设计
*   **数据集与场景：** 涵盖了 20 个 Android 应用程序，合成了 2.8K 条指令和 34K 个动作步骤。
*   **基准测试（Benchmarks）：**
    *   **AndroidWorld：** 行业标准基准，包含 116 个任务。
    *   **AndroidLab：** 系统化的 Android 智能体基准，包含 138 个任务。
    *   **MobileWorld：** 更具挑战性的长程、跨应用任务基准，包含 201 个任务。
*   **对比方法：**
    *   **商业/闭源模型：** GPT-4o, Gemini-3-Pro, Step-GUI, MAI-UI, UI-Venus-1.5 等。
    *   **开源数据模型：** UI-S1, ScaleCUA 等。

#### 4. 资源与算力
*   **模型微调：** 使用 LLaMA-Factory 框架，对 Qwen2.5-VL-7B 和 Qwen3-VL-8B 进行全参数微调。
*   **训练参数：** Batch size 为 32，学习率为 1e-5，训练 3 个 Epoch。
*   **算力细节：** 论文**未明确说明**具体的 GPU 型号和数量，但提到在推理阶段使用了 vLLM 库，并在强化学习实验中使用了包含 100 多个 Android 模拟器实例的 OS-Themis 框架。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在三个主流动态基准上进行了全面评估，并针对 Qwen2.5-VL 和 Qwen3-VL 两个不同代际的底座进行了验证。
*   **消融实验：** 针对任务合成策略（对比 OS-Genesis 等）和轨迹展开策略（对比专家蒸馏、自我进化、随机切换）做了详细对比。
*   **分析深度：** 进行了数据规模缩放实验、错误恢复能力量化分析，以及最为关键的**数据污染/重叠分析**（通过语义相似度检测合成数据与测试集的重叠度）。
*   **评价：** 实验设计非常充分且透明，尤其是对“性能提升是否来自过拟合”的探讨非常客观，具有很高的参考价值。

#### 6. 主要结论与发现
*   **性能突破：** 基于 OpenMobile 数据训练的 Qwen3-VL-8B 在 AndroidWorld 上达到 **64.7%** 的成功率，极大地缩小了开源与闭源模型之间的差距。
*   **错误恢复是关键：** 策略切换生成的纠错数据显著提升了智能体在真实环境中的生存能力。
*   **功能覆盖驱动泛化：** 性能的提升并非源于对测试指令的背诵，而是源于合成数据对应用功能的广泛覆盖。
*   **底座能力不可忽视：** 即使数据相同，更强的底座模型（Qwen3 vs Qwen2.5）依然能带来显著的性能增益。

#### 7. 优点
*   **方法论创新：** 提出的“解耦合成”和“错误干预切换”策略有效地解决了移动智能体训练中指令多样性不足和分布偏移的问题。
*   **开源贡献：** 不仅发布了模型，还开源了数据合成框架和完整的训练数据，对开源社区贡献巨大。
*   **透明度高：** 针对数据污染问题做了详尽的定量分析，证明了方法的有效性和泛化性。

#### 8. 不足与局限
*   **探索策略单一：** 目前环境探索主要依赖随机漫步，对于深层或隐藏功能的挖掘效率可能有限。
*   **RL 增益不明显：** 论文提到在动态基准上，强化学习（RL）相比监督微调（SFT）并未表现出显著优势，这反映出 GUI 领域的 RL 训练仍具挑战性。
*   **环境局限：** 实验主要集中在 Android 模拟器环境，尚未广泛验证在真实物理手机或 iOS 环境下的表现。

（完）
