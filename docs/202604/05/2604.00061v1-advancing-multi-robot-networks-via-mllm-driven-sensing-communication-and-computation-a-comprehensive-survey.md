---
title: "Advancing Multi-Robot Networks via MLLM-Driven Sensing, Communication, and Computation: A Comprehensive Survey"
title_zh: 通过 MLLM 驱动的感知、通信与计算推进多机器人网络：综述
authors: "Hyun Jong Yang, Howon Lee, Kyuhong Shim, Jeongho Kwak, Hyunsoo Kim, Donghoon Kim, Khoa Anh Ngo, Sehyun Ryu, Jaehyun Choi, Youbin Kim, Chanjun Moon, Michael Ryoo, Byonghyo Shim"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00061v1"
tags: ["query:tp"]
score: 8.0
evidence: 基于多模态大模型的机器人路径规划与意图理解
tldr: 本综述探讨了多模态大模型（MLLM）如何驱动多机器人网络中感知、通信与计算（R2X）的协同优化。针对复杂任务中海量传感器数据带来的网络压力，研究提出以MLLM理解的高层意图作为资源调度的核心，动态分配带宽与计算资源。文章通过四个端到端实验（如仓库导航、垃圾分类）验证了该架构在降低延迟和提升任务成功率方面的显著优势，为多机器人协同提供了系统级优化方案。
selection_source: fresh_fetch
motivation: 现实中的多机器人协作面临海量传感器数据传输与有限网络资源之间的矛盾，亟需一种能理解复杂指令并高效调度资源的方法。
method: 提出一种由MLLM驱动的意图到资源（intent-to-resource）编排框架，实现感知、通信与计算的联合优化。
result: 通过数字孪生导航和真机垃圾分类等四个演示场景，证明了系统级编排在有效载荷、延迟和任务成功率上均优于纯本地处理方案。
conclusion: MLLM引导的感知-通信-计算一体化设计是实现高效多机器人协同的关键，能显著提升受限资源下的任务执行效率。
---

## 摘要
想象一下，由多模态大语言模型（MLLM）驱动的高级人形机器人，在仓储物流、制造业和安全救援等行业中协同执行任务。虽然单个机器人表现出局部自主性，但现实任务要求多个智能体之间进行协作，并共享海量的传感器数据流。通信是必不可少的，但传输全部数据可能会使网络过载，特别是当系统级编排器或基于云的 MLLM 融合多模态输入以进行路径规划或异常检测时。这些任务通常由高级自然语言指令发起。这种意图充当了资源优化的过滤器：通过 MLLM 理解目标，系统可以有选择地激活相关的感知模态，动态分配带宽，并确定计算任务的部署位置。因此，R2X 从根本上说是一个“意图到资源”的编排问题，即在资源约束下联合优化感知、通信和计算，以最大化任务级的成功率。本综述探讨了集成设计如何为 MLLM 指导下的多机器人协作铺平道路。我们回顾了最先进的感知模态、通信策略和计算方法，重点介绍了推理如何在设备端模型与强大的边缘/云服务器之间进行切分。我们展示了四个端到端演示（感知 -> 通信 -> 计算 -> 行动）：(i) 具有预测链路上下文的数字孪生仓库导航，(ii) 移动驱动的主动 MCS 控制，(iii) 具有语义感知切换功能的 FollowMe 机器人，以及 (iv) 通过边缘辅助 MLLM 定位的真实硬件开放词汇垃圾分类。我们强调了系统级指标——负载、延迟和成功率——以展示为什么 R2X 编排优于纯设备端的基准方案。

## Abstract
Imagine advanced humanoid robots, powered by multimodal large language models (MLLMs), coordinating missions across industries like warehouse logistics, manufacturing, and safety rescue. While individual robots show local autonomy, realistic tasks demand coordination among multiple agents sharing vast streams of sensor data. Communication is indispensable, yet transmitting comprehensive data can overwhelm networks, especially when a system-level orchestrator or cloud-based MLLM fuses multimodal inputs for route planning or anomaly detection.   These tasks are often initiated by high-level natural language instructions. This intent serves as a filter for resource optimization: by understanding the goal via MLLMs, the system can selectively activate relevant sensing modalities, dynamically allocate bandwidth, and determine computation placement. Thus, R2X is fundamentally an intent-to-resource orchestration problem where sensing, communication, and computation are jointly optimized to maximize task-level success under resource constraints.   This survey examines how integrated design paves the way for multi-robot coordination under MLLM guidance. We review state-of-the-art sensing modalities, communication strategies, and computing approaches, highlighting how reasoning is split between on-device models and powerful edge/cloud servers. We present four end-to-end demonstrations (sense -> communicate -> compute -> act): (i) digital-twin warehouse navigation with predictive link context, (ii) mobility-driven proactive MCS control, (iii) a FollowMe robot with a semantic-sensing switch, and (iv) real-hardware open-vocabulary trash sorting via edge-assisted MLLM grounding. We emphasize system-level metrics -- payload, latency, and success -- to show why R2X orchestration outperforms purely on-device baselines.

---

## 论文详细总结（自动生成）

这篇论文《Advancing Multi-Robot Networks via MLLM-Driven Sensing, Communication, and Computation: A Comprehensive Survey》对多机器人网络在多模态大模型（MLLM）驱动下的感知、通信与计算集成设计进行了深入探讨。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然单体机器人的局部自主性已取得显著进步，但现实中的复杂任务（如仓储物流、灾难救援）需要多机器人协同。
*   **核心矛盾**：多机器人系统面临海量传感器数据传输需求与有限网络带宽/延迟约束之间的矛盾。传统的通信优化（如最大化吞吐量）并不直接等同于任务的成功。
*   **核心问题**：如何将人类的高层自然语言指令（意图）转化为底层的资源调度策略？论文提出了**R2X（Robot-to-Everything）**范式，旨在解决“意图到资源（Intent-to-Resource）”的编排问题，通过 MLLM 引导感知、通信和计算的联合优化。

### 2. 论文提出的方法论
论文提出了一个统一的 R2X 架构，核心思想是利用 MLLM 作为系统编排器，实现“感知-通信-计算-行动”的闭环：
*   **感知编排**：采用语义感知和特征压缩技术。不再传输原始图像，而是提取与任务相关的特征向量（如 VQ 编码），将数据量压缩至原始的千分之一，同时保留关键语义。
*   **通信编排**：引入主动式链路适配。利用数字孪生和射线追踪技术预测未来信道质量，动态调整调制编码方案（MCS）和发射功率，以应对移动性带来的延迟反馈问题。
*   **计算编排**：采用分层架构。简单的反射性动作在本地处理，复杂的全局规划和语义推理（如开放词汇识别）卸载到边缘或云端服务器。
*   **意图映射**：MLLM 将自然语言指令解析为结构化的 JSON 配置，直接参数化底层的路径规划器和无线资源分配器。

### 3. 实验设计
论文通过四个端到端演示（Demo）验证了 R2X 范式：
*   **Demo I：数字孪生仓库导航**。在 NVIDIA Isaac Sim 中构建，对比了“走走停停（无通信）”、原始流传输（LORC-P）和语义通信（LORC-SC-P）。
*   **Demo II：主动 MCS 控制**。在城市移动场景下，利用 MLLM 预测信道变化，对比了理想 MCS、反应式 MCS（基于过期反馈）和 MLLM 驱动的主动 MCS。
*   **Demo III：FollowMe 机器人真机实验**。在 WiFi 环境下，根据 RSSI 信号强度动态切换 JPEG 压缩与 VQ 语义特征传输。
*   **Demo IV：开放词汇垃圾分类真机实验**。利用边缘 MLLM（如 Qwen2.5-VL）识别未见过的物体（如纸杯、特定垃圾桶），对比了本地 YOLO（闭集识别）和边缘辅助的开放词汇识别。

### 4. 资源与算力
论文明确列出了实验使用的硬件环境：
*   **计算平台**：AMD Ryzen 5950X CPU，2 × NVIDIA RTX 3090 GPU（24GB 显存）。
*   **模型使用**：
    *   编排器：LLaMA 3 8B。
    *   视觉识别与定位：Gemini 3 Flash, Qwen2.5-VL-7B。
    *   本地触发：YOLOv8n。
*   **延迟数据**：LLM 编排耗时约 0.88s，端到端闭环（感知到指令）总延迟约 1.02s。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了从高保真数字孪生（Ray-tracing）到移动性仿真，再到真实硬件（移动底盘+机械臂）的完整跨度。
*   **充分性**：每组实验均设置了合理的基准（Baselines），如纯本地处理、传统压缩方案等。实验不仅测量了链路层指标（BLER、吞吐量），更重点评估了任务层指标（任务完成时间、成功率、指令到动作延迟 CTA）。
*   **客观性**：论文诚实地记录了 LLM 带来的推理延迟，并讨论了在 1.4s 的网格移动周期内完成 1.02s 推理的实时可行性。

### 6. 主要结论与发现
*   **语义压缩的威力**：语义特征传输比原始图像流节省了 1000 倍以上的带宽，显著降低了上行链路拥塞导致的机器人停顿。
*   **预测优于反应**：在移动场景下，利用 MLLM 结合环境语义预测信道，比基于过期 CSI 的反应式调整能显著降低误码率（BLER）和重传延迟。
*   **开放词汇的灵活性**：边缘辅助的 MLLM 能够处理机器人本地模型无法识别的“长尾”物体，消除了重新训练模型的负担。
*   **系统级增益**：R2X 编排在复杂场景下的任务完成时间比纯本地方案缩短了约 40%-50%。

### 7. 优点
*   **全栈集成**：打破了机器人学、计算机视觉和无线通信之间的壁垒，提出了真正的跨层优化框架。
*   **意图驱动**：将 LLM 的角色从简单的“聊天机器人”提升为复杂的“系统资源编排器”。
*   **实战导向**：提供了详细的真机实验数据和延迟分解，具有很强的工程参考价值。

### 8. 不足与局限
*   **可扩展性挑战**：目前的实验主要集中在 1-2 个机器人的场景，在数十个机器人共享带宽和边缘算力时的竞争冲突（Contention）尚需进一步研究。
*   **模型延迟**：虽然 1s 左右的推理延迟对于路径规划尚可，但对于高频控制（如机械臂精细操作）仍然过慢。
*   **环境依赖**：Demo I 依赖于高精度的数字孪生和射线追踪，在未知或快速变化的环境中，这种“链路上下文”的获取成本较高。
*   **安全风险**：论文虽提到了安全方向，但未深入实验 LLM 受到提示词注入（Prompt Injection）攻击时对物理机器人行为的影响。

（完）
