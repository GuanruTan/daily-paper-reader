---
title: "Ufil: A Unified Framework for Infrastructure-based Localization"
title_zh: Ufil：一种基于基础设施定位的统一框架
authors: "Simon Schäfer, Lucas Hegerath, Marius Molz, Massimo Marcon, Bassam Alrifaee"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21471v1"
tags: ["query:tp"]
score: 8.0
evidence: 基于基础设施的道路用户定位与预测
tldr: Ufil是一个针对基础设施定位的统一框架，旨在解决现有系统感知、跟踪与中间件紧耦合导致的碎片化问题。该框架提供标准化的对象模型和可重用的多目标跟踪组件（如预测、关联、状态更新等），支持C++/ROS 2。通过集成车联网消息、激光雷达和路面传感器，Ufil在仿真和实测中均实现了车道级定位精度（误差约0.3米）和低延迟（小于100毫秒），显著提升了基础设施定位系统的开发效率和跨平台兼容性。
selection_source: fresh_fetch
motivation: 现有的基础设施定位系统通常采用碎片化、应用特定的技术栈，导致感知、跟踪与中间件紧耦合，难以重用和扩展。
method: 提出Ufil框架，通过标准化对象模型和模块化接口（涵盖预测、检测、关联、更新及轨迹管理），实现跨平台、多源数据融合的定位流水线。
result: 在CARLA仿真和小型测试床中，该框架实现了约0.3米的横向定位精度和低于100毫秒的端到端延迟。
conclusion: Ufil提供了一个开源、可扩展且具有比例无关执行模型的工具，有效促进了基础设施定位技术的研发与集成。
---

## 摘要
基于基础设施的定位通过提供道路使用者的状态估计来增强道路安全和交通管理。然而，其发展受到碎片化、特定于应用的软件栈的阻碍，这些软件栈将感知、跟踪和中间件紧密耦合。我们推出了 Ufil，这是一个具有标准化对象模型和可重用多目标跟踪组件的基于基础设施定位的统一框架。Ufil 为预测、检测、关联、状态更新和轨迹管理提供了接口和参考实现，允许研究人员在不重新实现整个流水线的情况下改进组件。Ufil 是开源的 C++/ROS 2 软件，配有文档和可执行示例。我们通过将三种异构数据源集成到一个单一的定位流水线中来演示 Ufil，该流水线结合了：(i) 广播 ETSI ITS-G5 协作感知消息的车载单元，(ii) 基于激光雷达的路侧传感器节点，以及 (iii) 路面敏感层。该流水线在 CARLA 模拟器和小型 CAV 试验台中无需修改即可运行，展示了 Ufil 与规模无关的执行模型。在模拟和试验台分别包含 423 辆和 355 辆汽车的三车道高速公路场景中，融合系统实现了车道级的横向精度，在 CARLA 中的平均横向位置均方根误差（RMSE）为 0.31 米，在 CPM Lab 中为 0.29 米，平均绝对航向误差约为 2.2°。在两种环境下，从感知到融合输出的中值端到端延迟在所有模态下均保持在 100 毫秒以下。

## Abstract
Infrastructure-based localization enhances road safety and traffic management by providing state estimates of road users. Development is hindered by fragmented, application-specific stacks that tightly couple perception, tracking, and middleware. We introduce Ufil, a Unified Framework for Infrastructure-Based Localization with a standardized object model and reusable multi-object tracking components. Ufil offers interfaces and reference implementations for prediction, detection, association, state update, and track management, allowing researchers to improve components without reimplementing the pipeline. Ufil is open-source C++/ROS 2 software with documentation and executable examples. We demonstrate Ufil by integrating three heterogeneous data sources into a single localization pipeline combining (i) vehicle onboard units broadcasting ETSI ITS-G5 Cooperative Awareness Messages, (ii) a lidar-based roadside sensor node, and (iii) an in-road sensitive surface layer. The pipeline runs unchanged in the CARLA simulator and a small-scale CAV testbed, demonstrating Ufil's scale-independent execution model. In a three-lane highway scenario with 423 and 355 vehicles in simulation and testbed, respectively, the fused system achieves lane-level lateral accuracy with mean lateral position RMSEs of 0.31 m in CARLA and 0.29 m in the CPM Lab, and mean absolute orientation errors around 2.2°. Median end-to-end latencies from sensing to fused output remain below 100 ms across all modalities in both environments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Ufil** 的开源框架，旨在解决基于基础设施的道路用户定位系统中存在的软件栈碎片化和紧耦合问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然基于基础设施的感知（如路侧传感器）能有效弥补车载感知的盲区并提升交通管理效率，但目前的开发流程高度碎片化。现有的系统通常将感知、跟踪算法与特定的中间件紧密耦合，导致算法难以重用，且从仿真到现实部署（Sim-to-Real）的迁移成本极高。
*   **核心目标**：开发一个统一、模块化、且与硬件/规模无关的定位框架，使研究人员能够专注于改进单一组件（如关联算法或运动模型），而无需重新构建整个流水线。

### 2. 方法论
Ufil 的核心是一个基于 C++ 和 ROS 2 的模块化多目标跟踪（MOT）流水线，其关键技术细节包括：
*   **统一对象模型**：定义了标准化的状态向量（位置、速度、航向角、角速度）、尺寸、存在概率及类别概率（7类）。该模型支持历史记录，便于回溯关联。
*   **模块化 MOT 循环**：
    *   **预测 (Prediction)**：内置 6 种运动模型（如恒定速度 CV、恒定加速度 CA 等），支持线性与非线性转换。
    *   **检测 (Detection)**：提供接口将原始传感器数据（点云、图像）转换为对象假设。
    *   **关联 (Association)**：支持多种代价度量（欧氏距离、马氏距离、Wasserstein 距离、IoU）和求解器（匈牙利算法、LAPJV、贪婪算法、单纯形法）。
    *   **更新 (Update)**：基于卡尔曼滤波（KF/EKF）及其变体，处理异构传感器的状态更新。
    *   **管理 (Management)**：负责轨迹的创建、删除（基于时间或存在概率）及历史剪枝。
*   **跨平台执行模型**：通过抽象层确保同一套代码可以在 CARLA 仿真器、缩微智能车实验室（CPM Lab）以及真实道路部署中无缝运行。

### 3. 实验设计
*   **实验场景**：一个三车道的高速公路路段。
*   **数据源融合**：集成了三种异构数据源：
    1.  **V2I 通信**：车载单元广播的 ETSI CAM 消息。
    2.  **路侧激光雷达 (Lidar)**：安装在路边的传感器节点。
    3.  **路面敏感层 (SSL)**：嵌入路面的压力感应层。
*   **对比与基准**：
    *   在 **CARLA 仿真环境**中进行高保真模拟。
    *   在 **CPM Lab 缩微测试床**中进行物理验证（使用缩微车 $\mu$Cars 和物理传感器）。
    *   对比了不同关联算法（如欧氏距离 vs. Wasserstein 距离）在交叉路径场景下的表现。

### 4. 资源与算力
*   **硬件设备**：实验使用了 Intel RealSense L515 激光雷达、Sensing Tex 压力感应垫以及分布式计算架构（RSU 路侧单元）。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、核心数或训练时长。由于该框架侧重于传统的 MOT 逻辑而非深度学习训练，其计算压力主要集中在实时推理和多源数据融合的 CPU 处理上。

### 5. 实验数量与充分性
*   **实验规模**：在仿真中测试了 423 辆车，在物理测试床中测试了 355 辆车，实验时长约为 15 分钟。
*   **充分性评价**：实验设计较为充分。作者不仅通过简单的“行人交叉”实验验证了算法组件的有效性，还通过大规模交通流实验验证了系统的整体精度和延迟。通过在仿真和物理缩微环境中使用完全相同的配置，客观地证明了框架的“规模无关性”。

### 6. 主要结论与发现
*   **定位精度**：融合系统实现了车道级精度。在仿真中横向位置 RMSE 为 0.31m，在物理测试床中为 0.29m；航向角误差均在 2.2° 左右。
*   **实时性**：端到端延迟（从感知到融合输出）的中位数保持在 **100ms 以下**，满足协同驾驶的要求。
*   **算法发现**：在目标身份维持方面，Wasserstein 距离在处理交叉路径时显著优于传统的欧氏距离。

### 7. 优点
*   **高度解耦**：标准化的接口允许“插件式”更换算法，极大降低了研发门槛。
*   **Sim-to-Real 桥梁**：通过缩微测试床作为中间阶段，有效验证了系统在面对真实网络延迟和硬件特性时的鲁棒性。
*   **开源贡献**：提供了完整的 C++ 库、ROS 2 封装、文档和示例，具有很高的实用价值。

### 8. 不足与局限
*   **真实环境覆盖有限**：虽然使用了缩微测试床，但尚未在全尺寸的真实高速公路上进行大规模部署测试（受限于全尺寸路面传感器的成本）。
*   **感知算法多样性**：目前的参考实现主要集中在激光雷达和 V2I，对于基于视觉（相机）的复杂感知算法集成讨论较少。
*   **算法创新性**：论文重点在于“框架集成”而非提出某种超越 SOTA 的全新跟踪算法。

（完）
