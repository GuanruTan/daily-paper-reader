---
title: Frozen LLMs as Map-Aware Spatio-Temporal Reasoners for Vehicle Trajectory Prediction
title_zh: 冻结的大语言模型作为地图感知的时空推理器用于车辆轨迹预测
authors: "Yanjiao Liu, Jiawei Liu, Xun Gong, Zifei Nie"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21479v2"
tags: ["query:tp"]
score: 10.0
evidence: 大语言模型作为车辆轨迹预测的时空推理器
tldr: 本研究探讨了冻结的大语言模型（LLM）在自动驾驶轨迹预测中的应用。通过引入一个评估框架，利用交通编码器和CNN分别提取动态轨迹与静态高精地图特征，并利用重编程适配器将其转化为LLM可理解的Token。该框架在保持LLM参数不变的情况下，验证了其在理解复杂交通场景和地图语义方面的推理能力，为多模态信息融合提供了统一的评估平台。
selection_source: fresh_fetch
motivation: 旨在评估和利用冻结的大语言模型在理解动态交通参与者行为及静态道路拓扑结构方面的时空推理潜力。
method: 采用交通编码器与CNN提取多模态特征，通过重编程适配器将特征映射为LLM Token，并结合线性解码器输出预测轨迹。
result: 实验定量分析了地图语义对预测准确性的影响，并证明了该框架在不同LLM架构下均具有良好的泛化性。
conclusion: 冻结的LLM能够作为有效的地图感知时空推理器，通过极小的适配代价实现高精度的车辆轨迹预测。
---

## 摘要
大语言模型（LLMs）近期展现出强大的推理能力，并在自动驾驶（AD）领域引起了越来越多的研究关注。然而，将 LLMs 安全应用于自动驾驶的感知和预测，仍需对动态交通参与者和静态道路基础设施有深入的理解。为此，本研究引入了一个框架，用于评估 LLMs 理解动态交通参与者行为和路网拓扑结构的能力。该框架利用冻结的 LLMs 作为推理引擎，采用交通编码器从观察到的代理轨迹中提取空间级场景特征，同时使用轻量级卷积神经网络（CNN）对局部高精（HD）地图进行编码。为了评估 LLMs 的内在推理能力，提取的场景特征随后通过重编程适配器转换为与 LLM 兼容的标记（tokens）。通过将预测任务交给 LLMs，应用一个更简单的线性解码器来输出未来轨迹。该框架能够定量分析多模态信息的影响，特别是地图语义对轨迹预测准确性的影响，并允许以极小的适配代价无缝集成冻结的 LLMs，从而展示了在不同 LLM 架构上的强大泛化能力，并为模型评估提供了一个统一平台。

## Abstract
Large language models (LLMs) have recently demonstrated strong reasoning capabilities and attracted increasing research attention in the field of autonomous driving (AD). However, safe application of LLMs on AD perception and prediction still requires a thorough understanding of both the dynamic traffic agents and the static road infrastructure. To this end, this study introduces a framework to evaluate the capability of LLMs in understanding the behaviors of dynamic traffic agents and the topology of road networks. The framework leverages frozen LLMs as the reasoning engine, employing a traffic encoder to extract spatial-level scene features from observed trajectories of agents, while a lightweight Convolutional Neural Network (CNN) encodes the local high-definition (HD) maps. To assess the intrinsic reasoning ability of LLMs, the extracted scene features are then transformed into LLM-compatible tokens via a reprogramming adapter. By residing the prediction burden with the LLMs, a simpler linear decoder is applied to output future trajectories. The framework enables a quantitative analysis of the influence of multi-modal information, especially the impact of map semantics on trajectory prediction accuracy, and allows seamless integration of frozen LLMs with minimal adaptation, thereby demonstrating strong generalizability across diverse LLM architectures and providing a unified platform for model evaluation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Frozen LLMs as Map-Aware Spatio-Temporal Reasoners for Vehicle Trajectory Prediction》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLMs）在自然语言处理中展现了卓越的推理能力，但将其应用于自动驾驶（AD）的轨迹预测仍面临挑战。现有的研究往往只关注动态代理的轨迹，忽略了静态道路基础设施（如高精地图）对预测的关键约束作用。
*   **核心问题**：如何评估并利用“冻结”状态下的 LLMs 来理解复杂的交通场景，特别是如何将动态轨迹信息与静态地图语义相结合，以实现更准确的时空推理和轨迹预测。

### 2. 论文提出的方法论
该研究提出了一个多模态评估框架，核心思想是**将 LLM 视为一个预训练好的通用推理引擎**，通过极小的适配代价处理自动驾驶数据。
*   **场景编码器 (Scene Encoder)**：将自车及周边参与者的空间状态向量化，利用交叉注意力机制（Cross-Attention）捕捉代理间的社会交互，生成场景特征。
*   **重编程适配器 (Reprogramming Adapter)**：将提取的场景特征映射到 LLM 的文本嵌入空间，将其转化为 LLM 可理解的“场景 Token”。
*   **地图编码器 (Map Encoder)**：使用轻量级 CNN 处理局部高精地图切片，提取车道几何、连接性和拓扑先验，生成“地图 Token”。
*   **特征融合模块**：通过交叉注意力机制将动态轨迹特征与静态地图特征融合，并与任务描述（Prompt）拼接，输入冻结的 LLM。
*   **线性解码器 (Linear Decoder)**：为了确保预测能力主要源于 LLM 而非解码器，仅使用一个简单的线性层将 LLM 的输出隐藏状态转换为未来轨迹坐标。

### 3. 实验设计
*   **数据集**：使用自动驾驶主流数据集 **nuScenes**。
*   **实验设置**：采样频率 2Hz，利用过去 2 秒的历史轨迹预测未来 6 秒的路径。
*   **评估指标**：平均位移误差 (ADE)、最终位移误差 (FDE)、漏失率 (MR) 和推理效率 (IE)。
*   **对比与消融**：
    *   **模态消融**：对比“仅自车”、“自车+邻车”、“自车+邻车+地图”三种配置。
    *   **模型泛化性**：在 LLaMA2、LLaMA3、Qwen2.5、Mistral、Vicuna 和 WizardLM 六种不同的 LLM 后端上进行测试。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。
*   **实现细节**：提到代码和预训练模型权重已在 GitHub 开源，通常此类基于冻结 LLM 的微调任务对算力要求相对较低，因为大部分参数（LLM 主体）不参与梯度更新。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多维度的消融实验，定量分析了地图语义对不同预测时长（2s, 4s, 6s）的影响。
    *   横向对比了 6 种主流开源 LLM，验证了框架的通用性。
    *   提供了可视化分析，展示了在直行、转弯、交叉口等典型场景下的预测效果。
*   **充分性评价**：实验设计较为客观、公平。通过保持 LLM 冻结并使用极简解码器，有效地隔离并评估了 LLM 本身的推理贡献。

### 6. 论文的主要结论与发现
*   **地图的重要性**：引入地图语义显著提升了长时预测（4s 和 6s）的准确性。例如，在 LLaMA3 上，加入地图后 6s FDE 降低了约 10%。
*   **LLM 的推理潜力**：冻结的 LLM 能够通过重编程技术有效处理非文本的时空数据，并能理解道路拓扑约束（如在转弯场景中自动修正轨迹以符合车道线）。
*   **泛化性强**：该框架在不同架构的 LLM 上表现稳健，证明了 LLM 作为通用时空推理器的可行性。

### 7. 优点（亮点）
*   **轻量化适配**：无需对庞大的 LLM 进行全参数微调，仅通过适配器和轻量编码器即可实现跨模态任务。
*   **评估纯粹性**：采用线性解码器是一个精巧的设计，避免了复杂解码器“反客为主”掩盖 LLM 真实推理能力的问题。
*   **多模态融合**：成功将静态地图语义引入 LLM 轨迹预测框架，填补了前作的空白。

### 8. 不足与局限
*   **推理延迟**：虽然使用了冻结模型，但 LLM 本身的推理开销（IE 约 0.034-0.037s）对于极高实时性要求的自动驾驶系统（通常要求 <10ms）仍有优化空间。
*   **复杂场景覆盖**：实验主要集中在 nuScenes 的标准场景，对于极端天气、罕见交通违章等长尾场景（Corner Cases）的推理能力尚待进一步验证。
*   **多目标预测**：目前框架似乎更侧重于单车轨迹预测，对于多代理协同预测的复杂交互建模深度略显不足。

（完）
