---
title: "OccDirector: Language-Guided Behavior and Interaction Generation in 4D Occupancy Space"
title_zh: OccDirector：4D 占用空间中语言引导的行为与交互生成
authors: "Zhuding Liang, Tianyi Yan, Dubing Chen, Jiasen Zheng, Huan Zheng, Cheng-zhong Xu, Yida Wang, Kun Zhan, Jianbing Shen"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22240v1"
tags: ["query:tp"]
score: 8.0
evidence: 4D占据空间中的多智能体交互生成
tldr: OccDirector是一个开创性的4D占用空间生成框架，旨在通过自然语言指令引导自动驾驶场景中的复杂多智能体行为和交互。它克服了现有模型依赖刚性几何条件的局限，利用VLM驱动的时空MMDiT和历史前缀锚定策略，实现了长时程的一致性生成。此外，研究还推出了包含8.5万条多级语言标注的OccInteract数据集及新的评估基准，将生成范式从外观合成转向了语言驱动的行为编排。
selection_source: fresh_fetch
motivation: 现有的4D占用生成模型过度依赖刚性几何先验或简单文本属性，无法有效模拟复杂且具有时序性的多智能体交互行为。
method: 提出了一种由视觉语言模型驱动的时空MMDiT架构，并结合历史前缀锚定策略，将自然语言脚本直接转化为物理一致的4D体素动态。
result: 通过在包含8.5万条多级语言标注的新数据集OccInteract上进行实验，证明了该方法在生成质量和指令遵循方面均达到了领先水平。
conclusion: OccDirector成功将4D占用生成从单纯的外观合成提升到了语言驱动的行为编排层面，为自动驾驶仿真提供了更强大的语义控制能力。
---

## 摘要
生成式世界模型日益依赖 4D 占用（4D occupancy）来实现逼真的自动驾驶模拟。然而，现有的生成框架依赖于僵化的几何条件（如显式轨迹）或简单的属性级文本，无法编排复杂的、序列化的多智能体交互。为了弥补这一语义-时空鸿沟，我们提出了 OccDirector，这是一个仅以自然语言为条件生成 4D 占用动态的开创性框架。OccDirector 充当“场景导演”，将自然语言剧本映射为符合物理规律的体素动态，而无需几何先验。在技术上，它采用了一种由视觉语言模型（VLM）驱动的时空 MMDiT，并配备了历史前缀锚定策略，以确保长时程交互的一致性。此外，我们推出了 OccInteract-85k，这是一个独特标注了多级语言指令的新型数据集：涵盖了从静态布局到复杂的多智能体行为，并配套了一个基于 VLM 的新型评估基准。大量实验表明，OccDirector 实现了最先进的生成质量和前所未有的指令遵循能力，成功地将范式从外观合成转向了语言驱动的行为编排。

## Abstract
Generative world models increasingly rely on 4D occupancy for realistic autonomous driving simulation. However, existing generation frameworks depend on rigid geometric conditions (e.g., explicit trajectories) or simplistic attribute-level text, failing to orchestrate complex, sequential multi-agent interactions. To address this semantic-spatiotemporal gap, we propose OccDirector, a pioneering framework that generates 4D occupancy dynamics conditioned solely on natural language. Operating as a ``scenario director'', OccDirector maps natural language scripts into physically plausible voxel dynamics without requiring geometric priors. Technically, it employs a VLM-driven Spatio-Temporal MMDiT equipped with a history-prefix anchoring strategy to ensure long-horizon interaction consistency. Furthermore, we introduce OccInteract-85k, a novel dataset uniquely annotated with multi-level language instructions: ranging from static layouts to intricate multi-agent behaviors, alongside a novel VLM-based evaluation benchmark. Extensive experiments demonstrate that OccDirector achieves state-of-the-art generation quality and unprecedented instruction-following capabilities, successfully shifting the paradigm from appearance synthesis to language-driven behavior orchestration.

---

## 论文详细总结（自动生成）

以下是对论文《OccDirector: Language-Guided Behavior and Interaction Generation in 4D Occupancy Space》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：自动驾驶（AD）仿真需要逼真的世界模型。虽然 4D 占用空间（4D Occupancy）能提供密集的几何描述，但现有生成框架过度依赖**刚性几何条件**（如预设轨迹、高精地图）或简单的属性文本，难以模拟复杂的、具有时序逻辑的多智能体交互行为（如“先跟随货车再超车”）。
*   **核心挑战**：存在“**语义-时空鸿沟**”，即如何将抽象的自然语言指令转化为物理上合理、时空一致的体素动态演化。
*   **整体含义**：OccDirector 提出了“场景导演”的概念，实现了仅凭自然语言脚本即可编排复杂的 4D 动态驾驶场景，将生成范式从单纯的“外观合成”提升到了“行为编排”。

### 2. 方法论
*   **核心思想**：利用视觉语言模型（VLM）的深度语义理解能力，结合可扩展的时空 Transformer 架构，通过流匹配（Flow Matching）实现文本到 4D 占用序列的直接映射。
*   **关键技术细节**：
    *   **联合潜空间表示**：使用预训练的 **Occ-VAE** 将 4D 占用数据压缩为紧凑的潜变量；利用冻结的 **VLM（Qwen3-VL）** 提取剧本的程序化语义，并通过 **Token Refiner**（轻量化双向 Transformer）弥合模态间隙。
    *   **时空 MMDiT 架构**：采用多模态扩散 Transformer（MMDiT），通过**时空分离注意力（STSA）**处理海量体素 Token。STSA 将操作分解为帧内 3D 空间联合注意力和跨帧时间自注意力，解决了计算复杂度问题。
    *   **历史前缀锚定（History-Prefix Anchoring）**：在训练中引入随机 Token 替换策略，在推理时通过每一步的强制锚定，确保生成的未来序列与给定的历史上下文（如有）在物理和逻辑上严格一致。
    *   **算法流程**：基于连续归一化流（CNF）框架，通过回归向量场来学习从高斯噪声到真实 4D 占用分布的变换。

### 3. 实验设计
*   **数据集**：推出了 **OccInteract-85k**，包含三个子集：
    *   *OccInteract-Static*：单帧布局标注，学习几何先验。
    *   *OccInteract-Env*：关键机动动作（如转弯）的剪辑。
    *   *OccInteract-Agent*：涵盖从常规驾驶到安全关键（如紧急制动、碰撞）的多智能体交互。
*   **对比方法**：对比了 DOME、COME、DynamicCity 等 SOTA 几何引导模型，以及将 DynamicCity 适配为文本引导的基准（DynamicCity + CLIP/T5）。
*   **评估指标**：
    *   **视觉质量**：IS、FID、KID、FVD（视频质量）、Precision/Recall。
    *   **指令遵循**：提出基于 VLM（Gemini-3.0-pro, GPT-5.2 等）的自动评分基准，从场景完整性、结构正确性、语义对齐三个维度评分。

### 4. 资源与算力
*   **硬件**：补充材料提到在 **NVIDIA A100 (80GB)** 上进行了推理资源消耗测试。
*   **训练参数**：训练迭代次数为 100k 次，Batch Size 为 32，学习率为 $1 \times 10^{-4}$，使用 AdamW 优化器。
*   **效率**：得益于 STSA 设计，显存占用随帧数线性增长，且对文本长度不敏感（L 从 77 增加到 2048，显存增加小于 0.1%）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定量对比（表 3、表 4）。
    *   针对架构组件（STSA、Token Refiner）、历史锚定比例、CFG 尺度等进行了详尽的消融实验（表 5、表 S3-S6）。
    *   包含多维度的定性分析（长时程叙事生成、细粒度文本对齐对比）。
    *   **用户研究**：邀请 10 名参与者对 30 个样本进行盲测，结果与 VLM 自动评分高度相关（相关系数 $\rho=0.88$）。
*   **充分性评价**：实验设计全面，涵盖了从底层视觉指标到高层语义对齐的评估，对比基准设置合理，消融实验论证了各模块的必要性，整体表现出较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：OccDirector 在视频生成质量（FVD 提升 29.1%）和指令遵循能力上显著优于现有方法。
*   **克服模式崩塌**：传统的 CLIP/T5 编码器在处理复杂指令时易出现模式崩塌（Recall 极低），而 VLM 驱动的架构能生成更多样、更符合逻辑的动态场景。
*   **一致性保障**：历史前缀锚定策略有效解决了长时程生成中的“场景漂移”问题，实现了逻辑连贯的多阶段叙事生成。

### 7. 优点
*   **范式创新**：首个实现纯文本驱动的 4D 占用交互生成框架，摆脱了对繁琐轨迹设计的依赖。
*   **架构高效**：STSA 机制在保持高性能的同时，极大地提升了处理长序列和长文本的扩展性。
*   **数据贡献**：OccInteract-85k 数据集填补了 4D 占用-语言对齐数据的空白，且自动化标注管线具有良好的可扩展性。

### 8. 不足与局限
*   **数据伪影**：受限于原始真实世界日志（如 nuScenes），生成的场景可能继承传感器的盲区或自车体素缺失等伪影。
*   **长时程漂移风险**：尽管有锚定策略，但在极长序列（如超过 8 秒）生成中，微小误差的累积仍可能导致物理一致性下降。
*   **语义粒度**：目前仅支持 11 类语义标签，尚未涵盖转向灯、刹车灯或行人手势等更细粒度的交互信号。
*   **评估偏差**：基于 VLM 的评估器虽然高效，但不同 VLM 模型之间存在评分标准不一的系统性偏差。

（完）
