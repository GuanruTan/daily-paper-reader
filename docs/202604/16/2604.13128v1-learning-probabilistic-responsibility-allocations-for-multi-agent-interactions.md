---
title: Learning Probabilistic Responsibility Allocations for Multi-Agent Interactions
title_zh: 学习多智能体交互中的概率性责任分配
authors: "Isaac Remy, Caleb Chang, Karen Leung"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.13128v1"
tags: ["query:tp"]
score: 9.0
evidence: 利用多智能体轨迹预测技术进行交互建模
tldr: 本研究探讨了多智能体交互中的责任分配问题，即个体为配合他人而偏离自身目标的程度。通过结合条件变分自编码器（CVAE）和多智能体轨迹预测技术，提出了一种学习概率责任分配模型的方法。该方法利用可微分优化层解决缺乏真实标签的问题，将责任分配映射为可观测的控制行为。在INTERACTION驾驶数据集上的实验证明，该模型不仅预测性能优异，还能为理解复杂的交互模式提供具有解释性的视角。
selection_source: fresh_fetch
motivation: 旨在通过理解人类在交互中如何分配责任（即为他人做出让步的程度），来设计更符合社会规范且可靠的自动驾驶系统。
method: 结合CVAE的潜空间与多智能体轨迹预测，并引入可微分优化层，在无真实责任标签的情况下学习受场景和上下文约束的概率责任分配分布。
result: 在INTERACTION真实驾驶数据集上实现了强大的预测性能，并能通过责任分配这一维度提供对多智能体交互模式的深入解释。
conclusion: 该研究证明了概率责任分配模型在捕捉交互不确定性和提升自动驾驶系统社会合规性方面的有效性与解释价值。
---

## 摘要
在交互场景中，人类行为不仅受个人目标的影响，还受到与他人共享的约束（如安全性）的塑造。理解人们如何分配责任（即一个人为了迁就他人而偏离其理想策略的程度），可以为设计符合社会规范且值得信赖的自主系统提供参考。在这项工作中，我们提出了一种学习概率性责任分配模型的方法，该模型捕捉了多智能体交互中固有的多模态不确定性。具体而言，我们的方法利用条件变分自编码器（CVAE）的隐空间，并结合多智能体轨迹预测技术，学习以场景和智能体上下文为条件的责任分配分布。尽管缺乏真实的责任标签，但通过引入一个可微分优化层，将责任分配映射到可获得的诱导控制量，该模型仍然是可处理的。我们在 INTERACTION 驾驶数据集上评估了我们的方法，并证明它不仅实现了强大的预测性能，还通过责任的视角为多智能体交互模式提供了可解释的见解。

## Abstract
Human behavior in interactive settings is shaped not only by individual objectives but also by shared constraints with others, such as safety. Understanding how people allocate responsibility, i.e., how much one deviates from their desired policy to accommodate others, can inform the design of socially compliant and trustworthy autonomous systems. In this work, we introduce a method for learning a probabilistic responsibility allocation model that captures the multimodal uncertainty inherent in multi-agent interactions. Specifically, our approach leverages the latent space of a conditional variational autoencoder, combined with techniques from multi-agent trajectory forecasting, to learn a distribution over responsibility allocations conditioned on scene and agent context. Although ground-truth responsibility labels are unavailable, the model remains tractable by incorporating a differentiable optimization layer that maps responsibility allocations to induced controls, which are available. We evaluate our method on the INTERACTION driving dataset and demonstrate that it not only achieves strong predictive performance but also provides interpretable insights, through the lens of responsibility, into patterns of multi-agent interaction.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Probabilistic Responsibility Allocations for Multi-Agent Interactions》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多智能体交互（如驾驶）中，人类如何平衡“个人目标”与“共享安全约束”？论文将其定义为**责任分配（Responsibility Allocation）**，即每个智能体为了维持整体安全而偏离其理想策略的程度。
*   **研究动机**：
    *   **现有局限**：先前的研究多采用确定性模型，无法捕捉人类行为中固有的多模态不确定性（例如，在路口谁该让谁往往有多种可能的演变）。
    *   **缺乏标签**：现实数据中不存在“责任分配”的真实标签（Ground-truth），难以直接监督学习。
    *   **目标**：开发一种概率生成模型，既能预测多智能体轨迹，又能提供关于交互责任的可解释量化指标。

### 2. 方法论
*   **核心思想**：将责任分配建模为条件变分自编码器（CVAE）中的中间变量，通过一个**可微分优化层**将抽象的责任权重映射为可观测的控制行为。
*   **关键技术细节**：
    1.  **控制屏障函数（CBF）**：利用 CBF 定义安全集合，确保系统状态在演化中保持安全。
    2.  **责任安全过滤器（Safety Filter）**：构建一个二次规划（QP）问题，目标是最小化控制量与理想控制量的偏差。其中，责任因子 $\gamma$ 作为权重，$\gamma$ 越大表示该智能体越不愿偏离目标（责任越小）。
    3.  **概率生成架构**：
        *   **编码器**：学习场景上下文 $s$ 和观测控制 $u$ 到隐空间 $z$ 的映射。
        *   **解码器**：将 $z$ 映射为责任分配 $\gamma$，再通过可微分 QP 层输出预测控制 $\hat{u}$。
    4.  **Transformer 骨干网络**：借鉴 AgentFormer 架构，利用注意力机制处理变长序列和多智能体间的时间/空间依赖关系。
*   **算法流程**：输入历史轨迹 -> Transformer 编码 -> 采样隐变量 $z$ -> 解码生成责任分布 $p(\gamma|s)$ -> 求解 QP 得到安全控制量 -> 最小化预测控制与真实控制的差异。

### 3. 实验设计
*   **数据集/场景**：
    1.  **合成数据集**：双智能体“走廊问题”，验证模型捕捉双峰分布（Bimodal）责任分配的能力。
    2.  **INTERACTION 真实驾驶数据集**：选取复杂的多路交叉口场景，包含大量车辆的汇入、避让和流出。
*   **Benchmark 与对比方法**：
    *   **对比方法**：直接预测控制量的 CVAE（无安全过滤层）、遵循理想控制的启发式方法。
    *   **消融实验**：对比了不同的 $\gamma$ 激活函数（如 Softmax, Tanh, Clip 等）对模型性能的影响。
*   **评价指标**：平均位移误差（ADE）和错过率（Miss Rate）。

### 4. 资源与算力
*   **软件框架**：使用 JAX 编写，利用 Equinox 构建神经网络，并使用 `qpax` 作为可微分 QP 求解器。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号和数量。
*   **训练/推理耗时**：提到模型在包含 32 个样本的批次下，单次推理时间约为 $0.463 \pm 0.008$ 秒。

### 5. 实验数量与充分性
*   **实验规模**：使用了 INTERACTION 数据集中 5 分钟的轨迹数据，产生约 3 万个数据点，按 80/20 比例划分训练和测试集。
*   **充分性**：
    *   **合成实验**证明了模型在数学逻辑上的正确性（能找回预设的双峰分布）。
    *   **消融实验**探讨了责任权重的约束方式，具有较好的客观性。
    *   **局限性**：实验仅集中在单一类型的交叉口场景，对于行人、自行车等异构智能体的复杂交互覆盖较少。

### 6. 主要结论与发现
*   **概率建模的必要性**：概率模型能有效捕捉交互中的多模态性（如在合成实验中成功重建了两种不同的责任分配模式）。
*   **预测性能**：引入责任过滤器的模型在轨迹预测精度上显著优于简单的启发式方法，且接近直接预测控制量的端到端模型。
*   **可解释性**：模型输出的 $\gamma$ 值符合人类直觉。例如，在路口先到的车辆通常表现出较高的 $\gamma$（坚持自身路径），而后到的车辆 $\gamma$ 较低（承担更多避让责任）。
*   **激活函数影响**：Tanh 激活函数在处理真实数据中的离群值和极端交互时表现最稳健。

### 7. 优点
*   **数学与深度学习的结合**：将硬性的安全约束（CBF）与灵活的生成模型（CVAE）结合，保证了输出的物理合理性。
*   **无需人工标注**：通过可微分优化层解决了责任分配标签缺失的问题，实现了弱监督学习。
*   **高度可解释**：不同于纯黑盒模型，该模型提供了一个量化的“责任”维度来解释智能体为何做出某种动作。

### 8. 不足与局限
*   **数据偏差风险**：INTERACTION 数据集中大部分车辆处于匀速行驶，导致模型在某些情况下倾向于预测零加速度，对极端冲突场景的挖掘可能不足。
*   **智能体筛选机制**：目前采用基于距离的启发式方法筛选相关智能体，可能忽略了远距离但具有高动态威胁的对象。
*   **计算开销**：由于每一层推理都需要求解 QP 优化问题，虽然使用了可微分求解器，但在实时性要求极高的自动驾驶系统中仍可能存在计算瓶颈。

（完）
