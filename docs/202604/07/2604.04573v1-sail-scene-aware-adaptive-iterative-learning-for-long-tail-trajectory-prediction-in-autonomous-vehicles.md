---
title: "SAIL: Scene-aware Adaptive Iterative Learning for Long-Tail Trajectory Prediction in Autonomous Vehicles"
title_zh: SAIL：面向自动驾驶车辆长尾轨迹预测的场景感知自适应迭代学习
authors: "Bin Rao, Haicheng Liao, Chengyue Wang, Keqiang Li, Zhenning Li, Hai Yang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04573v1"
tags: ["query:tp"]
score: 10.0
evidence: 自动驾驶车辆的长尾轨迹预测
tldr: 针对自动驾驶中长尾轨迹预测难题（如突发转向、高风险碰撞等），本文提出SAIL框架。该框架通过预测误差、碰撞风险和状态复杂度三个维度定义长尾场景，并结合属性引导的数据增强与自适应对比学习策略（含动态伪标签和难样本挖掘），显著提升了模型对罕见且关键事件的预测能力，在保持整体精度的同时大幅降低了极端长尾样本的误差。
selection_source: fresh_fetch
motivation: 现有模型在处理数据分布极不均衡、定义模糊且具有高安全风险的长尾交通场景时表现不佳。
method: 提出一种结合属性引导增强、动态伪标签聚类及相似度加权对比学习的迭代学习框架，重点强化对难样本的特征提取。
result: "在nuScenes和ETH/UCY数据集上，SAIL在最难的1%长尾样本上将预测误差降低了高达28.8%，并优于现有主流模型。"
conclusion: 该框架有效解决了自动驾驶轨迹预测中的长尾分布问题，为复杂交通环境下的安全导航提供了更可靠的技术支撑。
---

## 摘要
自动驾驶车辆（AV）依赖准确的轨迹预测以在多样化的交通环境中实现安全导航，然而现有模型在处理长尾场景时面临挑战——这些场景是罕见但对安全至关重要的事件，其特征是突发机动、高碰撞风险和复杂的交互。这些挑战源于数据不平衡、对长尾轨迹的定义不足，以及优先考虑常见行为而非罕见行为的次优学习策略。为了解决这一问题，我们提出了 SAIL，这是一个系统性应对长尾问题的新型框架，首先通过预测误差、碰撞风险和状态复杂度三个关键属性维度对轨迹进行定义和建模。随后，我们的方法将属性引导的增强和特征提取过程与高度自适应的对比学习策略相结合。该策略采用了连续余弦动量调度、相似度加权的难负样本挖掘，以及基于演化特征聚类的动态伪标签机制。此外，它还包含一种聚焦机制，以加强对每个识别类别中难正样本的学习。这种全面的设计使 SAIL 能够擅长识别和预测多样且具有挑战性的长尾事件。在 nuScenes 和 ETH/UCY 数据集上的广泛评估证明了 SAIL 的卓越性能，与现有最先进的基准模型相比，在最难的 1% 长尾样本上预测误差降低了高达 28.8%，同时在所有场景中保持了具有竞争力的准确性。该框架推进了现实世界混合自主驾驶环境下的可靠自动驾驶轨迹预测。

## Abstract
Autonomous vehicles (AVs) rely on accurate trajectory prediction for safe navigation in diverse traffic environments, yet existing models struggle with long-tail scenarios-rare but safety-critical events characterized by abrupt maneuvers, high collision risks, and complex interactions. These challenges stem from data imbalance, inadequate definitions of long-tail trajectories, and suboptimal learning strategies that prioritize common behaviors over infrequent ones. To address this, we propose SAIL, a novel framework that systematically tackles the long-tail problem by first defining and modeling trajectories across three key attribute dimensions: prediction error, collision risk, and state complexity. Our approach then synergizes an attribute-guided augmentation and feature extraction process with a highly adaptive contrastive learning strategy. This strategy employs a continuous cosine momentum schedule, similarity-weighted hard-negative mining, and a dynamic pseudo-labeling mechanism based on evolving feature clustering. Furthermore, it incorporates a focusing mechanism to intensify learning on hard-positive samples within each identified class. This comprehensive design enables SAIL to excel at identifying and forecasting diverse and challenging long-tail events. Extensive evaluations on the nuScenes and ETH/UCY datasets demonstrate SAIL's superior performance, achieving up to 28.8% reduction in prediction error on the hardest 1% of long-tail samples compared to state-of-the-art baselines, while maintaining competitive accuracy across all scenarios. This framework advances reliable AV trajectory prediction in real-world, mixed-autonomy settings.

---

## 论文详细总结（自动生成）

### 论文总结：SAIL——面向自动驾驶车辆长尾轨迹预测的场景感知自适应迭代学习

#### 1. 核心问题与整体含义（研究动机和背景）
自动驾驶中的轨迹预测模型在处理“头部”数据（如平稳跟车、匀速行驶）时表现优异，但在处理“长尾”场景（如突发转向、紧急制动、高风险交互）时性能大幅下降。
*   **研究动机**：现有的长尾预测研究存在定义单一（仅关注频率或损失）、识别依赖模型、学习策略不均衡（优化长尾时往往牺牲整体精度）等问题。
*   **核心目标**：建立一套系统性的框架，通过多维度定义长尾场景，并利用自适应对比学习提升模型对罕见、复杂且高风险事件的预测能力。

#### 2. 方法论：核心思想与关键技术
SAIL 框架采用多阶段流水线设计，核心组件包括：
*   **多维度长尾属性定义**：从三个互补维度量化轨迹特征：
    1.  **预测误差 ($y_e$)**：基于基准模型的 FDE，捕捉上下文的不可预测性。
    2.  **碰撞风险 ($y_r$)**：利用逆碰撞时间（InvTTC）量化多智能体间的时空冲突。
    3.  **状态复杂度 ($y_s$)**：结合最大加加速度（Jerk）和偏航率（Yaw rate），识别非线性机动。
*   **属性引导轨迹增强 (AGTA)**：根据上述属性动态生成增强策略（简化、平移、掩码、子集提取），为对比学习提供高质量的正样本。
*   **自适应动量对比学习 (AMCL)**：引入连续余弦动量调度平滑训练，并采用相似度加权的难负样本挖掘，强化特征区分度。
*   **演化特征聚类 (EFC)**：定期对特征空间进行 K-means 聚类，生成动态伪标签，以适应不断进化的特征流形。
*   **聚焦解耦对比学习 (FDCL)**：在有监督对比学习中引入聚焦机制（类似 Focal Loss），迫使模型关注类内难正样本，使聚类更紧凑。
*   **属性感知生成器**：通过门控机制融合解耦的属性特征，最终由 GRU 解码器输出多模态轨迹。

#### 3. 实验设计
*   **数据集**：
    *   **nuScenes**：大型自动驾驶数据集，包含复杂城市场景和 HD 地图。
    *   **ETH/UCY**：行人轨迹预测基准，用于验证跨智能体类型的鲁棒性。
*   **评估子集**：专门构建了 Top 1% - 5% 的长尾评估子集（基于预测误差、风险和复杂度）。
*   **对比方法**：包括 Trajectron++、FEND、TrACT、CSD、LAformer、UniTraj (MTR) 等十余种 SOTA 模型。
*   **指标**：minADE、minFDE、Miss Rate (MR)。

#### 4. 资源与算力
*   **硬件**：单张 NVIDIA RTX 3090 GPU。
*   **训练配置**：总计 120 个 epoch，包含 10 个 epoch 的预热阶段；Batch size 为 32；学习率为 0.0005。
*   **推理效率**：在 nuScenes 上平均推理时间仅为 **18ms**（处理 12 个智能体），显著优于对比模型。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流数据集上进行了详尽测试，涵盖了不同预测时界（1s-6s）和不同长尾维度的专项评估。
*   **消融实验**：设计了 6 组消融实验（Model A-F），逐一验证了 AGTA、属性解耦、AMCL、EFC 和 FDCL 的有效性。
*   **客观性**：引入了“模型无关的差情况评估”（Model-agnostic worst-case），即针对每个模型自身的最高误差样本进行排名对比，确保了评估的公平性。

#### 6. 主要结论与发现
*   **性能突破**：在 nuScenes 最难的 1% 样本上，SAIL 将 minFDE 降低了 **28.8%**。
*   **整体平衡**：在显著提升长尾场景表现的同时，SAIL 在全量数据集上的表现依然保持 SOTA 水平，未出现明显的“顾此失彼”。
*   **属性互补性**：实验证明三个属性维度（误差、风险、复杂度）之间的重合度较低，验证了多维度定义的必要性。

#### 7. 优点
*   **定义严谨**：将抽象的数学指标与具体的物理驾驶语义（如碰撞风险、机动复杂度）相结合。
*   **架构高效**：采用非对称设计，复杂的属性预测和对比学习仅在训练期存在，推理期被移除，保证了实时性。
*   **自适应强**：动态伪标签和聚焦机制解决了长尾类别内部方差大的问题。

#### 8. 不足与局限
*   **极端环境失效**：在严重遮挡（缺乏历史观测）或缺乏交通信号灯信息（如开放路口）的极端情况下，模型仍可能失效。
*   **外部信息依赖**：目前主要依赖车载传感器数据，未来可能需要引入 V2X（车路协同）信息来消除感知盲区带来的预测歧义。
*   **超参数敏感性**：聚类数量 $C$ 和损失权重 $\lambda$ 对结果有一定影响，需针对不同数据集微调。

（完）
