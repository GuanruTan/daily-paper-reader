---
title: "Super Agents and Confounders: Influence of surrounding agents on vehicle trajectory prediction"
title_zh: 超级代理与混杂因素：周围代理对车辆轨迹预测的影响
authors: "Daniel Jost, Luca Paparusso, Martin Stoll, Jörg Wagner, Raghu Rajan, Joschka Bödecker"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03463v1"
tags: ["query:tp"]
score: 10.0
evidence: 周围智能体对车辆轨迹预测的影响
tldr: 本研究深入分析了当前主流的轨迹预测模型，发现周围智能体的信息往往会降低预测精度而非提升。通过Shapley归因分析，揭示了模型容易学习到不稳定且非因果的决策模式。为此，作者提出了一种条件信息瓶颈（CIB）方法，在无需额外监督的情况下压缩智能体特征并过滤干扰信号。实验证明，该方法在多个数据集上提升了预测性能和鲁棒性，为处理复杂交通场景中的上下文信息提供了新思路。
selection_source: fresh_fetch
motivation: 现有轨迹预测模型在整合周围智能体信息时，常因学习到非因果的虚假关联而导致预测性能下降和决策不稳定。
method: 提出集成条件信息瓶颈（CIB）机制，通过压缩特征表达来自动筛选并忽略对预测任务无益的干扰信息。
result: 实验表明该方法在多种模型架构和数据集上均提升了预测精度，并显著增强了模型应对环境扰动的鲁棒性。
conclusion: 选择性地整合上下文信息对轨迹预测至关重要，利用信息瓶颈过滤误导性信号是实现稳健预测的有效途径。
---

## 摘要
在高度互动的驾驶场景中，轨迹预测以周围交通参与者（如汽车和行人）的信息为条件。我们的主要贡献是对最先进的轨迹预测器进行了全面分析，揭示了一个令人惊讶且关键的缺陷：许多周围代理（agents）反而降低了预测精度，而非提高。利用基于 Shapley 的归因方法，我们严谨地证明了模型学习到了不稳定且非因果的决策方案，这些方案在不同的训练运行中存在显著差异。基于这些见解，我们提出集成条件信息瓶颈（CIB），该方法不需要额外的监督，通过训练可以有效地压缩代理特征，并忽略那些对预测任务无益的特征。使用多个数据集和模型架构的全面实验表明，这种简单而有效的方法不仅在许多情况下提高了整体轨迹预测性能，还增强了对不同扰动的鲁棒性。我们的结果强调了在轨迹预测中选择性集成上下文信息的重要性，因为这些信息通常包含伪造或误导性的信号。此外，我们提供了用于识别非鲁棒行为的可解释指标，并为解决该问题提供了一条有前景的途径。

## Abstract
In highly interactive driving scenes, trajectory prediction is conditioned on information from surrounding traffic participants such as cars and pedestrians. Our main contribution is a comprehensive analysis of state-of-the-art trajectory predictors, which reveals a surprising and critical flaw: many surrounding agents degrade prediction accuracy rather than improve it. Using Shapley-based attribution, we rigorously demonstrate that models learn unstable and non-causal decision-making schemes that vary significantly across training runs. Building on these insights, we propose to integrate a Conditional Information Bottleneck (CIB), which does not require additional supervision and is trained to effectively compress agent features as well as ignore those that are not beneficial for the prediction task. Comprehensive experiments using multiple datasets and model architectures demonstrate that this simple yet effective approach not only improves overall trajectory prediction performance in many cases but also increases robustness to different perturbations. Our results highlight the importance of selectively integrating contextual information, which can often contain spurious or misleading signals, in trajectory prediction. Moreover, we provide interpretable metrics for identifying non-robust behavior and present a promising avenue towards a solution.

---

## 论文详细总结（自动生成）

这是一份关于论文《Super Agents and Confounders: Influence of surrounding agents on vehicle trajectory prediction》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在自动驾驶轨迹预测中，增加周围智能体（车辆、行人等）的信息是否总能提升预测精度？
*   **研究动机**：
    *   现有模型通常假设更多的上下文信息有助于预测，但近期研究发现，移除某些看似无关的智能体反而会剧烈改变预测结果，甚至提高精度。
    *   模型表现出极大的脆弱性，容易受到微小扰动的影响，这暗示模型可能学习到了非因果的“虚假关联”。
    *   论文旨在量化周围智能体对预测性能的真实贡献，并解决模型过度依赖“混杂因素”（Confounders）的问题。

### 2. 方法论
*   **Shapley 归因分析**：采用基于博弈论的 Shapley 值（通过 ApproShapley 算法近似计算）来量化每个周围智能体对预测指标（如负对数似然 NLL）的边际贡献。
    *   **超级代理（Super Agents）**：对提升预测精度有正向贡献的少数关键智能体。
    *   **混杂代理（Confounding Agents）**：引入误导性信号、降低预测精度的多数智能体。
*   **条件信息瓶颈（Conditional Information Bottleneck, CIB）**：
    *   **核心思想**：在不引入额外监督标签的情况下，通过压缩周围智能体的特征表达，强制模型只保留对预测目标 $Y$ 最关键的信息，同时忽略与目标无关的冗余信息。
    *   **公式目标**：最小化 $L_{CIB} = -I(Y; T_1 | X_2) + \beta I(X_1; T_1 | X_2)$。其中 $X_1$ 是周围智能体，$X_2$ 是目标代理状态，$T_1$ 是压缩后的表示，$\beta$ 控制压缩强度。
    *   **架构集成**：将 CIB 模块插入编码器与交互器之间，以目标代理的编码为条件进行特征过滤。

### 3. 实验设计
*   **数据集**：
    *   **nuScenes**：用于边缘轨迹预测（Marginal Prediction）。
    *   **Waymo Open Motion Dataset (WOMD)**：使用 20% 子集进行边缘预测，并使用全集进行联合预测（Joint Prediction）挑战。
*   **Benchmark 与对比方法**：
    *   **模型架构**：QEANet, LAformer (nuScenes); MTR, EDA (WOMD); BeTop (联合预测)。
    *   **对比设置**：原始模型 vs. 集成 CIB 的模型；全代理输入 vs. 仅超级代理输入 vs. 无代理输入。
*   **评估指标**：minADE, minFDE, Miss Rate, NLL，以及针对鲁棒性的 Abs(Δ) 扰动偏差。

### 4. 资源与算力
*   **算力平台**：论文提到使用了 bwUniCluster 和 bwHPC（德国巴登-符腾堡州的超级计算集群）。
*   **训练细节**：
    *   每个模型变体均使用 **5 个不同的随机种子** 进行训练以评估稳定性。
    *   由于 Shapley 计算成本极高，分析时限制了场景中的代理数量（如最多 24 个）。
    *   文中未明确给出具体的 GPU 型号、数量或总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种主流 SOTA 架构，跨越两个大型工业级数据集，并进行了消融实验。
*   **充分性**：
    *   **多维度验证**：不仅测试了预测精度，还通过“插入测试”（Insertion Test）验证了归因的准确性。
    *   **鲁棒性测试**：引入了基于噪声（高斯噪声）和基于移除（因果 vs. 非因果代理移除）的两种扰动实验。
    *   **客观性**：通过多随机种子实验揭示了模型在不同训练运行下决策逻辑的不一致性，实验设计严谨且具有统计学意义。

### 6. 主要结论与发现
*   **混杂因素悖论**：大多数周围智能体实际上是“混杂者”，它们提供的负面干扰几乎抵消了“超级代理”带来的正面收益。
*   **决策不稳定性**：相同架构在不同训练种子下，对“哪些代理是重要的”这一判断存在巨大分歧，说明模型未学到稳健的因果关系。
*   **CIB 的有效性**：集成 CIB 后，模型能显著缩小“全代理”与“仅超级代理”之间的性能差距，提高了对噪声和代理移除的鲁棒性，且在多个 Benchmark 上刷新了精度。

### 7. 优点
*   **深度可解释性**：首次将 Shapley 归因应用于个体代理级别，揭示了轨迹预测模型内部的决策黑盒。
*   **即插即用**：提出的 CIB 模块不需要额外的人工标注（如因果标签），易于集成到现有各种 Transformer 架构中。
*   **揭示行业盲点**：挑战了“上下文信息越多越好”的直觉，为未来鲁棒性设计提供了理论依据。

### 8. 不足与局限
*   **非因果本质**：虽然 CIB 提升了鲁棒性，但它本质上是一个正则化工具，并未从根本上赋予模型显式的因果推理能力。
*   **架构依赖性**：实验显示 CIB 在不同架构上的收益不一（如在 LAformer 上提升较小），说明其效果受模型原有过滤机制的影响。
*   **计算开销**：事后归因分析（Shapley）计算极其耗时，难以直接应用于实时系统的在线诊断。

（完）
