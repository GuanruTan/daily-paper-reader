---
title: "InsTraj: Instructing Diffusion Models with Travel Intentions to Generate Real-world Trajectories"
title_zh: InsTraj：利用出行意图指导扩散模型生成真实世界轨迹
authors: "Yuanshao Zhu, Yuxuan Liang, Xiangyu Zhao, Liang Han, Xinwei Fang, Xuetao Wei, James Jianqiao Yu"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04106v1"
tags: ["query:tp"]
score: 9.0
evidence: 基于大语言模型的出行意图解码与轨迹生成
tldr: InsTraj是一个利用自然语言指令引导扩散模型生成真实轨迹的框架。针对现有方法难以理解复杂出行意图和处理约束的问题，该研究首先利用大语言模型将非结构化意图转化为语义蓝图，随后通过多模态轨迹扩散Transformer生成符合指令的高保真轨迹。实验证明，该方法在轨迹的真实性、多样性以及语义一致性上均显著优于现有技术，为城市规划和移动性模拟提供了新方案。
selection_source: fresh_fetch
motivation: 现有轨迹生成方法难以理解复杂的自然语言出行意图，且在处理精细约束时难以兼顾人类行为的真实多样性。
method: 提出InsTraj框架，结合大语言模型的语义解析能力与多模态轨迹扩散Transformer，将自然语言指令转化为高保真轨迹。
result: 实验结果显示，InsTraj在生成轨迹的真实感、多样性以及对复杂指令的遵循程度方面均显著超越了现有的最先进模型。
conclusion: 该研究证明了通过自然语言指令引导扩散模型生成可控且真实的GPS轨迹的可行性，有效提升了轨迹生成的语义理解能力。
---

## 摘要
生成真实且可控的 GPS 轨迹是城市规划、移动模拟和隐私保护数据共享等应用中的一项基础任务。然而，现有方法面临双重挑战：它们缺乏解释复杂用户出行意图的深度语义理解能力，并且在保持人类行为固有的真实多样性的同时，难以处理复杂的约束条件。为了解决这一问题，我们提出了 InsTraj，这是一个直接根据自然语言描述指导扩散模型生成高保真轨迹的新型框架。具体而言，InsTraj 首先利用强大的大语言模型来解码以自然语言形式构成的非结构化出行意图，从而创建丰富的语义蓝图，并弥合意图与轨迹之间的表示鸿沟。随后，我们提出了一种多模态轨迹扩散 Transformer，它可以整合语义引导，生成符合细粒度用户意图的高保真且忠实于指令的轨迹。在真实世界数据集上的全面实验表明，InsTraj 在生成真实、多样且在语义上忠实于输入指令的轨迹方面，显著优于现有最先进的方法。

## Abstract
The generation of realistic and controllable GPS trajectories is a fundamental task for applications in urban planning, mobility simulation, and privacy-preserving data sharing. However, existing methods face a two-fold challenge: they lack the deep semantic understanding to interpret complex user travel intent, and struggle to handle complex constraints while maintaining the realistic diversity inherent in human behavior. To resolve this, we introduce InsTraj, a novel framework that instructs diffusion models to generate high-fidelity trajectories directly from natural language descriptions. Specifically, InsTraj first utilizes a powerful large language model to decipher unstructured travel intentions formed in natural language, thereby creating rich semantic blueprints and bridging the representation gap between intentions and trajectories. Subsequently, we proposed a multimodal trajectory diffusion transformer that can integrate semantic guidance to generate high-fidelity and instruction-faithful trajectories that adhere to fine-grained user intent. Comprehensive experiments on real-world datasets demonstrate that InsTraj significantly outperforms state-of-the-art methods in generating trajectories that are realistic, diverse, and semantically faithful to the input instructions.

---

## 论文详细总结（自动生成）

以下是对论文《InsTraj: Instructing Diffusion Models with Travel Intentions to Generate Real-world Trajectories》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何生成既符合真实世界物理规律（高保真度），又能精准遵循人类自然语言指令（语义一致性）的GPS轨迹。
*   **研究动机**：
    *   **语义-时空表示鸿沟**：现有模型难以理解人类抽象的出行意图（如“去购物”、“下午散步”），导致生成的轨迹虽然地理上合理，但缺乏目的性。
    *   **生成控制困境**：传统方法在面对复杂约束时，要么退化为确定性的路径规划（丧失多样性），要么为了保持随机性而偏离目标。
    *   **数据稀缺**：缺乏“自然语言指令-真实轨迹”的配对数据集。

### 2. 方法论
InsTraj 框架分为三个核心阶段：
*   **意图蒸馏（Intent Distillation）**：
    *   利用 OpenStreetMap (OSM) 的 POI 数据对城市进行网格化功能剖析（如居住区、商业区）。
    *   通过规则和模板提取原始轨迹的语义特征（起点/终点功能、时间、距离），并利用 **GPT-4** 进行改写，生成多样化、自然的人类出行指令。
*   **意图编码（Intent Encoding）**：
    *   采用 **LLM2Vec** 技术将仅解码（Decoder-only）的大语言模型（如 Llama-3-8B）转换为双向文本编码器。
    *   通过掩码预测和对比学习，将非结构化指令转化为高维的“语义蓝图”（Semantic Blueprints）。
*   **意图对齐生成（Intent-aligned Generation）**：
    *   **核心架构：多模态轨迹扩散 Transformer (MT-DiT)**。
    *   **联合注意力机制（Joint Attention）**：将轨迹 Token 和指令 Token 拼接后进行双向注意力计算，实现深度的多模态融合。
    *   **adaLN-Zero 调制**：利用扩散步长（Timestep）动态调整 Transformer 块的缩放和偏移，稳定训练并增强控制力。
    *   **TrajCLIP**：专门训练一个轨迹-文本对齐模型，用于量化评估生成轨迹与指令的匹配度（TIS 分数）。

### 3. 实验设计
*   **数据集**：两个大规模真实世界出租车数据集：**Taxi-Chengdu**（成都）和 **Taxi-Xi'an**（西安）。
*   **Benchmark 与对比方法**：
    *   **传统生成模型**：VAE, TrajGAN, DP-TrajGAN。
    *   **扩散模型**：Diffwave, DiffTraj, ControlTraj（其中后两者被增强了语义嵌入以实现公平对比）。
*   **评估指标**：
    *   **时空保真度**：JSD（空间密度、行程分布、轨迹长度）。
    *   **指令忠实度**：FCR（功能一致性率）、TIS（轨迹-指令相似度）。

### 4. 资源与算力
*   **模型规模**：MT-DiT 参数量约为 **34.5 MB**；文本编码器基于 **Llama-3-8B**。
*   **算力细节**：文中未明确给出具体的 GPU 型号和训练总时长。
*   **效率表现**：实验提到 InsTraj 在推理速度上比 DiffTraj 和 ControlTraj 快 **2倍以上**，且显存占用更低，这得益于其 Patch 级别的 Token 化处理。

### 5. 实验数量与充分性
*   **实验规模**：在两个城市数据集上进行了全面对比，测试集包含 50,000 条样本。
*   **消融实验**：对比了不同编码器（BERT vs. LLM2Vec）和不同架构（标准 DiT vs. MT-DiT）的影响，验证了各组件的必要性。
*   **鲁棒性与泛化性**：
    *   进行了**跨城市迁移学习**实验（成都预训练 -> 西安微调）。
    *   分析了不同**指令粒度**（粗粒度 vs. 细粒度）和指令质量对生成结果的影响。
*   **评价**：实验设计全面，涵盖了保真度、多样性、控制力和效率，对比基准选择合理，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：InsTraj 在所有时空保真度指标上均达到 SOTA，且在指令忠实度（FCR/TIS）上显著优于基准模型（如在成都数据集上 FCR 提升了约 7.5%）。
*   **语义理解力**：模型能够理解复杂的语义概念（如“午夜”对应的交通稀疏模式，“工业区”对应的空间分布），而非简单的标签匹配。
*   **效率优势**：Transformer 架构结合 Patch 策略，使其在处理长序列轨迹时比传统 U-Net 扩散模型更高效。

### 7. 优点
*   **创新性**：首次系统性地将“文本-图像生成”范式引入轨迹生成领域，解决了自然语言控制轨迹生成的难题。
*   **深度融合**：MT-DiT 的联合注意力机制比简单的条件拼接更能捕捉语义与时空动态的复杂关联。
*   **实用性**：自动化的指令生成流水线解决了标注数据匮乏的问题，且模型支持从抽象到精细的多级控制。

### 8. 不足与局限
*   **数据类型局限**：目前仅在出租车轨迹上验证，可能无法完全涵盖步行、骑行等其他交通模式的特殊行为。
*   **零样本泛化难题**：虽然迁移学习效果好，但在完全不进行微调的情况下，跨城市的零样本生成（Zero-shot）仍面临城市拓扑差异带来的挑战。
*   **环境依赖**：生成质量高度依赖于 POI 数据的准确性和时效性，若城市功能区发生剧变，模型表现可能受限。

（完）
