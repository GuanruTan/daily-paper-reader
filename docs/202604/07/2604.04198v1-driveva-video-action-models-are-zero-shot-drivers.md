---
title: "DriveVA: Video Action Models are Zero-Shot Drivers"
title_zh: DriveVA：视频动作模型是零样本驾驶员
authors: "Mengmeng Liu, Diankun Zhang, Jiuming Liu, Jianfeng Cui, Hongwei Xie, Guang Chen, Hangjun Ye, Michael Ying Yang, Francesco Nex, Hao Cheng"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04198v1"
tags: ["query:tp"]
score: 9.0
evidence: 联合解码未来视觉预测和动作序列
tldr: DriveVA是一个新型自动驾驶世界模型，旨在解决现有模型在未知场景和传感器配置下泛化能力不足的问题。它利用预训练的大规模视频生成模型（DiT架构）的运动动力学先验，通过共享潜空间联合解码未来视频预测和动作序列。该方法实现了规划与场景演化的高度一致性，并在NAVSIM、nuScenes和Bench2drive等基准测试中展现了卓越的闭环性能和零样本跨域泛化能力。
selection_source: fresh_fetch
motivation: 现有的自动驾驶世界模型在跨数据集泛化以及视频预测与轨迹规划的一致性方面存在局限。
method: 提出一种基于DiT解码器的DriveVA模型，通过共享潜生成过程联合预测未来动作和视频，并结合视频延续策略提升长时稳定性。
result: "在NAVSIM上达到90.9的PDM高分，并在nuScenes和Bench2drive的零样本测试中分别降低了约79%和53%的L2误差。"
conclusion: DriveVA证明了将视频生成先验与动作预测深度耦合，能有效提升自动驾驶系统的跨域泛化能力和物理一致性。
---

## 摘要
泛化是自动驾驶中的核心挑战，因为实际部署要求在未见场景、传感器域和环境条件下具备鲁棒的性能。最近基于世界模型的规划方法在场景理解和多模态未来预测方面表现出强大的能力，但它们在跨数据集和传感器配置方面的泛化能力仍然有限。此外，它们松耦合的规划范式通常会导致视觉想象过程中视频与轨迹的一致性较差。为了克服这些局限性，我们提出了 DriveVA，这是一种新型自动驾驶世界模型，它在共享的潜空间生成过程中联合解码未来视觉预测和动作序列。DriveVA 从预训练良好的大规模视频生成模型中继承了关于运动动力学和物理合理性的丰富先验，以捕捉连续的时空演化和因果交互模式。为此，DriveVA 采用基于 DiT 的解码器来联合预测未来动作序列（轨迹）和视频，从而实现规划与场景演化之间更紧密的对齐。我们还引入了一种视频延续策略，以增强长时演进的一致性。DriveVA 在 NAVSIM 挑战赛中实现了 90.9 PDM 分数的卓越闭环性能。大量实验还证明了 DriveVA 的零样本能力和跨域泛化性。与最先进的基于世界模型的规划器相比，它在 nuScenes 上的平均 L2 误差和碰撞率分别降低了 78.9% 和 83.3%，在基于 CARLA v2 构建的 Bench2drive 上分别降低了 52.5% 和 52.4%。

## Abstract
Generalization is a central challenge in autonomous driving, as real-world deployment requires robust performance under unseen scenarios, sensor domains, and environmental conditions. Recent world-model-based planning methods have shown strong capabilities in scene understanding and multi-modal future prediction, yet their generalization across datasets and sensor configurations remains limited. In addition, their loosely coupled planning paradigm often leads to poor video-trajectory consistency during visual imagination. To overcome these limitations, we propose DriveVA, a novel autonomous driving world model that jointly decodes future visual forecasts and action sequences in a shared latent generative process. DriveVA inherits rich priors on motion dynamics and physical plausibility from well-pretrained large-scale video generation models to capture continuous spatiotemporal evolution and causal interaction patterns. To this end, DriveVA employs a DiT-based decoder to jointly predict future action sequences (trajectories) and videos, enabling tighter alignment between planning and scene evolution. We also introduce a video continuation strategy to strengthen long-duration rollout consistency. DriveVA achieves an impressive closed-loop performance of 90.9 PDM score on the challenge NAVSIM. Extensive experiments also demonstrate the zero-shot capability and cross-domain generalization of DriveVA, which reduces average L2 error and collision rate by 78.9% and 83.3% on nuScenes and 52.5% and 52.4% on the Bench2drive built on CARLA v2 compared with the state-of-the-art world-model-based planner.

---

## 论文详细总结（自动生成）

这是一份关于论文《DriveVA: Video Action Models are Zero-Shot Drivers》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：自动驾驶系统在面对未见场景、不同传感器配置和环境条件时的**泛化能力**不足。
*   **现有局限**：
    1.  现有的视觉-语言-动作（VLA）模型多基于静态图像预训练，缺乏对世界运动规律（时空动力学和因果关系）的深刻理解。
    2.  现有的世界模型通常将视频预测与轨迹规划解耦（松耦合），导致“视觉想象”的内容与“实际规划”的路径不一致。
*   **研究动机**：探索大规模预训练视频生成模型（如 Sora 类模型）是否能作为自动驾驶的通用底座，通过学习视频中的物理规律来提升规划的泛化性和一致性。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将自动驾驶建模为一个**统一的视频-动作生成任务**。DriveVA 不再分阶段预测，而是在一个共享的潜空间（Latent Space）中，利用 Diffusion Transformer (DiT) 同时解码未来视频帧和动作 Token。
*   **关键技术细节**：
    *   **统一生成架构**：基于 Wan2.2-TI2V-5B 视频生成模型。输入包括历史帧、当前状态（速度）和可选的文本指令。
    *   **共享潜空间解码**：使用 DiT 解码器，将未来视频的潜变量（Latent Tokens）和动作 Token（Action Tokens）放在一起进行联合去噪。
    *   **流匹配（Flow Matching）**：采用连续时间变换模型替代传统的扩散模型，通过回归速度场 $v_\theta$ 来实现从噪声到数据分布的转换，推理效率更高。
    *   **视频延续策略（Video Continuation）**：通过滑动窗口机制，利用历史观测缓存不断生成后续视频片段，确保长时规划的结构一致性。
    *   **因果 VAE**：使用 3D 因果视觉自编码器，确保生成的每一帧仅依赖于过去和当前的潜变量。

### 3. 实验设计
*   **数据集与场景**：
    *   **NAVSIM v1**：主要用于闭环性能评估，衡量安全性、舒适度和进度。
    *   **nuScenes**：用于**零样本（Zero-shot）跨数据集**泛化测试（在 NAVSIM 上训练，直接在 nuScenes 上跑）。
    *   **Bench2Drive (CARLA v2)**：用于**跨域（实景到仿真）**泛化测试。
*   **Benchmark 与对比方法**：
    *   对比了传统端到端方法（如 UniAD, VADv2, TransFuser）和最新的世界模型方法（如 PWM, DriveVLA-W0, Epona）。
    *   主要指标包括 PDM 分数（NAVSIM）、L2 误差和碰撞率（nuScenes/CARLA）。

### 4. 资源与算力
*   **硬件环境**：使用了 **NVIDIA H20 GPU** 集群。
*   **训练细节**：
    *   第一阶段：Batch size 为 80，训练 20k 步。
    *   第二阶段（全量微调）：通过梯度累积实现 Batch size 640，再训练 10k 步。
    *   **推理效率**：尽管是生成式模型，但 DriveVA 仅需 **2 步采样**即可达到近乎最优的闭环性能。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的广泛实验，包括：
    *   **闭环基准测试**：在 NAVSIM 上刷新了 SOTA 记录（90.9 PDMS）。
    *   **零样本迁移实验**：这是本论文的重头戏，验证了模型在完全未见过的真实世界和仿真环境中的表现。
    *   **消融实验**：针对视频损失、混合训练、视频延续策略、采样步数、模型规模（5B vs 14B）、微调策略（LoRA vs 全量）等进行了 10 组以上的对比。
*   **客观性**：通过引入 **DPVO（深度补丁视觉里程计）** 进行外部验证，定量证明了生成的视频内容与预测轨迹在几何上是高度一致的，实验设计严谨且客观。

### 6. 主要结论与发现
*   **视频监督是核心**：视频级监督（Video Loss）对规划性能的提升至关重要，PDMS 分数从 71.4 飙升至 90.9。
*   **联合解码的优越性**：将动作视为视频生成的“接地”（Grounding），能有效消除视觉想象与物理动作之间的偏差。
*   **强大的零样本泛化**：在 NAVSIM 上训练的模型，在 nuScenes 上的碰撞率比现有最强模型降低了 **83.3%**，证明了视频大模型先验的强大迁移能力。
*   **仿真增强**：加入 CARLA 仿真数据混合训练能进一步提升模型在处理长尾场景（如碰撞规避）时的表现。

### 7. 优点（亮点）
*   **物理一致性强**：通过共享 DiT 架构，解决了“脑子里想的路径”和“脚下开的路径”不统一的问题。
*   **泛化范式创新**：证明了大规模视频生成模型可以作为自动驾驶的“通用驱动器”，而不仅仅是辅助工具。
*   **高效推理**：通过流匹配技术，将生成式模型的推理步数压缩至 2 步，具备了实时应用的潜力。

### 8. 不足与局限
*   **传感器覆盖**：目前主要基于**单前视摄像头**，尚未完全扩展到全向多相机融合（虽然架构上支持扩展）。
*   **计算开销**：尽管采样步数少，但 5B/14B 的参数量对于车载嵌入式芯片仍有巨大挑战。
*   **因果推理局限**：在某些极端复杂的交互场景下（如文中提到的自行车避让失败案例），模型生成的未来视觉预测仍可能偏离真实物理结果，导致规划错误。

（完）
