---
title: A Coordinate-Invariant Local Representation of Motion and Force Trajectories for Identification and Generalization Across Coordinate Systems
title_zh: 一种用于跨坐标系识别与泛化的运动和力轨迹坐标不变局部表示
authors: "Arno Verduyn, Erwin Aertbeliën, Maxim Vochten, Joris De Schutter"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10241v1"
tags: ["query:tp"]
score: 8.0
evidence: 用于预测的运动轨迹坐标不变局部表示
tldr: 针对机器人和生物力学中刚体运动及交互力轨迹识别在不同坐标系下一致性差的问题，本文提出了一种名为“双上三角不变表示”（DUTIR）的新型坐标不变局部表示方法。该方法通过将轨迹转换为不依赖坐标系的抽象形式，有效解决了传统表示法对测量噪声敏感及存在奇异点的问题，为轨迹的分割、识别和预测提供了一个鲁棒且通用的计算框架。
selection_source: fresh_fetch
motivation: 现有的坐标不变轨迹表示方法在处理测量噪声和奇异点时表现不佳，限制了其在跨坐标系任务中的泛化能力。
method: 提出了一种名为 DUTIR 的新型数学表示及其计算算法，能够统一处理刚体运动和交互力轨迹。
result: 该方法显著提高了对奇异点的鲁棒性，并实现了在不同坐标系下对轨迹特征的稳定提取与泛化。
conclusion: DUTIR 为机器人和生物力学领域的轨迹建模提供了一个通用且稳健的工具，增强了跨坐标系识别与预测的可靠性。
---

## 摘要
识别刚体轨迹和相互作用力轨迹对于机器人学、生物力学及相关领域的广泛任务至关重要。这些任务包括轨迹分割、识别和预测。在这些任务中，一个关键挑战在于当轨迹在不同坐标系中表达时，如何获得一致的结果。解决这一挑战的一种方法是利用能够跨坐标系泛化的轨迹模型。本文的重点是通过将轨迹转换为坐标不变表示来获得此类轨迹模型。然而，坐标不变表示通常对测量噪声敏感，且在表示中会出现奇异点（即表示定义不唯一的情况）。本文旨在通过引入一种新型的双上三角不变表示（Dual-Upper-Triangular Invariant Representation, DUTIR）及其计算算法来解决这一局限性，该表示对奇异性具有更高的鲁棒性。所提出的表示是在抽象层面上构建的，使其既适用于刚体轨迹，也适用于相互作用力轨迹，从而使其成为机器人学、生物力学及相关领域的通用工具。

## Abstract
Identifying the trajectories of rigid bodies and of interaction forces is essential for a wide range of tasks in robotics, biomechanics, and related domains. These tasks include trajectory segmentation, recognition, and prediction. For these tasks, a key challenge lies in achieving consistent results when the trajectory is expressed in different coordinate systems. A way to address this challenge is to utilize trajectory models that can generalize across coordinate systems. The focus of this paper is on such trajectory models obtained by transforming the trajectory into a coordinate-invariant representation. However, coordinate-invariant representations often suffer from sensitivity to measurement noise and the manifestation of singularities in the representation, where the representation is not uniquely defined. This paper aims to address this limitation by introducing the novel Dual-Upper-Triangular Invariant Representation (DUTIR), with improved robustness to singularities, along with its computational algorithm. The proposed representation is formulated at a level of abstraction that makes it applicable to both rigid-body trajectories and interaction-force trajectories, hence making it a versatile tool for robotics, biomechanics, and related domains.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：一种用于跨坐标系识别与泛化的运动和力轨迹坐标不变局部表示

#### 1. 核心问题与研究动机
在机器人学和生物力学中，识别刚体运动轨迹和交互力轨迹（如分割、识别、预测）是核心任务。然而，轨迹数据通常依赖于特定的坐标系（如传感器坐标系或机器人基座坐标系）。
*   **研究动机**：不同坐标系下的轨迹表示不一致，导致模型泛化能力差。传统的坐标校准方法费时费力，且难以处理坐标系关系未知的现有数据集。
*   **核心问题**：现有的坐标不变表示方法（如基于微分几何的方法）往往对测量噪声敏感，且在特定运动（如纯平移）下存在**奇异性**（表示不唯一或未定义），且部分优化算法计算复杂度高，难以实时应用。

#### 2. 方法论：DUTIR 与 SU 分解
本文提出了一种名为**双上三角不变表示（DUTIR）**的新型局部表示方法，其核心思想是通过数学分解将轨迹的局部演化与坐标系信息分离。
*   **核心思想**：利用螺旋理论（Screw Theory），将运动（Twist）和力（Wrench）统一抽象为六维向量（General Screws）。
*   **SU 分解**：将描述局部轨迹演化的 $6 \times 3$ 矩阵 $\Xi$（由连续三个时间点的螺旋向量组成）分解为一个螺旋变换矩阵 $S$（包含坐标系位置 $p$ 和姿态 $R$）和一个坐标不变矩阵 $U$。即 $\Xi = S U$。
*   **关键技术细节**：
    *   **矩阵结构**：$U$ 矩阵由两个 $3 \times 3$ 的上三角块堆叠而成，包含 12 个自由度，捕捉了轨迹的内在形状特征。
    *   **正则化处理**：针对奇异点（如纯平移运动导致螺旋轴定义失效），引入了基于几何尺度参数 $L$ 的正则化算法。通过投影和 Procrustes 对齐技术，确保在接近奇异点时表示依然稳定且具有可解释性。
    *   **计算流程**：该方法提供了解析形式的闭式解，无需迭代优化，计算效率极高。

#### 3. 实验设计
本文主要通过理论证明和数值示例来展示方法的有效性。
*   **数值示例**：使用了人类演示的“倒水任务”（kettle pouring）真实轨迹数据。
*   **场景设置**：对比了在两种不同坐标系下提取的扭转（Twist）坐标，并应用 SU 分解提取 $U$ 矩阵。
*   **Benchmark 与对比**：
    *   对比了**无正则化**与**有正则化**（$L=0.3m$）的情况。
    *   引用了作者之前的研究（Verduyn et al., 2025），在三个不同的刚体运动数据集上进行了识别实验，并与现有的不变表示方法（如基于奇异值分解或微分几何的方法）进行了对比。

#### 4. 资源与算力
*   **算力说明**：文中**未明确提到**具体的 GPU 型号或大规模训练时长。
*   **算法特性**：由于该方法是基于线性代数的解析解（Closed-form solution），其计算开销极低，主要涉及 QR 分解和简单的矩阵运算，适合在普通 CPU 上实时运行，无需高性能 GPU 算力支持。

#### 5. 实验数量与充分性
*   **实验规模**：本文作为理论框架的正式发布，重点在于数学证明（附录中提供了详细的几何解释证明）和单一典型案例的深度分析。
*   **充分性评价**：
    *   **理论层面**：非常充分，详细推导了 SU 分解的存在性、唯一性及坐标不变性。
    *   **实证层面**：本文内的实验较少，仅作为一个概念验证（Proof of Concept）。但作者指出，该方法在配套的博士论文和预印本中已通过 3 个数据集的广泛测试，识别准确率达到 96.5%，这在一定程度上弥补了本文实验部分的简洁。

#### 6. 主要结论与发现
*   **DUTIR 的有效性**：成功实现了一种纯局部、低计算复杂度且对坐标系变换完全不敏感的轨迹表示。
*   **鲁棒性提升**：通过正则化手段，有效解决了坐标不变表示在处理“近纯平移”运动时的数值不稳定性（Nervousness）。
*   **通用性**：该框架在抽象层面上统一了运动（位姿演化）和力（交互力/力矩）的表示，具有极强的跨领域适用性。

#### 7. 优点
*   **数学严谨性**：提供了完备的 SU 分解证明和几何解释。
*   **高效实时**：解析解避免了复杂的非线性优化，适合嵌入式机器人系统。
*   **鲁棒性强**：正则化机制解决了困扰不变表示法多年的奇异性问题。
*   **局部性**：仅需局部三个采样点即可计算，无需对整段轨迹进行预分割。

#### 8. 不足与局限
*   **正则化的副作用**：引入正则化参数 $L$ 后，表示在奇异点附近会产生微弱的坐标原点依赖性，虽然文中证明这种影响很小，但在极端精确要求的场景下需谨慎。
*   **信息丢失**：坐标不变表示本质上丢弃了轨迹在空间中的绝对位置和方向信息。如果任务本身与绝对坐标强相关（如“在桌子左侧操作”），该方法则不适用。
*   **实验覆盖**：本文未展示交互力（Wrench）轨迹的实际实验数据，仅在理论上说明了适用性。

（完）
