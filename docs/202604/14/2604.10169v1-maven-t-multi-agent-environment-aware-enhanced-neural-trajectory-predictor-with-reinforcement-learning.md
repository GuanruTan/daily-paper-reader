---
title: "MAVEN-T: Multi-Agent enVironment-aware Enhanced Neural Trajectory predictor with Reinforcement Learning"
title_zh: MAVEN-T：结合强化学习的多智能体环境感知增强型神经轨迹预测器
authors: Wenchang Duan
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10169v1"
tags: ["query:tp"]
score: 10.0
evidence: 动态多智能体场景下的轨迹预测，采用教师-学生框架
tldr: 针对自动驾驶中轨迹预测模型在复杂决策与实时部署间的矛盾，本文提出MAVEN-T框架。该框架采用教师-学生架构，通过多粒度蒸馏和自适应课程学习实现知识迁移，并创新性地引入强化学习，使学生模型能通过环境交互优化教师知识，突破传统蒸馏的性能上限。在NGSIM和highD数据集上，该方法在保持SOTA精度的同时，实现了6.2倍参数压缩和3.7倍推理加速。
selection_source: fresh_fetch
motivation: 现有的知识蒸馏方法在压缩轨迹预测模型时，难以在满足实时性要求的同时保留复杂的多智能体决策能力。
method: 提出一种结合混合注意力机制教师模型与高效学生模型的框架，利用多粒度蒸馏、自适应课程学习及强化学习进行知识迁移与优化。
result: 在NGSIM和highD数据集上，该模型实现了6.2倍的参数压缩和3.7倍的推理加速，且保持了最先进的预测精度。
conclusion: MAVEN-T证明了通过强化学习辅助蒸馏可以突破模仿上限，为资源受限环境下的复杂推理模型部署提供了新范式。
---

## 摘要
轨迹预测仍然是自动驾驶系统中一个关键且具有挑战性的组件，要求在满足严格实时部署约束的同时具备复杂的推理能力。虽然知识蒸馏在模型压缩方面已证明了其有效性，但现有方法往往无法保留复杂的决策能力，特别是在动态多智能体场景中。本文介绍了 MAVEN-T，这是一个教师-学生框架，通过互补的架构协同设计和渐进式蒸馏实现了最先进的轨迹预测。教师模型采用混合注意力机制以获得最大的表征能力，而学生模型则使用针对部署优化的轻量化架构。知识迁移通过多粒度蒸馏和自适应课程学习进行，后者可根据性能动态调整复杂度。重要的是，该框架结合了强化学习以克服传统蒸馏的模仿上限，使学生模型能够通过动态环境交互来验证、细化和优化教师知识，从而可能实现比教师模型本身更稳健的决策。在 NGSIM 和 highD 数据集上的广泛实验表明，该方法在保持最先进准确率的同时，实现了 6.2 倍的参数压缩和 3.7 倍的推理加速，为在资源受限下部署复杂推理模型建立了新的范式。

## Abstract
Trajectory prediction remains a critical yet challenging component in autonomous driving systems, requiring sophisticated reasoning capabilities while meeting strict real-time deployment constraints. While knowledge distillation has demonstrated effectiveness in model compression, existing approaches often fail to preserve complex decision-making capabilities, particularly in dynamic multi-agent scenarios. This paper introduces MAVEN-T, a teacher-student framework that achieves state-of-the-art trajectory prediction through complementary architectural co-design and progressive distillation. The teacher employs hybrid attention mechanisms for maximum representational capacity, while the student uses efficient architectures optimized for deployment. Knowledge transfer is performed via multi-granular distillation with adaptive curriculum learning that dynamically adjusts complexity based on performance. Importantly, the framework incorporates reinforcement learning to overcome the imitation ceiling of traditional distillation, enabling the student to verify, refine, and optimize teacher knowledge through dynamic environmental interaction, potentially achieving more robust decision-making than the teacher itself. Extensive experiments on NGSIM and highD datasets demonstrate 6.2x parameter compression and 3.7x inference speedup while maintaining state-of-the-art accuracy, establishing a new paradigm for deploying sophisticated reasoning models under resource constraints.

---

## 论文详细总结（自动生成）

以下是对论文《MAVEN-T: Multi-Agent enVironment-aware Enhanced Neural Trajectory predictor with Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于自动驾驶中的**轨迹预测**任务。核心矛盾在于：高性能模型通常架构复杂、计算量大，难以满足车载嵌入式设备的**实时部署**要求；而传统的知识蒸馏（KD）方法在压缩模型时，往往会丢失复杂的多智能体决策推理能力，且受限于“模仿天花板”（即学生模型性能很难超越教师模型）。
**MAVEN-T** 的提出旨在通过一种互补的教师-学生架构，结合强化学习（RL）和自适应课程学习，在大幅降低计算开销的同时，保持甚至提升预测的鲁棒性。

### 2. 方法论：核心思想与关键技术
该框架采用了**教师-学生（Teacher-Student）**协同设计模式：
*   **教师模型（高容量）**：
    *   **空间建模**：利用 GATv2 捕捉车辆间的动态交互。
    *   **时空推理**：结合了 **Mamba 块**（具备线性计算复杂度的状态空间模型）处理长序列，以及**移位窗口注意力（Shift-Window Attention）**捕捉局部空间特征。
    *   **计算优化**：引入**混合专家模型（MoE）**，仅激活部分专家网络以平衡容量与效率。
*   **学生模型（轻量化）**：
    *   采用基于 **GRU** 的编码器和 **SE（Squeeze-and-Excitation）** 注意力机制。
    *   使用 **LoRA（低秩自适应）** 参数化的策略头，直接输出驾驶动作。
*   **知识迁移机制**：
    *   **多粒度蒸馏**：在感知（特征层）、注意力（中间层）和语义（输出层）三个维度进行对齐。
    *   **强化学习增强（RL-augmented）**：引入 PPO 算法，利用环境反馈（安全、舒适、效率奖励）对学生模型进行微调，使其能纠正教师模型中的潜在错误，突破纯模仿的限制。
    *   **自适应课程学习**：根据学生模型的表现动态调整训练场景的复杂度（如车辆密度、相对速度等）。

### 3. 实验设计
*   **数据集**：使用了两个主流的真实世界高速公路数据集：**NGSIM**（美国 I-80 和 US 101）和 **highD**（德国高速公路）。
*   **评估指标**：RMSE（均方根误差）、ADE（平均位移误差）、FDE（最终位移误差）。
*   **对比基准（Baselines）**：
    *   经典序列模型：V-LSTM, S-LSTM, CS-LSTM。
    *   先进模型：STDAN, WSiP。
    *   SOTA 框架：**C2F-TP**（粗到精轨迹预测）。
    *   轻量化变体：MobileNet-Traj, DistilBERT-Traj 等。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA A100 GPU** 上运行。
*   **软件环境**：CUDA 11.8, PyTorch 2.0.1, Python 3.9。
*   **训练时长**：完整实验（含超参数搜索和消融实验）总计耗时约 **300 GPU 小时**。

### 5. 实验数量与充分性
论文进行了非常详尽的实验验证：
*   **主实验**：在两个数据集上对比了教师和学生模型的预测精度。
*   **消融实验**：逐一验证了 SE 注意力、LoRA、多粒度蒸馏、课程学习及 PPO 对性能的贡献。
*   **鲁棒性测试**：测试了在不同噪声水平（5%-15%）和对抗攻击（FGSM, PGD 等）下的表现。
*   **泛化性测试**：进行了跨数据集（NGSIM $\leftrightarrow$ highD）的迁移实验。
*   **统计显著性**：所有改进均通过了 $p < 0.01$ 的配对 t 检验。
实验设计客观、全面，覆盖了从精度、效率到安全性的多个维度。

### 6. 主要结论与发现
*   **高效压缩**：学生模型实现了 **6.2 倍的参数压缩**和 **3.7 倍的推理加速**。
*   **性能卓越**：在大幅减小规模后，MAVEN-T 学生模型的 ADE/FDE 依然优于当前的 SOTA 模型 C2F-TP。
*   **突破天花板**：通过 RL 引入环境反馈，学生模型在某些复杂场景下的决策稳健性超越了教师模型。
*   **收敛加速**：自适应课程学习使训练收敛速度提升了 37%。

### 7. 优点与亮点
*   **架构创新**：将 Mamba 和 MoE 引入轨迹预测领域，有效解决了长序列建模与计算开销的矛盾。
*   **闭环优化**：不同于传统的单向蒸馏，引入 RL 让学生模型在“模仿”的基础上具备了“自省”和“优化”能力。
*   **部署友好**：针对边缘设备设计的轻量化学生模型具有极高的实用价值。

### 8. 不足与局限
*   **极端场景限制**：在车辆密度极高（$N > 15$）、极端天气（雨雪）或施工区域等特殊场景下，模型性能仍有明显下降（ADE 增加 23%-31%）。
*   **特种车辆处理**：对于救护车等诱发非典型驾驶行为的紧急车辆，模型捕捉能力不足。
*   **计算成本**：虽然推理快，但训练过程涉及教师模型前向传播和 RL 交互，总训练算力需求（300 GPU 小时）对小型团队仍有门槛。

（完）
