---
title: "RAD-2: Scaling Reinforcement Learning in a Generator-Discriminator Framework"
title_zh: RAD-2：在生成器-判别器框架中扩展强化学习
authors: "Hao Gao, Shaoyu Chen, Yifan Zhu, Yuehao Song, Wenyu Liu, Qian Zhang, Xinggang Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15308v1"
tags: ["query:tp"]
score: 9.0
evidence: 为自动驾驶运动规划建模多模态未来不确定性
tldr: "RAD-2是一个针对自动驾驶的生成器-判别器框架，旨在解决扩散模型规划器在闭环交互中的不稳定性。它利用扩散模型生成多样化轨迹，并通过强化学习优化的判别器进行重排序。通过引入时间一致性策略优化和在线生成器优化，结合高效的BEV-Warp仿真环境，显著提升了规划的安全性和鲁棒性，碰撞率降低了56%。"
selection_source: fresh_fetch
motivation: 传统的扩散模型规划器在纯模仿学习下缺乏负反馈，且在闭环交互中存在随机不稳定性。
method: 提出RAD-2框架，结合扩散生成器与RL判别器，并引入TC-GRPO算法和BEV-Warp高效仿真环境。
result: "在闭环评估中比强力扩散规划器降低了56%的碰撞率，并在真实世界部署中表现出更优的安全性。"
conclusion: 该研究证明了通过强化学习优化判别器并反馈至生成器，能有效提升自动驾驶在复杂场景下的规划性能。
---

## 摘要
高阶自动驾驶要求运动规划器能够建模多模态未来不确定性，同时在闭环交互中保持鲁棒性。尽管基于扩散模型的规划器在建模复杂轨迹分布方面非常有效，但在纯模仿学习训练下，它们往往面临随机不稳定性以及缺乏纠正性负反馈的问题。为了解决这些问题，我们提出了 RAD-2，一个用于闭环规划的统一生成器-判别器框架。具体而言，该框架使用基于扩散模型的生成器产生多样化的候选轨迹，而经过强化学习（RL）优化的判别器则根据长期驾驶质量对这些候选轨迹进行重排序。这种解耦设计避免了将稀疏标量奖励直接应用于整个高维轨迹空间，从而提高了优化稳定性。为了进一步增强强化学习，我们引入了时间一致性群体相对策略优化（Temporally Consistent Group Relative Policy Optimization），利用时间相干性来缓解信用分配问题。此外，我们提出了同策略生成器优化（On-policy Generator Optimization），将闭环反馈转化为结构化的纵向优化信号，并逐步将生成器推向高奖励轨迹流形。为了支持高效的大规模训练，我们引入了 BEV-Warp，这是一种高吞吐量仿真环境，通过空间扭曲直接在鸟瞰图（BEV）特征空间中进行闭环评估。与强大的基于扩散模型的规划器相比，RAD-2 将碰撞率降低了 56%。真实世界部署进一步证明了其在复杂城市交通中提升了感知安全性和驾驶平顺性。

## Abstract
High-level autonomous driving requires motion planners capable of modeling multimodal future uncertainties while remaining robust in closed-loop interactions. Although diffusion-based planners are effective at modeling complex trajectory distributions, they often suffer from stochastic instabilities and the lack of corrective negative feedback when trained purely with imitation learning. To address these issues, we propose RAD-2, a unified generator-discriminator framework for closed-loop planning. Specifically, a diffusion-based generator is used to produce diverse trajectory candidates, while an RL-optimized discriminator reranks these candidates according to their long-term driving quality. This decoupled design avoids directly applying sparse scalar rewards to the full high-dimensional trajectory space, thereby improving optimization stability. To further enhance reinforcement learning, we introduce Temporally Consistent Group Relative Policy Optimization, which exploits temporal coherence to alleviate the credit assignment problem. In addition, we propose On-policy Generator Optimization, which converts closed-loop feedback into structured longitudinal optimization signals and progressively shifts the generator toward high-reward trajectory manifolds. To support efficient large-scale training, we introduce BEV-Warp, a high-throughput simulation environment that performs closed-loop evaluation directly in Bird's-Eye View feature space via spatial warping. RAD-2 reduces the collision rate by 56% compared with strong diffusion-based planners. Real-world deployment further demonstrates improved perceived safety and driving smoothness in complex urban traffic.

---

## 论文详细总结（自动生成）

这是一份关于论文《RAD-2: Scaling Reinforcement Learning in a Generator-Discriminator Framework》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于扩散模型（Diffusion-based）的自动驾驶规划器虽然能建模多模态轨迹，但在闭环交互中存在**随机不稳定性**，且纯模仿学习（IL）训练缺乏**负反馈机制**，导致在复杂场景下容易发生碰撞。
*   **研究背景**：直接对高维轨迹空间应用强化学习（RL）面临稀疏奖励和信用分配（Credit Assignment）难题。此外，现有的闭环仿真环境要么保真度低（游戏引擎），要么计算开销巨大（重建/生成式世界模型），限制了 RL 的规模化训练。

### 2. 方法论
RAD-2 提出了一个统一的生成器-判别器框架，将规划任务解耦为“多样化生成”与“高质量筛选”：
*   **生成器-判别器解耦**：
    *   **生成器 (G)**：基于扩散模型，负责生成多模态候选轨迹。
    *   **判别器 (D)**：基于 Transformer，通过 RL 优化，负责根据长期驾驶质量（安全、效率）对候选轨迹进行重排序。
*   **关键技术细节**：
    *   **TC-GRPO (时间一致性群体相对策略优化)**：为了解决信用分配问题，引入“轨迹复用”机制保持短期行为一致性，并采用群体相对评分来抵消奖励噪声，稳定策略梯度。
    *   **OGO (同策略生成器优化)**：不直接对高维轨迹做 RL，而是将闭环反馈转化为纵向（加速度）优化信号，引导生成器分布向高奖励流形偏移。
    *   **BEV-Warp 仿真环境**：利用 BEV 特征的空间等变性，通过空间扭曲（Warping）直接在特征层进行闭环仿真，绕过了昂贵的图像渲染，实现了高吞吐量训练。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用 50,000 小时真实世界驾驶数据。
    *   **RL 训练/评估**：从真实日志中提取 50,000 个片段，涵盖安全敏感和效率敏感场景。
*   **Benchmark**：
    *   **BEV-Warp 闭环评估**：对比安全性和导航效率。
    *   **3DGS 仿真**：在光流渲染的 3D 高斯泼溅环境下验证鲁棒性。
    *   **Senna-2 开路评估**：验证轨迹预测的准确性。
*   **对比方法**：包括 TransFuser (回归类)、VAD (向量化规划)、GenAD (生成式)、ResAD (扩散模型基准) 以及 Senna/RAD 等最新方法。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但考虑到其使用了 50,000 小时的数据规模以及大规模闭环仿真，推测其依赖于高性能计算集群（如 NVIDIA A100/H100 级算力）。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的闭环测试（512 个安全片段 + 512 个效率片段）和开路测试。
*   **消融实验**：非常充分。涵盖了执行时域 ($H_{reuse}$)、奖励过滤、模型初始化方式、GRPO 组大小、熵正则化以及训练场景组成等多个维度的对比。
*   **客观性**：通过引入推理时缩放（Inference-time Scaling）分析，展示了增加候选轨迹数量对性能的持续提升，证明了框架的有效性。实验设计涵盖了从特征级仿真到光学级仿真再到真实部署的完整链路，具有较高的说服力。

### 6. 主要结论与发现
*   **性能提升**：相比于强力扩散模型规划器（ResAD），RAD-2 将碰撞率降低了 **56%**。
*   **协同效应**：联合优化生成器和判别器比单一组件优化具有更好的扩展性（Scaling Behavior），能更有效地利用环境交互数据。
*   **推理缩放**：判别器能够通过在推理时评估更多候选轨迹（M=8 到 128）来进一步提升规划质量，无需重新训练。

### 7. 优点与亮点
*   **稳定性**：通过将 RL 限制在低维评分空间（判别器），解决了高维轨迹空间优化难的问题。
*   **高效仿真**：BEV-Warp 是一种极具创新的轻量化闭环方案，平衡了特征保真度与计算效率。
*   **物理先验**：OGO 机制利用纵向运动学先验来引导生成器，比纯黑盒 RL 更符合驾驶逻辑。

### 8. 不足与局限
*   **表示局限性**：BEV-Warp 强依赖于具有空间等变性的 BEV 特征，对于直接使用原始像素或非空间潜在变量的端到端架构可能不适用。
*   **Sim-to-Real 风险**：虽然在 3DGS 和真实车辆上做了测试，但特征级扭曲仿真仍可能忽略一些复杂的传感器噪声和动态遮挡细节。
*   **计算延迟**：扩散模型生成器配合大规模候选轨迹的判别器重排序，在车载端侧实时部署时可能面临推理延迟挑战。

（完）
