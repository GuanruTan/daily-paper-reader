---
title: "World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems"
title_zh: 世界-价值-动作模型：视觉-语言-动作系统的隐式规划
authors: "Runze Li, Hongyin Zhang, Junxi Jin, Qixin Zeng, Zifeng Zhuang, Yiqi Tang, Shangke Lyu, Donglin Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14732v1"
tags: ["query:tp"]
score: 8.0
evidence: 学习未来轨迹的结构化潜变量表示
tldr: 针对现有视觉-语言-动作（VLA）模型在长程任务中缺乏推理和评估能力的问题，本文提出了WAV模型。该模型通过学习结构化潜空间表示，结合世界模型预测未来状态和价值函数评估长程效用，将动作生成转化为潜空间推理。理论证明潜空间推理能有效解决动作空间搜索的指数级衰减问题，显著提升了复杂任务的成功率和泛化性。
selection_source: fresh_fetch
motivation: 现有的VLA模型多依赖直接动作预测，难以在复杂决策任务中进行长程轨迹推理和后果评估。
method: 提出WAV模型，通过世界模型预测未来状态并利用价值函数评估，在结构化潜空间中进行隐式规划以生成动作。
result: 实验表明WAV在仿真和真实场景中均优于现有SOTA方法，特别是在长程和组合任务中表现出更强的鲁棒性。
conclusion: 潜空间推理能有效重塑搜索分布，是提升VLA系统长程决策能力和泛化性能的关键路径。
---

## 摘要
视觉-语言-动作（VLA）模型已成为构建具身智能体的一种极具前景的范式，能够将感知和语言落地为动作。然而，大多数现有方法依赖于直接的动作预测，缺乏对长时程轨迹进行推理并评估其后果的能力，这限制了它们在复杂决策任务中的表现。在这项工作中，我们提出了世界-价值-动作（WAV）模型，这是一个在 VLA 系统中实现隐式规划的统一框架。WAV 模型并非进行显式的轨迹优化，而是学习一种以视觉观测和语言指令为

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for building embodied agents that ground perception and language into action. However, most existing approaches rely on direct action prediction, lacking the ability to reason over long-horizon trajectories and evaluate their consequences, which limits performance in complex decision-making tasks. In this work, we introduce World-Value-Action (WAV) model, a unified framework that enables implicit planning in VLA systems. Rather than performing explicit trajectory optimization, WAV model learn a structured latent representation of future trajectories conditioned on visual observations and language instructions. A learned world model predicts future states, while a trajectory value function evaluates their long-horizon utility. Action generation is then formulated as inference in this latent space, where the model progressively concentrates probability mass on high-value and dynamically feasible trajectories. We provide a theoretical perspective showing that planning directly in action space suffers from an exponential decay in the probability of feasible trajectories as the horizon increases. In contrast, latent-space inference reshapes the search distribution toward feasible regions, enabling efficient long-horizon decision making. Extensive simulations and real-world experiments demonstrate that the WAV model consistently outperforms state-of-the-art methods, achieving significant improvements in task success rate, generalization ability, and robustness, especially in long-horizon and compositional scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型大多依赖于直接的动作预测（即反应式策略），缺乏对长程轨迹的推理能力和对未来后果的评估机制。这导致模型在处理复杂、多步骤的决策任务时表现不佳。
*   **研究背景**：虽然世界模型（World Models）能预测未来，但通常缺乏评估轨迹质量的原则性机制；而模型预测控制（MPC）在动作空间直接搜索时，随着规划时界的增加，搜索到可行轨迹的概率会呈指数级衰减（可行性诅咒）。

### 2. 方法论
WAV 模型将规划视为一种**潜空间中的隐式推理过程**，而非显式的轨迹优化。
*   **核心思想**：通过联合建模环境动力学（世界）、轨迹效用（价值）和动作执行（动作），在结构化的潜空间中通过迭代推理，将概率质量集中在既符合物理规律又具有高价值的轨迹上。
*   **关键技术组件**：
    1.  **语言条件视频生成模块**：基于 Diffusion Transformer (DiT)，根据指令和当前观测预测未来视觉轨迹特征。
    2.  **轨迹价值模块**：评估预测视频轨迹的长程效用（累积折扣奖励）。
    3.  **动作解码模块**：融合视频特征和价值特征，解码出具体的机器人动作。
*   **训练与推理**：
    *   **训练**：采用**流匹配（Flow Matching）**技术，分三阶段训练：视频流损失、价值流损失、动作流损失。
    *   **推理（迭代潜空间规划）**：受 MPPI 启发，不直接在动作空间搜索，而是在视频和价值的潜噪声分布中进行迭代优化。通过多次采样、评估（计算信噪比 SNR）并更新潜分布参数，最终选择最优潜变量生成动作。

### 3. 实验设计
*   **仿真场景**：使用 **LIBERO** 基准测试集，包含四个子集：Spatial（空间推理）、Object（物体泛化）、Goal（目标条件）、Long（长程组合任务）。
*   **真实世界场景**：使用双臂 Piper 机器人执行：整理碗筷（组织）、铺平毛巾（形变物体）、抽屉任务（长程：开抽屉-放物体-关抽屉）。
*   **对比方法**：
    *   **反应式 VLA**：Diffusion Policy, Octo, OpenVLA, $\pi_0$, VLA-0 等。
    *   **基于世界模型的 VLA**：WorldVLA, DreamVLA, UniVLA, GE-ACT 等。

### 4. 资源与算力
*   **硬件**：使用 8 张 **NVIDIA A100-SXM4-80GB** GPU。
*   **训练时长**：
    *   LIBERO 任务：视频模块 4万步，价值/动作模块 3万步，总计约 **5 天**。
    *   真实世界任务：每个任务独立训练，总计约 **3 天**。
*   **参数量**：模型总参数量约为 **22 亿（2.2B）**。

### 5. 实验数量与充分性
*   **实验规模**：在 LIBERO 的数十个任务上进行了测试，并在真实世界中针对每个任务进行了 15 次独立实验。
*   **消融实验**：非常充分。研究了迭代次数 $K$、采样数 $M/N$、平滑参数 $\alpha/\beta$ 以及精英样本数 $K_1/K_2$ 对性能的影响。
*   **客观性**：对比了从 0.5B 到 8.5B 不同规模的最新 SOTA 模型，并提供了推理时间与显存占用的量化分析，实验设计较为公平、透明。

### 6. 主要结论与发现
*   **性能领先**：WAV 在 LIBERO 平均得分达到 **98.1**，显著优于现有模型，尤其在长程任务（Long）中优势明显。
*   **理论验证**：理论分析证明，潜空间推理能有效缓解动作空间搜索的指数级衰减问题，使搜索更集中于可行区域。
*   **真实世界鲁棒性**：在真实场景中，WAV 的平均成功率（75.6%）远高于基准模型 GE-ACT（35.6%），证明了隐式规划在减少误差累积方面的作用。
*   **迭代价值**：增加推理迭代次数（如 $K=3$）能显著提升成功率，但超过一定次数后收益递减。

### 7. 优点
*   **理论与实践结合**：为 VLA 中的规划提供了严谨的数学证明（引理 4.1 和命题 4.2）。
*   **模块化设计**：将预测、评估和执行解耦又统一在 DiT 架构下，结构清晰。
*   **长程处理能力**：通过潜空间重塑搜索分布，解决了传统 VLA 难以处理长序列任务的痛点。

### 8. 不足与局限
*   **推理开销**：由于涉及多次迭代采样和流匹配去噪，推理延迟（约 10-15s）和显存占用（约 32GB）高于传统的反应式模型，可能限制其在极高频实时控制中的应用。
*   **数据依赖**：模型性能高度依赖于高质量的视频预测和密集的奖励函数设计，而这些数据在某些复杂环境下难以获取。
*   **闭环频率**：目前主要展示了开环规划后的执行效果，未来在实时闭环部署上的表现仍需进一步验证。

（完）
