---
title: Collaborative Trajectory Prediction via Late Fusion
authors: "Nadya Abdel Madjid, Murad Mebrahtu, Zakhar Yagudin, Bilal Hassan, Naoufel Werghi, Jorge Dias, Dzmitry Tsetserukou, Majid Khonji"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22973v1"
tags: ["query:tp"]
score: 10.0
evidence: 通过 V2V 实现交通智能体的协作轨迹预测
tldr: 提出一种用于协作轨迹预测的后期融合方法，以减少感知不确定性。
selection_source: fresh_fetch
motivation: 通过 V2V 实现交通智能体的协作轨迹预测。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Predicting future trajectories of surrounding traffic agents is critical for safe autonomous navigation and collision avoidance. Despite all advances in the trajectory forecasting realm, the prediction models remains vulnerable to uncertainty caused by occlusions, limited sensing range, and perception errors. Collaborative vehicle-to-vehicle (V2V) approaches help reduce this uncertainty by sharing complementary information. Existing collaborative trajectory prediction methods typically fuse feature maps at the perception stage to construct a holistic scene view. Further this holistic representation is decoded into the future trajectories. Such design incurs substantial communication overhead due to the exchange of high-dimensional feature representations and often assumes idealized bandwidth and synchronization, limiting practical deployment. We address these limitations by shifting collaboration from perception to the prediction module and introducing a late-fusion framework for shared forecasts. The framework is model-agnostic and treats collaborating vehicles as independent asynchronous agents. We evaluate the approach on the OPV2V, V2V4Real, and DeepAccident datasets, comparing individual and collaborative forecasting. Across all datasets, late fusion consistently reduces miss rate and improves trajectory success rate ($\mathrm{TSR}_{0.5}$), defined as the fraction of ground-truth agents with final displacement error below 0.5 m. On the real-world V2V4Real dataset, collaborative prediction improves the success rate by $1.69\%$ and $1.22\%$ for both intelligent vehicles, respectively, compared with individual forecasting.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于后期融合（Late Fusion）的协作轨迹预测框架，旨在解决自动驾驶中因遮挡、传感器范围受限和感知错误导致的预测不确定性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：单车轨迹预测模型在面对遮挡（如行人被卡车挡住）或远距离目标时极易失效。
*   **研究动机**：
    *   **现有方法的局限**：目前的协作预测（V2V）多采用“中间融合”（共享高维特征图），这导致了巨大的通信带宽压力，且通常假设车辆间是理想同步的，难以在现实中部署。
    *   **本文目标**：将协作从感知阶段转移到预测阶段，通过共享稀疏的轨迹预测结果（后期融合）来降低通信开销，并处理车辆间的异步通信问题。

### 2. 方法论
核心思想是建立一个模型无关（Model-agnostic）的后期融合框架，将协作车辆视为独立的异步智能体。
*   **智能体级预测图（Prediction Map）**：每辆车维护一个局部地图，包含两类节点：
    *   **Category L（本地观测）**：本车传感器可见的目标。
    *   **Category S（仅共享）**：本车不可见，但由其他协作车告知的目标。
*   **关键技术细节**：
    *   **数据关联与时间对齐**：利用全局时钟（如GPS）处理异步消息，通过算法进行轨迹的时间对齐和空间匹配，无需联合训练。
    *   **高斯过程（GP）融合**：使用高斯过程回归作为融合算子。对于Category L节点，采用基于卡尔曼滤波门控（KF-gate）的残差修正；对于Category S节点，直接从共享轨迹池中重建轨迹。
    *   **通信协议模拟**：遵循C-V2X和5G NR-V2X标准，限制消息大小（1500字节以内）和频率（10Hz）。

### 3. 实验设计
*   **数据集**：
    *   **OPV2V**：仿真数据集，支持多达7辆协作车。
    *   **DeepAccident**：基于CARLA的仿真数据集，专注于碰撞场景。
    *   **V2V4Real**：真实世界数据集，包含两辆协作车的实际路测数据。
*   **实验场景设置**：
    *   **受控设置（Controlled）**：使用射线投射（Ray-casting）算法模拟遮挡，排除感知错误干扰，专注于评估融合效果。
    *   **真实部署设置（Real-deployment）**：在V2V4Real上使用完整的LiDAR感知栈（CenterPoint + PointPillars）。
*   **对比基准（Benchmark）**：主要对比“无融合（单车预测）”在有/无遮挡情况下的表现。
*   **评价指标**：ADE（平均位移误差）、FDE（最终位移误差）、MR（漏检率）和 **TSR₀.₅（轨迹成功率）**。

### 4. 资源与算力
*   **硬件**：论文提到在生成检测结果和推理时使用了 **NVIDIA RTX 3090 (24 GB)** GPU。
*   **训练/推理时长**：未明确说明具体训练总时长，但提供了推理耗时分析。单车预测耗时约12.9-15.4ms，加入后期融合后总耗时增加到43.6-69.6ms，仍满足实时性要求（<100ms）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流数据集上进行了详尽测试。
    *   对比了不同的预测器架构（Transformer vs. LSTM）和不同的预测时界（1s历史/2s预测 vs. 2s历史/3s预测）。
    *   进行了消融实验，分析了协作车辆数量（1到6辆）对性能的影响。
    *   加入了通信延迟和丢包模拟实验。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到现实、从理想感知到噪声感知的多种情况，验证了框架的鲁棒性和可扩展性。

### 6. 主要结论与发现
*   **性能提升**：后期融合在所有数据集上均显著降低了漏检率（MR）并提升了TSR。在V2V4Real真实数据集上，TSR₀.₅分别提升了1.69%和1.22%。
*   **遮挡恢复**：该方法能有效恢复被完全遮挡的目标（Category S），并微幅修正已观测目标的轨迹（Category L）。
*   **通信效率**：共享的轨迹数据量极小，平均消息大小远低于5G V2X的1500字节限制。
*   **鲁棒性**：在模拟的通信延迟和丢包环境下，融合带来的增益虽然有所衰减，但依然优于单车预测。

### 7. 优点
*   **通信友好**：相比特征融合，后期融合极大地降低了带宽需求，更符合现有的V2V通信标准。
*   **模块化与通用性**：框架与具体的检测器、跟踪器或预测器解耦，易于集成到现有的自动驾驶栈中。
*   **异步处理**：通过算法解决时间对齐问题，不依赖于严格的车辆间同步。

### 8. 不足与局限
*   **计算开销**：高斯过程（GP）的超参数优化在高密度场景下计算成本较高，可能成为实时性的瓶颈。
*   **精度提升上限**：对于本地已观测节点（Category L），融合带来的ADE/FDE改善非常微小（约1-3cm），说明残差修正的效果受限于预测器本身的性能。
*   **不确定性校准**：目前的融合高度依赖于预测器输出的方差，若预测器本身对不确定性的估计不准，融合效果会受限。

（完）
