---
title: "Behavioral Score Diffusion: Model-Free Trajectory Planning via Kernel-Based Score Estimation from Data"
title_zh: 行为评分扩散：通过基于核函数的数据评分估计实现无模型轨迹规划
authors: "Shihao Li, Jiachen Li, Jiamin Xu, Dongmei Chen"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00391v1"
tags: ["query:tp"]
score: 8.0
evidence: 通过得分估计进行无模型轨迹规划
tldr: 针对扩散模型在轨迹规划中依赖大规模训练或解析动力学模型的问题，本文提出行为分数扩散（BSD）。这是一种无需训练且无需模型的规划器，通过核加权估计直接从轨迹库计算分数函数。BSD利用扩散邻近度、状态上下文和目标相关性的三重核权重，在去噪过程中实现从全局模式到局部细节的多尺度非参数回归，有效处理非线性动力学并保证安全性。
selection_source: fresh_fetch
motivation: 现有的扩散轨迹优化方法通常需要昂贵的模型训练或精确的动力学解析模型，限制了其在复杂或未知环境中的应用。
method: 提出BSD算法，利用三重核加权方案和Nadaraya-Watson估计，直接从历史轨迹数据中计算扩散分数函数，实现从粗到细的轨迹生成。
result: "在多种机器人系统实验中，BSD仅需1000条轨迹即可达到基于模型方法98.5%的性能，且显著优于最近邻检索方法。"
conclusion: BSD证明了结合扩散去噪机制与非参数核估计可以在无需显式建模的情况下，实现高效且鲁棒的机器人轨迹规划。
---

## 摘要
基于扩散的轨迹优化已成为一种强大的规划范式，但现有方法要么需要在大规模数据集上训练评分网络，要么需要解析动力学模型来进行评分计算。我们提出了行为评分扩散（Behavioral Score Diffusion, BSD），这是一种无需训练且无模型的轨迹规划器，它通过核加权估计直接从轨迹数据库中计算扩散评分函数。在每个去噪步骤中，BSD 使用三重核加权方案（扩散邻近度、状态上下文和目标相关性）检索相关轨迹，并计算去噪轨迹的 Nadaraya-Watson 估计。扩散噪声调度自然地控制了核带宽，从而构建了一个多尺度非参数回归：在高噪声下对全局行为模式进行宽泛平均，在低噪声下进行细粒度的局部插值。这种由粗到精的结构无需线性化或参数化假设即可处理非线性动力学。通过对核估计的状态轨迹应用屏蔽展开（shielded rollout），可以保证安全性，这与现有的基于模型的方法一致。我们在泊车场景中，针对四个复杂度递增（3D 至 6D 状态空间）的机器人系统评估了 BSD。在不需要动力学模型且仅使用 1,000 条预采集轨迹的情况下，具有固定带宽的 BSD 在各系统中的平均奖励达到了基于模型的基准方法的 98.5%。BSD 的表现显著优于最近邻检索（提升了 18-63%），证实了扩散去噪机制对于有效的数据驱动规划至关重要。

## Abstract
Diffusion-based trajectory optimization has emerged as a powerful planning paradigm, but existing methods require either learned score networks trained on large datasets or analytical dynamics models for score computation. We introduce \emph{Behavioral Score Diffusion} (BSD), a training-free and model-free trajectory planner that computes the diffusion score function directly from a library of trajectory data via kernel-weighted estimation. At each denoising step, BSD retrieves relevant trajectories using a triple-kernel weighting scheme -- diffusion proximity, state context, and goal relevance -- and computes a Nadaraya-Watson estimate of the denoised trajectory. The diffusion noise schedule naturally controls kernel bandwidths, creating a multi-scale nonparametric regression: broad averaging of global behavioral patterns at high noise, fine-grained local interpolation at low noise. This coarse-to-fine structure handles nonlinear dynamics without linearization or parametric assumptions. Safety is preserved by applying shielded rollout on kernel-estimated state trajectories, identical to existing model-based approaches. We evaluate BSD on four robotic systems of increasing complexity (3D--6D state spaces) in a parking scenario. BSD with fixed bandwidth achieves 98.5\% of the model-based baseline's average reward across systems while requiring no dynamics model, using only 1{,}000 pre-collected trajectories. BSD substantially outperforms nearest-neighbor retrieval (18--63\% improvement), confirming that the diffusion denoising mechanism is essential for effective data-driven planning.

---

## 论文详细总结（自动生成）

以下是对论文《Behavioral Score Diffusion: Model-Free Trajectory Planning via Kernel-Based Score Estimation from Data》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于扩散模型的轨迹优化方法存在两难困境：要么需要在大规模数据集上进行昂贵的**神经网络训练**（如 Diffusion Policy），要么需要精确的**解析动力学模型**来计算得分函数（如 MBD）。
*   **研究动机**：在许多现实场景中，获取精确的动力学模型（如软体机器人、复杂轮胎建模）非常困难，且重新训练模型耗时耗力。本文旨在提出一种**无需训练（Training-free）且无需模型（Model-free）**的规划框架，直接利用已有的轨迹数据实现高质量的规划。

### 2. 方法论
*   **核心思想**：将扩散去噪过程中的“得分估计”转化为一个基于核函数的非参数回归问题。通过从轨迹库中检索相似行为，利用 **Nadaraya-Watson 估计器**直接合成去噪后的轨迹。
*   **关键技术细节**：
    *   **三重核加权方案**：计算轨迹库中每条参考轨迹的权重，权重由三部分组成：
        1.  **扩散核（Diffusion Kernel）**：衡量当前噪声轨迹与参考控制序列的相似度。
        2.  **上下文核（Context Kernel）**：衡量当前初始状态与参考轨迹起始点的匹配度。
        3.  **目标核（Goal Kernel）**：衡量参考轨迹终点与全局目标的接近程度。
    *   **多尺度回归**：扩散过程的噪声调度（Noise Schedule）自然充当了核带宽控制器。高噪声时，核函数较宽，捕捉全局行为模式；低噪声时，核函数变窄，进行细粒度的局部非线性插值。
    *   **多样本屏蔽选择**：从核加权分布中抽取 $K$ 个候选样本，应用安全屏蔽（Safety Shield）处理约束冲突，最后通过奖励加权的 Softmax 选择最优轨迹。
*   **理论支撑**：证明了该估计器在任意连续动力学下的点对点一致性，并揭示了其在 LTI（线性时不变）系统下等价于正则化的 DeePC（数据驱动预测控制）。

### 3. 实验设计
*   **实验场景**：复杂的机器人泊车任务，要求在充满障碍物的空间内完成精确停放。
*   **实验对象**：四个复杂度递增的动力学系统：
    1.  **Bicycle (3D)**：自行车模型。
    2.  **TT2D (4D)**：带单挂车的拖车模型。
    3.  **NTrailer (5D)**：多挂车模型。
    4.  **AccTT2D (6D)**：考虑加速度状态的高维拖车模型。
*   **对比基准（Benchmarks）**：
    *   **MBD (Model-Based Diffusion)**：基于解析动力学模型的扩散规划（性能上限）。
    *   **NN (Nearest-Neighbor)**：简单的最近邻轨迹检索（无扩散过程，性能下限）。
    *   **BSD-fix / BSD-adaptive**：本文提出的方法，分别采用固定带宽和自适应带宽。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 4080 (12 GB)** GPU 的工作站上运行。
*   **训练时长**：由于该方法是 **Training-free** 的，因此**无需训练时间**。
*   **推理耗时**：单次规划时间在 2.5 到 6.9 秒之间（取决于系统维度），比基于模型的方法慢约 1.5-3.8 倍，主要开销在于大规模样本的核权重计算。

### 5. 实验数量与充分性
*   **实验规模**：每个系统、每种方法均进行了 **50 次独立试验**，并使用了共享随机种子以确保公平对比。
*   **统计严谨性**：报告了均值、标准差，并利用 10,000 次重采样计算了 **95% 置信区间**。
*   **消融实验**：对比了扩散机制与单纯检索的区别，以及带宽调度策略对性能的影响。
*   **充分性评价**：实验覆盖了从低维到高维的非线性系统，对比基准设置合理，数据统计充分，能够客观反映算法在数据稀疏情况下的鲁棒性。

### 6. 主要结论与发现
*   **性能接近上限**：在仅使用 1,000 条参考轨迹的情况下，BSD-fix 达到了基于模型方法（MBD）**98.5%** 的平均奖励。
*   **扩散机制的必要性**：BSD 显著优于最近邻检索（NN），性能提升达 18-63%，证明了迭代去噪在非线性插值中的关键作用。
*   **带宽策略**：发现固定带宽配合多样本选择优于自适应带宽，因为后者在去噪后期会因带宽过窄而导致方差激增。
*   **维度扩展性**：随着系统维度增加，BSD 与 NN 的差距拉大，显示出 BSD 在处理复杂动力学时的优势。

### 7. 优点
*   **无需模型与训练**：极大地降低了扩散规划器的使用门槛，适用于动力学未知或难以建模的系统。
*   **安全性保障**：继承了安全屏蔽机制，能够直接在核估计的状态空间上强制执行几何约束。
*   **理论完备**：建立了非参数统计学习与经典控制理论（DeePC）之间的形式化联系。

### 8. 不足与局限
*   **计算开销**：由于每一步都需要遍历轨迹库计算核权重，实时性弱于基于模型或预训练网络的方法。
*   **数据依赖**：性能高度依赖于轨迹库的覆盖范围。在数据极度稀疏的边界区域，性能会出现下降（如 6D 系统中的表现）。
*   **仿真局限**：目前仅在仿真环境中验证，尚未在具有传感器噪声和非平稳动力学的真实硬件上测试。
*   **基准缺失**：未直接与非线性 DeePC 变体进行实验对比。

（完）
