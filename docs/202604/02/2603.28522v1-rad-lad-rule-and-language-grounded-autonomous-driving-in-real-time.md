---
title: "RAD-LAD: Rule and Language Grounded Autonomous Driving in Real-Time"
title_zh: RAD-LAD：基于规则与语言落地的实时自动驾驶
authors: "Anurag Ghosh, Srinivasa Narasimhan, Manmohan Chandraker, Francesco Pittaluga"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28522v1"
tags: ["query:tp"]
score: 8.0
evidence: 自动驾驶的实时运动规划
tldr: 本研究提出了RAD-LAD系统，旨在解决自动驾驶中实时性与可解释性的平衡问题。LAD是一个实时语言-动作规划器，支持快速运动规划及文本推理，延迟比前代模型低3倍；RAD是改进的规则规划器，克服了PDM-Closed的局限。两者结合形成的混合系统在nuPlan等基准测试中达到SOTA水平，兼具规则的可靠性与语言的自适应解释能力。
selection_source: fresh_fetch
motivation: 现有的自动驾驶语言模型推理速度慢且规则规划器灵活性不足，难以在复杂场景下兼顾实时性、可靠性与可解释性。
method: 提出实时语言规划器LAD和改进型规则规划器RAD，并通过混合架构将两者的逻辑推理与运动控制能力相结合。
result: LAD在nuPlan Test14-Hard等测试中刷新了学习型模型的SOTA，且RAD在规则型规划器中表现最优。
conclusion: 规则与语言的结合为自动驾驶提供了互补优势，实现了兼具高性能、低延迟和高可解释性的实时规划系统。
---

## 摘要
我们提出了 LAD，这是一种具有可中断架构的实时语言-动作规划器，它可以在单次前向传递中生成运动规划（约 20 Hz），或者在生成运动规划的同时产生文本推理（约 10 Hz）。LAD 的速度足以进行实时闭环部署，其延迟比之前的驾驶语言模型低约 3 倍，同时在 nuPlan Test14-Hard 和 InterPlan 上创下了基于学习的新最先进水平（SOTA）。我们还介绍了 RAD，这是一种旨在解决 PDM-Closed 结构局限性的基于规则的规划器。RAD 在 nuPlan Test14-Hard 和 InterPlan 上取得了基于规则的规划器中的最先进性能。最后，我们展示了结合 RAD 和 LAD 可以实现混合规划，从而融合两种方法的优势。该混合系统证明了规则和学习提供了互补的能力：规则支持可靠的操控，而语言则实现了自适应且可解释的决策。

## Abstract
We present LAD, a real-time language--action planner with an interruptible architecture that produces a motion plan in a single forward pass (~20 Hz) or generates textual reasoning alongside a motion plan (~10 Hz). LAD is fast enough for real-time closed-loop deployment, achieving ~3x lower latency than prior driving language models while setting a new learning-based state of the art on nuPlan Test14-Hard and InterPlan. We also introduce RAD, a rule-based planner designed to address structural limitations of PDM-Closed. RAD achieves state-of-the-art performance among rule-based planners on nuPlan Test14-Hard and InterPlan. Finally, we show that combining RAD and LAD enables hybrid planning that captures the strengths of both approaches. This hybrid system demonstrates that rules and learning provide complementary capabilities: rules support reliable maneuvering, while language enables adaptive and explainable decision-making.

---

## 论文详细总结（自动生成）

### 论文总结：RAD-LAD —— 基于规则与语言落地的实时自动驾驶规划

#### 1. 论文的核心问题与整体含义
自动驾驶规划目前面临两大挑战：**几何可行性**与**语义推理**。
*   **研究动机**：现有的规则规划器（如 PDM-Closed）在标准场景表现优异，但在“长尾”复杂场景（如车道被堵、多车博弈）中因拓扑结构固定、缺乏语义理解而失效。
*   **核心问题**：现有的基于大语言模型（LLM）的规划器虽然具备推理能力，但推理速度极慢（通常仅 2-3 Hz），无法满足实时闭环控制的需求。
*   **整体含义**：本文旨在开发一套既能像规则系统一样保证安全和实时性，又能像语言模型一样处理复杂语义博弈的混合规划系统。

#### 2. 论文提出的方法论
论文提出了两个互补的组件及一个混合框架：

*   **LAD (Language-Based Autonomous Driving)**：
    *   **核心思想**：一种“随时可中断”（Interruptible Anytime）的多模态语言模型规划器。
    *   **关键技术**：
        *   **场景编码**：将地图和代理（Agent）向量化，通过适配器（Adapter）投影到 LLM 的 Token 空间。
        *   **规划头（Plan Head）**：在 LLM 的输出端添加一个专门的 `<|plan|>` Token，通过分类方式从预定义的轨迹词汇表中选择最优轨迹。
        *   **可中断推理**：在算力紧张时，模型仅进行一次前向传递直接输出规划（~20Hz）；在算力允许时，先生成推理文本再输出规划（~10Hz）。
*   **RAD (Rule-Based Autonomous Driving)**：
    *   **核心思想**：改进 PDM-Closed 的结构局限。
    *   **关键技术**：
        *   **动态拓扑重规划**：每一步都重新搜索路径，而非仅在初始化时确定。
        *   **车道变更能力**：通过增强道路拓扑（加入相邻车道中心线）支持换道。
        *   **目标导向优化**：引入欧几里得距离目标成本，防止局部最优。
        *   **规则松弛**：在检测到死锁时，允许有条件地违反某些交通规则（如临时借道）。
*   **RAD-LAD 混合系统**：将 LAD 生成的轨迹作为 RAD 候选轨迹池的一部分，利用 RAD 的安全评分机制进行最终筛选。

#### 3. 实验设计
*   **数据集**：使用 **nuPlan** 模拟器（包含 100 万个真实驾驶场景）。
*   **Benchmark**：
    *   **nuPlan Test14-Hard**：专门针对 PDM-Closed 失败场景筛选的困难子集。
    *   **InterPlan**：测试多车交互和死锁处理的合成基准测试。
    *   **Val14**：标准驾驶场景测试。
*   **对比方法**：
    *   **规则型**：IDM, PDM-Closed。
    *   **学习型**：PlanTF, PLUTO, DiffusionPlanner, FlowPlanner。
    *   **语言型（延迟对比）**：DriveVLM, DriveGPT4。

#### 4. 资源与算力
*   **模型规模**：LAD 使用了 **Qwen3-0.6B** 作为语言模型骨干。
*   **硬件平台**：推理实验在单张 **NVIDIA RTX A6000** GPU 上完成。
*   **训练细节**：在 100 万个 nuPlan 场景上训练。论文提到了两阶段训练：阶段 A 训练适配器（冻结 LLM），阶段 B 进行 LoRA 微调。
*   **延迟表现**：LAD 无推理文本时延迟为 43ms (20Hz)，带推理文本（10 tokens）时为 102ms (10Hz)。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从标准场景到极端长尾场景的全面评估，使用了超过 100 万个场景进行训练。
*   **消融实验**：非常充分。分别对 LAD 的组件（对象数量、静态物体、语言监督等）和 RAD 的改进点（重规划、换道、目标优化、规则松弛）进行了详尽的消融测试。
*   **客观性**：通过与人类专家轨迹的差距分析（Human-gap analysis）来评估模型，并对比了不同下游控制器（LQR vs iLQR）对结果的影响，体现了实验的深度和公平性。

#### 6. 论文的主要结论与发现
*   **规则与语言互补**：规则（RAD）解决了大部分几何避障问题，而语言（LAD）在语义复杂的博弈场景中提供了关键的决策支持。
*   **实时性突破**：证明了语言模型规划器可以通过架构优化达到 10-20Hz 的实时频率，打破了“LLM 规划必慢”的固有印象。
*   **基准测试洞察**：发现许多所谓的“困难场景”其实只需改进规则规划器的拓扑搜索能力即可解决，而真正的语义长尾场景才需要 LLM。

#### 7. 优点
*   **架构创新**：提出的“可中断推理”架构非常实用，解决了自动驾驶中安全重规划与深度推理之间的矛盾。
*   **性能卓越**：在 Test14-Hard 和 InterPlan 上均达到了 SOTA 水平，且 RAD 显著提升了规则规划器的上限。
*   **可解释性**：LAD 能够生成与动作一致的文本推理，增强了系统的透明度。

#### 8. 不足与局限
*   **输入模态限制**：目前主要基于向量化输入（Vectorized input），尚未直接处理原始图像或点云数据（虽然论文提到适配器模式易于扩展）。
*   **模型规模较小**：为了实时性使用了 0.6B 的小模型，虽然在驾驶任务上足够，但在处理极其复杂的常识推理时可能弱于更大的模型。
*   **控制器耦合**：实验发现 nuPlan 默认的 LQR 控制器限制了规划器的表现，这表明评估结果仍受限于模拟器的控制层实现。

（完）
