---
title: "C-TRAIL: A Commonsense World Framework for Trajectory Planning in Autonomous Driving"
title_zh: C-TRAIL：一种用于自动驾驶轨迹规划的常识世界框架
authors: "Zhihong Cui, Haoran Tang, Tianyi Li, Yushuai Li, Peiyuan Guan, Amir Taherkordi, Tor Skeie"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29908v1"
tags: ["query:tp"]
score: 8.0
evidence: 利用大语言模型进行自动驾驶轨迹规划
tldr: 针对自动驾驶轨迹规划中大语言模型（LLM）常识推理不可靠的问题，本文提出了C-TRAIL框架。该框架通过构建“常识世界”，采用“召回-规划-更新”闭环机制，结合双重信任模型量化LLM输出的可靠性。通过将信任加权的常识注入蒙特卡洛树搜索（MCTS）并根据环境反馈动态优化，显著提升了复杂场景下的规划性能与安全性。
selection_source: fresh_fetch
motivation: 大语言模型在提供常识推理时存在固有的不可靠性，直接应用于安全关键的自动驾驶轨迹规划具有较高风险。
method: 提出一种闭环框架，利用双重信任机制评估LLM常识，并通过狄利克雷信任策略将其集成至蒙特卡洛树搜索中。
result: "实验表明，C-TRAIL在多个仿真和真实数据集上显著优于现有基准，平均降低了40.2%的ADE和51.7%的FDE，成功率提升16.9个百分点。"
conclusion: C-TRAIL通过信任机制有效耦合了LLM常识与传统规划算法，为提高自动驾驶系统的可靠性与规划性能提供了新方案。
---

## 摘要
自动驾驶轨迹规划日益利用大语言模型（LLMs）进行常识推理，但 LLM 的输出本质上具有不可靠性，在安全关键型应用中构成了风险。我们提出了 C-TRAIL，这是一个基于“常识世界”构建的框架，它将源自 LLM 的常识与信任机制相结合来指导轨迹规划。C-TRAIL 通过“召回（Recall）、规划（Plan）和更新（Update）”的闭环周期运行：召回模块向 LLM 查询语义关系，并通过双重信任机制量化其可靠性；规划模块通过狄利克雷信任策略将信任加权的常识注入蒙特卡洛树搜索（MCTS）；更新模块则根据环境反馈自适应地优化信任分数和策略参数。在 Highway-env 的四个模拟场景和两个真实世界 levelXData 数据集（highD, rounD）上的实验表明，C-TRAIL 持续优于最先进的基准方法，平均降低了 40.2% 的 ADE 和 51.7% 的 FDE，并将 SR 提高了 16.9 个百分点。源代码可在 https://github.com/ZhihongCui/CTRAIL 获取。

## Abstract
Trajectory planning for autonomous driving increasingly leverages large language models (LLMs) for commonsense reasoning, yet LLM outputs are inherently unreliable, posing risks in safety-critical applications. We propose C-TRAIL, a framework built on a Commonsense World that couples LLM-derived commonsense with a trust mechanism to guide trajectory planning. C-TRAIL operates through a closed-loop Recall, Plan, and Update cycle: the Recall module queries an LLM for semantic relations and quantifies their reliability via a dual-trust mechanism; the Plan module injects trust-weighted commonsense into Monte Carlo Tree Search (MCTS) through a Dirichlet trust policy; and the Update module adaptively refines trust scores and policy parameters from environmental feedback. Experiments on four simulated scenarios in Highway-env and two real-world levelXData datasets (highD, rounD) show that C-TRAIL consistently outperforms state-of-the-art baselines, reducing ADE by 40.2%, FDE by 51.7%, and improving SR by 16.9 percentage points on average. The source code is available at https://github.com/ZhihongCui/CTRAIL.

---

## 论文详细总结（自动生成）

以下是对论文《C-TRAIL: A Commonsense World Framework for Trajectory Planning in Autonomous Driving》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：虽然大语言模型（LLM）具备强大的常识推理能力，可以辅助自动驾驶进行决策，但其输出具有**不可靠性**（如幻觉、违反物理规律）。直接将 LLM 用于安全关键的轨迹规划存在巨大风险。
*   **研究动机**：受人类驾驶启发，人类在陌生环境能结合地图常识进行“脑内模拟”和评估。论文旨在构建一个“常识世界”（Commonsense World）框架，将 LLM 的常识推理与严谨的数学规划算法（MCTS）耦合，并通过信任机制动态调节对 LLM 的依赖程度。

### 2. 方法论：核心思想与技术细节
C-TRAIL 框架由“召回-规划-更新”三个闭环模块组成：
*   **信任感知常识召回（Recall）**：
    *   **语义推理**：将传感器数据转化为结构化 Prompt，查询 LLM 获取车辆间的语义关系（如“左前方”）和动作建议。
    *   **双重信任机制**：通过**常识信任**（多次查询的一致性熵值）和**运动学信任**（建议动作是否符合物理可行性）计算综合信任分数 $C_t$。
*   **信任引导规划（Plan）**：
    *   **狄利克雷信任策略**：将 LLM 的建议转化为狄利克雷分布的先验概率，信任分数 $C_t$ 决定了该先验的集中程度。
    *   **信任感知 MCTS**：在蒙特卡洛树搜索的 PUCT 公式中引入 $C_t$。当信任度高时，搜索受 LLM 引导；信任度低时，系统退化为纯探索模式，确保安全。
*   **信任校准更新（Update）**：
    *   利用环境反馈（新状态和奖励信号）在线修正信任分数。
    *   采用指数移动平均（EMA）平滑更新策略参数，确保规划的连续性和稳定性。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：使用 Highway-env，涵盖高速公路（Highway）、汇入（Merge）、环岛（Roundabout）和交叉路口（Intersection）四种场景。
    *   **真实数据集**：使用德国 levelXData 系列中的 highD（高速）和 rounD（环岛）无人机轨迹数据。
*   **对比基准（Baselines）**：
    *   传统方法：DQN、MCTS、GRAD。
    *   LLM 相关方法：LMTraj（微调类）、DiLu（推理类）、LangMPC、GPT-Driver。
*   **评估指标**：ADE（平均位移误差）、FDE（最终位移误差）、SR（成功率）和 RPA（关系预测准确率）。

### 4. 资源与算力
*   **硬件环境**：使用 NVIDIA TITAN RTX GPU (24GB 显存)，操作系统为 Ubuntu 22.04。
*   **模型配置**：主要基于 GPT-4o 和 GPT-3.5-turbo。
*   **训练细节**：学习率 $1 \times 10^{-4}$，Batch Size 为 64，训练 100 个 Epoch。
*   **推理耗时**：单步推理约 18 秒，其中 LLM API 调用占约 74%，MCTS 占 21%。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同场景（4 仿真 + 2 真实）下进行了测试，每个场景收集了 $10^5$ 步样本，并重复 5 次随机种子实验。
*   **实验维度**：
    *   **泛化性测试**：设置了“可见”和“未见（更高密度/更多车道）”环境。
    *   **消融实验**：分别验证了信任权重、狄利克雷策略和自适应更新的必要性。
    *   **安全性分析**：模拟了 LLM 错误注入和攻击性驾驶场景。
*   **评价**：实验设计非常充分，涵盖了从仿真到现实的迁移，且对比了多种不同类型的 SOTA 方法，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：C-TRAIL 在所有指标上均优于基准方法，平均降低 ADE 40.2%，降低 FDE 51.7%，成功率（SR）提升 16.9 个百分点。
*   **鲁棒性强**：在未见过的复杂交通分布下，C-TRAIL 的成功率下降极小（仅约 1.7%），而其他 LLM 方法下降显著。
*   **信任机制有效**：当 LLM 出现幻觉或错误建议时，信任分数会迅速下降，系统能自动转向基于物理规则的探索，避免碰撞。

### 7. 优点与亮点
*   **闭环设计**：首次提出了“召回-规划-更新”的闭环常识世界框架，解决了 LLM 在自动驾驶中“只说不做”或“乱做”的问题。
*   **双重校准**：结合了语义一致性和物理可行性，为 LLM 的黑盒输出提供了可量化的“安全阀”。
*   **即插即用**：框架不依赖于特定的 LLM 微调，具有良好的通用性和可扩展性。

### 8. 不足与局限
*   **实时性挑战**：单步推理耗时（约 18 秒）远超实时驾驶要求（通常需 <100ms），目前仅适用于高层战略规划，需异步运行。
*   **动作空间受限**：目前使用离散的元动作（Meta-actions），对于极度复杂的连续避障场景可能不够精细。
*   **依赖 API**：对 LLM API 的稳定性依赖较高，且推理成本可能较大。
*   **运动学模型简化**：实验主要基于运动学模型，未充分考虑复杂的动力学约束（如轮胎侧滑等）。

（完）
