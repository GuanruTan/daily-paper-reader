---
title: "World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems"
title_zh: 世界-价值-动作模型：视觉-语言-动作系统的隐式规划
authors: "Runze Li, Hongyin Zhang, Junxi Jin, Qixin Zeng, Zifeng Zhuang, Yiqi Tang, Shangke Lyu, Donglin Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14732v2"
tags: ["query:tp"]
score: 9.0
evidence: 视觉-语言-动作系统中的未来轨迹结构化潜变量表示
tldr: 本文提出WAV模型，旨在解决VLA系统在长程任务中缺乏推理能力的问题。该模型通过世界模型预测未来并结合价值函数评估，在结构化潜空间内实现隐式规划。相比直接动作预测，WAV通过潜空间推理重塑搜索分布，有效解决了可行轨迹概率随时间增加而指数衰减的问题，显著提升了复杂决策任务的成功率、泛化性与鲁棒性。
selection_source: fresh_fetch
motivation: 现有VLA模型多依赖直接动作预测，缺乏对长程轨迹的推理和后果评估能力。
method: 构建统一的WAV框架，利用世界模型和价值函数在结构化潜空间中进行隐式规划与动作生成。
result: 实验表明WAV在仿真和现实任务中均显著优于SOTA方法，尤其在长程和组合场景下表现优异。
conclusion: 在潜空间进行隐式规划能有效克服动作空间搜索的效率瓶颈，是增强具身智能体决策能力的有效方案。
---

## 摘要
视觉-语言-动作（VLA）模型已成为构建具身智能体的一种极具前景的范式，能够将感知和语言落地为动作。然而，大多数现有方法依赖于直接的动作预测，缺乏对长时程轨迹进行推理并评估其后果的能力，这限制了它们在复杂决策任务中的表现。在这项工作中，我们提出了世界-价值-动作（WAV）模型，这是一个在 VLA 系统中实现隐式规划的统一框架。WAV 模型并非进行显式的轨迹优化，而是学习一种以视觉观测和语言指令为条件的未来轨迹结构化潜表示。一个学习到的世界模型负责预测未来状态，而轨迹价值函数则评估其长时程效用。动作生成随后被表述为在该潜空间中的推理过程，模型逐渐将概率质量集中在高价值且动力学可行的轨迹上。我们提供了一个理论视角，表明直接在动作空间中进行规划会面临随着时程增加，可行轨迹概率呈指数级衰减的问题。相比之下，潜空间推理将搜索分布重塑向可行区域，从而实现了高效的长时程决策。大量的仿真和真实世界实验表明，WAV 模型始终优于现有最先进的方法，在任务成功率、泛化能力和鲁棒性方面取得了显著提升，特别是在长时程和组合场景中。代码可在 https://github.com/Win-commit/WAV 获取。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for building embodied agents that ground perception and language into action. However, most existing approaches rely on direct action prediction, lacking the ability to reason over long-horizon trajectories and evaluate their consequences, which limits performance in complex decision-making tasks. In this work, we introduce World-Value-Action (WAV) model, a unified framework that enables implicit planning in VLA systems. Rather than performing explicit trajectory optimization, WAV model learn a structured latent representation of future trajectories conditioned on visual observations and language instructions. A learned world model predicts future states, while a trajectory value function evaluates their long-horizon utility. Action generation is then formulated as inference in this latent space, where the model progressively concentrates probability mass on high-value and dynamically feasible trajectories. We provide a theoretical perspective showing that planning directly in action space suffers from an exponential decay in the probability of feasible trajectories as the horizon increases. In contrast, latent-space inference reshapes the search distribution toward feasible regions, enabling efficient long-horizon decision making. Extensive simulations and real-world experiments demonstrate that the WAV model consistently outperforms state-of-the-art methods, achieving significant improvements in task success rate, generalization ability, and robustness, especially in long-horizon and compositional scenarios. Code is available at https://github.com/Win-commit/WAV.

---

## 论文详细总结（自动生成）

以下是对论文《World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型大多采用“直接动作预测”的反应式范式，缺乏对长程（Long-horizon）轨迹的推理能力和对动作后果的评估机制。这导致模型在处理复杂、多步骤任务时容易出现误差累积，且难以在动作空间中高效搜索可行路径。
*   **核心目标**：提出一种名为 **WAV** 的统一框架，将“规划”从显式的优化模块转化为生成模型内部的“隐式推理过程”，通过联合建模环境动力学（世界模型）和轨迹效用（价值函数）来增强具身智能体的决策能力。

### 2. 方法论
*   **核心思想**：WAV 不直接在原始动作空间搜索，而是在结构化的潜空间（Latent Space）中进行迭代推理。它通过预测未来视觉演变并评估其价值，将概率质量集中在既符合物理规律又具有高任务回报的轨迹上。
*   **关键技术组件**：
    *   **语言条件视频生成模块（World Model）**：基于 Diffusion Transformer (DiT) 架构，根据当前观测和语言指令预测未来的视觉特征序列。
    *   **轨迹价值模块（Value Function）**：同样采用 DiT 结构，通过交叉注意力机制评估预测视频轨迹的累积折扣奖励（Return）。
    *   **动作解码模块（Action Module）**：将视频特征和价值特征融合，解码为具体的机器人执行动作。
*   **算法流程（迭代潜空间推理）**：
    1.  从初始高斯分布采样视频潜变量，生成候选未来视觉轨迹。
    2.  对每条轨迹采样价值潜变量，计算其预测价值的信噪比（SNR）作为评分。
    3.  筛选“精英样本”（Top-K），利用其均值和方差更新潜分布（类似 MPPI 算法）。
    4.  经过 $K$ 次迭代后，从优化后的分布中采样并解码出最终动作。
*   **训练目标**：采用**流匹配（Flow Matching）**技术，分三阶段训练视频生成、价值预测和动作解码。

### 3. 实验设计
*   **仿真场景**：使用 **LIBERO** 基准测试，涵盖 Spatial（空间推理）、Object（物体泛化）、Goal（目标变更）和 Long（长程组合）四个任务套件。
*   **现实场景**：在双臂机器人 Piper 上执行“整理碗筷”、“铺平毛巾”和“开抽屉放物”等复杂长程任务。
*   **对比方法**：
    *   **策略模型**：Diffusion Policy, Octo, OpenVLA, $\pi_0$, GR00T-N1 等。
    *   **世界模型驱动模型**：WorldVLA, DreamVLA, UniVLA, GE-ACT 等。

### 4. 资源与算力
*   **硬件配置**：使用了 8 张 **NVIDIA A100-SXM4-80GB** GPU。
*   **训练时长**：
    *   LIBERO 仿真实验：全参数微调约耗时 **5 天**。
    *   现实世界 Piper 机器人实验：每个任务的模型训练约耗时 **3 天**。
*   **软件框架**：基于 PyTorch，使用 Adam 优化器，包含 T5-XXL 语言编码器。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **仿真**：在 LIBERO 的 4 个子集上进行了全面测试，对比了超过 15 种 SOTA 模型。
    *   **消融实验**：详细探讨了迭代次数 $K$、采样数 $M/N$、平滑参数 $\alpha/\beta$ 以及精英选择比例对性能的影响。
    *   **现实验证**：每个任务进行 15 次独立实验，并提供了定性的失败案例分析。
*   **充分性评价**：实验设计非常充分且具有说服力。不仅涵盖了从 0.5B 到 8.5B 不同参数规模的基准，还通过理论分析（引理 4.1 和命题 4.2）解释了潜空间规划优于动作空间规划的根本原因（解决“可行性概率指数级衰减”问题）。

### 6. 主要结论与发现
*   **性能领先**：WAV 在 LIBERO 基准上取得了 **98.1%** 的平均成功率，刷新了 SOTA 纪录，尤其在长程任务（LIBERO-Long）中优势明显。
*   **规划的必要性**：消融实验显示，移除“潜轨迹规划”会导致性能显著下降（尤其在长程任务中下降约 2.6%），证明了隐式规划对缓解误差累积的关键作用。
*   **现实鲁棒性**：在真实世界中，WAV 的平均成功率（75.6%）远高于基线模型 GE-ACT（35.6%），表现出更强的空间感知和多步协调能力。

### 7. 优点
*   **理论与实践结合**：不仅提出了模型，还给出了严谨的数学证明，解释了为什么在潜空间做重塑（Reweighting）能克服高维动作空间的搜索难题。
*   **统一架构**：将预测、评估和执行整合在基于流匹配的 DiT 框架内，结构优雅且易于扩展。
*   **迭代优化机制**：引入 SNR 作为评估稳定性的指标，并通过迭代推理实现了比单次预测更精准的控制。

### 8. 不足与局限
*   **推理延迟**：由于采用了迭代采样（如 $K=3$ 次迭代，每次采样数十个样本），推理速度较慢（约 2.5s-15s 每次决策），难以满足高频实时控制需求。
*   **显存占用**：推理时的多样本并行计算对 GPU 显存要求较高（约 32GB 预留显存）。
*   **奖励函数依赖**：价值函数的训练依赖于预定义的密集奖励（Dense Reward），在缺乏真值奖励的开放域场景下，其泛化能力可能受限。

（完）
