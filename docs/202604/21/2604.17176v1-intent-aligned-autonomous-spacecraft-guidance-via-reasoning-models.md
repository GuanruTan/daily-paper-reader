---
title: Intent-aligned Autonomous Spacecraft Guidance via Reasoning Models
title_zh: 基于推理模型的意图对齐自主航天器制导
authors: "Yuji Takubo, Simone D'Amico"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17176v1"
tags: ["query:tp"]
score: 9.0
evidence: 航天器意图对齐的行为计划与轨迹优化
tldr: "针对未来航天器自主运行中难以理解高层任务意图且过度依赖专家建模的问题，本文提出一种意图对齐的引导框架。该框架通过行为序列和航路点约束作为中间抽象，将大语言模型的推理能力与安全轨迹优化相结合。实验表明，该方法在近距离操作场景中实现了超过90%的收敛率，且在意图满足度上比传统启发式方法高出1.5倍，为基础模型应用于安全关键的航天自主任务提供了可行路径。"
selection_source: fresh_fetch
motivation: 现有的航天器轨迹优化缺乏对高层任务意图的理解能力，且高度依赖专家手动设计的复杂公式。
method: 提出一种解耦框架，利用基础模型生成行为计划，再通过航路点生成模型将其转化为约束，最后由优化算法计算安全轨迹。
result: "在近距离操作实验中，该方法实现了超过90%的SCP算法收敛率，且意图优先性能指标比启发式决策高出1.5倍。"
conclusion: 引入中间行为抽象层是连接基础模型推理与安全关键型航天器自主控制的一种实用且有效的接口。
---

## 摘要
未来的航天器操作需要具备在保障安全的同时解释高层任务意图的自主能力。然而，现有的轨迹优化仍严重依赖专家设计的公式，且不支持受意图约束的决策。本文提出了一种意图对齐的航天器制导框架，通过基于行为序列和航路点约束的显式中间抽象，将高层推理与安全轨迹优化相连接。该框架首先利用基础模型预测意图对齐的行为计划，接着由航路点生成模型将其转换为航路点约束，最后通过优化计算出安全轨迹。这种分解方法在不牺牲安全性的前提下实现了可扩展的监督。在近距离操作场景下的数值实验表明，所提出的流程实现了超过 90% 的 SCP 收敛率，且在生成满足最高意图优先级性能标准的轨迹方面，其成功率比启发式决策高出 1.5 倍。这些结果支持将中间行为抽象作为基础模型推理与安全关键型航天器在轨自主系统之间的实用接口。

## Abstract
Future spacecraft operations require autonomy that can interpret high-level mission intent while preserving safety. However, existing trajectory optimization still relies heavily on expert-crafted formulations and does not support intent-conditioned decision-making. This paper proposes an intent-aligned spacecraft guidance framework that links high-level reasoning and safe trajectory optimization through explicit intermediate abstractions, based on behavior sequences and waypoint constraints. A foundation model first predicts an intent-aligned behavior plan, a waypoint generation model then converts it into waypoint constraints, and the safe trajectory is computed via optimization. This decomposition enables scalable supervision without sacrificing safety. Numerical experiments in close-proximity operation scenarios demonstrate that the proposed pipeline achieves over 90\% SCP convergence and yields a $1.5\times$ higher rate of generating trajectories that satisfy the top intent-prioritized performance criteria than heuristic decision-making. These results support the use of intermediate behavior abstraction as a practical interface between foundation-model reasoning and safety-critical onboard spacecraft autonomy.

---

## 论文详细总结（自动生成）

### 论文总结：基于推理模型的意图对齐自主航天器制导

#### 1. 核心问题与整体含义（研究动机和背景）
未来的航天任务（如在轨物流、卫星维修）要求航天器具备高度的自主性，能够理解人类操作员的高层任务意图（如“优先省油”或“优先观测”），同时必须严格保障飞行安全和动力学可行性。
目前存在两个主要痛点：
*   **专家依赖性强**：现有的轨迹优化方法（如序列凸规划 SCP）需要专家手动将任务意图转化为复杂的数学约束和目标函数。
*   **推理与执行脱节**：基础模型（FM/LLM）虽具备强大的逻辑推理能力，但难以直接生成符合物理规律和安全要求的控制指令。
本文旨在建立一个连接“高层语义推理”与“底层安全优化”的桥梁，实现意图驱动的自主制导。

#### 2. 方法论
论文提出了一种**三级解耦决策架构**，通过中间抽象层将意图转化为轨迹：

1.  **推理模型（Reasoning Model）**：基于微调的大语言模型（Qwen2.5-7B），输入任务背景和意图优先级（如：燃料 > 时间 > 观测），输出行为序列（Behavior Sequence）、总时长及推理过程。
2.  **航路点生成模型（Waypoint Generation Model）**：一个多层感知机（MLP），将抽象的行为序列转化为具体的几何航路点约束（位置和时间）。
3.  **轨迹优化模块（SCP Solver）**：利用序列凸规划（SCP）求解非凸优化问题，在满足航路点约束的同时，确保动力学可行性和被动安全性（避开禁飞区 KOZ）。

**关键技术细节：**
*   **中间抽象**：引入“行为原语”（如漂移、扩展/缩小 R/N 间距等）作为推理与几何约束之间的接口。
*   **阶段式引导策略（Staged Bootstrapping）**：先利用 SCP 生成的数据训练航路点模型；再利用已训练的航路点模型作为“黑盒模拟器”，通过 LLM 自动标注数据来微调推理模型。

#### 3. 实验设计
*   **实验场景**：近距离交会操作，包括三种典型任务：绕飞（Circumnavigation）、飞越（Flyby）和潜行（Ducking）。
*   **基准方法（Benchmark）**：
    *   **启发式决策（Heuristic）**：基于规则随机采样行为序列和航路点。
    *   **消融对比**：对比了未加权模仿学习、加权模仿学习（基于奖励函数）以及不同模块组合（启发式 vs. 神经网络）的性能。
*   **评估指标**：SCP 算法收敛率、燃料成本、观测质量、意图与推理的一致性、意图与最终轨迹的匹配度。

#### 4. 资源与算力
*   **模型规模**：使用了 **Qwen2.5-7B-Instruct** 作为基础推理模型。
*   **训练技术**：采用了 **LoRA（低秩适配）** 参数高效微调技术，并使用了 **4-bit 量化** 加载基础模型以降低显存占用。
*   **算力细节**：文中**未明确指出**具体的 GPU 型号、数量及训练总时长，但提到了训练数据集包含 50,000 个样本。

#### 5. 实验数量与充分性
*   **实验规模**：航路点模型训练使用了 50,000 个样本（90/10 比例拆分），端到端评估在 500 个独立测试场景下进行。
*   **充分性**：实验涵盖了多种任务类型和不同的意图优先级组合，并进行了统计学分析（均值、标准差、成功率）。
*   **客观性**：通过对比启发式方法和不同训练策略（加权 vs 不加权），验证了神经网络模块的增益。实验设计较为客观，通过自动化 LLM 标注减少了人工偏见。

#### 6. 主要结论与发现
*   **高收敛性**：所提出的框架在下游 SCP 求解中实现了超过 **90%** 的收敛率，显著高于启发式方法的 72.2%。
*   **意图对齐**：训练后的推理模型生成的行为序列 100% 可行。在满足高优先级意图指标方面，该框架比启发式决策高出 **1.5 倍**。
*   **语义一致性**：超过 50% 的推理轨迹完全匹配意图中的前两个优先级，所有轨迹至少匹配一个最高优先级。

#### 7. 优点
*   **架构解耦**：将逻辑推理与数值优化分离，既发挥了 LLM 的常识推理能力，又保留了传统优化算法的安全保证。
*   **可解释性**：通过“推理迹（Reasoning Trace）”和中间行为原语，操作员可以理解并干预决策过程，而非纯黑盒操作。
*   **数据驱动的闭环**：通过阶段式引导，将底层的物理性能反馈给高层推理模型，实现了跨层级的监督学习。

#### 8. 不足与局限
*   **场景覆盖有限**：实验仅限于近距离交会的特定几何场景，尚未验证在更复杂、非结构化环境下的泛化能力。
*   **原语依赖**：系统性能高度依赖于预定义的“行为原语”库，如果任务超出原语定义的范围，模型可能失效。
*   **实时性挑战**：虽然 SCP 求解很快，但 7B 规模的 LLM 在航天器载板计算资源上的实时推理能力仍存疑（文中未讨论板载部署延迟）。
*   **偏差风险**：推理模型的微调依赖于预训练 LLM（如 GPT-4o-mini）的自动标注，可能引入教师模型的固有偏差。

（完）
