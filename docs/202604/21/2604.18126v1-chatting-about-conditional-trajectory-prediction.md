---
title: Chatting about Conditional Trajectory Prediction
title_zh: 论条件轨迹预测
authors: "Yuxiang Zhao, Wei Huang, Haipeng Zeng, Huan Zhao, Yujie Song"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18126v1"
tags: ["query:tp"]
score: 10.0
evidence: 为轨迹预测建模复杂的社交互动和行为意图
tldr: 针对现有轨迹预测方法忽视自车运动且仅基于静态信息建模的局限，本文受“心理理论”启发，提出了一种跨时域意图交互的条件轨迹预测方法（CiT）。CiT通过联合分析不同时域的行为意图，实现信息的互补与集成，并能根据自车潜在运动生成周边智能体的预测轨迹。实验证明，CiT在基准测试中达到了SOTA性能，能有效提升机器人路径规划的安全性。
selection_source: fresh_fetch
motivation: 现有轨迹预测方法大多未考虑自车运动对周围智能体的影响，且缺乏对复杂社交互动的动态建模。
method: 提出CiT方法，通过跨时域意图交互机制修正意图表示，并结合自车运动规划生成条件预测结果。
result: 在多个基准测试中，CiT显著优于现有方法，实现了最先进的预测性能。
conclusion: CiT通过模拟人类的心理理论，实现了更精准的交互式轨迹预测，为机器人安全避障提供了有力支持。
---

## 摘要
人类行为具有相互依赖的本质，这要求人机交互系统通过建模复杂的社交互动来预测周围智能体的轨迹，从而避免碰撞并执行安全路径规划。虽然现有的轨迹预测方法很多，但大多数方法并未纳入自我智能体（ego agent）自身的运动，且仅基于静态信息对交互进行建模。受人类在轨迹选择过程中的心理理论（theory of mind）启发，我们提出了一种用于条件轨迹预测的跨时域意图交互方法（CiT）。我们提出的 CiT 对随时间变化的行为意图进行联合分析，并实现了跨不同时域的信息互补与整合。自身时域内的意图可以通过来自另一时域的社交互动信息进行修正，从而获得更精确的意图表示。此外，CiT 旨在与机器人运动规划和控制模块紧密集成，能够根据自我智能体的潜在运动，为所有周围智能体生成一组可选的轨迹预测结果。大量实验表明，所提出的 CiT 显著优于现有方法，在基准测试中达到了最先进的性能。

## Abstract
Human behavior has the nature of mutual dependencies, which requires human-robot interactive systems to predict surrounding agents trajectories by modeling complex social interactions, avoiding collisions and executing safe path planning. While there exist many trajectory prediction methods, most of them do not incorporate the own motion of the ego agent and only model interactions based on static information. We are inspired by the humans theory of mind during trajectory selection and propose a Cross time domain intention-interactive method for conditional Trajectory prediction(CiT). Our proposed CiT conducts joint analysis of behavior intentions over time, and achieves information complementarity and integration across different time domains. The intention in its own time domain can be corrected by the social interaction information from the other time domain to obtain a more precise intention representation. In addition, CiT is designed to closely integrate with robotic motion planning and control modules, capable of generating a set of optional trajectory prediction results for all surrounding agents based on potential motions of the ego agent. Extensive experiments demonstrate that the proposed CiT significantly outperforms the existing methods, achieving state-of-the-art performance in the benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Chatting about Conditional Trajectory Prediction》（CiT）的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在人机交互场景中，结合**自车（Ego Agent）的未来运动意图**，更精准地预测周围智能体的动态轨迹。
*   **研究背景**：
    *   **现有局限**：多数预测模型忽视了自车运动对他人的影响，且往往基于静态信息建模，缺乏对社交互动随时间动态演变的考量。
    *   **理论启发**：受人类“心理理论”（Theory of Mind）启发，即人类在导航时会推断他人的心理状态和意图。
    *   **实际需求**：机器人系统需要根据不同的候选路径方案，评估周围智能体的反馈，以选择最优且安全的执行轨迹。

### 2. 方法论
CiT（Cross time domain intention-interactive method）的核心思想是通过**跨时域意图交互**来修正和增强特征表示。
*   **核心流程**：
    1.  **意图图构建（Intention Graphs Construction）**：利用1D-CNN和LSTM编码轨迹。构建两个社交张量：**当前时域意图图**（基于目标与邻居的历史）和**未来时域意图图**（基于目标与自车的未来粗略轨迹）。
    2.  **跨时域交互（Interaction Cross Domain, ICD）**：借鉴Transformer的Query-Key-Value机制，让两个时域的意图图相互“对话”。通过跨时域的信息互补，修正单一时域可能存在的粗糙或片面意图表示。
    3.  **意图影响评估（Intention Influence Evaluation, IIE）**：通过全连接层和Softmax学习不同时域社交信息对目标智能体的权重（$\beta_1, \beta_2$），实现对交互行为的优先级建模。
    4.  **意图融合解码（Intention Fusion Decoder）**：将融合后的意图特征映射回以自车为中心的坐标系，结合预定义的6种机动类别（如换道、刹车等），通过LSTM解码生成多模态的概率轨迹（双变量高斯分布）。

### 3. 实验设计
*   **数据集**：使用了两个真实世界的公开数据集：**NGSIM** 和 **HighD**（均为高速公路场景）。
*   **实验设置**：利用过去3秒（15帧）的轨迹预测未来5秒（25帧）的轨迹。
*   **Benchmark与对比方法**：
    *   对比了包括 S-LSTM, C-LSTM, PiP, WSiP, BAT, C2F-TP 等在内的十余种先进（SOTA）方法。
    *   **评价指标**：均方根误差（RMSE）和负对数似然（NLL）。
*   **条件输入**：在测试阶段，自车的未来轨迹被降采样至1Hz（仅5帧），以验证模型对“粗略意图”输入的鲁棒性。

### 4. 资源与算力
*   **硬件环境**：论文明确指出实验在 **NVIDIA RTX 4090 (24GB)** 上完成。
*   **模型细节**：输入维度从2维升至32维，编码器LSTM状态维度为64，解码器为128。
*   **训练时长**：文中未明确提及具体的训练总时长，但给出了详细的超参数设置（如Leaky-ReLU的$\alpha=0.1$）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型数据集上进行了全量对比。
    *   **消融实验**：设计了6组变体（Variant 1-5 及 Ours），分别验证了社交信息、自车运动输入、ICD模块、IIE模块以及FCN融合结构的有效性。
*   **客观性评价**：实验设计较为充分。通过对比自注意力机制（Variant 4）与ICD模块（Variant 5），证明了性能提升源于跨时域意图修正而非单纯增加参数量。同时，使用降采样的自车轨迹进行测试，增强了实验在实际机器人系统应用中的说服力。

### 6. 主要结论与发现
*   **性能领先**：CiT在NGSIM和HighD数据集上的平均RMSE和NLL均达到最优。在NGSIM上，RMSE和NLL分别提升了4.02%和5.18%。
*   **意图修正能力**：可视化结果显示，CiT能纠正前序方法（如PiP）对目标动机的误判（例如在换道后的直行预测上更准确）。
*   **鲁棒性**：模型仅需自车的“粗略意图”即可实现高质量预测，这降低了对下游规划模块精度的依赖。

### 7. 优点
*   **紧密集成机器人系统**：设计了条件预测接口，能直接服务于下游的运动规划和决策任务。
*   **动态建模**：通过跨时域交互捕捉了意图随时间变化的动态特性，比静态社交池化更精细。
*   **多模态覆盖**：能够同时生成多种机动模式下的轨迹，并给出概率分布，符合人类行为的不确定性。

### 8. 不足与局限
*   **场景局限性**：实验主要集中在高速公路场景（NGSIM/HighD），对于交通极其复杂的城市路口（如非结构化道路、人车混行）的泛化能力尚待验证。
*   **自车依赖**：虽然模型对自车轨迹精度要求不高，但仍需预知自车的粗略意图。在完全被动观察（无自车控制权）的场景下，该模型的优势可能无法完全发挥。
*   **计算开销**：引入跨时域交互和多模块融合虽然提升了精度，但在嵌入式机器人平台上实时运行的推理延迟（Latency）数据未在文中详细展示。

（完）
