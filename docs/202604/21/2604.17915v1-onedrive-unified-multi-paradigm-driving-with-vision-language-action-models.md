---
title: "OneDrive: Unified Multi-Paradigm Driving with Vision-Language-Action Models"
title_zh: OneDrive：基于视觉-语言-动作模型的统一多范式驾驶
authors: "Yiwei Zhang, Xuesong Chen, Jin Gao, Hanshi Wang, Fudong Ge, Weiming Hu, Shaoshuai Shi, Zhipeng Zhang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17915v1"
tags: ["query:tp"]
score: 9.0
evidence: 使用视觉语言模型的统一驾驶框架及轨迹回归
tldr: "本研究提出OneDrive，一个基于预训练视觉语言模型（VLM）的统一自动驾驶框架。针对现有系统因多任务需求导致架构碎片化的问题，OneDrive将文本生成、目标检测和轨迹规划等异构解码行为整合进单一Transformer解码器中。通过在因果解码器中组织视觉和结构化查询令牌，实现了跨任务的稳定联合优化。该模型在nuScenes和NAVSIM基准测试中均达到SOTA性能，且推理延迟降低了约40%，展示了VLM在复杂驾驶任务中的强大迁移能力。"
selection_source: fresh_fetch
motivation: 解决现有自动驾驶系统因处理异构任务（如文本生成与轨迹回归）而导致的架构碎片化和骨干网络复用受限的问题。
method: 在单一因果Transformer解码器中统一组织视觉、文本和结构化查询令牌，使轨迹规划与感知任务共享预训练的注意力机制。
result: "在nuScenes开环评估中实现0.28 L2误差和0.18碰撞率，并在NAVSIM闭环评估中取得领先成绩，同时推理延迟降低40%。"
conclusion: OneDrive证明了预训练VLM的注意力机制在纯语言建模之外具有极强的迁移性，能有效统一多范式自动驾驶任务。
---

## 摘要
视觉语言模型（VLM）擅长自回归文本生成，但端到端自动驾驶需要具有结构化输出和异构解码行为的多任务学习，例如自回归语言生成、并行目标检测和轨迹回归。为了适应这些差异，现有系统通常引入分离或级联的解码器，导致架构碎片化且骨干网络复用受限。在这项工作中，我们提出了一种基于预训练 VLM 构建的统一自动驾驶框架，其中异构解码行为在单个 Transformer 解码器中得到了统一。我们证明了预训练 VLM 的注意力机制在纯语言建模之外表现出强大的可迁移性。通过在单个因果解码器中组织视觉和结构化查询 token，结构化查询可以通过原始注意力机制自然地以视觉上下文为条件。文本和结构化输出共享通用的注意力骨干网络，从而实现了跨异构任务的稳定联合优化。通过引入结构化轨迹查询，在同一个因果大语言模型（LLM）解码器中实现了轨迹规划。这种统一的表述使得规划能够与图像和感知 token 共享预训练的注意力骨干网络。在端到端自动驾驶基准测试上的大量实验证明了其最先进的性能，包括在 nuScenes 开环评估中获得 0.28 的 L2 误差和 0.18 的碰撞率，以及在 NAVSIM 闭环评估中获得具有竞争力的结果（86.8 PDMS）。完整模型保留了多模态生成能力，而高效推理模式则实现了约 40% 的延迟降低。代码和模型可在 https://github.com/Z1zyw/OneDrive 获取。

## Abstract
Vision-Language Models(VLMs) excel at autoregressive text generation, yet end-to-end autonomous driving requires multi-task learning with structured outputs and heterogeneous decoding behaviors, such as autoregressive language generation, parallel object detection and trajectory regression. To accommodate these differences, existing systems typically introduce separate or cascaded decoders, resulting in architectural fragmentation and limited backbone reuse. In this work, we present a unified autonomous driving framework built upon a pretrained VLM, where heterogeneous decoding behaviors are reconciled within a single transformer decoder. We demonstrate that pretrained VLM attention exhibits strong transferability beyond pure language modeling. By organizing visual and structured query tokens within a single causal decoder, structured queries can naturally condition on visual context through the original attention mechanism. Textual and structured outputs share a common attention backbone, enabling stable joint optimization across heterogeneous tasks. Trajectory planning is realized within the same causal LLM decoder by introducing structured trajectory queries. This unified formulation enables planning to share the pretrained attention backbone with images and perception tokens. Extensive experiments on end-to-end autonomous driving benchmarks demonstrate state-of-the-art performance, including 0.28 L2 and 0.18 collision rate on nuScenes open-loop evaluation and competitive results (86.8 PDMS) on NAVSIM closed-loop evaluation. The full model preserves multi-modal generation capability, while an efficient inference mode achieves approximately 40% lower latency. Code and models are available at https://github.com/Z1zyw/OneDrive

---

## 论文详细总结（自动生成）

这篇论文介绍了 **OneDrive**，一个旨在统一自动驾驶中异构任务（感知、规划、语言生成）的视觉-语言-动作（VLA）模型框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：视觉语言模型（VLM）在多模态推理上表现卓越，但将其应用于自动驾驶面临挑战。自动驾驶需要处理**异构输出**：文本生成是自回归的，而目标检测和轨迹规划通常是并行的结构化输出。
*   **核心痛点**：现有系统多采用分离或级联的解码器（如 Q-Former 结构），导致架构碎片化、骨干网络复用率低，且难以利用大规模通用领域数据进行联合训练。
*   **核心问题**：能否在不改变 VLM 原生架构的前提下，用单个预训练模型同时处理文本生成、并行感知和轨迹规划？

### 2. 方法论
*   **核心思想**：利用预训练 VLM 的**因果注意力机制（Causal Attention）**作为统一的条件建模接口，将所有任务转化为统一的 Token 序列处理。
*   **关键技术细节**：
    *   **统一 Token 表示**：将图像 Token ($X_{img}$)、检测查询 ($Q_{det}$)、车道线查询 ($Q_{lane}$)、规划查询 ($Q_{plan}$) 和文本 Token ($X_{text}$) 拼接成一个序列。
    *   **混合解码器层（Mixed Decoder Layers）**：
        *   **共享注意力**：保留预训练 LLM 的因果注意力，使查询 Token 能自然地以视觉上下文为条件。
        *   **任务特定适配**：在浅层引入感知查询间的自注意力以支持并行推理，并为不同任务替换特定的前馈网络（FFN），因为实验发现预训练的文本 FFN 难以迁移到驾驶任务。
    *   **三阶段训练策略**：
        1.  **感知-语言预训练**：冻结 ViT，训练混合解码器的感知和文本分支。
        2.  **规划适配**：引入规划 Token，优化规划相关的 FFN 和头部。
        3.  **联合微调**：全模型（含 ViT）在感知、规划和文本目标下共同优化。

### 3. 实验设计
*   **数据集与场景**：
    *   **nuScenes**：用于开环评估（1000 个城场景），关注 3D 检测、车道线和轨迹精度。
    *   **NAVSIM**：用于闭环评估，侧重于安全性和交互性。
*   **Benchmark 与指标**：
    *   开环：L2 误差、碰撞率（Collision Rate）。
    *   闭环：PDMS 综合得分（包含安全性、规则遵守等）。
*   **对比方法**：
    *   文本驱动模型：DriveVLM, OmniDrive, EMMA, SOLVE-VLM。
    *   动作驱动模型：UniAD, VAD, ColaVLA 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **64 张 NVIDIA H20 GPU**。
*   **训练细节**：
    *   nuScenes 阶段：每个阶段训练 20 个 epoch，学习率 $1 \times 10^{-4}$，Batch Size 为 64。
    *   NAVSIM 阶段：Batch Size 为 128。
*   **推理效率**：在单张 H20 上测试，OneDrive 的延迟比级联架构（如 ReCogDrive）降低了约 **40%**（264ms vs 156ms）。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流自动驾驶基准上进行了全面测试，并进行了多项消融实验。
*   **消融实验内容**：
    *   预训练权重迁移性分析（Attention vs FFN）。
    *   文本监督对感知/规划的影响（证明了文本损失具有正则化作用）。
    *   规划损失权重 ($\lambda_{plan}$) 的敏感度分析。
    *   Token 序列顺序的影响（证明“检测 -> 车道 -> 规划”顺序最优）。
*   **评价**：实验设计客观、公平，通过对比相同骨干网络下的不同架构，充分验证了统一解码器的有效性。

### 6. 主要结论与发现
*   **注意力机制的迁移性**：预训练 VLM 的注意力模块在处理非语言任务（如查询-视觉对齐）时具有极强的迁移能力，而 FFN 则高度任务特定。
*   **性能领先**：在 nuScenes 开环测试中达到 SOTA（0.28 L2 误差，0.18% 碰撞率）；在 NAVSIM 闭环测试中取得 86.8 PDMS 的高分。
*   **效率提升**：统一架构避免了复杂的级联逻辑，支持 FlashAttention 等标准加速技术，显著降低了推理延迟。

### 7. 优点
*   **架构优雅**：消除了感知和规划之间的结构壁垒，实现了真正的单解码器多任务统一。
*   **硬件友好**：不依赖自定义注意力掩码，能充分利用现有的 Transformer 加速算子。
*   **性能与通用性兼顾**：在提升驾驶性能的同时，保留了 VLM 原有的多模态对话和推理能力。

### 8. 不足与局限
*   **感知精度瓶颈**：受限于 VLM 视觉骨干网络的 Token 分辨率（下采样较多），其纯视觉 3D 检测性能虽优于同类 VLM 方案，但仍低于专门优化的感知模型。
*   **预训练目标偏差**：InternVL 等模型的语言中心化预训练目标并非为检测任务设计，可能限制了视觉特征的表达。
*   **扩展性验证**：目前主要在 1B-2B 规模模型上验证，更大规模参数下的表现及长尾场景的鲁棒性仍需进一步观察。

（完）
