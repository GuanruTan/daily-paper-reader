---
title: "EdgeVTP: Exploration of Latency-efficient Trajectory Prediction for Edge-based Embedded Vision Applications"
title_zh: EdgeVTP：针对边缘嵌入式视觉应用的低延迟轨迹预测探索
authors: "Seungjin Kim, Reza Jafarpourmarzouni, Christopher Neff, Hamed Tabkhi, Vinit Katariya"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16783v1"
tags: ["query:tp"]
score: 10.0
evidence: 高速公路及拥挤场景下的车辆轨迹预测
tldr: EdgeVTP是专为路侧边缘设备设计的车辆轨迹预测模型，旨在满足嵌入式部署对低延迟和确定性运行时间的要求。它结合了交互感知图建模、轻量级Transformer骨干网络和单次曲线解码器，通过预测紧凑的曲线参数而非传统的自回归航点，显著降低了计算开销并保证了轨迹平滑性。在Jetson平台上的测试显示，该模型在保持高精度的同时，实现了最低的端到端延迟。
selection_source: fresh_fetch
motivation: 针对路侧边缘设备在处理拥挤交通场景时，现有轨迹预测模型面临的端到端延迟高且运行时间不确定的挑战。
method: 结合了带邻居上限的局部图建模、轻量级Transformer骨干网络以及一种基于单次预测的曲线参数解码器。
result: 在三个高速公路基准测试和两个Jetson平台上，EdgeVTP实现了最低的端到端延迟，并在其中两个数据集上达到了SOTA精度。
conclusion: EdgeVTP证明了通过优化交互复杂度和解码方式，可以在嵌入式边缘端实现兼顾实时性与高精度的轨迹预测。
---

## 摘要
车辆轨迹预测是高速公路感知的核心，但在路侧边缘设备上的部署需要有界且确定性的端到端延迟。我们提出了 EdgeVTP，这是一种嵌入式优先的轨迹预测器，它结合了交互感知图建模、轻量级 Transformer 骨干网络和单次曲线解码器。通过将未来运动预测为紧凑的曲线参数（锚定在最后观测位置），而非随预测时长缩放的自回归路径点，EdgeVTP 在生成平滑轨迹的同时降低了解码开销。为了在拥挤场景中保持运行时间的可预测性，我们通过具有硬性邻居上限的局部图显式地限制了交互复杂度。在三个高速公路基准测试和两个 Jetson 级平台上，EdgeVTP 在包含图构建和后处理的测试流程下实现了最低的实测端到端延迟，同时在其中两个数据集上达到了最先进（SotA）的预测精度，并在其他基准测试中取得了具有竞争力的误差表现。我们的代码可在 https://github.com/SeungjinStevenKim/EdgeVTP 获取。

## Abstract
Vehicle trajectory prediction is central to highway perception, but deployment on roadside edge devices necessitates bounded, deterministic end-to-end latency. We present EdgeVTP, an embedded-first trajectory predictor that combines interaction-aware graph modeling with a lightweight transformer backbone and a one-shot curve decoder. By predicting future motion as compact curve parameters (anchored at the last observed position) rather than horizon-scaled autoregressive waypoints, EdgeVTP reduces decoding overhead while producing smooth trajectories. To keep runtime predictable in crowded scenes, we explicitly bound interaction complexity via a locality graph with a hard neighbor cap. Across three highway benchmarks and two Jetson-class platforms, EdgeVTP achieves the lowest measured end-to-end latency under a protocol that includes graph construction and post-processing, while attaining state-of-the-art (SotA) prediction accuracy on two of the three datasets and competitive error on other benchmarks. Our code is available at https://github.com/SeungjinStevenKim/EdgeVTP.

---

## 论文详细总结（自动生成）

以下是对论文《EdgeVTP: Exploration of Latency-efficient Trajectory Prediction for Edge-based Embedded Vision Applications》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的车辆轨迹预测（VTP）模型虽然在精度上取得了长足进步，但在部署于路侧边缘设备（如 NVIDIA Jetson）时面临两大瓶颈：
    1.  **场景依赖的延迟波动**：在拥挤交通中，交互图（Interaction Graph）的规模随车辆增加而爆炸式增长，导致端到端延迟不可控。
    2.  **自回归解码开销**：传统的逐步预测方式（Autoregressive）计算量随预测时长线性增加，且容易产生累积误差。
*   **研究背景**：路侧相机网络需要实时、确定性的推理能力来支持交通安全监控，而边缘设备算力、内存和功耗预算极其有限。

### 2. 方法论
EdgeVTP 采用了“嵌入式优先”的设计理念，核心思想是通过显式限制计算复杂度和非自回归解码来优化延迟。
*   **边界交互构建（Edge Builder）**：
    *   **半径过滤（Radius Gating）**：仅考虑距离目标车辆 $r$ 米内的邻居。
    *   **硬性邻居上限（Top-K Cap）**：在半径范围内仅保留最近的 $K$ 个邻居。这确保了无论场景多拥挤，图消息传递的计算量都有上限。
*   **轻量级编码器**：
    *   使用简单的全连接层（FC）进行时间特征投影。
    *   采用图同构网络（GIN）进行空间交互建模。
    *   **残差分离路径**：可选地对绝对位置和位移流进行独立建模，并通过可学习的标量权重（$\alpha, \beta$）进行融合。
*   **单次贝塞尔曲线解码（One-shot Bezier Decoding）**：
    *   **核心技术**：不预测未来的每一个坐标点，而是利用 Transformer 解码器输出的特征，通过一个线性头直接预测 4 个控制点的偏移量。
    *   **公式原理**：结合起始锚点，构建一个 4 阶贝塞尔曲线，通过解析求值一次性生成整个预测时界的平滑轨迹。这彻底消除了循环解码的开销。

### 3. 实验设计
*   **数据集与场景**：
    *   **NGSIM**：经典的高速公路密集交互数据集（US-101, I-80）。
    *   **CHD**：包含“平视（Eye-level）”和“高角度（High-angle）”两种视角的监控场景数据集，用于测试模型对透视畸变和跟踪噪声的鲁棒性。
*   **基准测试（Benchmark）**：
    *   对比了 S-STGCNN, GRIP++, Pishgu, VT-Former (轻量/中等/标准版), STA-LSTM, DeepTrack 等模型。
*   **评估指标**：ADE（平均位移误差）、FDE（最终位移误差）、1-5秒的 RMSE，以及最重要的**端到端（E2E）延迟**。

### 4. 资源与算力
*   **训练设备**：使用 **NVIDIA H100 GPU** 进行模型训练。
*   **训练细节**：训练 80 个 epoch，Adam 优化器，初始学习率 $1 \times 10^{-2}$，Batch Size 为 16。
*   **推理测试平台**：
    *   **Jetson Nano**（5W/10W 模式）。
    *   **Jetson Xavier NX**（10W/15W/20W 模式，涉及 2核/4核/6核配置）。
    *   **NVIDIA H100**（用于对比受控环境下的相对计算缩放）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的消融实验（共 17 组主要配置），涵盖了不同的交互半径 $r$、邻居上限 $K$、是否使用残差分支以及 Transformer 层数/头数。
*   **充分性与客观性**：
    *   实验不仅报告了模型前向传播时间，还首次强调并测量了包含“图构建”和“轨迹重建”在内的**完整端到端延迟**（占总延迟的 25-35%）。
    *   在三个不同特性的数据集上验证，证明了模型在不同视角下的泛化能力，实验设计客观且贴近实际应用。

### 6. 主要结论与发现
*   **延迟优势**：EdgeVTP 在 Jetson 平台上实现了最低的端到端延迟（如在 Xavier NX 上仅需约 11-19ms），远优于基于 Transformer 的同类模型。
*   **精度表现**：在 CHD 数据集的两个视角上均达到了 **SotA（最先进）** 精度；在 NGSIM 上保持了极具竞争力的误差水平，同时速度提升了数倍。
*   **关键发现**：在边缘设备上，延迟的瓶颈往往不在于参数量，而在于**场景相关的图构建成本**和**自回归解码的序列开销**。

### 7. 优点与亮点
*   **部署导向**：不同于单纯追求精度的论文，EdgeVTP 提供了可调节的“操作点”（Latency-focused, Balanced, Error-focused），方便开发者根据硬件预算选择。
*   **轨迹平滑性**：通过贝塞尔曲线参数化建模，天然保证了预测轨迹的物理平滑性，避免了逐点预测可能出现的抖动。
*   **确定性运行时间**：通过 Top-K 邻居上限，解决了边缘计算中最忌讳的“长尾延迟”问题。

### 8. 不足与局限
*   **应用限制**：目前主要针对结构化的高速公路场景，对于具有复杂拓扑结构（如十字路口、环岛）的城市道路，贝塞尔曲线可能难以捕捉极度复杂的转向意图。
*   **误差来源**：定性分析显示模型在处理大幅度转向或侧向偏移时仍存在一定误差，这可能受限于历史轨迹长度或缺乏地图拓扑信息的输入。
*   **视角依赖**：实验发现最佳操作点（$r, K$ 的选择）高度依赖于相机视角，这意味着在实际部署时可能需要针对特定相机位置进行微调。

（完）
