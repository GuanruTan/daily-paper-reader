---
title: "OneVL: One-Step Latent Reasoning and Planning with Vision-Language Explanation"
title_zh: OneVL：基于视觉-语言解释的一步式隐式推理与规划
authors: "Jinghui Lu, Jiayi Guan, Zhijian Huang, Jinlong Li, Guang Li, Lingdong Kong, Yingyan Li, Han Wang, Shaoqing Xu, Yuechen Luo, Fang Li, Chenxu Dang, Junli Wang, Tao Xu, Jing Wu, Jianhua Wu, Xiaoshuai Hao, Wen Zhang, Tianyi Jiang, Lingfeng Zhang, Lei Zhou, Yingbo Tang, Jie Wang, Yinfeng Gao, Xizhou Bu, Haochen Tian, Yihang Qiu, Feiyang Jia, Lin Liu, Yigu Ge, Hanbing Li, Yuannan Shen, Jianwei Cui, Hongwei Xie, Bing Wang, Haiyang Sun, Jingwei Zhao, Jiahui Huang, Pei Liu, Zeyu Zhu, Yuncheng Jiang, Zibin Guo, Chuhong Gong, Hanchao Leng, Kun Ma, Naiyang Wang, Guang Chen, Kuiyuan Yang, Hangjun Ye, Long Chen"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18486v1"
tags: ["query:tp"]
score: 9.0
evidence: 基于VLA的自动驾驶轨迹预测中的潜空间推理
tldr: OneVL 针对自动驾驶中视觉语言动作（VLA）模型推理速度慢的问题，提出了一种单步潜空间推理与规划框架。它通过将思维链（CoT）压缩为紧凑的潜标记，并利用语言解码器和视觉世界模型解码器进行双重监督，使模型能够同时理解语义逻辑和环境因果动力学。该方法在推理时仅需单次并行计算，在保持极高速度的同时，性能首次超越了传统的显式 CoT 方法，实现了高精度与实时性的平衡。
selection_source: fresh_fetch
motivation: 传统的显式思维链推理因自回归特性导致高延迟，而现有的潜空间推理方法因缺乏对环境因果动力学的建模而性能不足。
method: 提出 OneVL 框架，通过语言和视觉双重辅助解码器监督潜标记，并采用三阶段训练流程对齐轨迹、语言和视觉目标。
result: 在四个基准测试中，OneVL 成为首个超越显式 CoT 的潜空间推理方法，在保持单步推理速度的同时达到了最先进的准确率。
conclusion: 结合语言和世界模型监督的紧凑潜空间表征，比冗长的逐标记推理具有更强的泛化能力和更高的部署效率。
---

## 摘要
思维链（CoT）推理已成为基于视觉-语言-动作（VLA）的自动驾驶中轨迹预测的强大驱动力，但其自回归特性带来的延迟成本使得实时部署难以实现。隐式 CoT 方法试图通过将推理压缩到连续的隐藏状态中来弥补这一差距，但其表现始终逊色于显式推理方法。我们认为，这是因为纯语言的隐式表示压缩的是世界的符号抽象，而非真正主导驾驶的因果动力学。因此，我们提出了 OneVL（基于视觉-语言解释的一步式隐式推理与规划），这是一个统一的 VLA 和世界模型框架，它通过受双辅助解码器监督的紧凑隐式 token 来引导推理。除了重建文本 CoT 的语言解码器外，我们还引入了一个预测未来帧 token 的视觉世界模型解码器，迫使隐式空间内化道路几何、智能体运动和环境变化的因果动力学。三阶段训练流水线逐步将这些隐式表示与轨迹、语言和视觉目标对齐，确保了稳定的联合优化。在推理时，辅助解码器被舍弃，所有隐式 token 在单次并行传递中完成预填充，达到了仅预测答案的速度。在四个基准测试中，OneVL 成为首个超越显式 CoT 的隐式 CoT 方法，在仅预测答案的延迟下实现了最先进的准确率，并提供了直接证据表明：在语言和世界模型双重监督的引导下，更紧凑的压缩比冗长的逐 token 推理能产生更具泛化性的表示。

## Abstract
Chain-of-Thought (CoT) reasoning has become a powerful driver of trajectory prediction in VLA-based autonomous driving, yet its autoregressive nature imposes a latency cost that is prohibitive for real-time deployment. Latent CoT methods attempt to close this gap by compressing reasoning into continuous hidden states, but consistently fall short of their explicit counterparts. We suggest that this is due to purely linguistic latent representations compressing a symbolic abstraction of the world, rather than the causal dynamics that actually govern driving. Thus, we present OneVL (One-step latent reasoning and planning with Vision-Language explanations), a unified VLA and World Model framework that routes reasoning through compact latent tokens supervised by dual auxiliary decoders. Alongside a language decoder that reconstructs text CoT, we introduce a visual world model decoder that predicts future-frame tokens, forcing the latent space to internalize the causal dynamics of road geometry, agent motion, and environmental change. A three-stage training pipeline progressively aligns these latents with trajectory, language, and visual objectives, ensuring stable joint optimization. At inference, the auxiliary decoders are discarded and all latent tokens are prefilled in a single parallel pass, matching the speed of answer-only prediction. Across four benchmarks, OneVL becomes the first latent CoT method to surpass explicit CoT, delivering state-of-the-art accuracy at answer-only latency, and providing direct evidence that tighter compression, when guided in both language and world-model supervision, produces more generalizable representations than verbose token-by-token reasoning. Project Page: https://xiaomi-embodied-intelligence.github.io/OneVL

---

## 论文详细总结（自动生成）

这是一份关于小米汽车（Xiaomi EV）团队论文《OneVL: One-Step Latent Reasoning and Planning with Vision-Language Explanation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在基于视觉-语言-动作（VLA）模型的自动驾驶中，思维链（CoT）推理能显著提升轨迹预测的准确性。
*   **核心痛点**：
    1.  **高延迟**：传统的显式 CoT 需要逐个 token 自回归生成推理过程，导致推理速度极慢，无法满足自动驾驶的实时性要求。
    2.  **隐式推理性能差**：现有的隐式（Latent）CoT 方法试图将推理压缩进隐藏状态，但性能始终低于显式 CoT。
*   **核心假设**：作者认为现有隐式方法失败的原因在于它们仅压缩了**语言符号**（抽象概念），而忽略了主导驾驶的**物理因果动力学**。
*   **整体含义**：OneVL 旨在通过引入“世界模型”监督，将推理压缩为紧凑的隐式 token，实现既能达到显式 CoT 的高精度，又能匹配“仅输出答案”模型的超低延迟。

### 2. 方法论
OneVL 结合了 VLA 模型与世界模型（World Model）的思想，核心技术包括：
*   **隐式 Token 设计**：在模型输入中引入两类特殊的隐式 token：**视觉隐式 token ($Z_v$)** 和 **语言隐式 token ($Z_l$)**。
*   **双辅助解码器（训练阶段）**：
    *   **语言辅助解码器**：负责从隐式状态中重建人类可读的文本 CoT。
    *   **视觉辅助解码器（世界模型）**：负责预测未来 0.5s 和 1.0s 的视觉帧 token。这迫使隐式空间内化道路几何、物体运动等因果动力学。
*   **预填充推理（Prefill Inference）**：在推理阶段，**舍弃辅助解码器**。将所有隐式 token 作为固定 prompt 预填充进上下文。由于 Transformer 的预填充阶段是并行计算的，这使得推理延迟几乎等同于不带推理的直接预测。
*   **三阶段训练流程**：
    *   **阶段 0**：主模型预热，学习基础轨迹预测。
    *   **阶段 1**：辅助解码器预热，在主模型冻结的情况下对齐隐式表示。
    *   **阶段 2**：联合端到端微调，通过双重监督信号塑造隐式空间。

### 3. 实验设计
*   **数据集与场景**：
    1.  **NAVSIM**：基于 nuPlan 的大规模真实驾驶日志。
    2.  **ROADWork**：专注于施工区域等复杂长尾场景。
    3.  **Impromptu**：涵盖四类非结构化角落场景（Corner Cases）。
    4.  **APR1**：包含因果链（CoC）标注的复杂驾驶行为数据集。
*   **对比方法**：
    *   **AR Answer**：自回归直接输出答案（延迟底线）。
    *   **AR CoT**：显式思维链推理（性能上限）。
    *   **隐式 CoT 变体**：COCONUT、CODI、SIM-CoT。
    *   **SOTA 模型**：AdaThinkDrive、LaST-VLA、YNet 等。

### 4. 资源与算力
*   **基础模型**：使用 Qwen3-VL-4B-Instruct 作为骨干网络。
*   **视觉分词器**：采用 Emu3.5 的 IBQ 分词器（131,072 词表大小）。
*   **算力说明**：文中提到训练时由于需要同时运行主模型和两个辅助解码器，内存占用约为单模型的 **3 倍**。使用了 DeepSpeed ZeRO-2 优化。
*   **未明确信息**：论文未具体列出 GPU 的型号（如 A100 或 H100）及具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同的基准测试上进行了全面评估，结果具有一致性。
*   **消融实验**：非常充分。分别验证了视觉解码器、语言解码器以及三阶段训练策略对最终性能的影响。
*   **评估维度**：不仅评估了轨迹精度（PDM-score, ADE/FDE），还评估了推理延迟，并引入 **LLM-as-Judge**（使用 Gemini-1.5-Flash）对生成的解释质量进行了量化评分。
*   **客观性**：对比了多种现有的隐式推理框架，并证明了在自动驾驶领域，传统的纯语言隐式推理确实存在局限性。

### 6. 主要结论与发现
*   **性能突破**：OneVL 是**首个在性能上超越显式 CoT 的隐式推理方法**，同时保持了极低的延迟。
*   **世界模型的重要性**：视觉监督（预测未来帧）对性能的提升（+0.87 PDM-score）显著高于纯语言监督（+0.31），证明了物理因果建模是自动驾驶推理的关键。
*   **压缩即智能**：实验证明，将推理压缩进少量的隐式 token 能够过滤冗余信息，产生比冗长文本更具泛化性的表征。
*   **部署潜力**：通过添加 MLP 回归头，模型延迟可进一步降至 0.24s（约 4Hz），满足实车部署需求。

### 7. 优点
*   **兼顾速度与精度**：解决了 CoT 推理在实时系统中“好用但太慢”的顽疾。
*   **多模态解释性**：虽然推理过程是隐式的，但通过辅助解码器，系统仍能提供视觉（未来预览）和文本（逻辑说明）的双重解释。
*   **训练策略科学**：三阶段训练有效解决了多任务学习中的梯度冲突和过拟合问题。

### 8. 不足与局限
*   **训练成本**：由于引入了额外的辅助解码器和视觉 token 预测任务，训练时的显存压力和计算开销较大。
*   **轨迹生成瓶颈**：虽然推理部分实现了单步化，但默认的轨迹输出仍依赖自回归（除非使用 MLP 变体），这在一定程度上仍受限于 LLM 的解码速度。
*   **隐式 Token 容量**：论文中隐式 token 的数量（如 4 个视觉、2 个语言）是经验设定的，对于更复杂的场景是否需要动态调整 token 数量尚待研究。

（完）
