---
title: Self-Discovered Intention-aware Transformer for Multi-modal Vehicle Trajectory Prediction
title_zh: 用于多模态车辆轨迹预测的自发现意图感知 Transformer
authors: "Diyi Liu, Zihan Niu, Tu Xu, Lishan Sun"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07126v1"
tags: ["query:tp"]
score: 10.0
evidence: 用于多模态车辆轨迹预测的意图感知Transformer
tldr: 本研究针对自动驾驶中的车辆轨迹预测问题，提出了一种纯Transformer架构的多模态预测网络。为解决现有方法依赖特定图结构或显式意图标签的局限性，该模型采用双轨设计：一轨负责轨迹预测，另一轨结合邻车信息预测意图概率。研究表明，通过分离空间模块与轨迹生成模块，并利用残差偏移学习K条有序轨迹，该方法显著提升了预测性能与灵活性。
selection_source: fresh_fetch
motivation: 现有轨迹预测方法过度依赖特定图结构或显式意图标注，限制了模型的灵活性和泛化能力。
method: 提出一种纯Transformer网络，通过双轨结构分别处理轨迹生成和基于邻车信息的意图概率预测。
result: 双轨设计通过分离空间特征与生成模块提升了性能，且模型能通过预测残差偏移学习到有序的轨迹组。
conclusion: 该研究证明了自发现意图感知机制在多模态车辆轨迹预测中的有效性，为自动驾驶提供了更灵活的方案。
---

## 摘要
车辆轨迹预测在自动驾驶和智能交通系统（ITS）应用中发挥着重要作用。尽管已经设计了多种深度学习算法来预测车辆轨迹，但它们对特定图结构（如图神经网络）或显式意图标签的依赖限制了其灵活性。在本研究中，我们提出了一种纯基于 Transformer 的多模态网络，并考虑了邻近车辆的影响。该网络采用了两个独立的分支：一个分支专注于预测轨迹，另一个分支则专注于在考虑邻近车辆的情况下预测每种意图的概率。研究发现，这种双分支设计通过将空间模块与轨迹生成模块分离，能够提升模型性能。此外，我们还发现该模型可以通过预测 K 条轨迹之间的残差偏移量来学习一组有序的轨迹。

## Abstract
Predicting vehicle trajectories plays an important role in autonomous driving and ITS applications. Although multiple deep learning algorithms are devised to predict vehicle trajectories, their reliant on specific graph structure (e.g., Graph Neural Network) or explicit intention labeling limit their flexibilities. In this study, we propose a pure Transformer-based network with multiple modals considering their neighboring vehicles. Two separate tracks are employed. One track focuses on predicting the trajectories while the other focuses on predicting the likelihood of each intention considering neighboring vehicles. Study finds that the two track design can increase the performance by separating spatial module from the trajectory generating module. Also, we find the the model can learn an ordered group of trajectories by predicting residual offsets among K trajectories.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为“自发现意图感知 Transformer”（Self-Discovered Intention-aware Transformer）的模型，旨在解决自动驾驶中多模态车辆轨迹预测的灵活性与准确性问题。

### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖人工预定义意图标签（如“左转”、“换道”）和特定图结构（如 GNN）的情况下，实现准确的多模态车辆轨迹预测。
*   **研究动机**：
    *   **现有方法局限性**：传统的图神经网络（GNN）依赖预定义的图结构，而许多 Transformer 模型需要手动标注意图标签来引导多模态学习，这限制了模型在复杂、多变场景下的泛化能力。
    *   **决策随机性**：驾驶行为具有概率性，同一场景下不同驾驶员可能有不同意图，因此需要模型具备自发发现多种潜在意图的能力。

### 2. 方法论
核心思想是采用**纯 Transformer 架构**，通过**双轨（Dual-track）设计**将轨迹生成与意图评估解耦。
*   **双轨架构**：
    *   **轨迹解码器（Trajectory Decoder）**：负责生成 $K$ 条候选轨迹。引入了“模态间残差累加”机制（Inter-modal residual summation），通过预测 $K$ 条轨迹之间的残差偏移量，使生成的轨迹组具有自然的顺序性和自正则化特征。
    *   **概率解码器（Probability Decoder）**：专注于评估在考虑邻车交互的情况下，每种意图（轨迹）发生的可能性。
*   **关键技术细节**：
    *   **空间注意力模块**：在注意力机制中引入了相对偏置矩阵 $B$。该矩阵通过 MLP 融合车辆间的相对速度、偏航角等动态差异，而非仅依赖距离，从而捕捉非对称的交互关系。
    *   **时间注意力模块**：用于捕捉单车历史轨迹中的长距离时间依赖。
    *   **损失函数**：结合了“胜者为王”（Winner-Takes-All, WTA）轨迹损失和概率损失，仅对预测最准的模态进行反向传播，鼓励模态多样性。

### 3. 实验设计
*   **数据集**：使用开源的“Ubiquitous traffic eyes”数据集，专门选取了包含复杂冲突的高速公路合流与分流区域。
*   **实验场景**：利用前 5 秒的历史轨迹预测未来 5 秒的路径。
*   **对比方法（消融实验）**：
    *   **OV 1**：单模态预测，不考虑车辆交互。
    *   **OV K**：多模态预测（K=8），不考虑车辆交互。
    *   **MV K**：本文完整模型，多模态且包含空间交互建模。
*   **评价指标**：均方根误差（RMSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **算力说明**：文中提到模型训练了 100 个 epoch。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号、数量、内存占用以及具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：主要进行了针对模型组件的消融实验（如表 I 所示），验证了多模态机制和空间交互模块的有效性。
*   **充分性评价**：实验在特定复杂场景（合流区）下证明了模型的优越性。然而，实验数量相对较少，且**缺乏与当前行业领先模型（如 Wayformer, VectorNet 等）的直接横向对比**，这在一定程度上削弱了其结论的普适性说服力。

### 6. 主要结论与发现
*   **双轨设计有效性**：将空间交互建模与轨迹生成分离，能显著提升预测精度。
*   **有序轨迹学习**：通过残差偏移预测，模型无需标签即可学习到一组有序且合理的轨迹分布。
*   **交互偏置的重要性**：考虑相对运动状态（速度、角度）的自适应偏置比单纯的距离权重更能反映真实的车辆互动。
*   **模态平衡**：在高速场景下，设置 8 个意图模态能在轨迹多样性与模型复杂度之间达到较好平衡。

### 7. 优点
*   **自发现意图**：摆脱了对人工标注意图的依赖，降低了数据准备成本。
*   **架构简洁**：纯 Transformer 设计，易于并行化，且通过双轨结构实现了逻辑解耦。
*   **残差技巧**：创新的残差累加机制解决了多模态预测中常见的模态崩溃或无序问题。
*   **可解释性**：通过注意力权重可视化，提供了分析车辆决策依据的手段。

### 8. 不足与局限
*   **场景覆盖有限**：实验仅限于高速公路场景，未在城市路口（包含掉头、红绿灯交互等）进行验证。
*   **环境约束缺失**：模型未直接引入高精地图（HD Map）信息（如车道边缘、交通标志），可能导致预测轨迹在物理上不可行（如冲出路沿）。
*   **基准对比不足**：未与近年来主流的 SOTA 模型进行量化性能对比。
*   **应用限制**：对于 U 型弯道等极端转向行为，目前的残差预测机制可能难以精确覆盖。

（完）
