---
title: "SCT-MOT: Enhancing Air-to-Air Multiple UAVs Tracking with Swarm-Coupled Motion and Trajectory Guidance"
title_zh: SCT-MOT：利用集群耦合运动与轨迹引导增强空对空多无人机跟踪
authors: "Zhaochen Chu, Tao Song, Ren Jin, Shaoming He, Defu Lin, Siqing Cheng"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06883v1"
tags: ["query:tp"]
score: 9.0
evidence: 多无人机轨迹预测与集群运动依赖
tldr: 针对空对空集群无人机追踪中复杂的非线性运动和弱视觉特征挑战，本文提出SCT-MOT框架。该框架通过SMTP模块从集群层面建模运动依赖性，并利用TG-STFF模块融合轨迹引导的时空特征，有效解决了轨迹断裂和身份切换问题，在多个公开数据集上显著提升了追踪的准确性与鲁棒性。
selection_source: fresh_fetch
motivation: 现有追踪方法通常独立建模个体运动，忽略了集群间的运动耦合性，且缺乏运动预测与外观特征的深度融合。
method: 提出SCT-MOT框架，包含用于建模集群协同运动的SMTP模块和用于增强弱目标时空一致性的TG-STFF特征融合模块。
result: "在AIRMOT、MOT-FLY和UAVSwarm数据集上达到SOTA水平，SMTP模块相比现有主流方法使IDF1指标提升了1.21%。"
conclusion: 通过结合集群耦合运动建模与轨迹引导的特征融合，SCT-MOT有效解决了复杂空对空场景下的多无人机追踪难题。
---

## 摘要
集群无人机的空对空跟踪面临重大挑战，复杂的非线性群体运动和微小目标的微弱视觉线索常导致检测失败、轨迹碎片化和身份切换。尽管现有方法尝试通过引入轨迹预测来提升性能，但它们对每个目标进行独立建模，忽略了集群层面的运动依赖性。运动预测与外观表示之间的有限集成也削弱了在视觉模糊和杂乱环境中跟踪所需的时空一致性，导致难以维持连贯的轨迹和可靠的关联。为应对这些挑战，我们提出了 SCT-MOT，这是一个集成了集群耦合运动建模和轨迹引导特征融合的跟踪框架。首先，我们开发了一个集群运动感知轨迹预测（SMTP）模块，从集群视角联合建模历史轨迹和姿态感知外观特征，从而实现对非线性、耦合群体轨迹的更准确预测。其次，我们设计了一个轨迹引导时空特征融合（TG-STFF）模块，将预测位置与历史视觉线索对齐，并将其与当前帧特征深度集成，增强了弱目标的时空一致性和空间判别力。在 AIRMOT、MOT-FLY 和 UAVSwarm 三个公开的空对空集群无人机跟踪数据集上进行的广泛实验表明，SMTP 实现了更准确的轨迹预测；在集成到相同的多目标跟踪（MOT）框架中时，相比最先进的轨迹预测模块 EqMotion，其 IDF1 提升了 1.21%。总体而言，在复杂的集群场景下，我们的 SCT-MOT 在多个指标上均优于现有的最先进跟踪器，展现出卓越的准确性和鲁棒性。

## Abstract
Air-to-air tracking of swarm UAVs presents significant challenges due to the complex nonlinear group motion and weak visual cues for small objects, which often cause detection failures, trajectory fragmentation, and identity switches. Although existing methods have attempted to improve performance by incorporating trajectory prediction, they model each object independently, neglecting the swarm-level motion dependencies. Their limited integration between motion prediction and appearance representation also weakens the spatio-temporal consistency required for tracking in visually ambiguous and cluttered environments, making it difficult to maintain coherent trajectories and reliable associations. To address these challenges, we propose SCT-MOT, a tracking framework that integrates Swarm-Coupled motion modeling and Trajectory-guided feature fusion. First, we develop a Swarm Motion-Aware Trajectory Prediction (SMTP) module jointly models historical trajectories and posture-aware appearance features from a swarm-level perspective, enabling more accurate forecasting of the nonlinear, coupled group trajectories. Second, we design a Trajectory-Guided Spatio-Temporal Feature Fusion (TG-STFF) module aligns predicted positions with historical visual cues and deeply integrates them with current frame features, enhancing temporal consistency and spatial discriminability for weak objects. Extensive experiments on three public air-to-air swarm UAV tracking datasets, including AIRMOT, MOT-FLY, and UAVSwarm, demonstrate that SMTP achieves more accurate trajectory forecasts and yields a 1.21\% IDF1 improvement over the state-of-the-art trajectory prediction module EqMotion when integrated into the same MOT framework. Overall, our SCT-MOT consistently achieves superior accuracy and robustness compared to state-of-the-art trackers across multiple metrics under complex swarm scenarios.

---

## 论文详细总结（自动生成）

### SCT-MOT：利用集群耦合运动与轨迹引导增强空对空多无人机跟踪

#### 1. 核心问题与整体含义
论文聚焦于**空对空（Air-to-Air, A2A）场景下的多无人机（UAV）跟踪**。在集群飞行中，无人机目标通常极小、视觉特征微弱，且伴随复杂的非线性群体运动（如编队变换、协同转弯）。
*   **研究动机**：现有的多目标跟踪（MOT）方法通常独立建模每个目标的运动，忽略了集群内部的运动耦合性。此外，运动预测与视觉特征往往是分离的，导致在背景杂乱或检测失效时，难以维持轨迹的连贯性和身份（ID）的一致性。
*   **核心目标**：通过建模集群层面的运动依赖关系，并利用预测轨迹引导视觉特征融合，提升微小无人机目标在复杂动态环境下的跟踪鲁棒性。

#### 2. 方法论
SCT-MOT 框架采用了“跟踪-检测”范式，核心包含两个创新模块：

*   **集群运动感知轨迹预测（SMTP）模块**：
    *   **核心思想**：联合建模历史轨迹、速度以及“姿态感知”的外观特征。
    *   **技术细节**：
        1.  **集群级表示**：计算集群的平均位置和速度，提取个体相对于群心的相对运动。
        2.  **时间-姿态注意力**：利用多头注意力机制，将无人机的外观特征（隐含姿态信息，如前倾代表加速）与运动特征融合。
        3.  **全局-局部空间耦合**：通过图结构建模邻近无人机间的相互影响（如避障、跟随）。
        4.  **预测输出**：通过扩张因果卷积（Dilated Causal Convolution）和多层感知机（MLP）预测未来多帧的位置。
*   **轨迹引导时空特征融合（TG-STFF）模块**：
    *   **核心思想**：将预测的运动先验转化为空间权重，增强当前帧的特征表示。
    *   **技术细节**：
        1.  **预测特征图生成**：将历史视觉特征根据 SMTP 预测的位置，以高斯分布的形式映射到特征图上。
        2.  **交叉注意力融合**：使用多头交叉注意力机制，将当前帧的原始特征图与预测特征图进行深度融合，从而在预测位置“激活”微弱目标的特征，抑制背景噪声。

#### 3. 实验设计
*   **数据集**：
    *   **AIRMOT**：模拟空对空同构集群数据集，包含 8 个序列，5-16 个目标。
    *   **MOT-FLY**：真实世界数据集，包含 16 个序列，目标极小（占图像面积不足 5%）。
    *   **UAVSwarm**：包含 72 个序列，涵盖 13 种场景和 19 种无人机型号，具有复杂的编队变换。
*   **Benchmark 与对比方法**：
    *   对比了 9 种主流方法，包括经典方法（DeepSORT, ByteTrack）、高性能方法（OC-SORT, Hybrid-SORT）、端到端方法（MOTRv3）以及专门针对无人机的 SOTA 方法（UAVS-MOT, HOMATracker）。

#### 4. 资源与算力
*   **训练与测试设备**：使用单张 **NVIDIA RTX 3090 GPU**。
*   **边缘部署**：为了验证实际应用潜力，作者将模型通过 TensorRT 加速，部署在 **NVIDIA Jetson Orin NX** 嵌入式平台上。
*   **算力细节**：文中未明确给出具体的训练总时长，但提到在 Orin NX 上可达到约 **22-24 FPS** 的实时处理速度。

#### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了全量对比，涵盖了模拟与真实、同构与异构等多种复杂场景。
*   **消融实验**：
    *   测试了不同历史窗口长度（T=4, 8, 12, 16）对预测精度的影响。
    *   将 SMTP 模块与最先进的轨迹预测模型 EqMotion 进行了直接对比。
    *   验证了 TG-STFF 模块集成到不同基准跟踪器（FairMOT, HOMATracker）后的性能提升。
*   **评价**：实验设计客观、公平，通过可视化特征图和轨迹图直观展示了改进效果，实验数据充分支持了论文的论点。

#### 6. 主要结论与发现
*   **性能领先**：SCT-MOT 在所有三个数据集上均刷新了 SOTA 记录。在 UAVSwarm 数据集上，MOTA 提升了 2.70%，IDF1 提升了 1.35%。
*   **预测优势**：SMTP 模块相比 EqMotion，在集成到相同框架时使 IDF1 提升了 1.21%，证明了集群耦合建模的必要性。
*   **特征增强**：TG-STFF 模块能有效减少因视觉特征微弱导致的漏检（False Negatives）和误检（False Positives），尤其在背景杂乱的场景下表现优异。

#### 7. 优点
*   **集群视角**：突破了传统 MOT 独立建模目标的局限，引入了集群层面的运动约束。
*   **深度融合**：TG-STFF 不仅仅是简单的后处理关联，而是将运动先验反馈到特征提取阶段，实现了真正的时空特征增强。
*   **插件化设计**：SMTP 和 TG-STFF 模块具有良好的通用性，可以轻松集成到其他基于检测的跟踪框架中。
*   **落地性强**：在嵌入式设备上实现了实时运行，证明了算法的工程实用价值。

#### 8. 不足与局限
*   **冷启动问题**：SMTP 依赖至少 4-8 帧的历史轨迹，对于新进入视野的目标，初始阶段的预测可能不够准确。
*   **计算开销**：虽然在 Orin NX 上可实时运行，但相比于 ByteTrack 等极简方法，引入注意力机制和特征融合模块仍增加了额外的计算负担。
*   **极端遮挡**：虽然利用了集群运动，但在整个集群被大面积遮挡或目标极度密集导致 ID 严重重叠时，性能仍有提升空间。

（完）
