---
title: "CorridorVLA: Explicit Spatial Constraints for Generative Action Heads via Sparse Anchors"
title_zh: CorridorVLA：通过稀疏锚点为生成式动作头提供显式空间约束
authors: "Dachong Li, ZhuangZhuang Chen, Jin Zhang, Jianqiang Li"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21241v1"
tags: ["query:tp"]
score: 8.0
evidence: 轨迹生成的显式空间约束与锚点
tldr: 本文针对视觉-语言-动作（VLA）模型中空间引导通常隐含在潜特征中的局限性，提出了CorridorVLA。该方法通过预测稀疏空间锚点（如增量位置变化）来构建显式的物理约束区域（走廊），并利用流匹配动作头引导轨迹生成，对超出约束的轨迹施加修正梯度。在LIBERO-Plus基准测试中，该方法显著提升了SmolVLA和GR00T的成功率，证明了显式物理线索对生成式动作策略的有效约束作用。
selection_source: fresh_fetch
motivation: 现有的VLA模型主要依赖隐含的空间引导，缺乏显式的物理约束来确保动作生成的准确性和稳健性。
method: 通过预测稀疏空间锚点定义一个“走廊”容差区域，并利用该区域为流匹配动作头提供显式的修正梯度约束。
result: "在LIBERO-Plus基准上，CorridorVLA相比基线提升了3.4%至12.4%的成功率，其中GR00T-Corr变体达到了83.21%的成功率。"
conclusion: 动作对齐的物理线索能为生成式动作策略提供直接且可解释的显式约束，是视觉和潜空间引导的有效补充。
---

## 摘要
视觉-语言-动作 (VLA) 模型通常使用中间表示来连接多模态输入与连续控制，然而空间引导往往通过潜特征隐式注入。我们提出了 CorridorVLA，它将稀疏空间锚点预测为增量物理变化（例如 Δ-位置），并利用它们在动作生成的训练目标中施加显式的容差区域。这些锚点定义了一个引导流匹配 (flow-matching) 动作头的“走廊”：当轨迹所隐含的空间演化超出该走廊时，会接收到修正梯度，而接触和执行噪声引起的微小偏差则是允许的。在更具挑战性的 LIBERO-Plus 基准测试中，CorridorVLA 在 SmolVLA 和 GR00T 上均取得了持续的提升，相比相应的基线模型，成功率提高了 3.4%–12.4%；值得注意的是，我们的 GR00T-Corr 变体达到了 83.21% 的成功率。这些结果表明，与动作对齐的物理线索可以为生成式动作策略提供直接且可解释的约束，补充了以视觉或潜变量形式编码的空间引导。代码可在 https://github.com/corridorVLA 获取。

## Abstract
Vision--Language--Action (VLA) models often use intermediate representations to connect multimodal inputs with continuous control, yet spatial guidance is often injected implicitly through latent features. We propose $CorridorVLA$, which predicts sparse spatial anchors as incremental physical changes (e.g., $Δ$-positions) and uses them to impose an explicit tolerance region in the training objective for action generation. The anchors define a corridor that guides a flow-matching action head: trajectories whose implied spatial evolution falls outside it receive corrective gradients, while minor deviations from contacts and execution noise are permitted. On the more challenging LIBERO-Plus benchmark, CorridorVLA yields consistent gains across both SmolVLA and GR00T, improving success rate by $3.4\%$--$12.4\%$ over the corresponding baselines; notably, our GR00T-Corr variant reaches a success rate of $83.21\%$. These results indicate that action-aligned physical cues can provide direct and interpretable constraints for generative action policies, complementing spatial guidance encoded in visual or latent forms. Code is available at https://github.com/corridorVLA.

---

## 论文详细总结（自动生成）

### CorridorVLA：通过稀疏锚点为生成式动作头提供显式空间约束 —— 论文总结

#### 1. 核心问题与研究动机
现有的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）在连接多模态输入与连续控制时，空间引导信息通常是以**隐式**方式注入的（即通过视觉特征或潜变量影响动作生成）。这种方式缺乏直观的物理约束，导致模型在复杂任务中生成的轨迹可能偏离物理常识。
**研究动机：** 探索是否可以将空间引导表达为**显式的、文本风格的物理量**（如末端执行器的位移），并利用这些量在训练目标层面为动作生成施加约束，从而提高机器人策略的稳健性和可解释性。

#### 2. 方法论
CorridorVLA 的核心思想是预测未来的“稀疏空间锚点”，并以此构建一个“走廊（Corridor）”容差区域来约束动作轨迹。
*   **稀疏锚点预测：** 在 VLA 主干网络中引入少量可学习的槽位（Slots），预测未来 $K$ 个关键时间点的末端执行器（EE）增量位置（$\Delta$-positions）。相比绝对位置，增量位置对相机校准和场景偏移更具鲁棒性。
*   **动作头增强（extra-A）：** 将动作向量扩展，显式包含位移项，使动作头的监督信号与主干网络预测的空间锚点对齐。
*   **走廊约束目标（Corridor Regularizer）：**
    *   **走廊缓冲损失（$L_{buf}$）：** 定义一个基于地标的容差带。如果生成的轨迹演化超出了这个“走廊”，则施加修正梯度；在走廊内则允许微小的执行噪声。
    *   **走廊内一致性（$L_{cons}$）：** 在轨迹进入走廊后，通过阶段性累积进度进一步精细化调整，防止轨迹在可行区域内漂移。
*   **噪声感知加权：** 结合流匹配（Flow-matching）技术，在去噪过程的后期（噪声较小时）加大空间约束的权重，确保几何约束在接近真实数据分布时更有效。

#### 3. 实验设计
*   **数据集与基准：** 主要在 **LIBERO**（包含 Long, Goal, Object, Spatial 四个子集）和更具挑战性的 **LIBERO-Plus** 基准上进行评估。
*   **主干网络：** 选取了轻量级的 **SmolVLA**（0.45B）和高性能的 **GR00T** 作为基础模型，验证方法的通用性。
*   **对比方法：** 包括 $\pi_0$ (3.3B), OpenVLA, UniVLA, NORA, GraspVLA 等多种 SOTA 模型。
*   **评估指标：** 任务成功率（Success Rate）。

#### 4. 资源与算力
论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）及总训练时长。但提到 SmolVLA 的选择是为了快速迭代和细粒度消融实验，暗示该方法在计算资源消耗上相对友好，且推理开销几乎没有增加（仅增加了 3 个预测 Token）。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在两个主流基准上进行了详尽测试，涵盖了从简单到复杂的多种操作任务。
*   **消融实验：** 非常充分。作者对比了：
    1.  锚点类型（绝对位置 vs. 增量位置）；
    2.  损失函数组件（是否有缓冲损失、一致性损失）；
    3.  锚点选择策略（均匀采样 vs. 几何感知采样 RDP）；
    4.  主干网络交互方式（因果掩码 vs. 双向注意力）。
*   **客观性：** 通过在不同架构（SmolVLA 和 GR00T）上实现并取得一致提升，证明了结果的客观性和方法的普适性。

#### 6. 主要结论与发现
*   **显式约束有效：** CorridorVLA 在 LIBERO-Plus 上将 SmolVLA 的成功率提升了 **12.4%**，将 GR00T 的成功率提升了 **7.98%**（达到 83.21%）。
*   **增量位置优于绝对位置：** 预测 $\Delta$-positions 能更好地捕捉运动本质，减少对环境绝对坐标的依赖。
*   **几何感知很重要：** 使用 Ramer-Douglas-Peucker (RDP) 算法选择轨迹关键点作为锚点，比简单的均匀采样效果更好。
*   **物理线索的互补性：** 紧凑的物理量（文本风格）可以与视觉/潜变量引导互补，直接在动作流形上提供约束。

#### 7. 优点
*   **轻量且高效：** 仅需增加极少量的预测 Token 和损失函数项，无需大幅改变模型架构或增加参数量。
*   **可解释性强：** 预测的锚点是具有明确物理意义的位移，方便开发者理解模型的预期运动路径。
*   **即插即用：** 能够轻松集成到现有的基于扩散或流匹配的 VLA 模型中。

#### 8. 不足与局限
*   **缺乏真机实验：** 所有实验均在仿真环境（LIBERO）中完成，尚未在真实机器人平台上验证其对接触力学和传感器噪声的应对能力。
*   **对比覆盖面：** 虽然对比了多种 VLA，但未与一些重型视觉中间表示方法（如 ReconVLA）进行直接的端到端对比。
*   **走廊参数敏感性：** 走廊宽度 $\delta$ 和加权方案可能需要针对不同机器人硬件进行手动调优。

（完）
