---
title: "MISTY: High-Throughput Motion Planning via Mixer-based Single-step Drifting"
title_zh: MISTY：通过基于 Mixer 的单步漂移实现高吞吐量运动规划
authors: "Yining Xing, Zehong Ke, Yiqian Tu, Zhiyuan Liu, Wenhao Yu, Jianqiang Wang"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21489v1"
tags: ["query:tp"]
score: 9.0
evidence: 自动驾驶的多模态轨迹生成与潜流形构建
tldr: 针对自动驾驶中扩散模型规划器推理延迟高的问题，本文提出MISTY。它通过VAE将专家轨迹映射到低维流形，并利用轻量化MLP-Mixer实现单步推理。核心创新是引入潜空间漂移损失，在训练阶段模拟分布演化，使模型能生成如主动超车等超越专家数据的动作。在nuPlan测试中达到SOTA性能，且推理速度提升一个数量级。
selection_source: fresh_fetch
motivation: 现有的基于扩散模型的运动规划器因迭代推理导致延迟过高，难以满足自动驾驶的高吞吐量需求。
method: 结合向量化子图编码器、VAE潜空间表征与MLP-Mixer解码器，并引入潜空间漂移损失来引导单步生成。
result: 在nuPlan Test14-hard基准上取得SOTA成绩，推理速度超过99 FPS，端到端延迟仅为10.1毫秒。
conclusion: MISTY证明了通过单步生成和潜空间优化，可以在保持高规划性能的同时显著提升推理效率。
---

## 摘要
多模态轨迹生成对于安全自动驾驶至关重要，然而现有的基于扩散（diffusion-based）的规划器由于迭代式的神经函数评估，面临推理延迟高的问题。本文提出了 MISTY（Mixer-based Inference for Single-step Trajectory-drifting Yield），这是一种高吞吐量的生成式运动规划器，通过纯单步推理实现了最先进的闭环性能。MISTY 集成了一个向量化子图（Sub-Graph）编码器以捕获环境上下文，一个变分自编码器（VAE）将专家轨迹结构化为紧凑的 32 维潜流形（latent manifold），以及一个超轻量级的 MLP-Mixer 解码器以消除二次方的注意力复杂度。重要的是，我们引入了一种潜空间漂移损失（latent-space drifting loss），将复杂的分布演化完全转移到训练阶段。通过构建显式的引力和斥力，该机制使模型能够合成新颖且具有前瞻性的机动动作（如主动超车），而这些动作在原始专家演示中几乎是不存在的。在 nuPlan 基准测试上的广泛评估表明，MISTY 在具有挑战性的 Test14-hard 划分上取得了最先进的结果，在非反应式和反应式设置下的综合得分分别为 80.32 和 82.21。MISTY 的运行速度超过 99 FPS，端到端延迟为 10.1 ms，在实现显著鲁棒生成的同时，比迭代式扩散规划器快了一个数量级。

## Abstract
Multi-modal trajectory generation is essential for safe autonomous driving, yet existing diffusion-based planners suffer from high inference latency due to iterative neural function evaluations. This paper presents MISTY (Mixer-based Inference for Single-step Trajectory-drifting Yield), a high-throughput generative motion planner that achieves state-of-the-art closed-loop performance with pure single-step inference. MISTY integrates a vectorized Sub-Graph encoder to capture environment context, a Variational Autoencoder to structure expert trajectories into a compact 32-dimensional latent manifold, and an ultra-lightweight MLP-Mixer decoder to eliminate quadratic attention complexity. Importantly, we introduce a latent-space drifting loss that shifts the complex distribution evolution entirely to the training phase. By formulating explicit attractive and repulsive forces, this mechanism empowers the model to synthesize novel, proactive maneuvers, such as active overtaking, that are virtually absent from the raw expert demonstrations. Extensive evaluations on the nuPlan benchmark demonstrate that MISTY achieves state-of-the-art results on the challenging Test14-hard split, with comprehensive scores of 80.32 and 82.21 in non-reactive and reactive settings, respectively. Operating at over 99 FPS with an end-to-end latency of 10.1 ms, MISTY offers an order-of-magnitude speedup over iterative diffusion planners while while achieving significantly robust generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《MISTY: High-Throughput Motion Planning via Mixer-based Single-step Drifting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的基于扩散模型（Diffusion-based）的运动规划器虽然能生成高质量的多模态轨迹，但由于需要多次迭代神经函数评估（NFE），导致推理延迟高，难以满足自动驾驶车载系统的实时性要求。
*   **研究背景**：传统的回归模型容易陷入“众数平均”导致轨迹不可行；而现有的单步生成模型（如一致性模型）在复杂场景下容易出现“特征漂移”，导致轨迹偏离可行驶区域或发生碰撞。
*   **整体含义**：MISTY 旨在通过引入“漂移模型（Drifting Models）”理论，将复杂的分布演化过程转移到训练阶段，从而在保持高规划性能和多模态多样性的同时，实现极速的单步推理。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：结合 VAE 结构化的潜空间表征与单步漂移损失，利用轻量化架构实现高吞吐量的轨迹生成。
*   **关键技术细节**：
    *   **向量化子图编码器（Sub-Graph Encoder）**：采用类似 VectorNet 的层次化结构，通过可学习的注意力池化捕获高精地图约束和动态代理交互。
    *   **VAE 轨迹流形**：使用变分自编码器将高维专家轨迹压缩为 32 维的紧凑潜流形。通过相对位移编码（Relative Displacement）消除绝对坐标偏差，增强语义区分度。
    *   **MLP-Mixer 解码器**：放弃了计算量大的 Transformer 注意力机制，改用线性复杂度的 MLP-Mixer，通过交替的空间混合和通道混合显著降低 FLOPs。
    *   **潜空间漂移损失（Latent-space Drifting Loss）**：
        *   **引力（Attractive Force）**：将生成的特征向量拉向专家驾驶流形。
        *   **斥力（Repulsive Force）**：在生成样本间建立斥力，防止模式崩溃（Mode Collapse）。
        *   **引导缩放（Guidance Scale $\alpha$）**：通过调节 $\alpha$ 值（范围从 -0.5 到 1.5）来控制轨迹的多样性与合规性。
    *   **PCA 解码器**：将 Mixer 输出的潜特征映射回物理坐标，利用主成分分析的结构先验确保轨迹的运动学合理性。

### 3. 实验设计
*   **数据集与场景**：使用 **nuPlan** 基准测试，这是目前最大规模、高保真度的自动驾驶闭环评估平台，涵盖波士顿、新加坡等多个城市。
*   **Benchmark**：重点评估了 **Test14** 和更具挑战性的 **Test14-hard** 划分。
*   **评估设置**：
    *   **非反应式（Non-Reactive, NR）**：周围车辆按记录轨迹行驶。
    *   **反应式（Reactive, R）**：周围车辆根据智能驾驶模型（IDM）对主车行为做出反应。
*   **对比方法**：包括规则/混合方法（IDM, PDM-Closed）、基于学习的方法（PlanTF, PLUTO）以及生成式方法（Diffusion Planner）。

### 4. 资源与算力
*   **推理算力**：论文明确提到在 **NVIDIA RTX 4090 GPU** 上进行性能分析。
*   **训练细节**：VAE 特征提取器预训练了 200 个 epoch。
*   **训练算力**：文中未明确给出训练全模型所使用的具体 GPU 数量及总训练时长，但强调了其架构的轻量化特性。

### 5. 实验数量与充分性
*   **实验规模**：在 nuPlan 的大规模闭环仿真中进行了广泛测试，涵盖了数千个场景。
*   **消融实验**：对比了有无精细化模块（Refinement）的表现，并对不同表征空间（绝对坐标、PCA 空间、VAE 潜空间）的类间/类内距离进行了定量分析。
*   **充分性与公平性**：实验涵盖了从特征空间验证、多样性分析到闭环性能及推理效率的全方位对比，基准测试选择标准，对比对象均为近年来的 SOTA 模型，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能领先**：MISTY 在 Test14-hard 反应式设置下取得了 **82.21** 的综合得分，刷新了 SOTA 纪录。
*   **极高效率**：端到端延迟仅 **10.1 ms**，运行速度超过 **99 FPS**，核心生成延迟仅 4.4 ms，比迭代式扩散模型快了一个数量级。
*   **泛化能力**：模型能够生成专家数据中罕见的“主动超车”等探索性行为，证明了漂移机制在处理长尾场景和复杂交互时的鲁棒性。
*   **表征优势**：VAE 潜空间的类间/类内距离比（23.64）远优于 PCA 空间（0.42），为生成模型提供了更稳健的边界。

### 7. 优点：亮点总结
*   **理论创新**：首次将漂移模型（Drifting Models）理论应用于自动驾驶运动规划，解决了单步生成的分布对齐问题。
*   **架构优化**：通过 MLP-Mixer 和 PCA 解码器的组合，极大地压缩了计算开销，非常适合车载边缘设备的实时部署。
*   **行为合成**：不仅是模仿学习，通过漂移场的引导，模型具备了合成新颖、安全机动动作的能力。

### 8. 不足与局限
*   **平滑度挑战**：单步生成的轨迹平滑度略逊于多步迭代精炼的结果，在舒适性指标上仍有提升空间。
*   **长尾稳定性**：在极端稀疏的紧急制动或精确停车场景下，单步模型原生生成的减速曲线可能不够完美，仍需依赖轻量级的精细化模块（Refinement）来修复伪影。
*   **数据依赖**：虽然能生成新行为，但其底层流形的构建仍高度依赖于专家数据的质量和多样性。

（完）
