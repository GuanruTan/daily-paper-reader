---
title: "ARMove: Learning to Predict Human Mobility through Agentic Reasoning"
title_zh: ARMove：通过智能体推理学习预测人类移动性
authors: "Chuyue Wang, Jie Feng, Yuxi Wu, Shenglin Yi, Hang Zhang"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17419v1"
tags: ["query:tp"]
score: 8.0
evidence: 通过大语言模型的智能体推理预测人类移动性
tldr: 针对人类移动预测中存在的复杂性、黑盒推理及迁移性差等问题，本文提出ARMove框架。该框架通过智能体推理实现可迁移的移动预测，结合标准化特征管理、用户画像定制及大模型知识蒸馏技术。ARMove不仅在多个全球数据集上超越了现有SOTA模型，还通过决策路径可视化提升了预测的可解释性，并在跨区域、跨用户场景下展现出极强的鲁棒性。
selection_source: fresh_fetch
motivation: 现有人类移动预测方法面临黑盒推理、缺乏迭代学习能力以及在不同人群和地区间迁移性差的挑战。
method: 提出一种基于智能体推理的框架，通过标准化特征池管理、自动化特征生成及大小模型协同蒸馏，实现动态权重调整与可解释决策。
result: "在四个全球数据集的12项指标中，ARMove在6项上取得最优（提升0.78%至10.47%），其余指标也达到次优水平，并验证了跨场景的强迁移性。"
conclusion: ARMove通过智能体化决策和知识蒸馏，有效平衡了移动预测的准确性、可解释性与计算效率，为人类行为建模提供了新范式。
---

## 摘要
人类移动性预测是一项关键任务，但由于其在不同人群和地区间的复杂性和变异性，仍然面临挑战。最近，大语言模型（LLMs）在零样本预测方面取得了进展，但现有方法存在可解释性有限（归因于黑盒推理）、缺乏对新数据的迭代学习以及迁移性差等缺陷。在本文中，我们介绍了 ARMove，这是一个通过智能体推理预测人类移动性的完全可迁移框架。为了应对这些局限性，ARMove 采用了具有迭代优化和用户特定定制功能的标准化特征管理：包括用于基础知识的四个主要特征池、用于群体细分的用户画像，以及集成 LLM 知识的自动生成机制。通过智能体决策实现稳健的泛化，该决策通过调整特征权重以最大化准确性，同时提供可解释的决策路径。最后，大小模型协同将策略从大型 LLM（如 72B）蒸馏到较小型模型（如 7B），在降低成本的同时提升了性能上限。在四个全球数据集上的广泛实验表明，ARMove 在 12 个指标中的 6 个上优于最先进的基准模型（提升幅度为 0.78% 至 10.47%），另外 4 个指标也达到了次优结果。迁移性测试证实了其在不同地区、用户群体和模型规模上的稳健性，而可解释性分析则突显了其决策的透明度。我们的代码开源地址为：https://anonymous.4open.science/r/ARMove-F847。

## Abstract
Human mobility prediction is a critical task but remains challenging due to its complexity and variability across populations and regions. Recently, large language models (LLMs) have made progress in zero-shot prediction, but existing methods suffer from limited interpretability (due to black-box reasoning), lack of iterative learning from new data, and poor transferability. In this paper, we introduce \textbf{ARMove}, a fully transferable framework for predicting human mobility through agentic reasoning. To address these limitations, ARMove employs standardized feature management with iterative optimization and user-specific customization: four major feature pools for foundational knowledge, user profiles for segmentation, and an automated generation mechanism integrating LLM knowledge. Robust generalization is achieved via agentic decision-making that adjusts feature weights to maximize accuracy while providing interpretable decision paths. Finally, large-small model synergy distills strategies from large LLMs (e.g., 72B) to smaller ones (e.g., 7B), reducing costs and enhancing performance ceilings. Extensive experiments on four global datasets show ARMove outperforms state-of-the-art baselines on 6 out of 12 metrics (gains of 0.78\% to 10.47\%), with transferability tests confirming robustness across regions, users, and scales. The other 4 items also achieved suboptimal results. Transferability tests confirm its 19 robustness across regions, user groups, and model scales, while interpretability 20 analysis highlights its transparency in decision-making. Our codes are available at: https://anonymous.4open.science/r/ARMove-F847.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ARMove** 的新型人类移动性预测框架，旨在通过智能体推理（Agentic Reasoning）解决现有模型在可解释性、迭代学习能力和迁移性方面的不足。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：人类移动性预测（预测用户下一个目的地）具有高度的复杂性和个体差异性。
*   **研究动机**：
    *   **传统深度学习模型**（如 DeepMove）：虽然拟合能力强，但容易过拟合，且缺乏可解释性（黑盒模型），泛化能力差。
    *   **现有大语言模型（LLM）方法**（如 AgentMove）：虽然具备零样本预测能力，但通常无法从新数据中迭代学习，推理过程依然不够透明，且在大规模应用时计算成本高、迁移性受限。

### 2. 方法论：ARMove 框架
ARMove 采用多智能体协作架构，核心思想是将预测任务分解为特征优化、用户画像和行为预测三个环节：
*   **特征优化智能体 (Feature Optimization Agent)**：
    *   **标准化特征池**：构建涵盖轨迹（时间、频率）、空间（POI、行政区）、记忆（长短期偏好）和社会关系（社交网络）的四大特征池。
    *   **自动化生成与选择**：利用 LLM 的内部知识自动生成场景特定特征，并通过迭代优化机制调整特征权重。
    *   **目标函数**：通过最大化复合目标函数 $J$（Acc@1 和 Acc@5 的加权组合）作为代理损失，指导特征权重的动态调整。
*   **用户画像工程师智能体 (User Profiling Engineer Agent)**：
    *   **多级分组策略**：首先识别基础角色（如上班族），再挖掘次级兴趣（如健身、夜生活），最后动态合并相似群体，实现精细化且高效的用户建模。
*   **大小模型协同与知识转移 (Model Knowledge Transfer)**：
    *   **蒸馏机制**：将大模型（如 GPT-4o-mini）学习到的特征权重、用户画像和城市知识转移给小模型（如 Llama-3-8B），在降低成本的同时提升小模型的性能上限。
    *   **跨维度迁移**：支持同城不同用户间、不同城市间的知识迁移。

### 3. 实验设计
*   **数据集**：使用了四个全球城市的数据集，包括 Foursquare（莫斯科、东京、圣保罗）和 ISP（上海）。
*   **基准方法 (Benchmarks)**：对比了 10 种方法，包括：
    *   **传统/深度学习方法**：FPMC, RNN, DeepMove, LSTPM, GETNext, STHGCN。
    *   **LLM 相关方法**：LLM-Mob, LLM-ZS, LLM-Move, AgentMove。
*   **评价指标**：Accuracy@1 (Acc@1), Accuracy@5 (Acc@5), NDCG@5。

### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 型号、数量或训练时长。
*   **实现细节**：实验主要通过 API 调用实现。使用了 OpenAI API 访问 **GPT-4o-mini**，并通过 DeepInfra 和 OpenRouter 访问开源模型（如 **Llama-3-8B**）。由于 ARMove 属于零样本/提示词学习框架，不涉及传统意义上的端到端反向传播训练，因此算力消耗主要集中在推理和迭代 Prompt 的生成上。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集上进行了 12 项指标的对比，并进行了详尽的消融实验（针对特征生成、迭代机制、用户分组、特征选择）。
*   **充分性评价**：实验设计较为充分。除了主实验外，还专门设计了**迁移性测试**（跨模型、跨用户、跨城市）和**不同 LLM 底座的对比测试**（对比了 6 种不同规模的模型）。实验过程考虑了零样本设置下的公平性，对比了最先进的深度学习模型。

### 6. 主要结论与发现
*   **性能领先**：ARMove 在 12 项指标中的 6 项达到最优，4 项次优。相比最强基准 AgentMove，在 10 项指标上有显著提升（最高提升 18.06%）。
*   **大小模型协同有效**：经过大模型引导的 8B 小模型在多项指标上超过了未引导的原始模型，甚至在部分指标上超越了 AgentMove。
*   **迁移性强**：在跨城市迁移中，即使不针对目标城市进行本地训练，ARMove 也能保持稳定的预测性能。
*   **迭代优势**：实验证明 5 次迭代是性能与效率的最佳平衡点。

### 7. 优点（亮点）
*   **可解释性**：通过智能体推理路径，明确展示了模型为何选择特定特征进行预测，打破了黑盒限制。
*   **迭代学习能力**：引入了反馈机制，使模型能通过新数据不断优化特征权重，弥补了 LLM 零样本预测无法持续进化的短板。
*   **成本优化**：提出的大小模型协作策略为 LLM 在时空数据挖掘任务中的大规模落地提供了可行路径。

### 8. 不足与局限
*   **特定场景性能波动**：在东京数据集的 Acc@1 指标上，ARMove 显著弱于深度学习模型（GETNext），说明在某些极度依赖局部轨迹模式的场景下，LLM 的推理可能不如纯数据驱动的方法。
*   **数据分布敏感**：在上海数据集（ISP）上的跨城市迁移效果不佳，论文归因于该数据集采集周期短且存在分布偏移，这表明框架对数据质量和分布一致性仍有一定依赖。
*   **实时性挑战**：尽管通过小模型降低了成本，但多智能体多次迭代的推理模式在极高并发的实时预测场景下可能仍存在延迟压力。

（完）
