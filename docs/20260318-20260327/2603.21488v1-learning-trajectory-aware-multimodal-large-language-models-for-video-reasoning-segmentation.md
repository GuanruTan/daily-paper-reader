---
title: Learning Trajectory-Aware Multimodal Large Language Models for Video Reasoning Segmentation
title_zh: 学习用于视频推理分割的轨迹感知多模态大语言模型
authors: "Jingnan Luo, Mingqi Gao, Jun Liu, Bin-Bin Gao, Feng Zheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21488v1"
tags: ["query:tp"]
score: 9.0
evidence: 轨迹感知的多模态大语言模型
tldr: 针对视频推理分割中现有模型难以应对复杂动态场景的问题，本文提出TrajSeg框架。该框架基于多模态大语言模型，引入双向文本-轨迹对齐机制，通过定位和描述指令增强模型对物体轨迹的感知。结合帧级内容集成模块和统一掩码解码器，TrajSeg实现了简化的端到端训练，在多个视频分割基准测试中均达到SOTA性能。
selection_source: fresh_fetch
motivation: 现有的视频推理分割方法依赖单向且隐式的文本-轨迹对齐，在面对剧烈视频动态时难以准确感知物体轨迹。
method: 提出TrajSeg框架，引入双向文本-轨迹对齐机制，并利用帧级内容集成模块和统一掩码解码器实现端到端分割。
result: 实验结果显示，TrajSeg在所有参考和推理视频分割数据集的各项指标上均超越了现有的先进方法。
conclusion: TrajSeg通过增强轨迹感知能力和简化解码结构，显著提升了视频推理分割的性能，证明了双向对齐的有效性。
---

## 摘要
多模态大语言模型（MLLMs）的繁荣激发了对视频推理分割的需求，该任务旨在根据人类指令对视频对象进行分割。先前的研究依赖于单向且隐式的文本-轨迹对齐，在面对剧烈的视频动态时难以进行轨迹感知。在这项工作中，我们提出了 TrajSeg，这是一个基于 MLLMs 构建的简单且统一的框架。具体而言，我们引入了双向文本-轨迹对齐，其中 MLLMs 接收以定位为目的（文本到轨迹）和以描述为目的（轨迹到文本）的指令。通过这种方式，MLLMs 可以从增强的对应关系中获益，并更好地感知视频中的对象轨迹。从轨迹生成掩码是通过帧级内容整合（FCI）模块和统一掩码解码器实现的。前者将 MLLM 解析的轨迹级标记（token）适配到特定帧的信息，而后者将所有帧的分割统一到一个单一结构中，使所提出的框架得以简化并实现端到端训练。在指代和推理视频分割数据集上的广泛实验证明了 TrajSeg 的有效性，它在所有指标上都优于现有的视频推理分割方法。代码将在 https://github.com/haodi19/TrajSeg 公开。

## Abstract
The prosperity of Multimodal Large Language Models (MLLMs) has stimulated the demand for video reasoning segmentation, which aims to segment video objects based on human instructions. Previous studies rely on unidirectional and implicit text-trajectory alignment, which struggles with trajectory perception when faced with severe video dynamics. In this work, we propose TrajSeg, a simple and unified framework built upon MLLMs. Concretely, we introduce bidirectional text-trajectory alignment, where MLLMs accept grounding-intended (text-to-trajectory) and captioning-intended (trajectory-to-text) instructions. This way, MLLMs can benefit from enhanced correspondence and better perceive object trajectories in videos. The mask generation from trajectories is achieved via a frame-level content integration (FCI) module and a unified mask decoder. The former adapts the MLLM-parsed trajectory-level token to frame-specific information. The latter unifies segmentation for all frames into a single structure, enabling the proposed framework to be simplified and end-to-end trainable. Extensive experiments on referring and reasoning video segmentation datasets demonstrate the effectiveness of TrajSeg, which outperforms all video reasoning segmentation methods on all metrics. The code will be publicly available at https://github.com/haodi19/TrajSeg.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Trajectory-Aware Multimodal Large Language Models for Video Reasoning Segmentation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
视频推理分割（Video Reasoning Segmentation）要求模型根据复杂的人类指令（如“分割那个正在欺负另一只小猫的猫”）在视频中定位并分割目标。
*   **现有挑战**：视频具有强烈的**动态性**（物体属性随时间变化）和**时空一致性**要求。
*   **研究动机**：以往方法（如 VISA, TrackGPT）存在两个主要缺陷：
    1.  **对齐不充分**：仅采用单向的“文本到轨迹”对齐，模型对物体轨迹的感知是隐式的，难以应对剧烈的视频动态。
    2.  **流程复杂且次优**：通常采用“分割+独立跟踪器”的流水线，关键帧与非关键帧由不同模型处理，缺乏端到端的优化，且轨迹 Token 缺乏帧级别的空间细节。

### 2. 方法论：核心思想与关键技术
论文提出了 **TrajSeg** 框架，旨在通过增强轨迹感知和统一解码结构来解决上述问题。

*   **核心思想**：引入双向对齐机制增强 MLLM 的轨迹理解力，并利用统一的掩码生成器实现端到端训练。
*   **关键技术细节**：
    *   **双向文本-轨迹对齐（Bidirectional Alignment）**：除了常规的“根据文本找轨迹”（Grounding），还引入了“根据轨迹生成描述”（Captioning）任务。通过让模型描述特定轨迹的动作和属性，强制 MLLM 显式理解轨迹的动态语义。
    *   **帧级内容集成模块（FCI）**：MLLM 输出的 `<TRJ>` Token 包含全局语义但缺乏空间细节。FCI 模块通过交叉注意力机制，将该 Token 与各帧的视觉特征融合，生成具有帧特定空间信息的 Embedding。
    *   **统一掩码生成器（Unified Mask Generator）**：
        *   **关键帧**：使用 FCI 增强的 Token 作为 Prompt 进行分割。
        *   **非关键帧**：利用“记忆库”（Memory Bank）存储前序帧的掩码和特征，通过掩码传播确保时空一致性。
        *   **统一性**：所有帧在同一个解码结构下处理，支持端到端优化。

### 3. 实验设计
*   **数据集**：
    *   **指代视频分割（RVOS）**：Ref-YouTube-VOS, Ref-DAVIS, MeViS（侧重复杂运动描述）。
    *   **推理视频分割**：ReVOS（包含需要常识推理的复杂指令）。
*   **Benchmark 与对比方法**：
    *   对比了传统的 RVOS 方法（如 ReferFormer, MTTR, ReferDINO）。
    *   重点对比了基于 MLLM 的推理分割方法（如 TrackGPT, VISA, VideoLISA, AL-Ref-SAM2）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 GPU**（具体型号未在文中明确标出，通常为 A100 或 H100 级别）。
*   **训练配置**：
    *   使用 DeepSpeed 分布式训练引擎。
    *   Batch Size 为 80。
    *   **时长**：预训练阶段（图像数据）进行 10 个 Epoch；主训练阶段（视频混合数据）进行 14 个 Epoch。
*   **模型基础**：基于 LLaVA-7B 和 SAM-2 的解码器进行初始化。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流数据集上进行了大规模测试，并针对核心组件（双向对齐、FCI 模块、关键帧数量、视频长度鲁棒性）进行了详细的消融实验。
*   **充分性与公平性**：
    *   实验涵盖了从简单指代到复杂推理的多种场景。
    *   通过可视化注意力图（Attention Map）直观展示了双向对齐对物体感知能力的提升。
    *   对比实验均在相同的 MLLM 基座（如 LLaVA-7B）下进行，确保了比较的客观性。

### 6. 主要结论与发现
*   **性能领先**：TrajSeg 在所有测试的视频推理分割指标上均达到了 SOTA（最先进）水平，显著优于 VISA 和 TrackGPT。
*   **双向对齐的价值**：实验证明，加入“轨迹到文本”的描述任务能显著提升模型在推理子集上的表现（J&F 提升约 2.7%）。
*   **长视频优势**：得益于统一的掩码生成器和轨迹感知能力，TrajSeg 在长视频（50 帧以上）上的表现比同类模型更稳定，衰减更小。
*   **效率提升**：统一的结构减少了模型参数量（相比分离式架构减少了约 224MB 的额外开销），且支持端到端推理。

### 7. 优点（亮点）
*   **架构简洁性**：通过统一掩码生成器替代了复杂的“模型+独立跟踪器”架构，实现了真正的端到端视频推理分割。
*   **创新的对齐策略**：双向对齐任务的设计巧妙地利用了 LLM 的生成能力来反哺视觉轨迹的感知。
*   **时空一致性强**：FCI 模块有效解决了全局 Token 缺乏局部空间细节的问题，使分割掩码在帧间更加精准。

### 8. 不足与局限
*   **采样帧数限制**：模型性能仍受限于输入 MLLM 的采样帧数（文中提到这可能是进一步提升的瓶颈）。
*   **复杂歧义处理**：在面对极度模糊或涉及多层嵌套逻辑的指令时，模型仍会出现定位错误（如文中提到的 Failure Case）。
*   **算力需求**：尽管架构简化，但基于 7B 参数量的 MLLM 进行视频处理，其推理延迟和显存占用对于实时应用或移动端部署仍有挑战。

（完）
