---
title: "Learn for Variation: Variationally Guided AAV Trajectory Learning in Differentiable Environments"
title_zh: 为变分而学习：可微环境下的变分引导自主飞行器轨迹学习
authors: "Xiucheng Wang, Zhenye Chen, Nan Cheng"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18853v1"
tags: ["query:tp"]
score: 9.0
evidence: 自主飞行器的梯度告知轨迹学习
tldr: 针对6G物联网中无人机（AAV）轨迹规划面临的奖励稀疏和训练不稳定问题，本文提出了一种名为L4V的梯度引导学习框架。该框架通过构建端到端可微的计算图，将运动学、信道增益和数据采集进度整合，利用随时间反向传播（BPTT）获取精确的策略梯度，从而替代传统强化学习中的高方差标量奖励。实验证明，L4V在任务完成时间、传输速率和训练效率上均优于遗传算法及DQN、DDPG等主流强化学习算法。
selection_source: fresh_fetch
motivation: 传统强化学习在无人机轨迹规划中因标量奖励稀疏而难以处理长期非线性影响，导致信用分配困难和训练不稳定。
method: 提出L4V框架，通过构建可微环境并利用随时间反向传播（BPTT）作为伴随求解器，为策略网络提供稠密且精确的解析梯度。
result: 仿真结果显示，L4V在任务完成时间、平均传输速率和训练成本方面均显著优于遗传算法、DQN、A2C和DDPG等基准方法。
conclusion: 通过引入可微环境和结构化梯度，可以有效解决复杂移动物联网场景下的无人机轨迹优化难题，提升学习效率与性能。
---

## 摘要
自主飞行器 (AAV) 通过移动驱动的数据收集，赋能第六代 (6G) 物联网 (IoT) 网络。然而，传统的基于奖励驱动的 AAV 轨迹规划强化学习面临严重的信用分配问题和训练不稳定性，因为稀疏的标量奖励无法捕捉序列运动的长期和非线性效应。为应对这些挑战，本文提出了“为变分而学习”(L4V)，这是一种基于梯度的轨迹学习框架，它用密集且具有解析依据的策略梯度取代了高方差的标量奖励信号。具体而言，AAV 运动学、随距离变化的信道增益以及每个用户的数据收集进度之间的耦合演化，首先被展开为一个端到端的可微计算图。随后，随时间反向传播 (BPTT) 作为离散伴随求解器，将精确的灵敏度从累积任务目标传播到每个控制动作和策略参数。这些结构化梯度被用于训练具有时间平滑正则化和梯度裁剪的确定性神经策略。广泛的仿真结果表明，L4V 在任务完成时间、平均传输速率和训练成本方面，始终优于包括遗传算法、DQN、A2C 和 DDPG 在内的代表性基准方法。

## Abstract
Autonomous aerial vehicles (AAVs) empower sixth-generation (6G) Internet-of-Things (IoT) networks through mobility-driven data collection. However, conventional reward-driven reinforcement learning for AAV trajectory planning suffers from severe credit assignment issues and training instability, because sparse scalar rewards fail to capture the long-term and nonlinear effects of sequential movements. To address these challenges, this paper proposes Learn for Variation (L4V), a gradient-informed trajectory learning framework that replaces high-variance scalar reward signals with dense and analytically grounded policy gradients. Particularly, the coupled evolution of AAV kinematics, distance-dependent channel gains, and per-user data-collection progress is first unrolled into an end-to-end differentiable computational graph. Backpropagation through time then serves as a discrete adjoint solver, which propagates exact sensitivities from the cumulative mission objective to every control action and policy parameter. These structured gradients are used to train a deterministic neural policy with temporal smoothness regularization and gradient clipping. Extensive simulations demonstrate that L4V consistently outperforms representative baselines, including a genetic algorithm, DQN, A2C, and DDPG, in mission completion time, average transmission rate, and training cost

---

## 论文详细总结（自动生成）

### 论文总结：Learn for Variation (L4V) —— 可微环境下的变分引导 AAV 轨迹学习

#### 1. 核心问题与整体含义（研究动机和背景）
在 6G 物联网（IoT）场景中，自主飞行器（AAV）作为移动基站进行数据采集至关重要。然而，传统的轨迹规划面临两大挑战：
*   **信用分配困难（Credit Assignment）**：AAV 的移动对通信性能的影响具有长期性和非线性，传统的强化学习（RL）依赖稀疏的标量奖励，难以将最终的任务成功准确回溯到早期的某一特定动作。
*   **训练不稳定与收敛慢**：基于随机策略梯度的 RL 方法（如 DDPG、A2C）存在高方差问题，在复杂、长时程的任务中极易陷入局部最优或无法收敛。
**研究动机**：利用环境的物理特性（如运动学、信道模型），将轨迹优化问题转化为一个可微的变分问题，从而获得比标量奖励更丰富、更准确的梯度信号。

#### 2. 方法论：核心思想与关键技术
论文提出了 **L4V (Learn for Variation)** 框架，其核心思想是将整个物理环境建模为一个**端到端的可微计算图**。
*   **核心思想**：放弃传统的“黑盒”奖励反馈，转而使用“白盒”伴随梯度（Adjoint Gradients）。通过将 AAV 运动学、距离相关的信道增益和用户数据积压（Backlog）状态耦合在一起，构建一个可微的仿真环境。
*   **关键技术细节**：
    *   **离散伴随求解器**：利用“随时间反向传播”（BPTT）算法，将任务目标（最小化累积数据积压）的灵敏度直接传播回每一个控制动作和策略网络参数。
    *   **变分原理引导**：基于庞特里亚金极小值原理（PMP），推导出解析梯度，这为策略网络提供了具有物理意义的监督信号，实现了“零方差”的梯度估计。
    *   **确定性神经策略**：训练一个多层感知器（MLP）直接输出速度和航向角。
    *   **正则化与稳定性**：引入**时间平滑正则化**（使用余弦度量处理航向角的周期性）以确保飞行轨迹平滑，并使用**梯度裁剪**防止长时程优化中的梯度爆炸。

#### 3. 实验设计
*   **实验场景**：单 AAV 在给定区域内收集 $N$ 个分布随机、任务量随机的地面用户数据。
*   **Benchmark（基准方法）**：
    *   **遗传算法 (GA)**：代表启发式/进化类无梯度优化方法。
    *   **DQN**：代表基于值的离散动作强化学习。
    *   **A2C**：代表同步优势演员-评论家算法（在线策略）。
    *   **DDPG**：代表深度确定性策略梯度算法（离线策略，连续控制）。
*   **对比维度**：任务完成时间（Time Steps）、平均传输速率、训练成本（迭代次数与墙钟时间）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 RTX 3090/4090）或 CPU 核心数。
*   **训练时长**：文中通过图表展示了“Training Time (s)”，在不同场景下，L4V 的训练耗时通常在数千秒以内，显著低于 DDPG 和 A2C（后者在复杂场景下耗时呈指数级增长）。

#### 5. 实验数量与充分性
*   **实验规模**：进行了多组对比实验，变量包括：用户数量（2-10）、区域大小（5x5 到 25x25）、信道增益系数、噪声功率（0.05-0.25）。
*   **充分性与客观性**：
    *   每组实验均进行了 **10 次独立试验**并取平均值，以消除用户位置随机分布带来的偏差。
    *   实验覆盖了从通信质量到计算效率的全方位评估，对比了不同流派的算法（进化、值函数、演员-评论家），实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先**：L4V 在所有测试场景下的任务完成时间最短，平均传输速率最高。
*   **训练效率极高**：相比于 DDPG 等 RL 方法，L4V 达到收敛所需的训练时间缩短了数倍甚至数十倍，且收敛过程更加平滑（单调下降）。
*   **鲁棒性强**：随着环境规模扩大或噪声功率增加，传统 RL 方法的性能剧烈下降，而 L4V 依然能保持稳定的高性能，证明了解析梯度在处理长时程信用分配上的优越性。

#### 7. 优点（亮点）
*   **物理一致性**：将已知的无线信道物理模型融入学习过程，使梯度具有明确的物理含义。
*   **零方差梯度**：通过 BPTT 获取确定性梯度，彻底解决了强化学习中因随机采样导致的高方差和训练不稳定问题。
*   **端到端可微**：将仿真环境视为计算图的一部分，这种“白盒”建模思路是未来 AI 与通信融合的重要方向。

#### 8. 不足与局限
*   **模型依赖性**：L4V 依赖于环境模型的可微性。如果环境中存在不可微的因素（如复杂的遮挡模型、非连续的切换逻辑），该方法的有效性可能会受限。
*   **单智能体限制**：目前实验仅限于单 AAV 场景，尚未在多 AAV 协同（存在复杂的避障和干扰协调）中验证。
*   **Sim-to-Real 风险**：虽然考虑了噪声，但实验仍基于数学模型（LoS 信道），在存在多径效应和动态障碍物的真实复杂环境中，解析梯度的准确性可能下降。

（完）
