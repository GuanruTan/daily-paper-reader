---
title: Pedestrian Crossing Intent Prediction via Psychological Features and Transformer Fusion
title_zh: 基于心理特征与 Transformer 融合的行人过街意图预测
authors: "Sima Ashayer, Hoang H. Nguyen, Yu Liang, Mina Sartipi"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19533v1"
tags: ["query:tp"]
score: 10.0
evidence: 行人意图感知轨迹预测
tldr: 本研究针对自动驾驶中的行人过街意图预测，提出了一种轻量化且具有社会感知能力的架构。该方法融合了注意力、位置、场景和交互四个维度的行为流，利用Transformer进行特征融合，并引入变分瓶颈和马氏距离检测器来量化不确定性。在PSI 1.0和2.0基准测试中，该模型凭借可解释的结构化特征取得了优异性能，适用于资源受限平台的风险感知预测。
selection_source: fresh_fetch
motivation: 自动驾驶车辆在城市环境中行驶时，需要准确预测行人的过街意图以确保安全。
method: 提出一种融合四种行为流的轻量化Transformer架构，并结合变分瓶颈与马氏距离检测器来评估预测的不确定性。
result: 在PSI 1.0数据集上达到0.9的F1分数，并在PSI 2.0数据集上建立了领先的基准性能。
conclusion: 该方法通过结构化特征实现了高效且可解释的意图预测，为资源受限环境下的风险感知提供了可靠方案。
---

## 摘要
行人意图预测对于自动驾驶车辆在城市环境中安全行驶至关重要。我们提出了一种轻量级、具有社会感知能力的行人意图预测架构。该架构利用高速公路编码器（highway encoders）、紧凑型 4-token Transformer 以及全局自注意力池化，融合了四个行为流（注意力、位置、情境和交互）。为了量化不确定性，我们引入了两个互补的头部：一个通过 KL 散度捕捉认知不确定性（epistemic uncertainty）的变分瓶颈（variational bottleneck），以及一个识别分布偏移（distributional shift）的马氏距离（Mahalanobis distance）检测器。这些组件共同产生了校准后的概率和可操作的风险评分，且不牺牲效率。在 PSI 1.0 基准测试中，我们的模型仅使用结构化、可解释的特征，就实现了 0.9 的 F1 分数、0.94 的 AUC-ROC 和 0.78 的 MCC，优于近期的视觉语言模型。在更具多样性的 PSI 2.0 数据集上（据我们所知，目前尚无先前的研究结果），我们建立了 0.78 F1 和 0.79 AUC-ROC 的强力初始基准。基于马氏距离评分的选择性预测在 80% 的覆盖率下将测试准确率提高了多达 0.4 个百分点。定性注意力热图进一步展示了模型在存在歧义时如何转移其跨流关注点。所提出的方法与模态无关，易于与视觉语言流水线集成，并适用于资源受限平台上的风险感知意图预测。

## Abstract
Pedestrian intention prediction needs to be accurate for autonomous vehicles to navigate safely in urban environments. We present a lightweight, socially informed architecture for pedestrian intention prediction. It fuses four behavioral streams (attention, position, situation, and interaction) using highway encoders, a compact 4-token Transformer, and global self-attention pooling. To quantify uncertainty, we incorporate two complementary heads: a variational bottleneck whose KL divergence captures epistemic uncertainty, and a Mahalanobis distance detector that identifies distributional shift. Together, these components yield calibrated probabilities and actionable risk scores without compromising efficiency. On the PSI 1.0 benchmark, our model outperforms recent vision language models by achieving 0.9 F1, 0.94 AUC-ROC, and 0.78 MCC by using only structured, interpretable features. On the more diverse PSI 2.0 dataset, where, to the best of our knowledge, no prior results exist, we establish a strong initial baseline of 0.78 F1 and 0.79 AUC-ROC. Selective prediction based on Mahalanobis scores increases test accuracy by up to 0.4 percentage points at 80% coverage. Qualitative attention heatmaps further show how the model shifts its cross-stream focus under ambiguity. The proposed approach is modality-agnostic, easy to integrate with vision language pipelines, and suitable for risk-aware intent prediction on resource-constrained platforms.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种用于自动驾驶系统的行人过街意图预测模型。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自动驾驶车辆（AV）需要准确区分行人的“过街意图”（内部决策）与“过街预测”（物理轨迹）。现有的方法往往忽略了人类驾驶员在判断时所依赖的心理线索（如目光接触、犹豫感）和社会上下文。
*   **研究背景**：在复杂的城市环境中，误判行人意图会导致安全事故或不必要的车辆犹豫。论文旨在开发一种轻量级、具有社会感知能力且能提供不确定性估计的模型，以弥补机器感知与人类判断之间的差距。

### 2. 方法论：核心思想与关键技术
该模型采用多流融合架构，将行人行为分解为四个语义维度：
*   **核心思想**：通过结构化的心理和环境特征，利用 Transformer 捕捉不同行为流之间的交互，并引入双重不确定性检测机制。
*   **关键技术细节**：
    *   **四种行为流**：
        1.  **心理注意力流**：提取自文本描述的目光方向、头部朝向、犹豫程度等。
        2.  **位置流**：行人与路缘、人行道及车辆的相对空间关系。
        3.  **情境流**：交通背景、车辆运动、场景密度及语义文本嵌入。
        4.  **交互流**：上述特征的成对乘积（如“边走边看”）。
    *   **架构流程**：输入特征经由**高速公路编码器（Highway Encoders）**处理，添加**流嵌入（Stream Embedding）**后进入一个**4-token Transformer 编码器**。最后通过全局自注意力池化和残差 MLP 进行分类。
    *   **不确定性量化**：
        1.  **变分瓶颈（KL 散度）**：捕捉认知不确定性。
        2.  **马氏距离（Mahalanobis Distance）**：在嵌入空间中识别分布偏移（离群值）。
    *   **训练策略**：使用 MixUp 数据增强、标签平滑、特征多样性惩罚和温度校准。

### 3. 实验设计
*   **数据集**：使用了 **PSI 1.0**（110个视频）和 **PSI 2.0**（204个视频）数据集。PSI 数据集包含行人意图标注及人类驾驶员的自然语言解释。
*   **对比方法**：对比了 SF-GRU、SingleRNN、PCPA、ARN、TrEP 以及基于视觉语言模型的 **ClipCross** 等多种基准模型。
*   **评估指标**：Accuracy、F1-score、AUC-ROC、MCC（马修斯相关系数）以及校准指标（ECE、Brier Score）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **模型规模**：提到模型非常轻量级，参数量约为 **100万至200万（1–2 M）**，且在推理时不依赖视频骨干网络，适合资源受限的平台。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 PSI 1.0 上进行了详尽的对比实验和**消融实验**（逐一移除四个行为流以验证贡献）。
    *   在 PSI 2.0 上建立了首个基准性能（Baseline）。
    *   所有结果均基于 **5个随机种子** 的平均值及标准差。
*   **充分性评价**：实验设计较为充分，涵盖了分类性能、不确定性校准、错误检测能力及定性热图分析。通过消融实验证明了“情境流”对预测的重要性最大。

### 6. 主要结论与发现
*   **性能领先**：在 PSI 1.0 上，模型达到了 **0.9 的 F1 分数**和 **0.94 的 AUC-ROC**，优于复杂的视觉语言模型。
*   **PSI 2.0 基准**：在更具挑战性的 PSI 2.0 上实现了 0.78 F1 和 0.79 AUC-ROC。
*   **风险感知**：马氏距离是强有力的错误指示器。通过“选择性预测”（即放弃处理高风险样本），在保留 80% 样本的情况下，准确率可进一步提升约 4 个百分点。
*   **注意力转移**：定性分析显示，模型在遇到歧义场景（如部分遮挡）时，注意力会从位置特征转向交互和情境特征。

### 7. 优点
*   **轻量高效**：参数量极小，推理速度快，适合嵌入式自动驾驶硬件。
*   **可解释性强**：基于结构化特征和注意力热图，可以清晰地解释模型为何做出特定预测。
*   **鲁棒性高**：引入了双重不确定性头，能够识别出模型“不知道”的情况，增强了安全性。
*   **模态无关**：设计灵活，易于集成到现有的视觉语言流水线中。

### 8. 不足与局限
*   **依赖上游感知**：模型输入依赖于预先提取的结构化特征（如目光、位置），如果前端感知模块（如姿态估计或目标检测）出错，模型性能将受限。
*   **时间建模较弱**：虽然通过短窗口聚合捕捉了部分动态，但相比于长短期记忆网络（LSTM），其对复杂长时序依赖的建模可能不够深入。
*   **数据集局限**：PSI 数据集虽然丰富，但样本量相对较小，模型在更广泛、未见过的极端场景下的泛化能力仍需验证。

（完）
