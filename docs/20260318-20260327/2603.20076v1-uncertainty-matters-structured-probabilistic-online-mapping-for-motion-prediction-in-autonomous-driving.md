---
title: "Uncertainty Matters: Structured Probabilistic Online Mapping for Motion Prediction in Autonomous Driving"
title_zh: 不确定性至关重要：用于自动驾驶运动预测的结构化概率在线建图
authors: "Pritom Gogoi, Faris Janjoš, Bin Yang, Andreas Look"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20076v1"
tags: ["query:tp"]
score: 9.0
evidence: 自动驾驶中用于运动预测的概率在线建图
tldr: 本研究针对自动驾驶中在线地图生成的结构不确定性问题，提出了一种结构化概率建模框架。现有方法通常忽略空间相关性，而本文通过低秩加对角（LRPD）协方差分解，显式建模地图元素内部的依赖关系。该方法在nuScenes数据集上不仅提升了地图生成质量，还在基于地图的轨迹预测任务中达到了SOTA水平，证明了不确定性建模对自动驾驶规划的重要性。
selection_source: fresh_fetch
motivation: 现有的在线地图生成模型多为确定性模型，且现有的概率方法因采用对角协方差而无法捕捉道路几何的强空间相关性。
method: 提出一种基于低秩加对角（LRPD）协方差分解的结构化概率建模方法，通过预测稠密协方差矩阵来显式建模地图元素的内部依赖。
result: 在nuScenes数据集上的实验表明，该框架显著提升了在线地图生成的质量，并在运动预测任务中取得了当前最优性能。
conclusion: 显式建模地图结构的不确定性对于提升自动驾驶系统的感知精度和下游预测任务的可靠性至关重要。
---

## 摘要
在线地图生成和轨迹预测是自动驾驶“感知-预测-规划”流水线中的关键组件。虽然现代矢量化建图模型实现了较高的几何精度，但它们通常将地图估计视为确定性任务，忽略了结构不确定性。现有的概率方法通常依赖于对角协方差矩阵，这假设了点之间的独立性，无法捕捉道路几何中固有的强空间相关性。为了解决这一问题，我们提出了一种用于在线地图生成的结构化概率公式。我们的方法通过预测稠密协方差矩阵来显式建模元素内部的依赖关系，该矩阵通过低秩加对角（LRPD）协方差分解进行参数化。这种公式将不确定性表示为捕捉全局空间结构的低秩分量和代表独立局部噪声的对角分量的组合，从而在不产生全协方差矩阵高昂计算成本的情况下捕捉几何相关性。在 nuScenes 数据集上的评估表明，与确定性基准相比，我们的不确定性感知框架在在线地图生成质量方面取得了持续的改进。此外，我们的方法在基于地图的运动预测方面创造了新的最先进性能，突显了不确定性在规划任务中的关键作用。代码发布于 link-available-soon。

## Abstract
Online map generation and trajectory prediction are critical components of the autonomous driving perception-prediction-planning pipeline. While modern vectorized mapping models achieve high geometric accuracy, they typically treat map estimation as a deterministic task, discarding structural uncertainty. Existing probabilistic approaches often rely on diagonal covariance matrices, which assume independence between points and fail to capture the strong spatial correlations inherent in road geometry. To address this, we propose a structured probabilistic formulation for online map generation. Our method explicitly models intra-element dependencies by predicting a dense covariance matrix, parameterized via a Low-Rank plus Diagonal (LRPD) covariance decomposition. This formulation represents uncertainty as a combination of a low-rank component, which captures global spatial structure, and a diagonal component representing independent local noise, thereby capturing geometric correlations without the prohibitive computational cost of full covariance matrices. Evaluations on the nuScenes dataset demonstrate that our uncertainty-aware framework yields consistent improvements in online map generation quality compared to deterministic baselines. Furthermore, our approach establishes new state-of-the-art performance for map-based motion prediction, highlighting the critical role of uncertainty in planning tasks. Code is published under link-available-soon.

---

## 论文详细总结（自动生成）

### 论文总结：Uncertainty Matters: Structured Probabilistic Online Mapping for Motion Prediction in Autonomous Driving

#### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶中，在线地图生成（OMG）正逐渐取代高精地图（HD Map）以应对环境动态变化并降低维护成本。然而，现有的矢量化建图模型（如 MapTR）大多是**确定性**的，忽略了传感器噪声和遮挡带来的不确定性。
*   **现有痛点**：少数概率建图方法假设地图点之间是相互独立的（采用对角协方差矩阵），这导致预测的地图在空间上不连贯（如出现锯齿状波动），无法捕捉道路几何结构中固有的强空间相关性（例如：车道线某一点的偏移通常意味着相邻点也会同向偏移）。
*   **研究目标**：提出一种能够显式建模地图元素内部空间相关性的结构化概率框架，以提升建图精度并增强下游轨迹预测的鲁棒性。

#### 2. 方法论
论文提出了一种**结构化概率在线建图框架**，核心在于对地图元素（折线）的不确定性进行精细化建模。
*   **核心思想（LRPD 分解）**：为了在不增加全协方差矩阵高昂计算成本的情况下捕捉相关性，提出**低秩加对角（Low-Rank plus Diagonal, LRPD）**协方差分解：
    $$\Sigma_{\phi,k} = D_{\phi,k} + \kappa L_{\phi,k} L_{\phi,k}^T$$
    *   $D$：对角矩阵，表示每个坐标点的独立局部噪声。
    *   $L$：低秩因子矩阵（秩 $R \ll 2N$），捕捉全局空间结构和几何相关性。
    *   $\kappa$：缩放因子，用于控制训练过程中低秩分量的影响。
*   **学习目标**：使用负对数似然（NLL）损失函数，通过最大化观测到地面真值（GT）坐标的概率来训练模型。
*   **下游预测集成**：
    *   **显式编码**：将预测的均值、对角方差和低秩因子向量拼接，作为轨迹预测编码器的输入。
    *   **FiLM 调制**：利用特征线性调制（FiLM）机制，根据地图元素的分类置信度动态调整点位嵌入特征。

#### 3. 实验设计
*   **数据集**：使用大规模真实世界数据集 **nuScenes**（包含波士顿和新加坡的 1000 个场景）。
*   **Benchmark 与基准方法**：
    *   **建图基准**：MapTR、MapTRv2、MapTRv2-CL（中心线版本）。
    *   **预测基准**：HiVT（分层矢量 Transformer）。
    *   **对比对象**：确定性基准、基于独立同分布（iid）拉普拉斯/高斯分布的概率方法、以及最新的 Dual Decoder 等方法。
*   **评估指标**：
    *   建图：平均精度均值（mAP）。
    *   预测：minADE₆、minFDE₆、漏检率（Miss Rate, MR）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了使用 AdamW 优化器，初始学习率为 $6 \times 10^{-4}$，采用余弦退火调度。建图模型训练了 45 个 epoch（比标准 24 epoch 有所延长以保证收敛）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在三种不同的主流建图骨干网络（MapTR 系列）上验证了 LRPD 的通用性。
    *   进行了消融实验，对比了 FiLM 调制与直接拼接（Concatenation）的效果。
    *   对比了不同不确定性表示（iid vs. LRPD）对下游预测的影响。
*   **充分性评价**：实验设计较为充分且客观。作者不仅复现了现有 SOTA 方法的代码以确保公平对比，还通过延长训练周期证明了概率模型在长时间训练下的潜力。定性分析（可视化采样结果）直观地展示了 LRPD 在生成平滑几何结构方面的优势。

#### 6. 主要结论与发现
*   **建图精度提升**：引入结构化不确定性后，所有骨干网络的 mAP 均有显著提升（例如 MapTRv2 从 0.554 提升至 0.634）。
*   **预测性能 SOTA**：在基于在线地图的运动预测任务中，LRPD 框架达到了新的最先进水平，其表现非常接近使用地面真值（GT）地图的性能上限（差距仅约 2-3%）。
*   **相关性的价值**：显式建模空间相关性（非对角元素）能有效消除独立不确定性假设带来的“锯齿状”预测，使模型生成的地图样本在物理上更合理。

#### 7. 优点（亮点）
*   **数学建模优雅**：通过 LRPD 分解巧妙地平衡了全协方差矩阵的表达能力与计算效率（参数量从 $O(N^2)$ 降至 $O(NR)$）。
*   **训练稳定性**：引入了课程学习（Curriculum Scheduling）策略（先训练对角阵，再逐渐引入低秩项），解决了全协方差矩阵训练易不稳定的难题。
*   **下游增益明显**：证明了高质量的地图不确定性估计能直接转化为更安全、更准确的轨迹预测结果。

#### 8. 不足与局限
*   **元素间相关性缺失**：目前仅建模了单个地图元素（如一条线）内部的点相关性，尚未考虑不同元素之间（如相邻车道线、车道线与路沿）的空间约束。
*   **两阶段依赖**：预测模块依赖于冻结的建图模块输出，虽然稳定，但可能限制了端到端联合优化的潜力。
*   **算力信息不透明**：缺乏具体的硬件开销和推理耗时数据，难以评估其在嵌入式车载平台上的实时性表现。

（完）
