---
title: "GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories"
title_zh: GHOST：基于观测到的运动恢复结构轨迹的地面投影假设
authors: "Tomasz Frelek, Rohan Patil, Akshar Tumu, Henrik I. Christensen"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20583v2"
tags: ["query:tp"]
score: 9.0
evidence: 城市环境下自动驾驶的可行车辆轨迹分割
tldr: 本研究提出GHOST，一种自监督学习方法，旨在从单目图像中分割出可行的车辆行驶轨迹。通过利用大规模行车记录仪视频，将自我运动作为隐式监督，利用SfM技术恢复相机轨迹并投影至地面生成空间掩码。该模型无需手动标注即可训练，能从单张RGB图像预测路径提案，隐式捕捉道路布局和车道拓扑，在NuScenes等数据集上表现出良好的泛化能力和跨平台迁移性。
selection_source: fresh_fetch
motivation: 传统的自动驾驶路径规划依赖昂贵的手动标注，且难以应对复杂的城市环境和多样的相机配置。
method: 利用大规模视频中的自我运动数据，通过单目SfM恢复轨迹并投影到地面生成伪标签，训练深度分割网络预测路径。
result: 在NuScenes数据集上实现了可靠的轨迹预测，并成功通过微调迁移至电动滑板车平台，展现了极强的泛化性。
conclusion: 大规模自我运动蒸馏能有效生成结构化的路径提案，证明了通过图像分割进行轨迹假设估计的可行性。
---

## 摘要
我们提出了一种可扩展的自监督方法，用于在复杂城市环境下从单目图像中分割自动驾驶的可行车辆轨迹。利用大规模行车记录仪视频，我们将记录的本车运动视为隐式监督，并通过单目运动恢复结构（SfM）恢复相机轨迹，将其投影到地面上，从而在无需人工标注的情况下生成已行驶区域的空间掩码。这些自动生成的标签被用于训练一个深度分割网络，该网络在运行时仅凭单张 RGB 图像即可预测受运动约束的路径提议，而无需对道路或车道线进行显式建模。该模型在多样化、无约束的网络数据上进行训练，隐式地捕捉了场景布局、车道拓扑和交叉口结构，并能泛化到不同的相机配置。我们在 NuScenes 数据集上评估了我们的方法，展示了可靠的轨迹预测能力，并进一步证明了通过轻量级微调将其迁移到电动滑板车平台的可行性。我们的结果表明，大规模本车运动蒸馏可以产生超出演示轨迹的结构化且可泛化的路径提议，从而实现通过图像分割进行轨迹假设估计。

## Abstract
We present a scalable self-supervised approach for segmenting feasible vehicle trajectories from monocular images for autonomous driving in complex urban environments. Leveraging large-scale dashcam videos, we treat recorded ego-vehicle motion as implicit supervision and recover camera trajectories via monocular structure-from-motion, projecting them onto the ground plane to generate spatial masks of traversed regions without manual annotation. These automatically generated labels are used to train a deep segmentation network that predicts motion-conditioned path proposals from a single RGB image at run time, without explicit modeling of road or lane markings. Trained on diverse, unconstrained internet data, the model implicitly captures scene layout, lane topology, and intersection structure, and generalizes across varying camera configurations. We evaluate our approach on NuScenes, demonstrating reliable trajectory prediction, and further show transfer to an electric scooter platform through light fine-tuning. Our results indicate that large-scale ego-motion distillation yields structured and generalizable path proposals beyond the demonstrated trajectory, enabling trajectory hypothesis estimation via image segmentation.

---

## 论文详细总结（自动生成）

以下是对论文《GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的自动驾驶路径规划和可行驶区域检测高度依赖昂贵的人工语义标注、高精地图（HD Maps）或校准后的多传感器套件。
*   **研究动机**：互联网（如 YouTube）上存在海量的行车记录仪视频，这些视频隐含了人类驾驶员在各种复杂环境下的真实行驶轨迹。
*   **整体含义**：本文提出了一种名为 **GHOST** 的自监督框架，旨在通过“运动恢复结构”（SfM）技术从无标注的单目视频中提取车辆行驶轨迹，并将其转化为地面投影掩码，作为伪标签训练分割网络。该模型不仅能预测当前行驶路径，还能学习到场景的结构化先验，从而预测出多种潜在的可行轨迹（Hypotheses）。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将本车运动轨迹视为对环境可行性的“演示”，通过几何视觉方法将其蒸馏为图像空间的分割任务。
*   **关键流程**：
    1.  **SfM 轨迹恢复**：使用 COLMAP 管道处理原始视频，提取稀疏 3D 点云和 6-DoF 相机位姿。
    2.  **地面投影与高度估计**：
        *   由于单目 SfM 具有尺度不确定性，通过识别图像下方的地面特征点，利用 **Theil-Sen 稳健回归算法** 拟合地面平面，估算相机相对于地面的相对高度 $H_{cam}$。
        *   将未来 5 秒的相机轨迹投影到地面，生成具有一定物理宽度（与高度成比例）的二维空间掩码。
    3.  **轨迹分割模型**：
        *   采用带有 ResNet34 编码器的 UNet 结构（或 SegFormer）作为骨干网络。
        *   **加权非对称损失函数**：为了让模型预测出“所有可能”的路径而非仅仅重复“演示过”的那一条，设计了非对称损失，大幅降低对假阳性（False Positive）的惩罚，使模型倾向于覆盖所有潜在的可行驶区域。
    4.  **多假设生成**：模型输出概率图，反映了场景中不同区域作为可行路径的置信度。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练集**：从 YouTube 收集的 30 小时、跨越 14 个城市的各种天气和光照条件下的行车记录仪视频，最终生成约 65 万帧标注数据。
    *   **评估集**：使用 **NuScenes** 数据集（OpenLane-V2 子集），利用其高精地图生成的车道连通性作为 Ground Truth。
    *   **迁移场景**：在加州大学圣地亚哥分校（UCSD）校园内收集的 40 分钟电动滑板车视频（2.1 万帧）。
*   **Benchmark 与对比**：
    *   **指标**：使用 Soft IoU（Ruzicka 相似度）评估概率掩码的重合度。
    *   **对比模型**：对比了 ResNet34 和 SegFormer 两种骨干网络；对比了“从头训练”与“大规模预训练后微调”在滑板车任务上的表现。

### 4. 资源与算力
*   **算力说明**：文中提到使用了 **Ceres Solver** 进行 GPU 加速的优化计算（用于 SfM 过程）。
*   **训练细节**：模型在预训练阶段运行了 25 个 epoch，学习率为 $3 \times 10^{-4}$，使用 Adam 优化器。
*   **缺失信息**：论文**未明确指出**具体的 GPU 型号（如 A100 或 RTX 4090）、GPU 数量以及总的训练耗时。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 65 万帧的大规模自监督数据，涵盖了雨、雪、夜间等极端环境，数据量较为充分。
    *   在 NuScenes 上进行了零样本（Zero-shot）泛化测试，验证了模型在未见过的城市（波士顿、新加坡）和不同驾驶规则（左/右舵）下的鲁棒性。
*   **充分性评价**：实验设计较为客观。通过多车道场景的专门评估，证明了模型在存在多个潜在转向选择时不会发生预测崩溃。滑板车的迁移实验进一步证明了该方法在非汽车平台上的适用性。

### 6. 主要结论与发现
*   **自监督可行性**：仅通过单目视频的自我运动蒸馏，即可训练出高性能的轨迹预测模型，无需任何人工语义标注。
*   **多假设覆盖**：非对称损失函数成功引导模型学习到场景的结构先验，使其能预测出超出实际行驶路径的其他可行选项（如交叉口的其他出口）。
*   **跨域泛化**：在大规模多样化数据上训练的模型具有极强的泛化能力，能够适应不同的相机畸变、安装高度以及地理环境。
*   **微调优势**：在小规模特定领域数据（如滑板车）上，预训练模型比从头训练的模型表现出更好的多路径预测能力和稳定性。

### 7. 优点
*   **高可扩展性**：摆脱了对昂贵标注和高精地图的依赖，理论上可以利用无限的互联网视频数据。
*   **架构简洁**：将复杂的轨迹预测简化为图像分割任务，易于部署和集成。
*   **鲁棒性强**：对相机参数不敏感，能处理严重的镜头畸变和非标准的安装位置。
*   **多模态预测**：通过损失函数设计实现了单头网络输出多路径假设的能力。

### 8. 不足与局限
*   **几何约束限制**：SfM 在车辆纯直线行驶（低视差）时容易失效，导致约 35% 的原始数据因无法恢复轨迹而被丢弃。
*   **平面假设偏差**：高度估计依赖于局部地面平坦的假设，在剧烈的坡度变化（如山坡或立交桥）处可能产生误差。
*   **动态障碍物处理**：目前的伪标签生成未显式考虑动态物体（如前方车辆遮挡），虽然模型学习到了避让，但训练信号中仍包含被遮挡区域的噪声。
*   **评估闭环缺失**：目前的评估主要基于静态图像分割指标，缺乏在实际车辆控制闭环中的表现测试。

（完）
