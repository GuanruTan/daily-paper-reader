---
title: Goal-Oriented Reactive Simulation for Closed-Loop Trajectory Prediction
title_zh: 面向闭环轨迹预测的目标导向反应式仿真
authors: "Harsh Yadav, Tobias Meisen"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24155v1"
tags: ["query:tp"]
score: 10.0
evidence: 面向目标的反应式轨迹预测模拟
tldr: 针对轨迹预测模型在闭环部署中存在的协变量偏移和复合误差问题，本文提出了一种面向目标的反应式闭环训练范式。通过引入基于Transformer的场景解码器构建反应式仿真环境，并结合开环数据与自诱导状态进行训练，使模型学会纠偏与避障。实验表明，该方法在nuScenes和DeepScenario数据集上显著降低了碰撞率，提升了高频重规划下的交互安全性。
selection_source: fresh_fetch
motivation: 传统的开环训练难以应对闭环部署中的协变量偏移，且非反应式仿真破坏了智能体与交通流之间的交互反馈。
method: 提出一种基于Transformer的面向目标场景解码器，通过混合开环数据与闭环仿真状态进行策略内训练。
result: "在nuScenes和DeepScenario上分别实现了27.0%和79.5%的碰撞率降幅，显著增强了模型在高频重规划下的避障能力。"
conclusion: 结合反应式与非反应式代理的混合仿真能在即时交互性与长期行为稳定性之间取得最佳平衡。
---

## 摘要
当前的轨迹预测模型主要以开环方式进行训练，这在部署于现实世界的闭环环境时，往往会导致协变量偏移和复合误差。此外，依赖静态数据集或非反应式的日志回放仿真器会切断交互循环，阻碍自车智能体学习如何主动与周围交通进行协商。在这项工作中，我们提出了一种针对高频、收缩时界自车预测进行优化的同策略闭环训练范式。为了使自车预测建立在真实的交通交互表征之上并实现反应一致性，我们引入了一种基于 Transformer 的目标导向场景解码器，从而构建了一个本质上具有反应性的训练仿真。通过让自车智能体接触开环数据和模拟的自诱导状态的混合体，模型学会了纠正自身执行误差的恢复行为。广泛的评估表明，闭环训练显著增强了高频重规划下的避障能力，与开环基准相比，在 nuScenes 上碰撞率相对降低了高达 27.0%，在密集的 DeepScenario 交叉口降低了 79.5%。此外，我们还展示了结合反应式与非反应式周围智能体的混合仿真，在即时交互性与长期行为稳定性之间实现了最佳平衡。

## Abstract
Current trajectory prediction models are primarily trained in an open-loop manner, which often leads to covariate shift and compounding errors when deployed in real-world, closed-loop settings. Furthermore, relying on static datasets or non-reactive log-replay simulators severs the interactive loop, preventing the ego agent from learning to actively negotiate surrounding traffic. In this work, we propose an on-policy closed-loop training paradigm optimized for high-frequency, receding horizon ego prediction. To ground the ego prediction in a realistic representation of traffic interactions and to achieve reactive consistency, we introduce a goal-oriented, transformer-based scene decoder, resulting in an inherently reactive training simulation. By exposing the ego agent to a mixture of open-loop data and simulated, self-induced states, the model learns recovery behaviors to correct its own execution errors. Extensive evaluation demonstrates that closed-loop training significantly enhances collision avoidance capabilities at high replanning frequencies, yielding relative collision rate reductions of up to 27.0% on nuScenes and 79.5% in dense DeepScenario intersections compared to open-loop baselines. Additionally, we show that a hybrid simulation combining reactive with non-reactive surrounding agents achieves optimal balance between immediate interactivity and long-term behavioral stability.

---

## 论文详细总结（自动生成）

### 1. 核心问题与研究动机
*   **核心问题**：当前的轨迹预测模型大多采用**开环（Open-loop）**训练，但在实际部署（闭环环境）中会面临**协变量偏移（Covariate Shift）**和**复合误差**。此外，传统的日志回放（Log-replay）仿真缺乏交互性，导致自车（Ego）无法学习如何与周围交通进行主动协商。
*   **研究动机**：
    *   **消除信息差**：开环模型在遇到训练分布之外的陌生状态时表现脆弱。
    *   **增强交互性**：静态数据集让智能体倾向于“被动等待”而非“主动交互”。
    *   **优化高频重规划**：在模型预测控制（MPC）框架下，高频重规划对预测的稳定性提出了极高要求，而开环模型在此时容易崩溃。

### 2. 方法论
*   **核心思想**：提出一种**同策略（On-policy）闭环训练范式**，结合目标导向的 Transformer 场景解码器，构建具有反应一致性的仿真环境。
*   **关键技术细节**：
    *   **双解码器架构**：
        *   **自车解码器（Ego Decoder）**：生成多模态预测，用于处理现实中的不确定性。
        *   **场景解码器（Scene Decoder）**：生成单模态、目标导向的周围代理轨迹。通过随机采样未来目标点（Goal Token）作为条件，强制模型理解代理的导航意图。
    *   **同策略闭环训练（Algorithm 1）**：
        *   将开环数据与自车执行产生的“漂移状态”混合。
        *   **隔离优化**：每个预测模态仅根据其自身执行产生的闭环样本进行优化，避免了由于其他模态导致的偏置（Off-policy bias）。
    *   **全协方差 NLL 损失**：引入马氏距离（Mahalanobis distance）来显式建模空间不确定性的相关性，并进行坐标系旋转变换以保持一致性。
    *   **混合仿真策略**：在训练中混合反应式代理（由场景解码器控制）和非反应式代理（日志回放），以平衡即时交互性和长期行为稳定性。

### 3. 实验设计
*   **数据集**：
    *   **nuScenes**：主要基准，提供完整的周围代理未来轨迹。
    *   **DeepScenario**：用于验证高密度、高交互场景（如复杂交叉口）下的鲁棒性。
*   **Benchmark 与对比方法**：
    *   **对比基准**：主要对比**开环训练（OL）**与**闭环训练（CL）**在不同重规划频率（$T_{sim}$ 从 0.5s 到 6.0s）下的表现。
    *   **评估指标**：碰撞率（Collision Rate，衡量安全性）和 L2 距离（衡量路径遵循精度）。
*   **实验设置**：采用确定性规划策略（执行置信度最高的模态），以纯粹评估预测模型的内在安全性和鲁棒性。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了原生 PyTorch 实现的 GPU 加速仿真器，旨在提高训练效率并降低集成门槛（相比于基于 JAX 的 Waymax）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 nuScenes 上进行了 7 次独立运行（涵盖 3 个随机种子）以确保统计显著性。
    *   在 DeepScenario 的多个不同交叉口（Frankfurt, Wuppertal, Kronach 等）进行了泛化性测试。
*   **消融实验**：非常充分，涵盖了：
    1.  协方差矩阵旋转变换的影响。
    2.  同策略（On-policy）与异策略（Off-policy）的对比。
    3.  仿真可微性（Differentiability）的影响。
    4.  联合场景嵌入（Joint Embeddings）的作用。
    5.  训练回放时长（$T_{sim}$）和仿真掩码（$M_{sim}$）比例的调节。
*   **客观性**：通过在测试阶段统一使用日志回放（Log-replay）来消除场景预测器精度对评估结果的干扰，确保了对比的公平性。

### 6. 主要结论与发现
*   **高频优势**：闭环训练在**高频重规划**（$T_{sim} \le 1.0s$）下优势最明显。在 nuScenes 上碰撞率降低 27.0%，在 DeepScenario 上降低高达 79.5%。
*   **纠偏能力**：通过暴露在自诱导的漂移状态下，模型学会了从执行误差中恢复，而不会牺牲路径遵循性能（L2 距离保持稳定）。
*   **混合仿真最优**：反应式代理提升了短期交互安全性，而日志回放代理作为“行为锚点”，保证了长期的行为稳定性。
*   **非可微性更佳**：实验发现，在状态更新时切断计算图（非可微仿真）能防止模型利用“未来信息泄漏”走捷径，从而获得更稳健的预测。

### 7. 优点
*   **反应一致性**：通过目标导向的场景解码器解决了传统仿真中代理“视而不见”的问题。
*   **架构优雅**：采用 Query-centric Transformer 架构，实现了平移/旋转等变性，计算效率高。
*   **实用性强**：针对 MPC 风格的高频重规划进行了专门优化，更符合现代自动驾驶堆栈的实际需求。
*   **开源贡献**：提供了 PyTorch 原生的 GPU 仿真框架，对学术界更友好。

### 8. 不足与局限
*   **时界限制**：预测时界仅为 6 秒，对于极长距离的规划可能不足。
*   **规划器简化**：评估时使用了简单的确定性规划（选最高分模态），未考虑动态模态切换或更复杂的下游规划逻辑。
*   **地图静态性**：仿真器目前将地图元素视为静态，未考虑动态交通灯或道路拓扑变化对闭环的影响。
*   **算力细节缺失**：缺乏具体的训练资源消耗数据，难以评估该方法的训练成本。

（完）
