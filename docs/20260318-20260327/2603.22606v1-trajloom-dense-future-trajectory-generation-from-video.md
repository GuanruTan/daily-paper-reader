---
title: "TrajLoom: Dense Future Trajectory Generation from Video"
title_zh: TrajLoom：基于视频的密集未来轨迹生成
authors: "Zewei Zhang, Jia Jun Cheng Xian, Kaiwen Liu, Ming Liang, Hang Chu, Jun Chen, Renjie Liao"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22606v1"
tags: ["query:tp"]
score: 9.0
evidence: 密集未来轨迹生成与表示学习
tldr: 本研究提出TrajLoom框架，旨在从视频中预测密集的未来轨迹。针对长序列运动预测难题，该框架通过网格锚点偏移编码、时空潜空间VAE以及基于流匹配的轨迹生成技术，将预测长度从24帧显著提升至81帧。此外，研究还推出了TrajLoomBench基准测试。该方法在运动真实感和稳定性上超越了现有技术，并能有效支持视频生成与编辑等下游任务。
selection_source: fresh_fetch
motivation: 现有的密集点轨迹预测方法在处理长序列和复杂视频背景时，难以保持运动的真实感与时空一致性。
method: 采用网格锚点偏移编码减少位置偏差，利用VAE构建时空潜空间，并通过流匹配技术在潜空间内生成未来轨迹。
result: 在提出的TrajLoomBench基准上，该方法将轨迹预测长度扩展至81帧，并在真实感和采样稳定性方面达到SOTA水平。
conclusion: TrajLoom为长时密集轨迹预测提供了高效稳定的解决方案，显著增强了可控视频生成与理解的能力。
---

## 摘要
预测未来运动在视频理解和可控视频生成中至关重要。密集点轨迹是一种紧凑且具有表现力的运动表示，但从观测视频中建模其未来演变仍然具有挑战性。我们提出了一个框架，可以根据过去的轨迹和视频上下文预测未来的轨迹和可见性。我们的方法包含三个组件：(1) 网格锚点偏移编码（Grid-Anchor Offset Encoding），通过将每个点表示为其像素中心锚点的偏移量来减少位置依赖偏差；(2) TrajLoom-VAE，通过掩码重建和时空一致性正则化项，为密集轨迹学习一个紧凑的时空潜空间；(3) TrajLoom-Flow，通过流匹配（flow matching）在潜空间中生成未来轨迹，并结合边界线索和同策略（on-policy）K步微调以实现稳定采样。我们还引入了 TrajLoomBench，这是一个涵盖真实和合成视频的统一基准，其标准化设置与视频生成基准保持一致。与现有最先进的方法相比，我们的方法将预测范围从 24 帧扩展到 81 帧，同时提高了跨数据集的运动真实感和稳定性。预测的轨迹直接支持下游的视频生成和编辑。代码、模型权重和数据集可在 https://trajloom.github.io/ 获取。

## Abstract
Predicting future motion is crucial in video understanding and controllable video generation. Dense point trajectories are a compact, expressive motion representation, but modeling their future evolution from observed video remains challenging. We propose a framework that predicts future trajectories and visibility from past trajectories and video context. Our method has three components: (1) Grid-Anchor Offset Encoding, which reduces location-dependent bias by representing each point as an offset from its pixel-center anchor; (2) TrajLoom-VAE, which learns a compact spatiotemporal latent space for dense trajectories with masked reconstruction and a spatiotemporal consistency regularizer; and (3) TrajLoom-Flow, which generates future trajectories in latent space via flow matching, with boundary cues and on-policy K-step fine-tuning for stable sampling. We also introduce TrajLoomBench, a unified benchmark spanning real and synthetic videos with a standardized setup aligned with video-generation benchmarks. Compared with state-of-the-art methods, our approach extends the prediction horizon from 24 to 81 frames while improving motion realism and stability across datasets. The predicted trajectories directly support downstream video generation and editing. Code, model checkpoints, and datasets are available at https://trajloom.github.io/.

---

## 论文详细总结（自动生成）

### TrajLoom：基于视频的密集未来轨迹生成论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在视频理解和可控视频生成领域，运动（Motion）是核心要素。密集点轨迹（Dense Point Trajectories）能有效表达长程对应关系和遮挡模式。然而，现有的轨迹预测方法面临两大挑战：
*   **不确定性与多模态性：** 未来运动具有多种可能性，传统的确定性回归方法容易导致预测结果趋于保守或产生漂移。
*   **缺乏运动历史约束：** 现有生成式方法（如 WHN）主要依赖图像外观或文本提示，忽视了已观测到的运动历史对未来的强约束作用。
*   **位置偏差：** 使用绝对坐标表示轨迹会将运动与全局位置耦合，导致模型难以学习纯粹的运动规律。

**TrajLoom** 旨在通过结合视频上下文和运动历史，生成长时（达81帧）、稳定且具有时空一致性的密集未来轨迹。

#### 2. 方法论：核心思想与关键技术
该框架由三个核心组件构成：
*   **网格锚点偏移编码（Grid-Anchor Offset Encoding）：** 放弃绝对图像坐标，将每个轨迹点表示为其所在像素中心锚点的偏移量（$\Delta x, \Delta y$）。这种表示法消除了位置依赖的统计偏差，使模型更专注于局部位移。
*   **TrajLoom-VAE（时空潜空间学习）：**
    *   将高维的密集轨迹场压缩为紧凑的时空 Token。
    *   **时空一致性正则化项：** 在重建损失之外，引入了时间速度匹配和多尺度空间邻域关系匹配，强制要求重建轨迹在时间上平滑、在空间上局部协调，从而消除抖动。
*   **TrajLoom-Flow（基于流匹配的生成）：**
    *   在潜空间中使用整流操作（Rectified Flow）生成未来轨迹。
    *   **边界线索（Boundary Hints）：** 通过将未来预测的起始状态锚定在历史最后一帧，并注入 Token 级别的融合模块，确保预测与历史的无缝衔接。
    *   **同策略 K 步微调（On-policy K-step Fine-tuning）：** 针对 ODE 采样中的训练-测试不匹配问题，通过在模型自身生成的轨迹上进行多步滚动微调，减少长时预测的漂移。

#### 3. 实验设计
*   **数据集与基准：** 提出了 **TrajLoomBench**，整合了真实世界数据（Kinetics-700, RoboTAP）和合成数据（Kubric MOVi-A），并使用 MagicData 进行训练。
*   **对比方法：** 主要对比了目前最先进的生成式轨迹预测模型 **WHN (L)**。
*   **评价指标：** 
    *   **FVMD：** 衡量运动真实感和一致性。
    *   **FlowTV 与 DivCurlE：** 衡量轨迹场的空间撕裂感和局部稳定性。
    *   **VEPE：** 衡量 VAE 的重建精度。

#### 4. 资源与算力
论文明确列出了算力使用情况：
*   **TrajLoom-VAE 训练：** 使用 2 台 **RTX PRO 6000** GPU，有效 Batch Size 为 16，训练 3 万步。
*   **TrajLoom-Flow 训练：** 使用 4 台 **NVIDIA H100** GPU，有效 Batch Size 为 128，训练 10 万步。
*   **微调阶段：** 在上述基础上进行同策略微调，学习率为 $1 \times 10^{-5}$。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度对比：** 在四个不同性质的数据集（真实 vs 合成）上进行了定量对比。
*   **消融实验：** 详细验证了偏移编码、VAE 正则化项、边界锚定、Token 融合以及同策略微调对最终性能的贡献。
*   **采样分析：** 对比了不同 ODE 求解器（Euler vs. DOPRI5）和采样步数对结果的影响。
*   **下游应用：** 展示了预测轨迹如何驱动 Wan-Move 模型生成高质量的视频，证明了其实用价值。

#### 6. 主要结论与发现
*   **长时预测能力：** TrajLoom 将可靠的预测范围从 24 帧扩展到了 **81 帧**。
*   **偏移编码的必要性：** 实验证明，去除偏移编码会导致长时预测出现严重的撕裂和漂移。
*   **稳定性提升：** 相比 WHN，TrajLoom 在 FVMD 指标上提升了 2.5 到 3.6 倍，显著减少了空间不连续性。
*   **微调效果：** 同策略 K 步微调能有效抑制 ODE 积分过程中的累积误差。

#### 7. 优点
*   **创新性表示：** 提出的网格锚点偏移编码简单但极其有效，解决了轨迹预测中的位置偏差问题。
*   **时空约束严密：** 通过 VAE 的正则化和 Flow 模型的边界处理，保证了生成运动的物理合理性。
*   **基准统一：** 贡献了 TrajLoomBench，为该领域的后续研究提供了标准化的评估平台。
*   **应用导向：** 预测结果可直接用于视频编辑和生成，具有很强的工程落地潜力。

#### 8. 不足与局限
*   **依赖预训练追踪器：** 训练数据的质量高度依赖于前端追踪算法（如 AllTracker），若追踪器在复杂遮挡下失效，会影响模型学习。
*   **计算开销：** 相比于简单的回归模型，基于流匹配的生成过程涉及多次 ODE 迭代，推理成本相对较高。
*   **交互性限制：** 目前主要关注从历史自动预测未来，尚未深入探讨如何通过用户实时交互（如“拖拽”点）来动态干预生成过程。

（完）
