---
title: "GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories"
title_zh: GHOST：基于观测到的运动恢复结构轨迹的地面投影假设
authors: "Tomasz Frelek, Rohan Patil, Akshar Tumu, Henrik I. Christensen"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20583v1"
tags: ["query:tp"]
score: 9.0
evidence: 预测车辆的运动条件路径建议
tldr: 本研究提出GHOST，一种自监督的单目图像可行轨迹分割方法。通过利用大规模行车记录仪视频，将自车运动作为隐式监督，利用SfM恢复相机轨迹并投影至地面生成空间掩码，从而无需人工标注即可训练深度分割网络。该模型能从单张RGB图像预测运动条件下的路径建议，隐式捕捉场景布局和车道拓扑，在NuScenes等数据集上表现出良好的泛化性和跨平台迁移能力。
selection_source: fresh_fetch
motivation: 旨在解决复杂城市环境下自动驾驶轨迹预测对大规模人工标注数据的依赖问题。
method: 利用SfM从大规模视频中提取自车运动轨迹并投影至地面，生成自动标注以训练单目图像分割网络。
result: 在NuScenes数据集上实现了可靠的轨迹预测，并成功通过微调迁移至电动滑板车平台。
conclusion: 实验证明大规模自车运动蒸馏能使模型在无需显式建模的情况下，学习到结构化且具有泛化性的路径预测能力。
---

## 摘要
我们提出了一种可扩展的自监督方法，用于在复杂城市环境下从单目图像中分割自动驾驶的可行车辆轨迹。利用大规模行车记录仪视频，我们将记录的本车运动视为隐式监督，并通过单目运动恢复结构（SfM）恢复相机轨迹，将其投影到地面上，从而在无需人工标注的情况下生成已行驶区域的空间掩码。这些自动生成的标签被用于训练一个深度分割网络，该网络在运行时仅凭单张 RGB 图像即可预测受运动约束的路径提议，而无需对道路或车道线进行显式建模。该模型在多样化、无约束的网络数据上进行训练，隐式地捕捉了场景布局、车道拓扑和交叉口结构，并能泛化到不同的相机配置。我们在 NuScenes 数据集上评估了我们的方法，展示了可靠的轨迹预测能力，并进一步证明了通过轻量级微调将其迁移到电动滑板车平台的可行性。我们的结果表明，大规模本车运动蒸馏可以产生超出演示轨迹的结构化且可泛化的路径提议，从而实现通过图像分割进行轨迹假设估计。

## Abstract
We present a scalable self-supervised approach for segmenting feasible vehicle trajectories from monocular images for autonomous driving in complex urban environments. Leveraging large-scale dashcam videos, we treat recorded ego-vehicle motion as implicit supervision and recover camera trajectories via monocular structure-from-motion, projecting them onto the ground plane to generate spatial masks of traversed regions without manual annotation. These automatically generated labels are used to train a deep segmentation network that predicts motion-conditioned path proposals from a single RGB image at run time, without explicit modeling of road or lane markings. Trained on diverse, unconstrained internet data, the model implicitly captures scene layout, lane topology, and intersection structure, and generalizes across varying camera configurations. We evaluate our approach on NuScenes, demonstrating reliable trajectory prediction, and further show transfer to an electric scooter platform through light fine-tuning. Our results indicate that large-scale ego-motion distillation yields structured and generalizable path proposals beyond the demonstrated trajectory, enabling trajectory hypothesis estimation via image segmentation.

---

## 论文详细总结（自动生成）

以下是对论文《GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖昂贵的人工语义标注、高精地图（HD Maps）或校准传感器套件的情况下，让自动驾驶系统学习理解复杂城市环境中的可行行驶轨迹？
*   **研究动机**：
    *   **标注成本高**：传统的行驶区域分割和轨迹预测依赖大量人工标注。
    *   **数据潜力巨大**：互联网（如 YouTube）存在海量的行车记录仪视频，每一段视频本质上都是人类驾驶员在特定场景下对可行路径的一次“演示”。
    *   **自监督学习**：利用运动恢复结构（SfM）技术将视频中的自车运动转化为空间掩码，作为隐式监督信号来训练模型。

### 2. 方法论
GHOST 框架主要包含三个阶段：
*   **SfM 流水线**：
    *   使用 **COLMAP** 对无约束的单目视频进行处理，提取稀疏 3D 点云和 6-DoF 相机位姿。
    *   为了处理大规模数据，将长视频切分为 20 分钟的片段并行处理，并限制每帧特征点数量以提高效率。
*   **后处理与掩码生成**：
    *   **地面估计**：利用 **Theil-Sen 估计器**（一种鲁棒的非参数回归方法）对相机下方的 3D 点进行拟合，计算相机相对于地面的高度 $H_{cam}$。
    *   **轨迹投影**：假设车辆遵循运动学单车模型，将未来 5 秒的相机轨迹投影到地面，并根据相机高度赋予一定的物理宽度，生成空间轨迹掩码（GT Mask）。
*   **模型训练**：
    *   **架构**：采用标准的 UNet 结构，骨干网络可选 ResNet34 或 SegFormer。
    *   **非对称损失函数**：设计了一种加权的负对数似然损失，**加大对漏报（False Negatives）的惩罚**。这种设计鼓励模型预测出所有潜在的可行路径（多假设），而不仅仅是视频中实际行驶的那一条。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：从 YouTube 收集了 30 小时的多样化行车记录仪视频，涵盖 14 个城市、多种天气和光照条件，最终生成约 65 万帧标注数据。
    *   **评估**：使用 **NuScenes**（OpenLane-V2 Subset B）作为基准。该数据集包含高精地图，用于生成测试用的“可达车道”掩码。
    *   **迁移实验**：使用 GoPro 在电动滑板车上采集了 40 分钟数据进行微调。
*   **对比方法与指标**：
    *   对比了 ResNet34 和 SegFormer 两种骨干网络。
    *   使用 **Soft IoU**（Ruzicka 相似度）作为主要评价指标，衡量预测概率图与真实掩码的重合度。

### 4. 资源与算力
*   **算力说明**：文中提到使用 **GPU 加速的 Ceres Solver** 来优化 COLMAP 的计算。
*   **训练细节**：
    *   预训练：25 个 epoch，学习率 $3 \times 10^{-4}$，Adam 优化器。
    *   微调：10 个 epoch，学习率降至 $10^{-5}$。
*   **硬件型号**：文中**未明确指出**具体的 GPU 型号（如 A100 或 RTX 4090）及具体数量。

### 5. 实验数量与充分性
*   **实验规模**：使用了 65 万帧的大规模自建数据集进行训练，并在完全脱离训练分布（Out-of-distribution）的 NuScenes 数据集上验证，实验规模较大。
*   **充分性**：
    *   涵盖了不同骨干网络的对比。
    *   进行了**多假设场景**（Multi-Lane）的专项评估，证明模型在分叉路口能预测多条路径。
    *   通过电动滑板车的跨平台迁移实验，验证了方法的通用性。
*   **客观性**：评估时考虑了高精地图作为 GT 的局限性（如地图连接不全、动态障碍物遮挡等），分析较为客观。

### 6. 主要结论与发现
*   **自监督可行性**：仅通过单目视频的位姿蒸馏，模型就能隐式学习到车道拓扑、交叉口结构和避障逻辑。
*   **多假设预测**：通过非对称损失，模型不会坍缩到单一路径，而是能预测出场景中所有合理的行驶选择。
*   **强泛化性**：在左行车环境（美国）训练的模型，能直接泛化到右行车环境（新加坡）和完全不同的交通工具（滑板车）。

### 7. 优点
*   **高可扩展性**：无需人工标注，理论上可以利用无限的互联网视频数据。
*   **架构简单高效**：使用成熟的分割网络即可实现复杂的轨迹假设预测。
*   **跨平台潜力**：不依赖特定车辆的传感器校准，适用于从汽车到微出行（Micro-mobility）的多种设备。

### 8. 不足与局限
*   **几何约束限制**：SfM 在车辆直线行驶（低视差）时容易失效，导致部分数据无法生成有效掩码（约 35% 的数据被丢弃）。
*   **地面平坦假设**：高度估计依赖局部地面平坦的假设，在剧烈坡度变化（如山路、立交桥）时可能产生误差。
*   **动态物体干扰**：目前的 SfM 流程未显式建模动态物体，可能导致在拥堵交通中的轨迹重建出现偏差。
*   **评估噪声**：使用静态高精地图作为基准无法反映动态避障行为，可能导致评估指标无法完全体现模型性能。

（完）
