---
title: "Rectify, Don't Regret: Avoiding Pitfalls of Differentiable Simulation in Trajectory Prediction"
title_zh: 纠正而非后悔：规避轨迹预测中可微仿真的陷阱
authors: "Harsh Yadav, Christian Bohn, Tobias Meisen"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23393v1"
tags: ["query:tp"]
score: 10.0
evidence: 解决自动驾驶轨迹预测中的陷阱
tldr: 针对自动驾驶轨迹预测中开环模型易产生复合误差及闭环微分模拟器存在“捷径学习”导致非因果优化的缺陷，本文提出了一种分离式退避时界滚动方法。通过切断模拟步骤间的计算图，强制模型从漂移状态中学习真实的反应性恢复行为，而非利用未来信息修正过去。实验证明该方法在nuScenes等数据集上显著降低了碰撞率并提升了预测多样性。
selection_source: fresh_fetch
motivation: 现有的闭环微分模拟器存在梯度泄露问题，导致模型通过非因果方式“后悔”过去错误而非学习真实的纠错能力。
method: 提出分离式退避时界滚动框架，通过显式切断模拟步骤间的计算图，迫使模型在漂移状态下学习真实的恢复策略。
result: "在nuScenes和DeepScenario数据集上，该方法相比全微分闭环训练减少了高达33.24%的碰撞，相比开环基准减少了27.74%的碰撞。"
conclusion: 通过消除微分模拟中的非因果捷径，模型能够学习到更鲁棒的恢复行为，显著提升了复杂环境下的轨迹预测性能。
---

## 摘要
当前的开环轨迹模型在现实自动驾驶中面临挑战，因为微小的初始偏差往往会演变成复合误差，使智能体进入分布外（out-of-distribution）状态。虽然全可微闭环仿真器试图解决这一问题，但它们存在捷径学习（shortcut learning）的问题：损失梯度通过诱导的状态输入反向传播，无意中将未来的真值（ground truth）信息直接泄露到模型之前的预测中。模型利用这些信号人为地避免漂移，以非因果的方式“后悔”过去的错误，而不是学习真正的反应式恢复。为了解决这一问题，我们引入了一种分离式滚动时界展开（detached receding horizon rollout）。通过显式切断仿真步骤之间的计算图，模型从漂移状态中学习真实的恢复行为，迫使其“纠正”错误，而非非因果地优化过去的预测。在 nuScenes 和 DeepScenario 数据集上的广泛评估表明，我们的方法产生了更稳健的恢复策略，在高重规划频率下，与全可微闭环训练相比，目标碰撞率降低了高达 33.24%。此外，与标准的开环基准相比，我们的不可微框架在密集环境中将碰撞率降低了高达 27.74%，同时提高了多模态预测的多样性和车道对齐度。

## Abstract
Current open-loop trajectory models struggle in real-world autonomous driving because minor initial deviations often cascade into compounding errors, pushing the agent into out-of-distribution states. While fully differentiable closed-loop simulators attempt to address this, they suffer from shortcut learning: the loss gradients flow backward through induced state inputs, inadvertently leaking future ground truth information directly into the model's own previous predictions. The model exploits these signals to artificially avoid drift, non-causally "regretting" past mistakes rather than learning genuinely reactive recovery. To address this, we introduce a detached receding horizon rollout. By explicitly severing the computation graph between simulation steps, the model learns genuine recovery behaviors from drifted states, forcing it to "rectify" mistakes rather than non-causally optimizing past predictions. Extensive evaluations on the nuScenes and DeepScenario datasets show our approach yields more robust recovery strategies, reducing target collisions by up to 33.24% compared to fully differentiable closed-loop training at high replanning frequencies. Furthermore, compared to standard open-loop baselines, our non-differentiable framework decreases collisions by up to 27.74% in dense environments while simultaneously improving multi-modal prediction diversity and lane alignment.

---

## 论文详细总结（自动生成）

### 论文总结：纠正而非后悔——规避轨迹预测中可微仿真的陷阱

#### 1. 核心问题与整体含义（研究动机和背景）
传统的自动驾驶轨迹预测模型多在**开环（Open-loop）**数据集上训练，但在实际**闭环（Closed-loop）**部署时，微小的初始预测偏差会随时间累积成复合误差，导致车辆进入训练分布之外（OOD）的危险状态。
为了解决这一“开闭环鸿沟”，近期研究转向使用**全可微仿真器**进行闭环微调。然而，本文揭示了这种方法的一个严重缺陷：**捷径学习（Shortcut Learning）**。在全可微计算图中，损失函数的梯度可以穿透仿真环境回传，将未来的真值信息泄露给过去的预测。这导致模型学会了以非因果的方式“后悔”并修正过去的预测，而不是真正学习在发生漂移后如何“纠正”行为。

#### 2. 论文提出的方法论
核心思想是**“切断梯度，强制纠正”**。
*   **分离式退避时界滚动（Detached Receding Horizon Rollout）：** 在仿真过程中，模型预测全时界轨迹，但仿真器仅执行一小段（$H_{step}$），然后更新状态并重新查询模型。
*   **梯度分离（Gradient Detachment）：** 显式切断仿真步骤之间的计算图。通过使用非可微仿真器，阻止梯度从当前状态回传到前一时刻的预测。这使得过去的错误成为“既定事实”，迫使模型在当前漂移的状态下学习如何重新规划回到真值路径。
*   **LMFormer-D 架构：** 提出了一种纯解码器（Decoder-only）的 Transformer 架构。通过移除编码器层，大幅提升了计算效率，以支持高频的迭代闭环预测。
*   **优化目标：** 修改标准负对数似然（NLL）损失，将自生成的闭环样本与静态开环样本同等对待进行优化。

#### 3. 实验设计
*   **数据集：** 
    *   **nuScenes：** 提供完整的未来轨迹标注，适合闭环仿真评估。
    *   **DeepScenario (Busy Frankfurt)：** 用于模拟高密度的路口场景，测试模型在复杂交互下的安全性。
*   **Benchmark 与对比方法：**
    *   **OL (Open-Loop)：** 标准开环训练基准。
    *   **CL+Diff (Closed-Loop Differentiable)：** 使用全可微仿真器训练的模型。
    *   **CL+Non Diff (本文方法)：** 使用非可微、梯度分离框架训练的模型。
*   **评估指标：** 除了传统的 minADE、minFDE、MR（漏检率）外，重点引入了**碰撞率（Collision Rate）**和**越野率（OffRoad Rate）**。

#### 4. 资源与算力
*   **算力说明：** 论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率提升：** 文中强调了 LMFormer-D 的效率优势：相比基准 LMFormer，参数量减少了 **45%**，推理计算量（GFLOPS）降低了 **62%**。这种轻量化设计是实现高频闭环训练的前提。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 nuScenes 和 DeepScenario 两个数据集进行了详尽测试。
*   **重规划频率分析：** 实验覆盖了从 0.5s 到 6.0s 的多种重规划步长（$H_{step}$），验证了模型在不同部署频率下的鲁棒性。
*   **统计严谨性：** 所有核心实验均运行了 5 次（使用 3 个不同随机种子），并给出了均值和标准差（$\pm 1\sigma$），结果具有较高的客观性和说服力。
*   **消融实验：** 对比了 Decoder-only 架构与标准架构的性能差异，证明了轻量化并未牺牲精度。

#### 6. 主要结论与发现
*   **可微仿真的脆弱性：** CL+Diff 模型在训练步长与评估步长一致时表现尚可，但在高频重规划（步长减小）时性能会剧烈崩溃，证明其依赖非因果捷径而非真实恢复能力。
*   **非可微训练的鲁棒性：** 本文方法（CL+Non Diff）在高频重规划下表现极佳。在 nuScenes 上碰撞率降低了 **23.14%**，在 DeepScenario 密集场景下碰撞率降低了 **33.24%**。
*   **综合性能提升：** 相比开环基准，该方法不仅更安全（碰撞减少 27.74%），还提升了多模态预测的**多样性**和**车道对齐度**。

#### 7. 优点
*   **理论深刻：** 敏锐地指出了当前可微仿真训练中存在的“非因果后悔”逻辑缺陷。
*   **简单高效：** 无需复杂的正则化，仅通过梯度分离这一简洁的操作就解决了捷径学习问题。
*   **实用性强：** 提出的轻量化架构 LMFormer-D 在保持 SOTA 性能的同时显著降低了算力需求，更符合车载端部署要求。

#### 8. 不足与局限
*   **环境反应性有限：** 仿真中周围智能体采用的是 Log-replay（回放历史），即周围车辆不会对主车的错误行为做出反应，这与真实的完全交互式闭环仍有差距。
*   **算力细节缺失：** 缺乏具体的训练资源消耗记录，不便于其他研究者评估复现成本。
*   **应用范围：** 目前主要针对轨迹预测任务，尚未扩展到端到端的感知-规划全流程。

（完）
