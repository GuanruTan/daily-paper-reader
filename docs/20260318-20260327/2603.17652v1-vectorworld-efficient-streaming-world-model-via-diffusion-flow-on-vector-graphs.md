---
title: "VectorWorld: Efficient Streaming World Model via Diffusion Flow on Vector Graphs"
title_zh: VectorWorld：基于向量图扩散流的高效流式世界模型
authors: "Chaokang Jiang, Desen Zhou, Jiuming Liu, Kevin Li Sun"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17652v1"
tags: ["query:tp"]
score: 9.0
evidence: 用于自动驾驶策略和智能体矢量图切片的流式世界模型
tldr: 针对自动驾驶闭环仿真中存在的初始化不匹配、实时性差及长时运动学不合理等问题，本文提出VectorWorld。这是一个基于矢量图的流式世界模型，通过运动感知VAE对齐策略初始化，利用单步扩散流实现实时地图扩展，并引入物理对齐的NPC策略ΔSim。该模型在Waymo和nuPlan数据集上显著提升了地图保真度，支持超过1公里的稳定实时闭环仿真。
selection_source: fresh_fetch
motivation: 现有的生成式世界模型在闭环评估中面临初始化不匹配、采样延迟高以及长时仿真运动学失效的挑战。
method: 提出一种流式矢量图生成框架，结合运动感知VAE、单步扩散流DiT以及具有运动学约束的NPC策略ΔSim。
result: 在Waymo和nuPlan基准测试中，该方法在地图结构保真度和初始化有效性上表现优异，并实现了1公里以上的稳定闭环仿真。
conclusion: VectorWorld通过高效的矢量图生成和物理约束策略，为自动驾驶提供了一个实时、稳定且高保真度的交互式仿真环境。
---

## 摘要
自动驾驶策略的闭环评估需要超越日志回放的交互式仿真。然而，现有的生成式世界模型在闭环中往往会性能下降，原因在于：(i) 无历史信息的初始化与策略输入不匹配；(ii) 多步采样延迟违反了实时性预算；(iii) 长时界下运动学不可行性的累积。我们提出了 VectorWorld，这是一种流式世界模型，能够在推演过程中增量生成以自车为中心的 $64 \mathrm{m}\times 64\mathrm

## Abstract
Closed-loop evaluation of autonomous-driving policies requires interactive simulation beyond log replay. However, existing generative world models often degrade in closed loop due to (i) history-free initialization that mismatches policy inputs, (ii) multi-step sampling latency that violates real-time budgets, and (iii) compounding kinematic infeasibility over long horizons. We propose VectorWorld, a streaming world model that incrementally generates ego-centric $64 \mathrm{m}\times 64\mathrm{m}$ lane--agent vector-graph tiles during rollout. VectorWorld aligns initialization with history-conditioned policies by producing a policy-compatible interaction state via a motion-aware gated VAE. It enables real-time outpainting via solver-free one-step masked completion with an edge-gated relational DiT trained with interval-conditioned MeanFlow and JVP-based large-step supervision. To stabilize long-horizon rollouts, we introduce $Δ$Sim, a physics-aligned non-ego (NPC) policy with hybrid discrete--continuous actions and differentiable kinematic logit shaping. On Waymo open motion and nuPlan, VectorWorld improves map-structure fidelity and initialization validity, and supports stable, real-time $1\mathrm{km}+$ closed-loop rollouts (\href{https://github.com/jiangchaokang/VectorWorld}{code}).

---

## 论文详细总结（自动生成）

### VectorWorld: 基于向量图扩散流的高效流式世界模型论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的自动驾驶仿真依赖于日志回放（Log Replay），无法处理反事实查询或扩展超出记录范围的场景。现有的生成式世界模型在闭环仿真中面临三大挑战：
*   **初始化不匹配（冷启动）**：许多模型生成静态快照，而反应式策略需要运动历史，导致仿真开始时出现不稳定的瞬态（如抖动）。
*   **采样延迟高**：基于扩散的模型通常需要多步迭代，难以满足流式实时生成的需求。
*   **长时运动学失效**：微小的每步误差会随时间累积，导致长距离推演中出现违反物理规律的行为。
**VectorWorld** 旨在通过流式生成以自车为中心的向量图切片，实现高效、稳定且符合物理规律的公里级闭环仿真。

#### 2. 核心方法论
论文提出了一个由三个核心组件构成的流式框架：
*   **运动感知门控 VAE（Motion-aware Gated VAE）**：
    *   引入“交互状态”接口，通过门控机制选择性地编码智能体的运动历史。
    *   对于静止智能体抑制噪声，对于运动智能体保留动力学特征，为下游策略提供“热启动”输入。
*   **边门控关系 DiT（Edge-Gated Relational DiT）**：
    *   采用异构向量图表示（车道节点和智能体节点）。
    *   在 Transformer 中引入边条件偏置（Bias）和值门控（Value Gating），显式约束车道拓扑连接性和智能体-车道对齐关系。
*   **区间条件 MeanFlow 与 JVP 监督**：
    *   利用 MeanFlow 学习区间平均速度，支持**无求解器单步采样（Solver-free one-step）**。
    *   引入基于雅可比-向量积（JVP）的一阶修正项，在训练中监督大步长传输，确保单步生成的结构保真度。
*   **$\Delta$Sim 物理对齐 NPC 策略**：
    *   结合离散动作（k-disks）与连续残差修正。
    *   使用可微运动学代价函数对动作 Logit 进行形状调整（Shaping），并配合可微运动学对齐损失（DKAL）进行训练，减少长时漂移。

#### 3. 实验设计
*   **数据集**：Waymo Open Motion Dataset 和 nuPlan。
*   **Benchmark 与对比方法**：
    *   **离线生成对比**：与 SLEDGE (L/XL) 和 ScenDream (B/L) 等先进向量生成模型对比。
    *   **闭环仿真对比**：评估 IDM 驱动器和 PPO 策略在生成环境中的表现，对比 Ctrl-Sim。
*   **评价指标**：
    *   **离线**：FD（感知相似度）、路线长度、端点距离（EPD，衡量拓扑连续性）、初始碰撞率。
    *   **闭环**：碰撞率、越野率、成功率、路线进度、自车抖动（Jerk）。

#### 4. 资源与算力
论文明确列出了训练配置：
*   **GPU 型号与数量**：使用 **NVIDIA H20 GPU**。
*   **训练时长/规模**：
    *   VAE 编码器：8 张 GPU，训练 60,000 步。
    *   MeanFlow 生成器：8 张 GPU，训练 100,000 步。
    *   $\Delta$Sim 行为模型：4 张 GPU，训练 10,000 步。
*   **推理效率**：在单步模式下，生成一个 $64\mathrm{m} \times 64\mathrm{m}$ 的切片仅需约 **6ms**。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流大规模数据集上进行了验证，涵盖了从离线指标到闭环表现的全方位评估。
*   **消融实验**：非常充分。分别针对 VAE 的门控机制、DiT 的关系模块、MeanFlow 的 JVP 项以及 $\Delta$Sim 的各个组件进行了详细消融。
*   **客观性**：引入了“跨后端验证”，即在 VectorWorld 中训练的策略在日志回放和其他仿真器中也能表现提升，证明了实验的公平性和模型的泛化能力，而非仅仅过拟合于自身环境。

#### 6. 主要结论与发现
*   **高效性**：VectorWorld 实现了实时流式外推，单步生成质量接近多步扩散模型。
*   **稳定性**：通过交互状态初始化，显著降低了仿真初期的自车抖动（从 16.6 降至 9.6）。
*   **长时能力**：支持超过 **1 公里** 的稳定闭环推演，而传统模型在长距离下常因拓扑断裂或碰撞而失败。
*   **训练价值**：在 VectorWorld 中重新训练的 PPO 策略，在压力测试中的成功率从 25.0% 提升至 56.0%。

#### 7. 优点与亮点
*   **单步生成技术**：通过 MeanFlow 和 JVP 修正，解决了扩散模型实时性差的痛点。
*   **结构保真度**：边门控机制有效解决了向量图生成中常见的车道断裂和智能体“乱飞”问题。
*   **物理约束集成**：将运动学约束融入可微训练和推理，保证了长时仿真的真实感。

#### 8. 不足与局限
*   **地图元素覆盖**：目前主要基于中心线表示，尚未涵盖精细的道路边界、路缘几何或复杂的交通信号细节。
*   **微观分布偏差**：在某些细粒度指标（如 nuPlan 上的智能体间距 JSD）上，单步生成相比于极高步数的扩散模型仍有微小差距。
*   **极端工况**：$\Delta$Sim 虽然减少了物理违规，但在极端避障等高动态场景下的绝对物理准确性仍有提升空间。

（完）
