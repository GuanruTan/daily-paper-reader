---
title: "Think over Trajectories: Leveraging Video Generation to Reconstruct GPS Trajectories from Cellular Signaling"
title_zh: 轨迹深思：利用视频生成从蜂窝信令中重建 GPS 轨迹
authors: "Ruixing Zhang, Hanzhang Jiang, Leilei Sun, Liangzhe Han, Jibin Wang, Weifeng Lv"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26610v1"
tags: ["query:tp"]
score: 8.0
evidence: 重建GPS轨迹与轨迹表示学习
tldr: 本研究针对手机信令数据定位粗糙的问题，提出了Sig2GPS框架，将GPS轨迹重建任务创新性地转化为图像到视频的生成任务。该方法在地图视觉域内操作，将信令轨迹渲染为图像，并利用微调后的视频生成模型绘制连续的GPS路径。通过引入轨迹感知的强化学习优化，该方法在真实数据集上显著提升了重建精度，并展现出良好的跨城市迁移能力，为轨迹数据挖掘提供了全新的视觉生成范式。
selection_source: fresh_fetch
motivation: 手机信令数据覆盖广但定位精度低，传统重建方法依赖复杂工程管线或坐标回归，难以满足高精度应用需求。
method: 将信令轨迹重建重构为地图视觉域的视频生成任务，利用视频模型生成连续路径并引入强化学习奖励机制优化生成保真度。
result: 实验证明该方法在多个大规模真实数据集上优于强基线模型，并在下一位置预测和跨城市迁移中表现出极高的扩展性。
conclusion: 地图视觉视频生成技术能够有效利用地图约束，为从粗糙信令数据中恢复高精度连续轨迹提供了一种实用的新途径。
---

## 摘要
移动设备持续与蜂窝基站交互，产生海量的信令记录，为理解人类移动性提供了广泛的覆盖。然而，此类记录仅提供粗粒度的位置线索（例如服务小区标识符），因此限制了其在需要高精度 GPS 轨迹的应用中的直接使用。本文研究了 Sig2GPS 问题：从蜂窝信令中重建 GPS 轨迹。受领域专家通常将信令轨迹置于地图上并勾勒相应 GPS 路线的启发，不同于依赖复杂多阶段工程流水线或回归坐标的传统方案，Sig2GPS 被重新定义为一种直接在地图视觉领域操作的图像到视频生成任务：将信令轨迹渲染在地图上，并训练视频生成模型来绘制连续的 GPS 路径。为了支持这一范式，本文构建了一个成对的信令到轨迹视频数据集以微调开源视频模型，并引入了一种基于轨迹感知的强化学习优化方法，通过奖励来提高生成的保真度。在大规模真实世界数据集上的实验表明，该方法相较于强大的工程化和基于学习的基准模型有显著提升，而关于下一时刻 GPS 预测的额外结果则显示了其可扩展性和跨城市迁移能力。总之，这些结果表明，地图视觉视频生成通过在地图约束下实现连续路径的直接生成和细化，为轨迹数据挖掘提供了一个实用的接口。

## Abstract
Mobile devices continuously interact with cellular base stations, generating massive volumes of signaling records that provide broad coverage for understanding human mobility. However, such records offer only coarse location cues (e.g., serving-cell identifiers) and therefore limit their direct use in applications that require high-precision GPS trajectories. This paper studies the Sig2GPS problem: reconstructing GPS trajectories from cellular signaling. Inspired by domain experts often lay the signaling trace on the map and sketch the corresponding GPS route, unlike conventional solutions that rely on complex multi-stage engineering pipelines or regress coordinates, Sig2GPS is reframed as an image-to-video generation task that directly operates in the map-visual domain: signaling traces are rendered on a map, and a video generation model is trained to draw a continuous GPS path. To support this paradigm, a paired signaling-to-trajectory video dataset is constructed to fine-tune an open-source video model, and a trajectory-aware reinforcement learning-based optimization method is introduced to improve generation fidelity via rewards. Experiments on large-scale real-world datasets show substantial improvements over strong engineered and learning-based baselines, while additional results on next GPS prediction indicate scalability and cross-city transferability. Overall, these results suggest that map-visual video generation provides a practical interface for trajectory data mining by enabling direct generation and refinement of continuous paths under map constraints.

---

## 论文详细总结（自动生成）

以下是对论文《Think over Trajectories: Leveraging Video Generation to Reconstruct GPS Trajectories from Cellular Signaling》的结构化深入总结：

### 1. 核心问题与整体含义
*   **核心问题**：如何从覆盖范围广但空间分辨率粗糙的**蜂窝信令数据**（Cellular Signaling，仅记录连接的基站ID）中，重建出高精度的**GPS轨迹**（Sig2GPS任务）。
*   **研究动机**：
    *   **传统方法局限**：现有的工业级方案通常依赖复杂的多阶段工程管线（如卡尔曼滤波、地图匹配、路径推理），延迟高且难以自动化。
    *   **人类直觉启发**：领域专家在处理此类数据时，通常会将信令点标在地图上，通过视觉观察“勾勒”出可能的路径。
    *   **视觉生成潜力**：受视频生成模型在几何推理（如走迷宫）方面表现的启发，论文提出将轨迹重建视为一个**图像到视频的生成任务**，在地图视觉域内直接“画”出轨迹。

### 2. 方法论
*   **核心思想**：提出“Think Over Trajectories”范式。将信令轨迹渲染在地图背景上作为输入图像，训练视频模型生成一段展示GPS路径逐渐绘制过程的视频。
*   **关键技术细节**：
    1.  **数据对构建**：通过时空一致性准则，将匿名化的出租车GPS轨迹与蜂窝信令记录进行跨系统关联，构建了2万对高置信度的“信令-GPS”样本。
    2.  **监督微调（SFT）**：基于开源的流匹配（Flow-matching）视频生成模型（Wan2.1-TI2V-5B），使用LoRA技术进行微调，使模型学会根据地图背景和信令线索生成初步视频。
    3.  **轨迹感知强化学习（Traj-GDPO）**：为了修正SFT模型可能出现的断裂、逆行或偏离道路等细微错误，引入了基于可验证奖励的强化学习：
        *   **距离奖励（Distance Reward）**：计算生成路径点与真实点之间的测地距离。
        *   **方向奖励（Direction Reward）**：利用余弦相似度衡量路径的整体行进方向。
        *   **连续性奖励（Continuity Reward）**：惩罚生成路径中的断裂或多余端点。
    4.  **优化算法**：采用分组解耦策略优化（GDPO），对不同量纲的奖励进行归一化处理，并锚定初始SFT模型进行KL散度约束，防止模型崩溃。

### 3. 实验设计
*   **数据集**：
    *   **Sig2GPS任务**：使用在北京收集的10,000对信令-GPS数据，按轨迹长度分为小、中、大三个子集。
    *   **下一位置预测任务**：使用成都和西安的滴滴盖亚（GAIA）公开数据集进行跨城市迁移验证。
*   **Benchmark与对比方法**：
    *   **深度学习基准**：GRU, MLP, TCN, GPT。
    *   **轨迹专用模型**：TrajFormer, SigFormer。
    *   **工业级基准**：`Rule_sig`（一套包含卡尔曼滤波和地图匹配的复杂工程管线）。
    *   **预测任务基准**：DeepMove, AgentMove, STAN, Traj-MLLM等。

### 4. 资源与算力
*   **算力配置**：使用了 **4张 NVIDIA RTX 4090 GPU**。
*   **分配细节**：3张显卡用于模型训练，1张显卡专门用于计算与初始SFT模型之间的KL散度。
*   **模型规模**：基于5B参数量的视频生成模型，采用LoRA（秩为28）进行高效微调。
*   **推理效率**：单条轨迹处理时间约30秒，显著优于工业级管线的2分钟以上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在1000条测试轨迹上进行了性能评估。
    *   进行了**测试时缩放（Test-time Scaling）**实验，验证了增加视频帧数对精度的提升。
    *   进行了**消融实验**，分别验证了强化学习阶段、方向奖励以及KL约束策略的有效性。
    *   进行了**跨城市迁移实验**（西安模型预测成都轨迹）。
*   **充分性评价**：实验设计较为全面，涵盖了重建精度、推理效率、泛化能力和消融分析，对比了从简单RNN到最新大模型及工业方案的多种基准，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：在所有评估指标（MAE, RMSE, L100等）上，该方法均显著优于现有最强基线。
*   **视觉推理优势**：视频生成模型能够自然地捕捉地图拓扑约束（如靠右行驶、转弯减速），生成的轨迹更符合物理直觉。
*   **泛化性强**：模型在未见过的城市（跨城市测试）上依然表现出极强的鲁棒性，证明其学到的是通用的地图-路径关联规律。
*   **效率提升**：将复杂的多阶段工程问题简化为单步生成问题，大幅缩短了工业部署中的处理延迟。

### 7. 优点
*   **范式创新**：首次将轨迹重建定义为视频生成任务，避开了复杂的坐标回归和硬编码的地图匹配规则。
*   **端到端设计**：统一了输入和输出的视觉空间，减少了信息在不同模态转换过程中的损失。
*   **强化学习闭环**：通过可验证的物理奖励（距离、方向）指导生成，有效解决了生成模型常见的“幻觉”或不连贯问题。

### 8. 不足与局限
*   **信令稀疏性挑战**：论文提到未来需进一步研究在信令极度稀疏情况下的鲁棒性。
*   **地图样式依赖**：模型性能可能受输入地图瓦片样式（如颜色、标注）的影响，跨地图源的适配性有待验证。
*   **计算成本**：虽然比传统工程管线快，但相比于极轻量级的MLP或GRU，视频生成的推理成本（显存占用和计算量）仍然较高。

（完）
