---
title: Learning Dynamic Belief Graphs for Theory-of-mind Reasoning
title_zh: 学习用于心理理论推理的动态信念图
authors: "Ruxiao Chen, Xilei Zhao, Thomas J. Cova, Frank A. Drews, Susu Xu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20170v1"
tags: ["query:tp"]
score: 8.0
evidence: 用于推断潜在信念和行动的认知轨迹模型
tldr: 针对大语言模型在灾难响应等高风险动态环境下心理理论（ToM）推理不足的问题，本文提出一种结构化认知轨迹模型。该模型将心理状态表示为动态信念图，能够联合推断潜在信念及其随时间变化的依赖关系，并将信念演化与决策过程相链接。通过引入文本到概率图模型的投影、能量因子图表示及ELBO优化目标，该方法在灾难疏散任务中显著提升了行动预测准确性，并提供了可解释的推理轨迹。
selection_source: fresh_fetch
motivation: 现有大语言模型在处理动态环境下的心理理论推理时，往往将信念视为静态或独立的，导致心理模型不连贯且推理能力较弱。
method: 提出一种基于动态信念图的结构化模型，利用能量因子图捕捉信念间的依赖关系，并通过ELBO目标函数优化信念积累与决策过程。
result: 在多个真实灾难疏散数据集上的实验表明，该模型显著提高了行动预测性能，并能生成符合人类逻辑的可解释信念轨迹。
conclusion: 本研究为在高不确定性环境下增强大语言模型的心理理论推理能力提供了一个有效且具有可解释性的模块化框架。
---

## 摘要
使用大语言模型（LLMs）进行心理理论（ToM）推理，需要推断人们隐式的、不断演化的信念如何塑造他们在不确定性下的信息寻求和行动方式——特别是在灾难响应、急诊医学和人机协作自主系统等高风险场景中。先前的方法要么直接提示 LLMs，要么使用将信念视为静态且独立的隐状态模型，这往往会导致随时间产生不连贯的心理模型，并在动态语境中表现出较弱的推理能力。我们为基于 LLM 的 ToM 引入了一种结构化认知轨迹模型，该模型将心理状态表示为动态信念图，共同推断潜在信念，学习其随时间变化的依赖关系，并将信念演化与信息寻求和决策联系起来。我们的模型贡献包括：(i) 一种从文本化概率陈述到一致的概率图模型更新的新型投影；(ii) 一种基于能量的因子图表示，用于描述信念间的相互依赖关系；(iii) 一个基于 ELBO 的目标函数，用于捕捉信念积累和延迟决策。在多个真实世界的灾难疏散数据集上，我们的模型显著提高了行动预测能力，并恢复了与人类推理一致的可解释信念轨迹，为在高不确定性环境中增强 LLM 的 ToM 能力提供了一个原则性的模块。

## Abstract
Theory of Mind (ToM) reasoning with Large Language Models (LLMs) requires inferring how people's implicit, evolving beliefs shape what they seek and how they act under uncertainty -- especially in high-stakes settings such as disaster response, emergency medicine, and human-in-the-loop autonomy. Prior approaches either prompt LLMs directly or use latent-state models that treat beliefs as static and independent, often producing incoherent mental models over time and weak reasoning in dynamic contexts. We introduce a structured cognitive trajectory model for LLM-based ToM that represents mental state as a dynamic belief graph, jointly inferring latent beliefs, learning their time-varying dependencies, and linking belief evolution to information seeking and decisions. Our model contributes (i) a novel projection from textualized probabilistic statements to consistent probabilistic graphical model updates, (ii) an energy-based factor graph representation of belief interdependencies, and (iii) an ELBO-based objective that captures belief accumulation and delayed decisions. Across multiple real-world disaster evacuation datasets, our model significantly improves action prediction and recovers interpretable belief trajectories consistent with human reasoning, providing a principled module for augmenting LLMs with ToM in high-uncertainty environment. https://anonymous.4open.science/r/ICML_submission-6373/

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Dynamic Belief Graphs for Theory-of-mind Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：心理理论（Theory of Mind, ToM）是理解他人潜在心理状态（信念、意图等）并预测其行为的能力。在灾难响应、医疗急救等高风险动态环境下，人类的信念是**相互关联且随时间演化**的。
*   **核心问题**：现有的基于大语言模型（LLM）的 ToM 推理方法存在两大缺陷：
    1.  **静态性与独立性**：通常将信念视为彼此独立且静态的变量，忽略了信念间的逻辑一致性和时间积累效应。
    2.  **缺乏因果约束**：直接通过提示词（Prompting）推断出的信念往往存在语义漂移或事后合理化，无法真正解释观察到的行动。
*   **整体含义**：本文旨在构建一个结构化的认知轨迹模型，将心理状态建模为**动态信念图**，使 LLM 能够以一种逻辑连贯、行为负责（Accountable）的方式进行深度推理。

### 2. 方法论：核心思想与技术细节
模型将人类认知建模为一个序列决策过程，核心组件包括：
*   **动态信念图（Dynamic Belief Graph）**：使用马尔可夫随机场（MRF）表示 $K$ 个二元信念。通过能量函数 $E_\theta(b_t)$ 捕捉信念的演化。
*   **语义到势能投影（Semantic-to-Potential Projection）**：
    *   利用冻结的 LLM 提取观测值和历史信念的语义嵌入。
    *   设计了一个投影层，将这些嵌入映射为图模型的**一元势能**（锚定语义方向，防止语义翻转）和**二元势能**（学习信念间的相互增强或抑制关系）。
*   **动作生成模型**：采用**动作特定自注意力机制（Action-specific Self-attention）**。它将不同信念的边际概率与动作嵌入结合，捕捉信念组合如何非线性地驱动最终决策。
*   **变分学习目标（ELBO）**：
    *   由于信念是不可观测的潜在变量，引入推理模型 $q_\phi$（近似后验）。
    *   通过最大化证据下界（ELBO）进行训练，包含**动作重构损失**（确保信念能解释行动）和**KL 散度正则项**（确保推理结果与时间演化先验一致）。

### 3. 实验设计
*   **数据集与场景**：使用了两个真实世界的野火疏散调查数据集（**Kincade Fire** 和 **Marshall Fire**）。这些数据包含居民在火灾期间的观测信息、中间行动、最终疏散决策以及事后自我报告的心理状态评分。
*   **对比方法（Benchmarks）**：
    *   **AutoToM**：基于 LLM 采样假设和贝叶斯逆向规划的方法。
    *   **Model Reconciliation**：基于 LLM 提示词的事后解释方法。
    *   **FLARE**：结合行为理论（PADM）与 LLM 的推理框架。
    *   **LLM Prior**：直接使用 LLM 原始先验进行推理。

### 4. 资源与算力
*   **模型后端**：使用了 **Qwen-8B** 模型作为提取语义嵌入的后端。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。但提到 LLM 部分是冻结的，仅训练顶层的投影层、势能函数和动作模型，这表明其训练算力需求远低于全参数微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同年份、不同地点的野火数据集上进行了验证。
    *   进行了**行动预测准确率**实验。
    *   进行了**信念恢复质量**实验（使用 Spearman 相关系数对比模型预测信念与人类真实评分）。
    *   进行了**消融实验**：分别移除“二元相互作用”和“时间维度”，验证了各组件对结构学习和时间一致性的贡献。
*   **充分性评价**：实验设计较为充分且客观。特别地，作者利用了调查数据中人类自述的信念作为“代理真值”（Proxy Ground Truth），这在潜在变量模型评估中是非常严谨的做法。

### 6. 主要结论与发现
*   **预测性能提升**：该模型在预测中间行动和最终疏散决策方面显著优于所有基线模型。
*   **信念结构恢复**：模型不仅能准确预测单个信念的变化，还能成功恢复信念之间的协变结构（例如：对房屋的担忧通常与对邻里的担忧正相关）。
*   **无需信念监督**：模型在仅有“行动轨迹”监督的情况下，能够学习到具有解释性的潜在信念轨迹，证明了 ELBO 框架在 ToM 推理中的有效性。

### 7. 优点
*   **神经符号结合**：巧妙地将 LLM 的强大语义理解能力与概率图模型的结构化约束结合。
*   **可解释性**：动态信念图提供了透明的心理状态演化路径，允许研究者审计 AI 的推理过程。
*   **行为接地（Grounding）**：通过变分推理确保了推断出的心理状态与实际观察到的行为在逻辑上是自洽的。

### 8. 不足与局限
*   **信念维度限制**：实验中信念维度 $K=6$ 是基于特定心理学量表预定义的，对于更复杂的开放域场景，如何自动发现信念维度仍是挑战。
*   **依赖 LLM 嵌入质量**：模型性能高度依赖于后端 LLM 提取语义特征的准确性，若 LLM 对特定语境理解有偏，会传导至势能计算。
*   **应用场景单一**：目前仅在野火疏散这一特定高风险场景下验证，其在医疗、金融等其他动态决策领域的泛化能力有待进一步测试。

（完）
