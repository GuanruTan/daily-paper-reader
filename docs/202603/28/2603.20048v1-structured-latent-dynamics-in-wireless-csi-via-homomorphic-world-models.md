---
title: Structured Latent Dynamics in Wireless CSI via Homomorphic World Models
title_zh: 通过同态世界模型实现无线 CSI 中的结构化潜动力学
authors: "Salmane Naoumi, Mehdi Bennis, Marwa Chafii"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20048v1"
tags: ["query:tp"]
score: 8.0
evidence: 从轨迹中学习预测性和结构化表示
tldr: 本研究提出一种自监督框架，通过世界模型学习无线信道状态信息（CSI）的结构化潜在动力学。利用联合嵌入预测架构（JEPA）和基于李代数的同态更新，该方法能从CSI轨迹中学习动作条件的潜在表示。实验证明其在拓扑保持和未来嵌入预测方面优于基准，为无线场景感知和定位等下游任务提供了具有几何一致性的基础。
selection_source: fresh_fetch
motivation: 旨在解决无线信道状态信息（CSI）在复杂移动场景下难以建模和预测其结构化演化规律的问题。
method: 采用联合嵌入预测架构（JEPA）并结合李代数参数化的同态更新，在紧凑的潜在空间中建模受动作影响的信道动力学。
result: 在DICHASUS数据集上的评估显示，该方法在保持空间拓扑结构和预测未知环境中的未来嵌入方面显著优于现有基准。
conclusion: 该研究通过构建结构化潜在空间，为移动感知调度、定位和无线场景理解提供了一个可扩展且具有度量保真度的基础框架。
---

## 摘要
我们引入了一个自监督框架，通过在紧凑的潜空间中建模信道状态信息 (CSI) 的时间演化，来学习无线信道的预测性和结构化表示。我们的方法将该问题视为一个世界建模任务，并利用联合嵌入预测架构 (JEPA) 从 CSI 轨迹中学习动作条件下的潜动力学。为了提升几何一致性和组合性，我们使用源自李代数 (Lie algebra) 的同态更新对转换进行参数化，从而产生一个反映空间布局和用户运动的结构化潜空间。在 DICHASUS 数据集上的评估表明，我们的方法在保持拓扑结构和预测未知环境中的未来嵌入方面优于强基准模型。由此产生的潜空间实现了度量忠实的信道图谱，为移动感知调度、定位和无线场景理解等下游应用提供了可扩展的基础。

## Abstract
We introduce a self-supervised framework for learning predictive and structured representations of wireless channels by modeling the temporal evolution of channel state information (CSI) in a compact latent space. Our method casts the problem as a world modeling task and leverages the Joint Embedding Predictive Architecture (JEPA) to learn action-conditioned latent dynamics from CSI trajectories. To promote geometric consistency and compositionality, we parameterize transitions using homomorphic updates derived from Lie algebra, yielding a structured latent space that reflects spatial layout and user motion. Evaluations on the DICHASUS dataset show that our approach outperforms strong baselines in preserving topology and forecasting future embeddings across unseen environments. The resulting latent space enables metrically faithful channel charts, offering a scalable foundation for downstream applications such as mobility-aware scheduling, localization, and wireless scene understanding.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于**同态世界模型（Homomorphic World Models）**的自监督框架，旨在通过无线信道状态信息（CSI）学习具有几何结构和预测能力的潜在表示。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在复杂的无线环境中，将高维、多噪的 CSI 数据压缩为低维且具有物理意义（如反映空间位置和运动规律）的潜在表示，并实现对未来信道状态的准确预测。
*   **研究动机**：
    *   传统的信道图谱（Channel Charting）方法多为静态，缺乏时间感知能力，且依赖手工设计的相似性度量。
    *   现有的无线世界模型（World Models）虽能辅助决策，但其潜在空间往往缺乏明确的几何结构，导致在处理复杂运动序列时泛化性较差。
    *   需要一种能够捕捉信道演化物理规律（如平移对称性）的结构化学习方法。

### 2. 方法论
*   **核心思想**：结合**联合嵌入预测架构（JEPA）**与**李代数（Lie Algebra）**，将用户运动（速度）建模为潜在空间中的同态变换。
*   **关键技术细节**：
    *   **JEPA 架构**：采用教师-学生网络结构。在线编码器学习当前 CSI 的嵌入，目标编码器（通过 EMA 更新）提供未来状态的参考，预测器在潜在空间中执行前向预测。
    *   **同态潜在动力学**：这是本文的核心创新。模型不使用通用的神经网络（如 MLP）来预测状态转移，而是学习一个映射函数，将动作（速度 $a_t$）映射到李代数空间，通过**矩阵指数（Matrix Exponential）**生成变换矩阵 $\rho(a_t) = \exp(\phi(a_t))$。
    *   **代数组合性**：这种设计确保了潜在空间的变换具有组合性，即连续动作的效果等同于变换矩阵的乘积，从而保证了几何一致性。
    *   **损失函数**：综合了教师逼近损失（$L_{TF}$）、多步预测损失（$L_{roll}$）、VICReg 正则化（防止潜在空间崩溃）以及逆动力学建模损失（$L_{IDM}$，增强动作感知）。

### 3. 实验设计
*   **数据集**：使用 **DICHASUS 数据集**，包含室内工业环境下的宽带 CSI 测量数据（4 个基站，每个基站 8 根天线，1024 个子载波）。
*   **实验场景**：在 cf02、cf03、cf07 子集上训练，在 cf04、cf05、cf06 子集上测试，以验证模型对未见轨迹的泛化能力。
*   **Benchmark（基准方法）**：
    *   **MLP 预测器**：无约束的非线性映射。
    *   **FiLM**：基于特征线性调制的预测器。
    *   **GRU**：循环神经网络预测器。
*   **评估指标**：可信度（TW）、连续性（CT）、Kruskal 应力（KS）和 Rajski 距离（RD），用于衡量潜在空间与物理空间的拓扑一致性。

### 4. 资源与算力
*   **算力说明**：论文提到使用了纽约大学阿布扎比分校（NYU Abu Dhabi）的高性能计算（HPC）资源。
*   **具体细节**：文中**未明确列出具体的 GPU 型号和数量**。训练参数包括：100 个 epoch，AdamW 优化器，初始学习率 $10^{-4}$，Batch Size 为 128。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了超过 25,000 个样本进行训练。
    *   对比了四种不同的预测器架构。
    *   进行了**消融实验**，分别验证了 VICReg 正则化和 IDM 损失对模型性能的贡献。
*   **充分性评价**：实验设计较为充分。通过在不同轨迹子集上的交叉验证，证明了模型不仅能“记住”训练数据，还能泛化到新的运动路径。可视化结果（PCA 投影与 Procrustes 分析）直观地展示了潜在空间与物理轨迹的高度吻合。

### 6. 主要结论与发现
*   **同态约束的优越性**：引入李代数结构的同态转换模型在所有度量指标上均优于 MLP、FiLM 和 GRU，证明了结构化先验在建模物理动力学中的重要性。
*   **拓扑保持能力**：该模型生成的潜在空间（信道图谱）具有极高的度量忠实度，能够准确反映用户的物理位移。
*   **自监督预测**：模型仅通过 CSI 序列和速度信息即可学习到稳健的表示，无需显式的空间标签（如 GPS 坐标）。

### 7. 优点
*   **理论优雅**：将抽象的代数结构（李群/李代数）引入无线信道建模，解决了潜在空间变换的几何一致性问题。
*   **端到端自监督**：无需人工标注，直接从原始 CSI 轨迹中提取特征。
*   **泛化性强**：在未见过的环境和轨迹上表现稳定，适合动态变化的无线场景。

### 8. 不足与局限
*   **环境假设**：目前假设环境是静态的（仅用户移动），未充分考虑动态散射体（如移动的车辆或行人）对信道动力学的影响。
*   **计算复杂度**：矩阵指数运算和高维矩阵乘法在实时嵌入式设备上的计算开销和延迟尚未得到详细评估。
*   **应用范围**：虽然展示了良好的图谱效果，但尚未在具体的下游任务（如波束成形优化或越区切换预测）中验证其最终的系统级增益。

（完）
