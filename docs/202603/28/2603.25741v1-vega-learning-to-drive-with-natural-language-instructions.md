---
title: "Vega: Learning to Drive with Natural Language Instructions"
title_zh: Vega：基于自然语言指令的学习驾驶
authors: "Sicheng Zuo, Yuxuan Li, Wenzhao Zheng, Zheng Zhu, Jie Zhou, Jiwen Lu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25741v1"
tags: ["query:tp"]
score: 9.0
evidence: 包含驾驶指令和对应轨迹的数据集；视觉-语言-世界-动作模型
tldr: 本研究针对现有自动驾驶模型缺乏遵循多样化用户指令能力的问题，提出了Vega模型。首先构建了包含10万个场景的大规模指令驾驶数据集InstructScene。Vega采用统一的视觉-语言-世界-动作架构，结合自回归模型处理多模态输入，并利用扩散模型生成未来预测和行驶轨迹。实验证明，Vega在规划性能和指令遵循方面表现优异，为实现智能化和个性化驾驶提供了新路径。
selection_source: fresh_fetch
motivation: 现有自动驾驶模型多将语言用于场景描述，缺乏根据用户多样化指令进行个性化驾驶的能力。
method: 提出了Vega模型，结合自回归范式处理视听输入与扩散范式生成预测及轨迹，并构建了InstructScene数据集。
result: 实验结果显示Vega在自动驾驶规划性能上达到领先水平，并展现出极强的指令遵循能力。
conclusion: 该研究通过整合多模态指令遵循能力，为开发更智能、更个性化的自动驾驶系统奠定了基础。
---

## 摘要
视觉-语言-动作模型重塑了自动驾驶，将语言整合到决策过程中。然而，大多数现有流程仅利用语言模态进行场景描述或推理，缺乏遵循多样化用户指令以实现个性化驾驶的灵活性。为解决这一问题，我们首先构建了一个大规模驾驶数据集（InstructScene），包含约 100,000 个场景，并标注了多样化的驾驶指令及其对应的轨迹。随后，我们提出了一种统一的视觉-语言-世界-动作模型 Vega，用于基于指令的生成与规划。我们采用自回归范式处理视觉输入（视觉）和语言指令（语言），并采用扩散范式生成未来预测（世界建模）和轨迹（动作）。我们通过联合注意力机制实现模态间的交互，并为不同模态使用独立的投影层以增强能力。大量实验表明，我们的方法不仅实现了卓越的规划性能，还展现出强大的指令遵循能力，为更智能、更个性化的驾驶系统铺平了道路。

## Abstract
Vision-language-action models have reshaped autonomous driving to incorporate languages into the decision-making process. However, most existing pipelines only utilize the language modality for scene descriptions or reasoning and lack the flexibility to follow diverse user instructions for personalized driving. To address this, we first construct a large-scale driving dataset (InstructScene) containing around 100,000 scenes annotated with diverse driving instructions with the corresponding trajectories. We then propose a unified Vision-Language-World-Action model, Vega, for instruction-based generation and planning. We employ the autoregressive paradigm to process visual inputs (vision) and language instructions (language) and the diffusion paradigm to generate future predictions (world modeling) and trajectories (action). We perform joint attention to enable interactions between the modalities and use individual projection layers for different modalities for more capabilities. Extensive experiments demonstrate that our method not only achieves superior planning performance but also exhibits strong instruction-following abilities, paving the way for more intelligent and personalized driving systems.

---

## 论文详细总结（自动生成）

以下是对论文《Vega: Learning to Drive with Natural Language Instructions》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型在自动驾驶中主要用于场景描述或逻辑推理，但在遵循多样化、开放式的用户指令（如“超车”、“靠边停车”）方面表现欠缺。
*   **核心问题**：大多数模型仅能模仿平均专家策略或执行简单的导航命令（如“左转”），无法处理个性化的驾驶需求。此外，高维视觉输入与低维动作输出之间存在巨大的信息差异，导致模型难以学习从高层指令到底层动作的映射。
*   **整体含义**：本文旨在将自动驾驶从“模仿驾驶”提升为“指令驾驶”，使车辆能够理解并执行复杂的自然语言指令。

### 2. 方法论
*   **核心思想**：提出一种统一的**视觉-语言-世界-动作模型（Vega）**，通过引入“世界模型”（预测未来图像）作为密集监督信号，增强模型对指令与动作之间因果关系的理解。
*   **关键技术细节**：
    *   **InstructScene 数据集**：构建了包含约 10 万个场景的数据集，利用强大的 VLM（Qwen2.5-VL）结合基于规则的方法，自动生成多样化的驾驶指令和对应轨迹。
    *   **混合架构**：采用**自回归（AR）**范式处理视觉理解和指令解析，采用**扩散（Diffusion）**范式生成未来图像预测（世界建模）和行驶轨迹（动作规划）。
    *   **混合变换器（MoT）**：为了解耦不同模态的参数，设计了 MoT 架构，为视觉、文本和动作分配独立的 Transformer 权重，提高模型容量。
    *   **因果注意力机制**：在序列建模中确保信息流从指令到动作，再到视觉结果，符合驾驶的逻辑链条。
*   **算法流程**：输入历史图像、历史动作和当前指令 -> AR 模块编码理解 -> 扩散模块联合去噪生成未来动作序列 $A_t$ 和未来图像 $I_{t+1}$。

### 3. 实验设计
*   **数据集/场景**：主要基于 **NAVSIM v1**（非反应式仿真）和 **NAVSIM v2**（反应式交通仿真）基准。
*   **Benchmark**：使用 PDMS（预测驾驶模型得分）和 EPDMS（扩展得分）作为核心评价指标，涵盖碰撞率、车道保持、舒适度等。
*   **对比方法**：对比了 TransFuser、HydraMDP++、DriveVLA-W0、AutoVLA、DiffusionDrive 等 SOTA（先进）方法。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练细节**：训练时长为 20 万步（200k steps），学习率为 2e-5，采用单阶段训练范式。
*   **标注工具**：使用 Qwen2.5-VL-72B-Instruct 进行大规模数据标注。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流基准（NAVSIM v1/v2）上进行了全面测试。
    *   **消融实验**：针对“未来图像预测任务”、“图像-动作交织序列长度”、“独立动作专家模块”等关键设计进行了对比验证。
    *   **定性分析**：展示了在同一场景下，给定不同指令（如“加速”与“减速”）时，模型生成的不同轨迹和预测图像。
*   **充分性评价**：实验设计较为充分，不仅有量化指标对比，还通过可视化证明了模型在处理开放式指令时的灵活性，对比实验涵盖了当前主流的 VLA 和世界模型方案，具有较高的客观性。

### 6. 主要结论与发现
*   **密集监督的有效性**：未来图像预测（世界建模）提供的像素级监督信号显著提升了规划性能，解决了动作监督信号稀疏的问题。
*   **性能领先**：Vega 在 NAVSIM v2 上达到了 **89.4 EPDMS** 的顶尖水平，超越了现有的 VLA 模型。
*   **指令遵循能力**：模型能够根据不同的自然语言指令，在同一场景中生成完全不同的、符合逻辑的驾驶行为和未来场景预测。

### 7. 优点与亮点
*   **统一架构**：成功将 AR 理解与 Diffusion 生成整合在单个 Transformer 框架内，减少了信息瓶颈。
*   **数据集贡献**：InstructScene 为社区提供了大规模的指令-轨迹对数据，填补了该领域的空白。
*   **个性化驾驶**：突破了传统模仿学习的限制，实现了根据用户意图动态调整驾驶策略的能力。

### 8. 不足与局限
*   **传感器限制**：目前主要依赖单路前视摄像头，而许多 SOTA 模型使用环视相机或 LiDAR，这可能限制了其在复杂路口等场景的感知上限。
*   **指标偏差**：在 NAVSIM v1 上的表现略逊于某些特定优化的模型，论文指出这可能是由于 v1 的指标过于偏向保守策略，而 Vega 学习到了更多样化的驾驶风格。
*   **实时性挑战**：虽然使用了 H20 加速，但扩散模型在推理时的多步去噪过程可能带来比纯回归模型更高的延迟，论文未详细讨论实时部署的帧率表现。

（完）
