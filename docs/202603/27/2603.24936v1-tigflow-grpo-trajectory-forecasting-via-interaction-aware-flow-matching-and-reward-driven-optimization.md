---
title: "TIGFlow-GRPO: Trajectory Forecasting via Interaction-Aware Flow Matching and Reward-Driven Optimization"
title_zh: TIGFlow-GRPO：通过交互感知流匹配与奖励驱动优化的轨迹预测
authors: "Xuepeng Jing, Wenhuan Lu, Hao Meng, Zhizhi Yu, Jianguo Wei"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24936v1"
tags: ["query:tp"]
score: 10.0
evidence: 视觉复杂环境和人群中的行人轨迹预测
tldr: 针对自动驾驶和人群监控中的轨迹预测问题，本文提出 TIGFlow-GRPO 框架。该框架结合了条件流匹配（CFM）与轨迹交互图（TIG）来捕捉复杂的时空交互，并引入基于奖励驱动的 GRPO 优化阶段。通过将确定性流推导转化为随机 SDE 采样，并结合社会规范与地图约束的复合奖励进行后训练，显著提升了预测轨迹的社会合规性和物理可行性。
selection_source: fresh_fetch
motivation: 现有的流匹配轨迹预测方法主要依赖监督学习，难以充分反映复杂的社会规范和场景物理约束。
method: 提出两阶段框架，首先利用轨迹交互图增强上下文编码，随后通过随机微分方程采样与组相对策略优化（GRPO）进行行为对齐。
result: 在 ETH/UCY 和 SDD 数据集上的实验表明，该方法在提高预测准确性的同时，增强了长时稳定性和环境适应性。
conclusion: 该研究成功将流匹配生成模型与强化学习对齐技术相结合，为动态环境下的行为感知轨迹建模提供了有效方案。
---

## 摘要
行人轨迹预测对于在视觉复杂环境中运行的智能多媒体系统（如自动驾驶和人群监控）至关重要。尽管条件流匹配（CFM）在根据时空观测建模轨迹分布方面表现出强大的能力，但现有方法仍主要侧重于监督拟合，这可能导致生成的轨迹无法充分反映社交规范和场景约束。为解决这一问题，我们提出了 TIGFlow-GRPO，这是一个将基于流的轨迹生成与行为规则对齐的两阶段生成框架。在第一阶段，我们构建了一个带有轨迹交互图（TIG）模块的基于 CFM 的预测器，以建模细粒度的视觉空间交互并强化上下文编码。该阶段能更有效地捕捉智能体与智能体之间以及智能体与场景之间的关系，为后续对齐提供更具信息量的条件特征。在第二阶段，我们进行 Flow-GRPO 后训练，其中确定性流展开被重新表述为随机的 ODE 到 SDE 采样以实现轨迹探索，并且通过复合奖励结合了视角感知的社交合规性与地图感知的物理可行性。通过评估 SDE 展开探索的轨迹，GRPO 逐步引导多模态预测走向行为上合理的未来。在 ETH/UCY 和 SDD 数据集上的实验表明，TIGFlow-GRPO 在提高预测准确性和长时稳定性的同时，生成的轨迹更符合社交规范且更具物理可行性。这些结果表明，所提出的框架为在动态多媒体环境中将基于流的轨迹建模与行为感知对齐相结合提供了一种有效途径。

## Abstract
Human trajectory forecasting is important for intelligent multimedia systems operating in visually complex environments, such as autonomous driving and crowd surveillance. Although Conditional Flow Matching (CFM) has shown strong ability in modeling trajectory distributions from spatio-temporal observations, existing approaches still focus primarily on supervised fitting, which may leave social norms and scene constraints insufficiently reflected in generated trajectories. To address this issue, we propose TIGFlow-GRPO, a two-stage generative framework that aligns flow-based trajectory generation with behavioral rules. In the first stage, we build a CFM-based predictor with a Trajectory-Interaction-Graph (TIG) module to model fine-grained visual-spatial interactions and strengthen context encoding. This stage captures both agent-agent and agent-scene relations more effectively, providing more informative conditional features for subsequent alignment. In the second stage, we perform Flow-GRPO post-training,where deterministic flow rollout is reformulated as stochastic ODE-to-SDE sampling to enable trajectory exploration, and a composite reward combines view-aware social compliance with map-aware physical feasibility. By evaluating trajectories explored through SDE rollout, GRPO progressively steers multimodal predictions toward behaviorally plausible futures. Experiments on the ETH/UCY and SDD datasets show that TIGFlow-GRPO improves forecasting accuracy and long-horizon stability while generating trajectories that are more socially compliant and physically feasible. These results suggest that the proposed framework provides an effective way to connect flow-based trajectory modeling with behavior-aware alignment in dynamic multimedia environments.

---

## 论文详细总结（自动生成）

### 论文总结：TIGFlow-GRPO 轨迹预测框架

#### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶和人群监控等复杂视觉环境中，行人轨迹预测面临两大挑战：一是人类运动具有高度的多模态不确定性；二是预测结果必须符合社会规范（如避免碰撞）和物理约束（如不穿墙）。
现有的**条件流匹配（CFM）**方法虽然能很好地拟合轨迹的统计分布，但由于主要采用监督学习（MLE），难以直接优化非微分的物理和社会规则。这导致生成的轨迹虽然在统计上合理，但在实际场景中可能出现违背常识的行为。本文旨在通过引入强化学习对齐技术，弥合统计分布拟合与行为感知生成之间的差距。

#### 2. 方法论：核心思想与关键技术
论文提出了一个两阶段生成框架：
*   **第一阶段：预训练（TIG-GAT + CFM）**
    *   **TIG-GAT 模块**：构建“轨迹交互图”，利用**视野准则（Field-of-View）**筛选对目标智能体真正有影响的邻居，而非盲目聚合所有邻近个体。采用门控注意力机制（Gated Attention）提取精细的局部交互特征。
    *   **CFM 主干**：学习一个连续向量场，将高斯先验分布转换为目标轨迹分布。
*   **第二阶段：后训练对齐（Flow-GRPO）**
    *   **ODE 到 SDE 的转换**：将原本确定性的流推导（ODE）重新表述为随机微分方程（SDE）采样。这种随机性为强化学习提供了必要的“探索”空间。
    *   **GRPO 优化**：采用组相对策略优化（Group Relative Policy Optimization），通过计算一组生成样本的相对优势来更新模型，无需额外的判别器（Critic）网络，降低了计算开销。
    *   **复合奖励函数**：设计了**视角感知社会奖励**（基于方向敏感的几何关系惩罚碰撞）和**地图感知语义奖励**（利用有向距离场 SDF 惩罚越界行为）。

#### 3. 实验设计
*   **数据集**：
    *   **ETH/UCY**：包含 5 个子场景，侧重于评估人群中的社会交互。
    *   **Stanford Drone Dataset (SDD)**：校园航拍场景，具有复杂的物理边界约束。
*   **Benchmark 与对比方法**：
    *   对比了多种范式的方法，包括基于图的方法（GroupNet, EqMotion）、基于扩散模型的方法（MID, SingularTraj, LED）以及基于流匹配的方法（MoFlow）。
*   **评估指标**：采用 ADE（平均位移误差）、FDE（最终位移误差）的最小值和平均值，以及 **Col（碰撞率）**。

#### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA RTX 3090 GPU** 上完成。
*   **训练细节**：预训练阶段使用 AdamW 优化器，学习率为 $1 \times 10^{-4}$；后训练阶段每组采样 $G=4$ 条轨迹进行优势估计。论文未明确给出总训练时长，但强调了 GRPO 相比传统 RL 方法更节省显存。

#### 5. 实验数量与充分性
*   **实验规模**：在 ETH/UCY 的 5 个场景上进行了留一法交叉验证，并在 SDD 上进行了大规模测试。
*   **消融实验**：针对 TIG-GAT 模块、社会奖励、地图奖励以及 GRPO 阶段分别进行了消融分析，验证了各组件的有效性。
*   **充分性评价**：实验设计较为全面，不仅关注位移误差，还通过碰撞率和长时预测稳定性（1.2s 到 4.8s）验证了模型的鲁棒性。对比基线涵盖了近年来的主流模型，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能提升**：TIGFlow-GRPO 在 ETH/UCY 上的平均 ADE/FDE 达到 0.20/0.31，优于所有对比基线。
*   **社会合规性**：在长时预测（4.8s）中，该方法显著降低了碰撞率（如在 SDD 上从 19.74% 降至 12.29%）。
*   **分布优化**：GRPO 后训练不仅提升了最佳预测精度，更重要的是通过抑制低质量离群样本，优化了整体预测分布的质量。

#### 7. 优点
*   **创新性对齐**：首次将 GRPO 引入流匹配轨迹预测，解决了连续生成模型难以进行非微分规则优化的难题。
*   **视角感知交互**：TIG-GAT 模拟了人类真实的视觉感知范围，比单纯基于距离的交互建模更符合实际。
*   **物理可行性**：通过 SDF 引入连续的边界反馈，使模型能更好地感知环境拓扑。

#### 8. 不足与局限
*   **计算复杂度**：虽然 GRPO 减少了显存占用，但 SDE 采样和多样本奖励计算在推理或训练时仍比单步生成方法耗时。
*   **奖励权重敏感性**：复合奖励函数中多个权重的设置可能需要针对不同场景进行精细调优，缺乏自动化的权重平衡机制。
*   **输入模态限制**：目前主要依赖坐标和语义地图，尚未充分利用原始视频像素或视觉语言模型（VLM）进行更高层的语义推理。

（完）
