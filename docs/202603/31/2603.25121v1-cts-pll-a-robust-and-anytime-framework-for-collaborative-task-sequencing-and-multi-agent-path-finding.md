---
title: "CTS-PLL: A Robust and Anytime Framework for Collaborative Task Sequencing and Multi-Agent Path Finding"
title_zh: CTS-PLL：一种用于协作任务排序与多智能体路径规划的鲁棒且随时框架
authors: "Junkai Jiang, Yitao Xu, Ruochen Li, Shaobing Xu, Jianqiang Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25121v1"
tags: ["query:tp"]
score: 8.0
evidence: 密集环境中的多智能体路径规划与避障
tldr: 针对协作任务排序与多智能体路径规划（CTS-MAPF）中的组合复杂性难题，本文提出CTS-PLL分层框架。该框架引入了基于完备规划方法的死锁检测与释放机制，并结合大邻域搜索（LNS）实现解的随时优化。实验证明，该方法在稠密环境下具有极高的鲁棒性，能显著提升任务成功率和解质量，并已在真实机器人实验中得到验证。
selection_source: fresh_fetch
motivation: 旨在解决CTS-MAPF问题在复杂稠密环境下因组合爆炸导致的规划失败和解质量低下的挑战。
method: 提出了一种结合死锁检测释放机制与大邻域搜索（LNS）优化的分层规划框架。
result: 在稀疏和稠密基准测试中，该方法在成功率和解质量上均优于现有算法，且具备良好的运行效率。
conclusion: CTS-PLL是一个鲁棒且高效的随时规划框架，为解决实际场景中的多智能体协作任务提供了可行方案。
---

## 摘要
协作任务排序与多智能体路径规划（CTS-MAPF）问题要求智能体在避免碰撞的同时完成一系列任务，由于其组合复杂性，这带来了巨大的挑战。本研究提出了 CTS-PLL，这是一种分层框架，它通过两项关键改进扩展了基于配置的 CTS-MAPF 规划范式：一是利用完备规划方法进行局部重规划的死锁智能体检测与释放机制；二是基于大邻域搜索（LNS）的随时（anytime）优化程序。这些改进确保了在密集环境中的鲁棒性，并能够持续提升解的质量。在稀疏和密集基准测试上的广泛评估表明，与现有方法相比，CTS-PLL 在保持竞争力的运行效率的同时，实现了更高的成功率和解质量。真实世界的机器人实验进一步证明了该方法在实践中的可行性。

## Abstract
The Collaborative Task Sequencing and Multi-Agent Path Finding (CTS-MAPF) problem requires agents to accomplish sequences of tasks while avoiding collisions, posing significant challenges due to its combinatorial complexity. This work introduces CTS-PLL, a hierarchical framework that extends the configuration-based CTS-MAPF planning paradigm with two key enhancements: a lock agents detection and release mechanism leveraging a complete planning method for local re-planning, and an anytime refinement procedure based on Large Neighborhood Search (LNS). These additions ensure robustness in dense environments and enable continuous improvement of solution quality. Extensive evaluations across sparse and dense benchmarks demonstrate that CTS-PLL achieves higher success rates and solution quality compared with existing methods, while maintaining competitive runtime efficiency. Real-world robot experiments further demonstrate the feasibility of the approach in practice.

---

## 论文详细总结（自动生成）

以下是对论文《CTS-PLL: A Robust and Anytime Framework for Collaborative Task Sequencing and Multi-Agent Path Finding》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：解决**协作任务排序与多智能体路径规划（CTS-MAPF）**问题。该问题要求多个智能体在避免碰撞的前提下，按照特定顺序完成一系列协作任务，并最终到达目标点。
*   **研究背景与动机**：
    *   **组合复杂性**：CTS-MAPF 耦合了任务分配、排序与路径规划，搜索空间随智能体和任务数量呈指数级增长。
    *   **现有方法局限**：基于冲突搜索（CBS）的方法虽能保证最优性但扩展性差；基于配置的轻量级方法（如 CTS-PIBT）虽高效但**不完备**，在稠密环境下极易陷入死锁（Deadlock）或活锁（Livelock），且缺乏解质量的持续优化能力。
    *   **需求**：需要一个既能保证高成功率（鲁棒性），又能随时间推移不断优化解质量（随时性）的框架。

### 2. 方法论
CTS-PLL 采用分层架构，在 CTS-PIBT 的基础上引入了两个核心增强模块：
*   **分层规划架构**：上层进行联合任务排序，下层利用扩展 PIBT（Extended-PIBT）进行步进式路径规划。
*   **死锁检测与释放机制（Lock Detection & Release）**：
    *   **检测**：当规划步数超过阈值或智能体长时间停滞时，识别出“锁定智能体集”（包括未完成任务的智能体及阻碍它们的智能体）。
    *   **释放**：调用具有完备性保证的 MAPF 求解器（如 **LaCAM**）进行局部重规划。通过对锁定区域内的智能体位置进行随机扰动或重新排列，打破对称性冲突，引导智能体跳出死锁。
*   **基于大邻域搜索（LNS）的随时优化**：
    *   在获得初始可行解后，利用 LNS 进行迭代优化。
    *   **破坏（Destroy）策略**：采用随机采样、基于交点（拥堵区）采样、随机游走采样三种策略选择智能体子集。
    *   **修复（Repair）策略**：使用优先级规划（PP）重新为选定智能体生成路径。
    *   **自适应权重**：通过指数移动平均值动态调整三种破坏策略的权重，优先选择减效明显的策略。

### 3. 实验设计
*   **实验场景**：采用标准 CTS-MAPF 数据集中的四种网格地图：`empty`（空旷）、`random`（随机障碍）、`room`（房间）、`maze`（迷宫）。
*   **实验设置**：
    *   **稀疏场景**：5-20 个智能体，10-50 个任务。
    *   **稠密场景**：在最复杂的 `maze` 地图上设置 50 个智能体，60-180 个任务。
*   **对比基准（Baselines）**：
    *   **CTS-CBS / CBSS**：基于冲突搜索的组合优化方法。
    *   **CTS-PIBT**：目前最先进的轻量级配置类方法。
    *   **CTS-PIBT-anytime**：CTS-PIBT 的随时优化版本（仅通过搜索不同任务序列优化）。
*   **物理验证**：使用 **toio 机器人**进行实物实验，验证算法在处理真实物理冲突和通信延迟时的有效性。

### 4. 资源与算力
*   **硬件环境**：Intel Core i7-12700KF CPU，32GB RAM。
*   **软件环境**：Linux 系统，算法使用 C++ 实现。
*   **算力说明**：论文未提及使用 GPU，该算法主要依赖 CPU 进行逻辑运算和路径搜索。
*   **时间预算**：稀疏场景限时 60s-180s，稠密场景限时 180s。

### 5. 实验数量与充分性
*   **实验规模**：针对每种（智能体数量, 任务数量）组合，均生成了 **50 组随机实例**进行测试，总实验次数达数千次。
*   **消融实验**：设计了三个变体（v1: 仅死锁释放；v2: 死锁释放+序列搜索；v3: 全功能版）以验证各模块贡献。
*   **评价指标**：成功率（Success Rate）、运行时间（Runtime）、流时间（Flowtime，即总到达时间）。
*   **充分性评价**：实验覆盖了从简单到极高难度的地图，对比了最优求解器与启发式求解器，并包含物理实验，实验设计客观、全面且具有说服力。

### 6. 主要结论与发现
*   **成功率大幅提升**：在稀疏场景下，CTS-PLL 达到了 **100% 的成功率**，远超 CTS-CBS 和 CBSS。在极稠密场景下，相比 CTS-PIBT，成功率提升了 **28.95% 至 89.47%**。
*   **解质量优化**：通过 LNS 模块，CTS-PLL 在相同时间内生成的路径流时间比 CTS-PIBT 平均降低了约 **10.4%**。
*   **鲁棒性验证**：死锁检测与释放机制被证明是处理复杂迷宫环境和高密度交互的关键。
*   **实时性**：算法能够快速给出初始解，并随时间增加不断逼近更高质量的解，满足实时应用需求。

### 7. 优点
*   **鲁棒性强**：通过局部 LaCAM 重规划解决了 PIBT 类算法固有的死锁痛点。
*   **随时性（Anytime）**：结合了任务序列搜索与路径层面的 LNS 优化，能够充分利用可用计算时间。
*   **扩展性好**：分层设计使其能够处理大规模智能体和任务，且不依赖昂贵的离线训练。
*   **软硬件结合**：通过物理机器人实验证明了算法从仿真到现实的迁移能力。

### 8. 不足与局限
*   **局部重规划的随机性**：在释放死锁时采用随机排列目标位置的方法虽然高效，但在理论上不具备严格的完备性保证（尽管实验表现极佳）。
*   **计算开销**：在极度拥堵的情况下，LNS 的修复过程（优先级规划）可能会因为频繁避障而导致单次迭代耗时增加。
*   **动态环境缺失**：目前主要针对静态障碍物地图，未深入探讨环境中存在不可预测动态障碍物（如行人）时的表现。

（完）
