---
title: "Predictive Modeling in AUV Navigation: A Perspective from Kalman Filtering"
title_zh: AUV导航中的预测建模：卡尔曼滤波视角
authors: "Zizhan Tang, Yao Liu, Jessica Liu"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27422v1"
tags: ["query:tp"]
score: 9.0
evidence: 自主水下航行器轨迹预测
tldr: 针对自主水下航行器（AUV）在通信中断时的定位与搜寻难题，本文提出了一种结合声学信号与卡尔曼滤波的安全导向框架。该框架利用固定浮标采集到达时间差（TDOA）数据，并与预测模型融合，实现了高精度的实时定位与轨迹预测。此外，通过对不确定性增长的建模，该系统能在通信丢失后预测AUV位置，为搜救行动提供科学的搜索区域定义，显著提升了AUV的作业安全性。
selection_source: fresh_fetch
motivation: 旨在解决AUV在复杂水下环境中定位精度不足以及通信中断后难以追踪和搜寻的安全挑战。
method: 融合了基于浮标网络的TDOA声学测距技术与卡尔曼滤波算法，并引入了针对通信中断后的不确定性传播模型。
result: 实验表明该方法在定位精度和轨迹稳定性上显著优于仅使用TDOA的基准方案，并能有效缩小搜救范围。
conclusion: 该框架通过多源数据融合与预测建模，为AUV提供了稳健的导航支持和高效的失联搜寻方案。
---

## 摘要
我们提出了一种面向自主水下航行器（AUV）的安全导向框架，旨在提高定位精度、增强轨迹预测，并在通信中断期间支持高效的搜索行动。AUV发射的声学信号由固定浮标网络接收，该网络计算到达时间差（TDOA）距离差测量值作为位置观测。随后，这些观测值与基于卡尔曼滤波的预测模型相融合，以获得连续且抗噪的状态估计。与仅采用TDOA的基准方法相比，该融合方法显著提升了定位精度和轨迹稳定性。除了实时跟踪，该框架还通过预测断连后的运动并显式建模不确定性增长，提供了针对性的搜索与回收能力。搜索模块能够区分持续导航与动力失效，从而将搜索资源部署至最可能的回收区域。本框架通过融合多浮标声学数据、卡尔曼滤波及不确定性传播，在维持导航精度的同时，为通信中断期间提供了稳健的搜索区域定义。

## Abstract
We present a safety-oriented framework for autonomous underwater vehicles (AUVs) that improves localization accuracy, enhances trajectory prediction, and supports efficient search operations during communication loss. Acoustic signals emitted by the AUV are detected by a network of fixed buoys, which compute Time-Difference-of-Arrival (TDOA) range-difference measurements serving as position observations. These observations are subsequently fused with a Kalman-based prediction model to obtain continuous, noise-robust state estimates. The combined method achieves significantly better localization precision and trajectory stability than TDOA-only baselines.   Beyond real-time tracking, our framework offers targeted search-and-recovery capability by predicting post-disconnection motion and explicitly modeling uncertainty growth. The search module differentiates between continued navigation and propulsion failure, allowing search resources to be deployed toward the most probable recovery region. Our framework fuses multi-buoy acoustic data with Kalman filtering and uncertainty propagation to maintain navigation accuracy and yield robust search-region definitions during communication loss.

---

## 论文详细总结（自动生成）

这篇论文《AUV导航中的预测建模：卡尔曼滤波视角》针对自主水下航行器（AUV）在复杂水下环境中的定位与失联搜寻问题，提出了一套整合声学定位与状态估计的预测框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在GPS信号无法穿透的水下环境中，AUV依赖声学通信进行定位。然而，声学信号易受多径效应、噪声干扰且经常发生通信中断。当AUV失联时，如何准确估算其最后位置并预测其后续轨迹（无论是继续航行还是动力失效后的漂流），是搜救和回收任务中的关键挑战。
*   **研究背景**：现有的研究多集中于正常通信时的定位优化，或单纯的海洋漂流建模，缺乏一个将“失联前精确追踪”与“失联后不确定性演化”统一起来的闭环框架。

### 2. 核心方法论
该框架由三个核心模块组成：
*   **TDOA声学定位模块**：利用多个固定浮标接收AUV发射的声学信号。采用**Chan氏算法（Chan's Algorithm）**，这是一种非迭代的闭式解法，通过将非线性双曲方程组线性化，快速计算AUV的3D坐标。相比迭代最小二乘法，它计算效率更高，适合嵌入式实时应用。
*   **卡尔曼滤波（KF）融合模块**：将TDOA获取的观测值与AUV的运动学模型（常加速度模型）融合。通过预测步和更新步，平滑声学观测中的随机噪声，提供连续且稳健的状态估计（位置和速度）。
*   **失联搜索模型**：
    *   **场景1（预定导航）**：假设AUV仅通信故障，仍按计划航行。模型利用失联前的最后状态进行确定性外推。
    *   **场景2（动力失效）**：假设AUV失去动力，受洋流干扰。模型将其建模为随机游走过程，通过协方差矩阵 $P$ 的增长来定义随时间扩大的搜索区域（不确定性椭圆）。

### 3. 实验设计
*   **实验场景**：基于仿真环境。设置了5个固定浮标，分布在不同的3D坐标点。
*   **数据集**：在3D空间中均匀分布了1000个测试点，模拟AUV的运动轨迹。采样间隔为10秒。
*   **基准方法（Benchmark）**：
    *   **TDOA-only**：仅使用原始声学定位数据。
    *   **Ground Truth**：预设的真实轨迹。
*   **评估指标**：平均绝对误差（MAE）的累积分布函数（CDF）、均方误差（MSE）以及轨迹可视化对比。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如GPU型号或算力规模）。由于该方法基于闭式解和标准的线性卡尔曼滤波，计算复杂度较低，通常在普通的CPU或嵌入式处理器上即可实现实时运行。
*   **训练时长**：该方法属于传统的统计估计方法，不涉及深度学习的大规模训练过程。

### 5. 实验数量与充分性
*   **实验规模**：进行了1000个测试点的定位实验，并针对两种失联场景（t=50s和t=80s断连）进行了轨迹预测仿真。
*   **充分性评价**：实验设计较为基础但逻辑清晰。通过CDF图和MSE对比图直观证明了卡尔曼滤波对原始TDOA误差的显著抑制作用。然而，实验完全基于仿真，未包含真实海试数据，且对洋流噪声的建模较为简化（高斯噪声），在应对复杂非线性环境方面的验证略显不足。

### 6. 主要结论与发现
*   **定位精度提升**：卡尔曼滤波能有效滤除声学测量噪声，几乎所有测试点的定位误差均控制在4米以内，显著优于原始TDOA方法。
*   **轨迹稳定性**：在通信存在噪声的情况下，KF生成的轨迹更加平滑且接近真实值。
*   **搜救指导意义**：提出的搜索模型能够根据失联时长动态调整搜索半径，为搜救资源分配提供了科学依据，尤其是在区分“正常航行”与“动力失效”两种模式下具有实用价值。

### 7. 论文优点
*   **系统性强**：将定位、预测与搜救策略整合在一个数学框架内，具有很强的工程应用导向。
*   **计算高效**：选用Chan氏算法和线性KF，保证了算法在资源受限的AUV硬件上的实时性。
*   **不确定性建模**：显式地利用协方差矩阵传播来定义搜索区域，比单纯预测一个点更符合搜救实战需求。

### 8. 不足与局限
*   **环境假设过于理想**：假设声速恒定且噪声为高斯分布，未考虑水下声学中常见的声速梯度、多径效应及非高斯环境噪声。
*   **缺乏实测验证**：所有结论均来自仿真，缺乏在真实海洋环境（如存在强剪切流或障碍物）中的表现数据。
*   **模型简单**：运动模型为线性模型，对于具有复杂流体动力学特性的AUV在高机动转弯时的预测精度可能下降。

（完）
