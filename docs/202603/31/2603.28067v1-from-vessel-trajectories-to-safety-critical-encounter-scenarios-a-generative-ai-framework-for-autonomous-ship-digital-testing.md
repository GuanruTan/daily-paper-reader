---
title: "From Vessel Trajectories to Safety-Critical Encounter Scenarios: A Generative AI Framework for Autonomous Ship Digital Testing"
title_zh: 从船舶轨迹到安全关键会遇场景：一种用于自主船舶数字测试的生成式人工智能框架
authors: "Sijin Sun, Liangbin Zhao, Ming Deng, Xiuju Fu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28067v1"
tags: ["query:tp"]
score: 9.0
evidence: 自主船舶AIS数据的生成式轨迹建模
tldr: 本研究针对自主船舶数字测试中缺乏真实且多样化的安全关键会遇场景问题，提出了一种基于生成式人工智能的数据驱动框架。该框架利用多尺度时间变分自编码器（VAE）处理大规模AIS轨迹数据，将其转化为结构化的安全关键场景。实验证明，该方法在保持轨迹真实性的同时，能生成超越历史记录的多样化高风险场景，为自主导航系统的安全评估提供了实用的场景库构建方案。
selection_source: fresh_fetch
motivation: 现有的船舶会遇场景构建方法要么缺乏真实感，要么难以系统性地扩展稀有的高风险情况。
method: 提出一种结合多尺度时间变分自编码器与自动会遇配对的生成式框架，从大规模AIS数据中提取并生成结构化场景。
result: 实验表明该方法显著提升了轨迹的保真度与平滑度，并能生成比原始观测数据更丰富的安全关键会遇场景。
conclusion: 该框架为自主船舶和智能海事交通管理系统的数字测试、基准测试及安全评估提供了一条切实可行的路径。
---

## 摘要
数字测试已成为自主海上航行系统开发与验证的关键范式，但真实且多样化的安全关键会遇场景的可用性仍然有限。现有方法要么依赖于缺乏真实感的手工模板，要么直接从历史数据中提取案例，而后者无法系统地扩展罕见的高风险情况。本文提出了一种数据驱动的框架，将大规模自动识别系统（AIS）轨迹转换为结构化的安全关键会遇场景。该框架结合了生成式轨迹建模、自动化会遇配对和时间参数化，在保留真实交通特征的同时实现了可扩展的场景构建。为了增强轨迹的真实感以及在噪声 AIS 观测下的鲁棒性，引入了一种多尺度时间变分自编码器，以捕捉不同时间分辨率下的船舶运动动力学。在真实海上交通流上的实验表明，所提方法提高了轨迹的保真度和光滑度，保持了与观测数据的统计一致性，并能够生成超出直接记录范围的多样化安全关键会遇场景。由此产生的框架为构建场景库提供了一条切实可行的路径，以支持自主航行和智能海上交通管理系统的数字测试、基准测试和安全评估。代码可在 https://anonymous.4open.science/r/traj-gen-anonymous-review 获取。

## Abstract
Digital testing has emerged as a key paradigm for the development and verification of autonomous maritime navigation systems, yet the availability of realistic and diverse safety-critical encounter scenarios remains limited. Existing approaches either rely on handcrafted templates, which lack realism, or extract cases directly from historical data, which cannot systematically expand rare high-risk situations.   This paper proposes a data-driven framework that converts large-scale Automatic Identification System (AIS) trajectories into structured safety-critical encounter scenarios. The framework combines generative trajectory modeling with automated encounter pairing and temporal parameterization to enable scalable scenario construction while preserving real traffic characteristics. To enhance trajectory realism and robustness under noisy AIS observations, a multi-scale temporal variational autoencoder is introduced to capture vessel motion dynamics across different temporal resolutions.   Experiments on real-world maritime traffic flows demonstrate that the proposed method improves trajectory fidelity and smoothness, maintains statistical consistency with observed data, and enables the generation of diverse safety-critical encounter scenarios beyond those directly recorded. The resulting framework provides a practical pathway for building scenario libraries to support digital testing, benchmarking, and safety assessment of autonomous navigation and intelligent maritime traffic management systems. Code is available at https://anonymous.4open.science/r/traj-gen-anonymous-review.

---

## 论文详细总结（自动生成）

### 论文总结：从船舶轨迹到安全关键会遇场景的生成式 AI 框架

#### 1. 核心问题与整体含义（研究动机和背景）
自主船舶（AS）的开发与验证高度依赖数字测试，而测试的核心在于**安全关键会遇场景（Safety-Critical Encounter Scenarios）**。目前获取此类场景面临两大挑战：
*   **真实性不足**：基于规则或专家知识的手工模板难以捕捉真实海况下的复杂动态。
*   **稀疏性问题**：虽然 AIS（自动识别系统）提供了海量数据，但高风险的近距离会遇事件在现实中极少发生，直接提取难以覆盖多样化的潜在风险。
**研究动机**：开发一种数据驱动的框架，能够从常规 AIS 轨迹中学习运动模式，并生成既符合统计规律又具备多样性的高风险会遇场景，以支持自主导航系统的压力测试。

#### 2. 核心方法论
论文提出了一种结合生成式建模与自动化场景构建的统一流水线：
*   **ConfluxVAE 轨迹生成模型**：
    *   **核心思想**：在变分自编码器（VAE）中引入**多尺度时间指数移动平均（Conflux EMA）**模块。
    *   **关键技术**：设置三个并行分支（小、中、大尺度），分别捕捉局部动力学（瞬时速度）、片段级机动（转向）和全局趋势。通过可学习的 Softmax 门控融合这些特征。
    *   **损失函数**：采用 $\beta$-加权 KL 散度与重建误差（MSE）的组合，平衡潜在空间的正则化与重建质量。
*   **自动化会遇配对与参数化**：
    *   **配对逻辑**：在指定区域（ROI）内，通过空间邻近度和运动一致性（基于 CPA，即最近会遇点分析）筛选合成轨迹对。
    *   **场景表示**：将场景标准化为三个阶段：**预会遇阶段**（名义路径）、**会遇阶段**（安全关键测试窗口，以 $t^*$ 最近时刻为中心）、**后会遇阶段**（恢复路径）。

#### 3. 实验设计
*   **数据集**：使用新加坡航道为期一年的真实 AIS 记录。
    *   **Route 1**：北向流量（1094 条轨迹，用于基准对比）。
    *   **Route 2**：南向流量（2310 条轨迹，更复杂，用于鲁棒性评估）。
*   **基准模型（Benchmarks）**：对比了 CNN、GAN、LSTM、BiLSTM、标准 VAE、Transformer VAE 和 Social VAE。
*   **评估指标**：
    *   **点对点误差**：MAE、MSE。
    *   **分布相似度**：距离指标（DM）、最大平均差异（MMD）。

#### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA GeForce RTX 4080 GPU**，运行于 Ubuntu 22.04。
*   **训练细节**：
    *   Batch Size: 64。
    *   训练轮数：500 Epochs。
    *   潜在空间维度：100。
    *   轨迹采样间隔：10 秒。

#### 5. 实验数量与充分性
*   **实验规模**：针对两个不同特征的航道数据集进行了详尽测试，每个模型生成 1000 条轨迹进行评估。
*   **消融实验**：设计了四组消融实验（移除 Conflux EMA、移除整个 Conflux 模块、移除 $\beta$-KL 权重、移除 SG 平滑滤镜），验证了各组件对提升轨迹保真度的贡献。
*   **客观性**：通过定量指标（MAE/MMD）与定性案例分析（可视化轨迹形状）相结合，评估过程较为全面、客观。

#### 6. 主要结论与发现
*   **性能领先**：ConfluxVAE 在所有关键指标（尤其是 MAE 和 MMD）上均优于现有基准模型，证明了多尺度建模在处理噪声 AIS 数据方面的优越性。
*   **真实感增强**：相比于 GAN 的路径混乱或 LSTM 的模式坍塌，该方法生成的轨迹在保持平滑性的同时，能准确复现真实交通流的统计分布。
*   **场景扩展能力**：该框架能成功生成大量在原始数据中未直接记录、但符合物理规律的安全关键会遇场景，有效扩展了测试库。

#### 7. 优点（亮点）
*   **多尺度建模**：Conflux EMA 模块巧妙地解决了 AIS 数据中的高频噪声问题，同时保留了长程航行趋势。
*   **端到端流程**：从原始轨迹处理到结构化测试场景输出，提供了一套完整的工程化解决方案。
*   **标准化输出**：提出的三阶段场景表示法非常适合直接集成到现有的自主船舶仿真测试平台中。

#### 8. 不足与局限
*   **交互维度限制**：目前主要关注两船会遇（1-vs-1），尚未深入探讨多船（Multi-vessel）复杂交互场景的生成。
*   **环境因素缺失**：模型尚未显式考虑风、浪、流等环境载荷对船舶运动轨迹的影响。
*   **偏差风险**：生成场景的质量高度依赖于训练数据的覆盖范围，如果原始数据中某种极端机动完全缺失，模型可能难以凭空生成此类极端情况。

（完）
