---
title: Unified Number-Free Text-to-Motion Generation Via Flow Matching
title_zh: 通过流匹配实现统一的无需指定人数的文本到动作生成
authors: "Guanhe Huang, Oya Celiktutan"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.27040v1"
tags: ["query:tp"]
score: 8.0
evidence: 多智能体运动生成与反应建模
tldr: 本研究针对现有文本生成动作模型在处理变动人数时效率低且易累积误差的问题，提出了统一运动流（UMF）框架。该框架包含金字塔运动流（P-Flow）和半噪声运动流（S-Flow），通过流匹配技术将生成过程分解为单次运动先验生成和多次反应生成阶段。UMF利用统一潜空间弥合异构数据差异，在降低计算开销的同时缓解了误差累积，实现了高效且高质量的多人动作生成。
selection_source: fresh_fetch
motivation: 现有的自回归模型在处理变动人数的动作生成时存在效率低下和误差累积的问题。
method: 提出UMF框架，利用P-Flow进行分层分辨率的先验生成，并结合S-Flow进行自适应的反应变换与上下文重建。
result: 实验和用户研究表明，UMF在多人文本生成动作任务中表现出色，是一个高效的通用模型。
conclusion: UMF通过流匹配和统一潜空间设计，为变动人数的复杂动作生成提供了一种高效且稳健的解决方案。
---

## 摘要
生成模型在固定数量智能体的动作合成方面表现出色，但在处理可变数量智能体时难以泛化。基于有限的特定领域数据，现有方法采用自回归模型递归地生成动作，这存在效率低下和误差累积的问题。我们提出了统一动作流（Unified Motion Flow, UMF），它由金字塔动作流（Pyramid Motion Flow, P-Flow）和半噪声动作流（Semi-Noise Motion Flow, S-Flow）组成。UMF 将无需指定人数的动作生成分解为单次动作先验生成阶段和多次反应生成阶段。具体而言，UMF 利用统一的潜空间来弥合异构动作数据集之间的分布差异，从而实现有效的统一训练。对于动作先验生成，P-Flow 在以不同噪声水平为条件的分层分辨率上运行，从而减轻了计算开销。对于反应生成，S-Flow 学习一条联合概率路径，自适应地执行反应转换和上下文重建，从而缓解了误差累积。广泛的结果和用户研究证明了 UMF 作为一种通用的文本到多人动作生成模型的有效性。项目页面：https://githubhgh.github.io/umf/。

## Abstract
Generative models excel at motion synthesis for a fixed number of agents but struggle to generalize with variable agents. Based on limited, domain-specific data, existing methods employ autoregressive models to generate motion recursively, which suffer from inefficiency and error accumulation. We propose Unified Motion Flow (UMF), which consists of Pyramid Motion Flow (P-Flow) and Semi-Noise Motion Flow (S-Flow). UMF decomposes the number-free motion generation into a single-pass motion prior generation stage and multi-pass reaction generation stages. Specifically, UMF utilizes a unified latent space to bridge the distribution gap between heterogeneous motion datasets, enabling effective unified training. For motion prior generation, P-Flow operates on hierarchical resolutions conditioned on different noise levels, thereby mitigating computational overheads. For reaction generation, S-Flow learns a joint probabilistic path that adaptively performs reaction transformation and context reconstruction, alleviating error accumulation. Extensive results and user studies demonstrate UMF' s effectiveness as a generalist model for multi-person motion generation from text. Project page: https://githubhgh.github.io/umf/.

---

## 论文详细总结（自动生成）

以下是对论文《Unified Number-Free Text-to-Motion Generation Via Flow Matching》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的文本生成动作（Text-to-Motion）模型在处理**变动人数（Number-Free）**的场景时面临挑战。
*   **研究动机**：
    *   **泛化性差**：固定人数的模型难以适应拥挤或人数不确定的场景。
    *   **效率与误差**：现有的自回归方法在递归生成多人动作时，存在计算效率低下和严重的误差累积问题。
    *   **数据稀缺**：多人交互数据集（如 InterHuman）规模远小于单人数据集（如 HumanML3D），导致模型难以学习复杂的交互逻辑。
*   **整体含义**：提出一种名为 **UMF（Unified Motion Flow）** 的通用框架，通过流匹配（Flow Matching）技术，将多人动作生成分解为“单次先验生成”和“多次反应生成”，旨在统一异构数据并提升生成质量与效率。

### 2. 方法论：核心思想与技术细节
UMF 框架由三个核心组件构成：
*   **统一潜空间（Unified Latent Space）**：
    *   利用多标记（Multi-token）VAE 将异构的单人和多人动作数据映射到统一的非规范化 SMPL 骨架表示中。
    *   引入**潜空间适配器（Latent Adapter）**，在保持高重建精度的同时，通过压缩潜空间维度来降低生成任务的难度。
*   **金字塔运动流（P-Flow, Pyramid Motion Flow）**：
    *   **核心思想**：在生成动作先验（第一个人的动作）时，根据噪声水平（时间步 $t$）采用分层分辨率。
    *   **技术细节**：在早期时间步（噪声大、信息少）使用低分辨率采样，在后期时间步切换回高分辨率。通过 `JumpUpdate` 算法确保不同分辨率阶段之间的概率路径连续性，显著降低了多标记表示带来的计算开销。
*   **半噪声运动流（S-Flow, Semi-Noise Motion Flow）**：
    *   **核心思想**：用于生成后续智能体的“反应”动作。
    *   **技术细节**：S-Flow 同时学习两条路径：**反应转换路径**（从已有上下文动作到目标反应动作）和**上下文重建路径**（从高斯噪声到上下文动作）。这种联合训练模式使模型在预测反应时能更好地感知全局交互依赖，从而缓解自回归过程中的误差累积。

### 3. 实验设计
*   **数据集**：
    *   **InterHuman**：主要的多人交互基准。
    *   **HumanML3D**：用于提供单人动作先验。
    *   **InterHuman-AS**：带有角色（发起者/反应者）标注的交互数据集。
*   **对比方法**：
    *   **通用模型**：FreeMotion（主要竞争对手）。
    *   **专用模型**：TEMOS, T2M, MDM, InterGen, InterMask, TIMotion 等。
*   **评估指标**：FID（真实性）、R-Precision（文本匹配度）、Multimodal Distance（多模态距离）、Diversity（多样性）。

### 4. 资源与算力
*   **训练细节**：使用 AdamW 优化器，学习率 $10^{-4}$。
*   **训练时长**：VAE 阶段训练 6000 epoch，P-Flow 和 S-Flow 阶段各训练 2000 epoch。
*   **算力说明**：论文提到了使用 King’s CREATE 高性能计算集群（HPC），但**未明确列出具体的 GPU 型号（如 A100 或 V100）及具体显卡数量**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流基准（InterHuman, InterHuman-AS）上进行了详尽的定量对比。
    *   针对 $N > 2$ 的场景进行了**零照会（Zero-shot）**泛化实验。
    *   **消融实验**：涵盖了异构先验的影响、潜空间设计、P-Flow 的效率分析以及 S-Flow 的组件贡献（共 5 组主要消融实验）。
    *   **用户研究**：邀请 30 名用户对 20 组随机样本进行盲测。
*   **充分性评价**：实验设计较为全面，不仅对比了最先进的专用模型，还重点分析了框架在处理变动人数时的效率优势，客观地展示了模型在文本对齐和物理真实性上的提升。

### 6. 主要结论与发现
*   **性能领先**：UMF 在 InterHuman 基准上达到了 SOTA 性能（FID 4.772），显著优于之前的通用框架 FreeMotion。
*   **效率提升**：P-Flow 的金字塔结构使推理速度提升了近 5 倍，同时降低了 FLOPs。
*   **误差缓解**：S-Flow 的联合路径设计有效解决了长序列自回归生成中的动作漂移和不自然现象。
*   **跨域增益**：引入单人动作先验（HumanML3D）确实能增强多人交互生成的质量。

### 7. 优点
*   **架构优雅**：通过流匹配统一了单人先验和多人反应的生成逻辑。
*   **计算高效**：金字塔分辨率策略巧妙地平衡了多标记表示的精度与推理成本。
*   **泛化力强**：能够处理训练集中未见的 3 人及以上（$N > 2$）的复杂群组交互。

### 8. 不足与局限
*   **规模限制**：虽然支持变动人数，但目前主要针对中等规模群体（约 10 人以内），且通常围绕一个核心代理展开。
*   **复杂场景挑战**：对于极大规模（如 100 人以上）的密集人群动态，模型尚缺乏处理能力。
*   **视觉先验缺失**：目前仅依赖文本，未来可能需要引入视频扩散模型的视觉先验来进一步提升物理真实感。

（完）
