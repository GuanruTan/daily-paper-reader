---
title: A Distribution-to-Distribution Neural Probabilistic Forecasting Framework for Dynamical Systems
title_zh: 一种用于动力系统的分布到分布神经概率预测框架
authors: "Tianlin Yang, Hailiang Du, Louis Aslett"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25370v1"
tags: ["query:tp"]
score: 8.0
evidence: 动力系统与轨迹的概率预测
tldr: 该研究针对动力系统概率预测中传统方法依赖轨迹采样的问题，提出了一种分布到分布（D2D）的神经预测框架。该框架利用核均值嵌入表示输入分布，并通过混合密度网络参数化输出分布，实现了预测不确定性在统一端到端架构中的直接递归传播。在Lorenz63系统上的实验证明，该方法无需显式集合模拟即可捕捉非线性动力学下的复杂分布演化，性能优于或等同于传统基准。
selection_source: fresh_fetch
motivation: 现有的概率预测方法大多基于轨迹采样或集合模拟，缺乏直接演化预测分布作为动力学对象的能力。
method: 提出一种D2D框架，结合核均值嵌入进行分布编码和混合密度网络进行解码，实现分布的直接递归传播。
result: 在Lorenz63混沌系统上，该模型成功捕捉了非线性分布演化，且在预测技巧上达到或超过了简化完美模型基准。
conclusion: 该研究为概率预测提供了一种新范式，证明了直接学习和演化预测分布比间接重构集合更具优势。
---

## 摘要
概率预测通过将预测表示为概率分布而非确定性轨迹，为动力系统中的不确定性量化提供了一个原则性框架。然而，现有的预测方法，无论是基于物理的还是基于神经网络的，在根本上仍然是面向轨迹的：预测分布通常通过集成或采样获得，而不是作为动力学对象直接演化。本文开发了一种分布到分布（D2D）神经概率预测框架，直接对预测分布进行操作。该框架在可替换的神经预测模块周围引入了分布编码和解码结构，使用核均值嵌入（kernel mean embeddings）来表示输入分布，并使用混合密度网络（mixture density networks）来参数化输出预测分布。这种设计使得预测不确定性能够在统一的端到端神经架构内进行递归传播，且模型的训练和评估直接根据概率预测技巧进行。该框架在 Lorenz63 混沌动力系统上进行了演示。结果表明，D2D 模型捕捉到了非线性动力学下非平凡的分布演化，在无需显式集成模拟的情况下生成了有效的概率预测，并且与简化的完美模型基准相比具有竞争力，在某些情况下甚至表现更好。这些发现指向了概率预测的一种新范式，即预测分布是直接学习和演化的，而不是通过基于集成的不确定性传播间接重构的。

## Abstract
Probabilistic forecasting provides a principled framework for uncertainty quantification in dynamical systems by representing predictions as probability distributions rather than deterministic trajectories. However, existing forecasting approaches, whether physics-based or neural-network-based, remain fundamentally trajectory-oriented: predictive distributions are usually accessed through ensembles or sampling, rather than evolved directly as dynamical objects. A distribution-to-distribution (D2D) neural probabilistic forecasting framework is developed to operate directly on predictive distributions. The framework introduces a distributional encoding and decoding structure around a replaceable neural forecasting module, using kernel mean embeddings to represent input distributions and mixture density networks to parameterise output predictive distributions. This design enables recursive propagation of predictive uncertainty within a unified end-to-end neural architecture, with model training and evaluation carried out directly in terms of probabilistic forecast skill. The framework is demonstrated on the Lorenz63 chaotic dynamical system. Results show that the D2D model captures nontrivial distributional evolution under nonlinear dynamics, produces skillful probabilistic forecasts without explicit ensemble simulation, and remains competitive with, and in some cases outperforms, a simplified perfect model benchmark. These findings point to a new paradigm for probabilistic forecasting, in which predictive distributions are learned and evolved directly rather than reconstructed indirectly through ensemble-based uncertainty propagation.

---

## 论文详细总结（自动生成）

### 论文总结：一种用于动力系统的分布到分布（D2D）神经概率预测框架

#### 1. 核心问题与研究动机
*   **核心问题**：传统的动力系统预测方法（无论是基于物理方程还是神经网络）本质上都是“面向轨迹”的，即演化单个系统状态。不确定性量化通常通过集成（Ensemble）采样等辅助手段实现，计算成本高且不确定性并非模型演化的内在组成部分。
*   **研究动机**：旨在建立一种“面向分布”的预测范式，将预测分布视为动力学对象直接进行演化。通过构建一个端到端的神经架构，使模型能够直接学习分布到分布（D2D）的映射，从而实现不确定性在时间上的相干传播。

#### 2. 方法论
该框架由三个核心模块组成，形成一个闭环的递归结构：
*   **分布编码器（Distribution Encoder）**：采用**核均值嵌入（Kernel Mean Embedding, KME）**技术。将输入的概率分布（如高斯混合分布）映射到再生核希尔伯特空间（RKHS）中的有限维表示。具体使用高斯径向基函数（RBF）核，其带宽参数 $\ell$ 是可学习的。
*   **神经预测模块（Neural Forecasting Module）**：这是一个可替换的骨干网络，文中使用了**长短期记忆网络（LSTM）**。它接收编码后的分布特征，处理动力系统的时序演化。
*   **分布解码器（Distribution Decoder）**：采用**混合密度网络（Mixture Density Network, MDN）**。将神经模块的输出解码为参数化的预测分布（由 $M$ 个高斯分量组成的混合模型），直接输出均值、方差和权重。
*   **训练目标与策略**：
    *   **损失函数**：使用**对数评分（Logarithmic Score）**，即负对数似然（NLL）。这是唯一具有原则性的局部真诚评分规则。
    *   **迭代训练策略**：引入**课程学习（Curriculum Learning）**，从短时步预测逐渐增加到长时步递归，以稳定训练并减少误差累积。

#### 3. 实验设计
*   **实验场景**：经典的 **Lorenz63 混沌动力系统**。该系统具有强烈的初值敏感性和非线性，是测试不确定性传播的标准基准。
*   **数据集**：通过四阶龙格-库塔法模拟生成轨迹，添加不同水平（0.01 到 0.08）的高斯观测噪声。
*   **基准模型（Benchmark）**：
    *   **完美模型（Perfect Model）**：使用真实的 Lorenz63 方程进行 128 个成员的集成预测，并结合核修饰（Kernel Dressing）生成分布。
    *   **直接预测策略（Direct Strategy）**：针对特定提前期单独训练的模型组合。
*   **对比维度**：不同课程学习长度（16, 32, 64, 128步）的 D2D 模型性能对比。

#### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）及训练总时长。
*   文中提到训练样本量为 30,000 个，验证集和测试集各 30,000 个，属于中等规模的数值模拟实验。

#### 5. 实验数量与充分性
*   **实验数量**：涵盖了 4 种不同的观测噪声水平，以及多种训练提前期的消融对比。
*   **充分性**：实验通过可视化 PDF 演化直观展示了模型捕捉非线性变形的能力，并使用自助法（Bootstrap）重采样计算 95% 置信区间，确保了定量评估的统计显著性。
*   **客观性**：对比了“完美模型”基准，证明了 D2D 框架在处理观测噪声和数据同化效应方面的潜在优势。

#### 6. 主要结论与发现
*   **分布演化能力**：D2D 模型能够成功捕捉非线性动力学导致的分布拉伸、变形及非高斯结构（如双峰分布）。
*   **迭代优于直接**：迭代训练的 D2D 模型在长程预测上远优于直接预测模型，证明了学习“动力学一致的分布演化算子”的重要性。
*   **超越完美模型**：在高噪声水平下，D2D 模型的表现甚至超过了简化的完美模型基准，因为它在训练中隐式学习了类似数据同化的校正效应。
*   **不确定性收敛**：在极长提前期下，模型预测会自动趋向于气候态分布（Climatology），符合物理直觉。

#### 7. 优点
*   **范式创新**：将不确定性从“辅助诊断”提升为“核心建模目标”，实现了分布的直接递归传播。
*   **理论严谨**：坚持使用对数评分作为训练和评估的统一标准，符合概率公理。
*   **计算效率**：推理时无需进行昂贵的多次集成采样，单次前向传播即可获得完整的连续概率密度函数。
*   **模块化**：编码-解码结构可以适配各种先进的深度学习骨干网络（如 Transformer）。

#### 8. 不足与局限
*   **维度局限**：目前仅实现了**边缘分布（Marginal Distributions）**的演化，尚未处理全状态向量的高维联合分布，忽略了变量间的瞬时协方差。
*   **场景单一**：实验仅限于低维的 Lorenz63 系统，在大规模地球物理流体或高维偏微分方程（PDE）系统上的扩展性有待验证。
*   **超参数敏感性**：核中心数量 $n_c$ 和混合分量数量 $M$ 的选择对复杂分布的表达精度有较大影响，且可能增加计算负担。

（完）
